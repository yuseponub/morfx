---
phase: standalone
slug: ui-redesign-editorial-core
status: draft
shadcn_initialized: true
preset: new-york (existing — not modified by this reskin)
created: 2026-06-05
---

# ui-redesign-editorial-core — UI Design Contract

> Visual and interaction contract for the editorial "paper / Bible / dictionary" RESKIN of 3 dashboard content areas (Conversaciones, CRM Contactos, CRM Pedidos) + the token system. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **This is a verbatim port, NOT a greenfield design.** The design system already exists fully in the handoff (`design_handoff_morfx v2.1`): token CSS + canonical HTML mocks + reference TSX. The job is to LOCK that system into a contract so planner/executors port it 1:1. The prior `ui-redesign-dashboard` attempt failed at ~35% fidelity by *reinterpreting*; the verbatim CRM retrofit reached 89%. Fidelity gate this round: **≥95% pixel-match per screen** (D-10).

---

## 0. Golden Rule (verbatim-port mandate)

From `HANDOFF.md §2`:

> Use EXCLUSIVELY the tokens (`--ink-*`, `--paper-*`, `--rubric-*`, `--space-*`, `--fs-*`) and the classes (`.sb`, `.btn`, `.tg`/`.mx-tag`, `table.dict`, `.mx-*`, `.chip`, `.tabs`, etc.) as defined in the `.theme-editorial` block. Do NOT invent colors or sizes outside the scale. The semantic HTML of each mock translates 1:1 to JSX, keeping the classes identical.

**The canonical mocks are the visual source of truth and override any interpretation.** The 16 TSX components in `handoff/src/**` are VISUAL reference only — NOT drop-in (D-08). Port markup + classes onto the REAL components, preserving existing data/Supabase/server-actions/realtime wiring.

**Token source of truth for `globals.css` (HANDOFF §1):** the inline `<style>.theme-editorial{…}` block in `ui_kits/crm/crm-editorial.html` (most complete: tokens + `.mx-*` utilities + `.mx-tag--*` + layout components + `.dark`). The standalone `colors_and_type.css` is a near-identical reference; where the two differ, **the mock's inline block wins** because that is what the mocks actually render and what HANDOFF §1 designates as the globals.css source. The full token-by-token reconciliation of the two sources is in **§4 (Typography)** — every `--fs-*` and `.mx-*` weight value below was extracted from the canonical mock inline blocks (`crm-editorial.html`, `pedidos-editorial.html`, `conversaciones/index.html`) and annotated where it overrides `colors_and_type.css`.

---

## 0.5 Locked Verbatim-Port Exceptions (pre-approved — D-01 + D-10)

> **Read this before applying any generic design-linter limit to §3 (Spacing) or §4 (Typography).** The generic greenfield guards (max 4 font sizes, max 2 weights, spacing only from {4,8,16,24,32,48,64}) DO NOT apply to this standalone, by user decision. Annotated here so re-verification can assess the locked-values framing against dimension *intent* rather than against absolute greenfield caps.

### Why faithful reproduction is the success criterion (not scale-minimization)

This standalone is a **verbatim port of an externally-designed, user-approved design system** — the Claude Design handoff `design_handoff_morfx v2.1`. It is NOT a greenfield design where a researcher invents a minimal token set. The token values, type hierarchy, and spacing grid were authored by the designer, approved by the user, and shipped as `colors_and_type.css` + canonical HTML mocks.

The user locked two decisions in `CONTEXT.md` that govern this:

- **D-01 (big-bang adopt):** "Big-bang replace del bloque `.theme-editorial` actual… se adopta el nuevo sistema como base." The new system is adopted wholesale, not re-derived.
- **D-10 (≥95% pixel-fidelity verbatim port):** verification is side-by-side headless screenshots, ≥95% pixel-match per screen.

The prior `ui-redesign-dashboard` round *reinterpreted* the handoff instead of porting it verbatim and failed at **~35% fidelity** (UI-REVIEW BLOCK; documented in MEMORY). The verbatim CRM retrofit reached 89%. **Collapsing the type scale to 4 sizes / 2 weights, or dropping the 12px/96px spacing steps, would re-derive the system and recreate that fidelity failure** — i.e. it would directly violate D-10. Therefore the values below are deliberate, locked exceptions, accepted by the user, not open issues for the checker to resolve.

### Deliberate, locked exceptions to the generic linter limits

**1. 9-step type scale (exceeds the generic 4-size cap) — LOCKED.**
Sourced verbatim from the **canonical mock inline blocks** (`--fs-display:44px` … `--fs-micro:10px`), which win over `colors_and_type.css` where the two differ (see §4 reconciliation). Each step serves a distinct editorial role in the "Bible / dictionary" hierarchy and is mapped to a specific `.mx-*` utility in §4:

| Token | Value (mock-winning) | colors_and_type.css | Distinct editorial role |
|-------|----------------------|---------------------|-------------------------|
| `--fs-display` | 44px | 64px (overridden) | `.mx-display` — page-level editorial display headline |
| `--fs-h1` | 32px | 44px (overridden) | `.mx-h1` — primary heading |
| `--fs-h2` | 26px | 32px (overridden) | `.mx-h2` — section heading (also chrome topbar `h1`) |
| `--fs-h3` | 24px | 24px (agree) | `.mx-h3` — sub-section heading |
| `--fs-h4` | 20px | 19px (overridden) | `.mx-h4` — minor heading |
| `--fs-body` | 16px | 16px (agree) | `.mx-body` / `.mx-body-long` — running serif prose |
| `--fs-body-sm` | 14px | 14px (agree) | `.mx-ui` — dense UI chrome, table/button text |
| `--fs-caption` | 12px | 12px (agree) | `.mx-caption` — italic captions, mono phones/dates |
| `--fs-micro` | 10px | 11px (overridden) | `.mx-smallcaps` / `.mx-rubric` / `mx-tag` — small-caps labels & eyebrows |

Collapsing these violates D-10 (≥95% fidelity) — the dictionary aesthetic depends on the full hierarchy being visible simultaneously on one screen.

**2. 5 font weights (exceeds the generic 2-weight cap) — LOCKED.**
The editorial type system uses **800 / 700 / 600 / 500 / 400**, sourced verbatim from the canonical mock inline blocks (`crm-editorial.html` + `pedidos-editorial.html` both declare `.mx-display{font-weight:800}` — this overrides `colors_and_type.css`, which has `.mx-display{font-weight:700}`):
- **800** — `.mx-display` only (the heaviest editorial display weight). *(mock 800 overrides colors_and_type.css 700)*
- **700** — `.mx-h1`, chrome topbar `h1`, unread conversation names, kanban column titles.
- **600** — `.mx-h2/h3/h4`, `.mx-smallcaps`, `.mx-rubric`, table column labels, buttons, tags, contact-name cells.
- **500** — `.mx-ui`, `.mx-mono`, conversation preview (unread state).
- **400** — `.mx-body`, `.mx-body-long`, `.mx-caption`, default body text.

Each weight is load-bearing in the hierarchy; reducing to 2 weights flattens the distinction between display/heading/label/body/caption and breaks fidelity.

**3. Spacing tokens 12px (`--space-3`) and 96px (`--space-9`) (not in the generic {4,8,16,24,32,48,64} set) — LOCKED.**
Both are **exact multiples of 4** (12 = 3×4, 96 = 24×4) and are part of the handoff's complete 4-point baseline grid, verbatim from the mock `.theme-editorial` block. They occupy real roles (see §3): `--space-3` = card inner padding / rule margins / pager margin; `--space-9` = hero / full-bleed spacing. They are scale tokens, on-grid, and pre-approved.

**4. Non-multiple-of-4 values in §3's exceptions list are LAYOUT/COMPONENT DIMENSION CONSTANTS and FONT SIZES — the "multiple of 4" spacing rule does NOT apply to them.**
The values **84.6px, 51px, 61px, 246px, 340px / 300px, 120px, 9px, 11.2px** listed under §3 "Documented exceptions" are NOT spacing-scale tokens. They are pixel-exact layout/component **dimension constants** (band heights, grid-column widths, flex-basis, max-widths, logo height) and **font sizes** required for alignment fidelity with the mock:
- `84.6px` = `.brand` band height (aligns workspace-switcher band to each module header band).
- `51px` = `.ws` workspace-switcher height; `120px` = `.wm img` logo height.
- `61px` = `.conv-head` / `.th-head` column-header band height.
- `246px` = `.kcol` kanban-column flex-basis; `340px / 300px` = inbox grid column widths; `240px` = shell grid sidebar width.
- `9px` = chat-bubble mono timestamp font-size; `11.2px` = sidebar `.cat` font-size.

The "multiples of 4 only" guard is a *spacing-scale* rule (margins/padding/gaps between elements). These are **dimensions of components**, drawn pixel-exact from the mock to hit ≥95% pixel-match. Rounding them to multiples of 4 would misalign the ported render against the canonical mock and fail D-10. They are intentionally and explicitly off-grid because the mock authored them that way.

### Status: accepted, not open

All exceptions above are **pre-approved by the user** via D-01 (big-bang adopt the handoff system) + D-10 (≥95% verbatim pixel-fidelity) in `CONTEXT.md`. They are recorded here as accepted locked values, not as unresolved findings. The checker dimensions 4 (Typography) and 5 (Spacing) should be assessed against this locked-port intent. No token values are to be changed — changing them breaks fidelity.

---

## 1. Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present, new-york preset). **This reskin does NOT re-init or change the preset.** |
| Preset | new-york (existing) — not applicable to this reskin |
| Component library | Radix (via shadcn) — kept; primitives are re-skinned via scoped CSS, not replaced |
| Icon library | Lucide React. **Pin a version, never `@latest`** (CHANGELOG §11). Treat icons as engraved hairline marks: stroke **1.5px** (not Lucide default 2px), size 16px UI / 20px nav / 14px in badges, color `currentColor` (never tinted), 8px gap to text |
| Fonts | **Already loaded** (D-03) — `src/app/(dashboard)/fonts.ts` exports EB Garamond (`--font-ebgaramond`), Inter (`--font-inter`), JetBrains Mono (`--font-jetbrains-mono`) via `next/font`. No new font work. Cormorant Garamond intentionally NOT loaded (cascade falls to Times/Georgia). |

### 1.1 Type families (cascade order — verbatim from mock)

```css
--font-display:   var(--font-ebgaramond), 'Cormorant Garamond', 'Times New Roman', Georgia, serif;
--font-serif:     var(--font-ebgaramond), Georgia, 'Times New Roman', serif;
--font-sans:      var(--font-inter), system-ui, -apple-system, 'Helvetica Neue', Arial, sans-serif;
--font-mono:      var(--font-jetbrains-mono), ui-monospace, 'SF Mono', Menlo, Consolas, monospace;
--font-small-caps: var(--font-ebgaramond), Georgia, serif;
```

Role assignment: **EB Garamond** carries display/headings/body/small-caps/italic. **Inter** is UI chrome only (table column labels, buttons, input text, sidebar, tags). **JetBrains Mono** for phone numbers, IDs, totals, timestamps.

---

## 2. Theme Isolation Scope (D-05 — CRITICAL)

**This section is part of the visual contract: every ported component carries the scope class named here.**

### 2.1 The problem (grounded in real code)

- Conversaciones editorial v2 is **LIVE in production** in Somnio under the CSS class `.theme-editorial`, gated by flag `workspaces.settings.ui_inbox_v2.enabled = true` (`src/lib/auth/inbox-v2.ts`).
- The current `.theme-editorial` block in `src/app/globals.css` (lines ~134–281) uses the **OLD warm-cream palette** (`--paper-0: oklch(0.995 0.008 85)`) and is **light-only** (`color-scheme: light`, dark explicitly out of scope per the v2 UI-SPEC).
- A second flag `workspaces.settings.ui_dashboard_v2.enabled` (`src/lib/auth/dashboard-v2.ts`, OFF in Somnio) ALSO applies the same `.theme-editorial` class at `(dashboard)/layout.tsx:54`.
- The new mock palette is **DIFFERENT**: `--paper-0: oklch(1 0 0)` (pure white paper), `--bg-app: oklch(0.996 0.0008 95)`, plus a full `.theme-editorial.dark { … }` override.

**A big-bang in-place replace of the `.theme-editorial` block (D-01) would mutate the tokens Conversaciones-v2-live renders against → instant production regression in Somnio.** This violates Regla 6.

### 2.2 The contract: new system lives under a DISTINCT scope class

> **Scope class for this reskin: `.theme-editorial-v3`**

The new token block + all `.mx-*` / `.mx-tag--*` / `.sb` / `.btn` / `table.dict` / `.tabs` / `.chip` / inbox / kanban component rules are authored under `.theme-editorial-v3` (NOT `.theme-editorial`). The legacy `.theme-editorial` block stays byte-untouched so `ui_inbox_v2`-live Somnio keeps rendering exactly as today until an explicit flip.

- Every ported component's root/section carries `theme-editorial-v3` (applied at the `(dashboard)/layout.tsx` wrapper, gated by the new flag — same integration point as the existing two flags).
- The mock CSS selectors `.theme-editorial X` are ported as `.theme-editorial-v3 X` verbatim (1:1 selector rename, no other change).
- **Exact isolation mechanics (whether the v3 scope also retires the two legacy flags at flip time, and how `(dashboard)/layout.tsx` chooses between `.theme-editorial` and `.theme-editorial-v3`) are research/plan discretion (D-05).** The contract only fixes that the new tokens live under `theme-editorial-v3` and that the legacy block is not edited.

### 2.3 Flag (D-04 — not a visual concern, recorded for completeness)

New per-workspace flag, default **OFF**, manual SQL flip after user QA — mirror `ui_inbox_v2.enabled` exactly. Suggested JSONB path `workspaces.settings.ui_editorial_v3.enabled` (final name = plan discretion). Fails closed to `false` (Regla 6). If the flag lives in a new column/table → migration applied in prod BEFORE pushing code that reads it (Regla 5). The flag does not change any visual value in this spec.

### 2.4 Dark-mode wiring contract (D-02)

The mocks use the **compound** selector `.theme-editorial.dark` (dark class ON THE SAME ELEMENT as the theme scope) — this is real dark mode, NOT the legacy `.dark .theme-editorial` defensive pattern (globals.css:309, which only re-asserts light shadcn tokens). For v3:

- Author overrides as **`.theme-editorial-v3.dark { … }`** (compound, same element).
- Map `next-themes` `theme === 'dark'` → toggle the `dark` class onto the same root element that carries `theme-editorial-v3` (mapping mechanism = discretion, D-02/Discretion).
- Logo rule (verbatim): `.theme-editorial-v3.dark .wm img { mix-blend-mode: screen; filter: invert(1) hue-rotate(180deg); }`. In light: `.wm img { mix-blend-mode: multiply; }`.

---

## 3. Spacing Scale

Verbatim from mock `.theme-editorial` block. 4-point baseline grid. **No px outside this scale** (golden rule).

> **Linter note:** `--space-3` (12px) and `--space-9` (96px) are intentional locked exceptions to the generic {4,8,16,24,32,48,64} set — both are multiples of 4 and verbatim from the handoff. The "Documented exceptions" block below lists LAYOUT/COMPONENT DIMENSION CONSTANTS and FONT SIZES, NOT spacing-scale tokens, so the "multiple of 4" rule does not apply to them. See **§0.5 Locked Verbatim-Port Exceptions** for the full justification (pre-approved via D-01 + D-10).

| Token | Value | Usage |
|-------|-------|-------|
| `--space-1` | 4px | Icon gaps, tag inner gap, dropcap pad |
| `--space-2` | 8px | Compact element spacing, sidebar/nav padding, button gap |
| `--space-3` | 12px | Card inner padding, rule margins, pager margin |
| `--space-4` | 16px | Default element spacing, list-row padding (conv) |
| `--space-5` | 24px | Section padding |
| `--space-6` | 32px | Layout gaps |
| `--space-7` | 48px | Major section breaks / outer gutter |
| `--space-8` | 64px | Page-level spacing |
| `--space-9` | 96px | Hero / full-bleed spacing |

**Documented exceptions (verbatim from mocks — these are literal LAYOUT/COMPONENT DIMENSION CONSTANTS and FONT SIZES, NOT spacing tokens; the "multiple of 4" rule does not apply — see §0.5):**
- Sidebar `.brand` height **84.6px** (standard — centers the workspace switcher band with each module's header band). Keep in ALL views.
- Workspace switcher `.ws` height **51px**; logo `.wm img` height **120px**.
- Inbox grid columns **340px / 1fr / 300px**; shell grid **240px / 1fr**.
- Column header bands `.conv-head` / `.th-head` height **61px**.
- Kanban column `.kcol` flex-basis **246px**.
- Chat bubble `.msg` max-width **64%**; `border-bottom-{left|right}-radius: 1px` (the "tail" notch).
- Sidebar `.cat` font-size **11.2px** (literal).
- Radii are NOT spacing but are paper-tight: `--radius-0:0 / -1:2px / -2:3px / -3:4px (default) / -pill:999px`. Nothing rounds > 4px except pills/avatars.

---

## 4. Typography

**Source-of-truth reconciliation (per §0 hierarchy: canonical mock inline block primary, `colors_and_type.css` fallback).** Every value below was extracted from the mock inline `<style>` blocks in `crm-editorial.html`, `pedidos-editorial.html`, and `conversaciones/index.html`. The three mock blocks agree with each other on all `--fs-*` and `.mx-*` weight values. Where a mock value differs from `colors_and_type.css`, the mock wins (because the mocks are what the ported components render against — HANDOFF §1) and the divergence is annotated inline.

```css
/* --fs-* — values that WIN are the mock-inline values: */
--fs-display:44px;  /* mock 44px overrides colors_and_type.css 64px */
--fs-h1:32px;       /* mock 32px overrides colors_and_type.css 44px */
--fs-h2:26px;       /* mock 26px overrides colors_and_type.css 32px */
--fs-h3:24px;       /* agree (mock = CSS = 24px) */
--fs-h4:20px;       /* mock 20px overrides colors_and_type.css 19px */
--fs-body:16px;     /* agree */
--fs-body-sm:14px;  /* agree */
--fs-caption:12px;  /* agree */
--fs-micro:10px;    /* mock 10px overrides colors_and_type.css 11px */
--lh-tight:1.08; --lh-display:1.05; --lh-heading:1.20; --lh-body:1.55; --lh-long:1.70;
```

> **Linter note:** This is a **9-step type scale with 5 weights (800/700/600/500/400)**, deliberately exceeding the generic 4-size / 2-weight caps. Both are locked, sourced verbatim from the canonical mock inline blocks (which override `colors_and_type.css` where they differ — see reconciliation above); each step and weight serves a distinct editorial role, and collapsing them violates the D-10 ≥95% fidelity gate. See **§0.5 Locked Verbatim-Port Exceptions** for the per-token role mapping and the user pre-approval (D-01 + D-10).

Canonical roles (the `.mx-*` utility classes — port verbatim under `.theme-editorial-v3`). Size/weight values below are the mock-winning values; inline notes flag every divergence from `colors_and_type.css`:

| Role (class) | Family | Size | Weight | Line-height | Notes |
|------|--------|------|--------|-------------|-------|
| `.mx-display` | display (EB Garamond) | 44px *(mock overrides CSS 64px)* | 800 *(mock 800 overrides colors_and_type.css 700)* | 1.05 | letter-spacing −0.02em |
| `.mx-h1` | display | 32px *(mock overrides CSS 44px)* | 700 | 1.20 | −0.015em |
| `.mx-h2` | display *(mock uses font-display; CSS uses font-serif — mock wins)* | 26px *(mock overrides CSS 32px)* | 600 | 1.20 | −0.015em |
| `.mx-h3` | display | 24px | 600 | 1.20 | −0.01em |
| `.mx-h4` | display | 20px *(mock overrides CSS 19px)* | 600 | 1.20 | −0.01em |
| `.mx-body` | serif | 16px | 400 | 1.55 | color ink-2 |
| `.mx-body-long` | serif | 16px | 400 | 1.70 | running prose |
| `.mx-caption` | serif italic | 12px | 400 | 1.4 | ink-3 |
| `.mx-smallcaps` | EB Garamond | 10px (`--fs-micro`, mock overrides CSS 11px) | 600 | 1 | uppercase, tracking 0.12em, ink-3 — does the "label" work |
| `.mx-rubric` | sans (Inter) | 10px *(mock overrides CSS 11px)* | 600 | — | uppercase, tracking 0.08em, **rubric-2** (red eyebrow) |
| `.mx-ui` | sans | 14px | 500 | — | dense UI chrome, tracking 0.01em |
| `.mx-mono` | mono (JetBrains) | 13px | 500 | — | phones, IDs, totals |

> **`.mx-display` weight resolution (revision spot-check):** the canonical mock inline blocks `crm-editorial.html` and `pedidos-editorial.html` both declare `.mx-display{font-weight:800}`; `colors_and_type.css` declares `font-weight:700`. Per §0, the mock wins → **800**. The weight list therefore stays at 5 (800/700/600/500/400). Executors must port `font-weight:800` for `.mx-display`, NOT 700.

**Chrome (non-`.mx-`) type — note the deliberate exception (verbatim from mocks):** topbar `h1` is **Inter 26px / 700** (sans, not serif); the `<em>` subtitle is Inter 15px / 400 / ink-3. The eyebrow `.eye` is Inter 11px / 600 / uppercase / 0.14em / rubric-2. This is intentional — operational chrome stays sans; serif is for editorial content. Do not "fix" the topbar to serif.

**Casing & content (README content fundamentals):** Sentence case everywhere (no Title Case, no ALL CAPS except small-caps treatment). es-CO. Em-dash `—` as subtitle separator (not colon), e.g. `Conversaciones — Bandeja de entrada`. Old-style figures (`onum`) in serif prose; lining figures in tables/KPIs. Colombian punctuation `1.234.567,50`. Dates es-CO (`21 may`, `2026-06-03`). Emoji ONLY as data (order-stage dots/avatars), never in chrome/nav/labels.

---

## 5. Color

Verbatim from mock `.theme-editorial` token block (LIGHT). The full ink/paper/rubric/accent scale below replaces the legacy block under the new `.theme-editorial-v3` scope.

### 5.1 Light palette (the 60/30/10 contract)

| Role | Token → value | Usage |
|------|-------|-------|
| **Dominant (60%)** | `--bg-app: oklch(0.996 0.0008 95)` / `--paper-1: oklch(0.998 0.0006 85)` | App + page background. White-paper neutral, NOT beige (verification checklist §5). Sidebar = same as app (continuous, no separator bar): `--bg-sidebar` = `--bg-app`. |
| **Secondary (30%)** | `--paper-0: oklch(1 0 0)` (cards, inputs, table body, kanban cards, bubbles-in), `--paper-2` (hover/sunken), `--paper-3` (active row / btn hover) | Cards one step *brighter* than the page (paper-0). Recessed/hover one step darker. |
| **Accent (10%)** | `--rubric-2: oklch(0.55 0.10 30)` (standard rubric red), `--rubric-1: oklch(0.45 0.09 28)` (deep) | RESERVED — see reserved-for list below. |
| **Destructive** | `--destructive: var(--rubric-2)` | Destructive actions only (= rubric). No separate red. |

Ink scale (foreground — never pure black): `--ink-1: oklch(0.18 0.02 60)` (primary), `--ink-2: 0.32` (body), `--ink-3: 0.48` (quiet/secondary labels), `--ink-4: 0.62` (faded annotation), `--ink-5: 0.78` (very faint / scrollbar thumb). `--border: oklch(0.80 0.025 72)`; `--border-strong / --rule = ink`.

Color-ink accents (used as INK only, sparse, never as fills/brand): `--accent-verdigris: oklch(0.52 0.035 180)`, `--accent-gold: oklch(0.68 0.055 80)`, `--accent-indigo: oklch(0.42 0.045 260)`. Semantics: `--semantic-success: oklch(0.50 0.08 145)`, `--semantic-warning: oklch(0.58 0.12 65)`, `--semantic-danger = rubric-2`, `--semantic-info = accent-indigo`.

**shadcn semantic mapping (CRITICAL — verbatim):** `--primary: var(--ink-1)` (ink, NOT rubric — binding primary to rubric floods the UI with accent and breaks 60/30/10), `--primary-foreground: var(--paper-0)`, `--background: var(--paper-1)`, `--card: var(--paper-0)`, `--secondary/--muted: var(--paper-2)`, `--muted-foreground: var(--ink-3)`, `--accent: var(--paper-3)`, `--border/--input: oklch(0.80 0.025 72)`, `--ring: var(--ink-1)`, `--radius: var(--radius-3)`.

### 5.2 Accent reserved for (rubric-2 ONLY — explicit list, never "all interactive")

1. `.eye` eyebrow text in topbar.
2. `.mx-rubric` red small-caps eyebrows.
3. Sidebar `.cat` category bullet (`::before` 5px red dot).
4. Active conversation left spine (`.conv.on::before` 2px rubric bar).
5. Unread count badge (`.conv .badge` background).
6. `.mx-dropcap::first-letter` color.
7. Destructive action affordance (= `--destructive`).
8. `mx-tag--rubric` pill (the only red tag).
9. Logo dot / wordmark accent (`.wm b`, the "·"/x mark).
10. Rubric-red links in running prose (double-underline on hover).
11. Section ornaments where rubric is specified.

No flat rubric fills, no rubric on generic buttons (primary button = ink-1 fill).

### 5.3 Dark palette (`.theme-editorial-v3.dark` — D-02, verbatim)

```css
.theme-editorial-v3.dark{
  --bg-app:oklch(0.215 0.006 60); --bg-sidebar:oklch(0.215 0.006 60);
  --paper-0:oklch(0.255 0.006 60); --paper-1:oklch(0.235 0.006 60);
  --paper-2:oklch(0.285 0.007 60); --paper-3:oklch(0.315 0.008 60); --paper-4:oklch(0.355 0.009 60);
  --ink-1:oklch(0.95 0.006 85); --ink-2:oklch(0.86 0.008 85); --ink-3:oklch(0.70 0.010 80);
  --ink-4:oklch(0.56 0.010 75); --ink-5:oklch(0.42 0.010 70);
  --border:oklch(0.37 0.008 70); --rubric-2:oklch(0.64 0.11 30); --rubric-1:oklch(0.72 0.10 30);
  --paper-grain:none; background-color:var(--bg-app);
}
.theme-editorial-v3.dark .wm img{mix-blend-mode:screen;filter:invert(1) hue-rotate(180deg)}
```

Charcoal-warm ("Claude-style") — warm grays, ink inverts to near-white, paper-grain disabled, rubric brightened. Logo: white letter + red dot, no box.

### 5.4 Paper texture (DEFERRED to discretion)

The mock applies `--paper-grain` (subtle SVG noise) on the scope root; `--paper-fibers` exists in `colors_and_type.css`. CHANGELOG §7 confirms grain belongs on root/body via the scope, NOT on individual cards/canvases. **Per Deferred Ideas, evaluate Safari-retina perf before applying grain to the root** (the legacy block already commented grain out for this reason — globals.css:268). Default to background-color only; grain is a plan-discretion enhancement gated on a perf check. Dark mode disables grain (`--paper-grain:none`).

---

## 6. Per-Screen Visual Contracts

Each screen ports the named mock 1:1. Distinctive elements that MUST survive the port:

### 6.1 WhatsApp · Conversaciones → `ui_kits/conversaciones/index.html`
Real target: `src/app/(dashboard)/whatsapp/components/**` + `whatsapp/page.tsx`.

- **3-column inbox** grid `340px / 1fr / 300px` (`.inbox`): list `.conv-col` | thread `.thread` | contact `.ficha`.
- **Conversation row** `.conv`: grid `40px / 1fr / auto`, 40px circular `.av` (EB Garamond initials), name (Inter 13/600, unread → 700), preview (`.pv` ink-3, unread → ink-2/500), right meta column (mono `.tm` timestamp + rubric `.badge` unread count), `.row3` for agent pill + tags. Active row: `.conv.on` paper-3 bg + 2px rubric left spine.
- **Filter chips** `.chip` row (Todas/Sin leer/Mías/Agente IA/Cerradas); active `.chip.on` = ink-1 fill.
- **Chat bubbles `.msg` in `font-family:'Helvetica Neue', Helvetica, Arial, var(--font-sans)`, 14px / 1.45** (verification checklist §5 — bubbles are Helvetica Neue, NOT serif). `.msg.in` = paper-0 + border, left-aligned, `border-bottom-left-radius:1px`; `.msg.out` = ink-1 fill / paper-0 text, right-aligned, `border-bottom-right-radius:1px`; `.msg.out.agent` = ink-2 bg + uppercase `.agent-mark` label; mono timestamp `.tm` 9px opacity .65; `.tmpl` template marker mono 9px opacity .5. Day separator `.daysep` pill.
- **Composer** `.composer`: serif `.field` placeholder + ink `.btn.pri` "Enviar".
- **Contact card `.ficha`**: 64px `.av-lg`, centered name/phone, `.ficha-actions` (Ver en CRM / Tarea), sections `.sect` with uppercase `h3` eyebrows + hairline top border, `dl/dt/dd` data grid (right-aligned dd), tag cloud, **order card `.ped-card` rendered as a card** (border + `--shadow-card`, `.src` pill + mono `.val`, footer tags), `.ped-vertodos` underlined "Ver todos", `.btn.pri` "Crear pedido", internal `.note` with 2px ink left border.
- **Topbar**: eyebrow "Agentes · Bandeja", `h1` "Conversaciones" + `<em>` count subtitle; actions "Asignar" + `.btn.pri` "Nueva conversación".
- Note: `src/app/(dashboard)/whatsapp/components/mx-tag.tsx` already uses `mx-tag--*` — keep it; ensure it resolves under the v3 scope.

### 6.2 CRM · Contactos → `ui_kits/crm/crm-editorial.html`
Real target: `src/app/(dashboard)/crm/contactos/**`.

- **`table.dict`**: full width, `border-collapse`, `background:--paper-0`, **outer `1px solid --ink-1`** (the heavy dictionary frame). `thead th`: Inter 10px / 600 / uppercase / 0.08em / ink-3, `--paper-1` bg, `1px solid --ink-1` bottom border. `td`: Inter 13px / ink-1, `1px solid --border` bottom; row hover → paper-2. Cell variants: `.entry` (600 weight, contact name), `.ph` (mono 12px, phone/counts), `.city` (ink-3 400), `.date` (mono 12px ink-3).
- **Toolbar**: search input (`.search`, max 320px, 30px left pad for icon) + filter chips (Todos/Con pedido/Sin actividad 30d/VIP).
- **Tabs** `.tabs`: Todos/Clientes/Leads/Mayoristas/Archivados, active `.on` = ink-1 text + 2px ink bottom border.
- **Pager** `.pager`: mono "1–8 de 4.281" + Anterior/Siguiente `.btn`.
- **Topbar**: eyebrow "CRM · Directorio", `h1` "Contactos" + count, actions Importar/Exportar/`.btn.pri` "Nuevo contacto".
- Tags in the Tags column use the official `mx-tag--*` system (§7), NOT legacy `.tg.*`.

### 6.3 CRM · Pedidos (tabla + Kanban) → `ui_kits/pedidos/pedidos-editorial.html`
Real target: `src/app/(dashboard)/crm/pedidos/**`.

- **View toggle** `.vtoggle` (Tabla / Tablero), active `.on` = ink fill — lives at right of toolbar.
- **Kanban `.board`** (flex, horizontal scroll, thin scrollbar): columns `.kcol` flex-basis **246px**, separated by **`border-left:1px solid --border` hairlines between stages — NO boxes** (`.kcol:first-child` has no left border). This is the signature: stages are divided by vertical hairlines, not bordered boxes.
- **Column head** `.kcol-head`: colored stage `.dot` (8px) + uppercase title `.t` (Inter 11/700/0.08em) + mono count `.c`. Dot color classes by stage: `.agend`=verdigris, `.web`=gold, `.nuevo`=ink-4, `.info`=indigo, `.conf`=gold, `.ok`=success.
- **Cards `.kcard` are loose** (no column container box): paper-0 + border + `--shadow-card`, hover → ink-3 border. Card anatomy: `.top` (name `.nm` with `◉` `.ci` mark + mono `.val` total), `.prod` (product line with `▢` mark, ink-3), `.tags` (mx-tag pills), `.foot` (top hairline border + mono `.date` + `↻`/`○` icon marks + right-aligned `.city`).
- **Empty columns show `.kempty` = serif italic "Sin pedidos"** (ink-4, centered, 24px pad) — verification checklist §5.
- **Pipeline bar `.pipes`** at bottom: pipeline chips, active `.pp.on` = ink border + 600.
- **Table view** `table.dict` (same as Contactos) with columns Pedido/Cliente/Productos/Total/Estado/Fecha; `.entry` = order # ; status cell uses mx-tag. Toggled via `.vtoggle`.
- **Topbar**: eyebrow "CRM · Pedidos", `h1` "Pedidos" + "312 este mes", actions Exportar/`.btn.pri` "Crear pedido".
- Tabs: Todos/Pendientes/Confirmados/Despachados/Entregados/Cancelados.

---

## 7. Official Tag System `mx-tag--*` (D-09)

**Replace legacy `.tg.red/.gold/.indi/.ver` (and the kanban-local `.tg.pw/.reco/.c/.ink`) with the official token-built `mx-tag` classes** when porting (CHANGELOG §6). Legacy mocks keep `.tg.*` only to preserve their own snapshots — production must use `mx-tag--*`.

```css
.mx-tag{display:inline-flex;align-items:center;gap:var(--space-1);font-family:var(--font-sans);
  font-weight:600;font-size:var(--fs-micro)/*10px*/;letter-spacing:0.01em;padding:2px 8px;
  border-radius:var(--radius-pill);border:1px solid transparent;white-space:nowrap}
```

| Class | Background | Text | Border | Maps legacy |
|-------|-----------|------|--------|-------------|
| `mx-tag--rubric` | `color-mix(in oklch, var(--rubric-2) 10%, var(--paper-0))` | `var(--rubric-1)` | `…rubric-2 40%…` | `.tg.red` (Pendiente/Por pagar) |
| `mx-tag--gold` | `color-mix(… --accent-gold 14% …)` | `color-mix(… --accent-gold 60%, --ink-1)` | `…gold 45%…` | `.tg.gold` (Cliente/Pagado/Confirmado) |
| `mx-tag--indigo` | `color-mix(… --accent-indigo 10% …)` | `var(--accent-indigo)` | `…indigo 40%…` | `.tg.indi` (Lead/Entregado), `.tg.pw` (P/W) |
| `mx-tag--verdigris` | `color-mix(… --accent-verdigris 10% …)` | `var(--accent-verdigris)` | `…verdigris 40%…` | `.tg.ver` (Mayorista/Recompra/WPP/Despachado) |
| `mx-tag--ink` | `var(--paper-0)` | `var(--ink-2)` | `var(--ink-3)` | `.tg` default / `.tg.ink` (Cancelado) |

Note: `colors_and_type.css` defines all 5 variants; the mock inline block omits `verdigris` — author all 5 under `.theme-editorial-v3` (verdigris is needed for Mayorista/Recompra/WPP). The reference `MxTag` component (`whatsapp/components/mx-tag.tsx`) already wraps `mx-tag--{variant}` for 5 variants — reuse this component pattern for CRM/Pedidos tags (do not re-introduce CVA).

Kanban-specific legacy tokens map: `P/W` → `mx-tag--indigo`, `RECO` → `mx-tag--indigo`, `C` (confirmado) → a success-tinted variant (mock used `color-mix(--semantic-success …)`). If a success tag is needed beyond the 5, author it as an additive `mx-tag--success` using the same `color-mix` recipe over `--semantic-success` (consistent with §6 construction); otherwise reuse `mx-tag--gold`. Plan discretion, but it MUST be `color-mix`-over-token, never hardcoded oklch.

---

## 8. Shared Chrome Components (ported under `.theme-editorial-v3`)

Buttons `.btn` (Inter 13/600, `1px solid ink`, paper-0, `box-shadow:0 1px 0 ink`, hover→paper-3; `.btn.pri` = ink fill / paper-0 text, press → `translateY(1px)` + lose shadow). Inputs/search (paper-0, `1px solid border`, focus → `border-color:ink-1`, radius-3). Chips `.chip` (pill, active = ink fill). Icon buttons `.icon-btn` (32px square). Tabs `.tabs` (ink underline active). Hairline rules `.mx-rule` (1px ink-2), `.mx-rule-double` (3px double), `.mx-rule-thick` (2px ink). Thin scrollbars (7–8px, `--ink-5` thumb, **no arrow buttons** — verification checklist §5). Animation minimal: 120ms ease-out color/opacity; 180ms cubic-bezier(0.2,0,0,1) position. No skeleton pulse — static italic small-caps "Cargando…".

**Sidebar (`.sb`, `.brand`, `.ws`, `.cat`, etc.) is DEFERRED (D-06)** — do NOT reskin it this round even though the mocks include it. It is global (affects all 9 modules). The `.brand` 84.6px / `.ws` 51px constants are documented here only because the 3 content areas' header bands align to them; the content-area headers must keep that vertical alignment, but the sidebar chrome itself is a follow-up round.

---

## 9. Copywriting Contract

es-CO, sentence case, em-dash separators, periods on sentences, no emoji in chrome. Reskin preserves existing real copy where present; the mock copy below is the canonical reference for tone/format.

| Element | Copy |
|---------|------|
| Primary CTA — Conversaciones | "Nueva conversación" (secondary: "Asignar") |
| Primary CTA — Contactos | "Nuevo contacto" (secondary: "Importar", "Exportar") |
| Primary CTA — Pedidos | "Crear pedido" (secondary: "Exportar") |
| Section header pattern | `{Módulo} — {subtítulo}` em-dash, e.g. "Conversaciones — Bandeja de entrada"; eyebrow uppercase small-caps "CRM · Directorio" / "CRM · Pedidos" / "Agentes · Bandeja" |
| Empty state — Pedidos kanban column | "Sin pedidos" (serif italic, ink-4, centered — `.kempty`) |
| Empty state — Conversaciones | "No hay conversaciones nuevas." (period, no emoji, no illustration) |
| Empty state — filtered list | typographic, no illustration (CHANGELOG §10 pattern) |
| Loading state | static italic small-caps "Cargando…" (NO pulsing skeleton) |
| Error state | complete sentence + period, e.g. "No fue posible enviar el mensaje." / "El canal no está disponible." |
| Destructive — Cerrar chat | "Cerrar chat" — existing real confirm flow preserved; styled via `--destructive` (rubric). No new destructive flows introduced (reskin only). |
| Destructive — Cancelar pedido | uses `mx-tag--ink` "Cancelado" status; existing confirm flow preserved. |

This is a reskin: NO new copy strings are introduced beyond matching the mock's labels to existing functionality. Where the real component already has a Spanish label, keep it; only restyle.

---

## 10. Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Existing project primitives only (Radix/new-york); re-skinned via scoped CSS, none added by this reskin | not required |
| third-party | none | not applicable |

No third-party registries declared. No `npx shadcn add` of new blocks is part of this reskin — it restyles existing components.

---

## 11. Fidelity Verification Gate (D-10)

Verification is **side-by-side headless screenshots (mock vs real render), ≥95% pixel-match per screen**, running the `HANDOFF.md §5` checklist before declaring done. Run per screen (Conversaciones, CRM Contactos, CRM Pedidos) AND in both light + dark.

HANDOFF §5 checklist (adapted to this round's scope — sidebar items are deferred but the workspace-switcher alignment still applies to content-area headers):
- [ ] Background white-paper neutral (NOT beige) with subtle grain (if grain enabled); sidebar continuous.
- [ ] Logo correct in light (red dot) and dark (white letter, red dot, no box). *(sidebar deferred — verify only that the v3 dark logo rule exists)*
- [ ] Content-area header bands (search / contact header / topbar divider) align to the 84.6px workspace-switcher standard.
- [ ] Header / tabs divider lines centered with the switcher.
- [ ] Tags (`mx-tag--*`: Cliente, P/W, RECO, C, etc.) aligned and using scale colors (NOT legacy hardcoded `.tg.*`).
- [ ] Kanban: hairlines between stages, no boxes; loose cards; "Sin pedidos" in empty columns.
- [ ] Chat bubbles in Helvetica Neue; order card renders as a card.
- [ ] Thin scrollbars, no arrows; no improper horizontal scroll.
- [ ] `.mx-display` renders at **font-weight 800** (mock-winning value, NOT the CSS 700) — type-token reconciliation per §4.
- [ ] Dark mode charcoal-warm with persistent toggle.

**Regression guard (Regla 6 / D-05):** before flip, confirm Somnio's `ui_inbox_v2`-live Conversaciones renders byte-identical to today (legacy `.theme-editorial` block untouched; new tokens live only under `.theme-editorial-v3`).

---

## 12. Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS *(assess against §0.5 locked-port exceptions — 9 sizes / 5 weights are pre-approved via D-01 + D-10, NOT a violation; type-token values reconciled to canonical mock source-of-truth in §4, mock overrides annotated per token)*
- [ ] Dimension 5 Spacing: PASS *(assess against §0.5 locked-port exceptions — 12px/96px are on-grid multiples of 4; the listed off-grid values are component dimensions/font sizes, not spacing tokens)*
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
