---
phase: 260611-w1a-contactos-v3-crud-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/crm/contactos/components/contacts-table.tsx
autonomous: true
requirements: [C-1, C-2, C-3]

must_haves:
  truths:
    - "En la tabla v3 (table.dict) cada fila tiene un menú de acciones con Ver detalles / Editar / Eliminar"
    - "El operador puede filtrar contactos por CUALQUIER tag del workspace (no solo las 4 tabs hardcodeadas) desde la toolbar v3"
    - "El operador puede abrir el gestor de etiquetas (TagManager) desde la toolbar v3"
    - "Editar abre el ContactDialog precargado; Eliminar pide confirmación y borra; Ver detalles navega al detalle"
    - "Las ramas legacy (shadcn DataTable) y v2 (ContactsViewV2) quedan byte-idénticas (Regla 6)"
  artifacts:
    - path: "src/app/(dashboard)/crm/contactos/components/contacts-table.tsx"
      provides: "Branch v3 con dropdown de acciones por fila, chip+popover de tags dinámico, y botón Gestionar etiquetas"
      contains: "tagManagerOpen"
  key_links:
    - from: "table.dict row dropdown (v3)"
      to: "onEdit/onDelete/onViewDetail handlers existentes en contacts-table.tsx"
      via: "reuso directo de la lógica del memo `columns` (setEditingContact+setDialogOpen / deleteContact+confirm / router.push detalle)"
      pattern: "setEditingContact|deleteContact\\(|/crm/contactos/"
    - from: "chip+popover de tags (v3)"
      to: "handleTagSelectionChange(tagIds) + currentTagIds"
      via: "mismo URL state que el TagFilter legacy (?tags=id,id)"
      pattern: "handleTagSelectionChange"
    - from: "botón Gestionar etiquetas (v3)"
      to: "setTagManagerOpen(true) → <TagManager> ya montado (L527-531)"
      via: "wiring del trigger que faltaba"
      pattern: "setTagManagerOpen\\(true\\)"
---

<objective>
Restaurar en la rama editorial v3 de CRM Contactos tres capacidades del UI legacy que se perdieron al reescribir el markup como `table.dict`:

- **C-1** — Menú de acciones por fila (Ver detalles / Editar / Eliminar).
- **C-2** — Filtro dinámico por cualquier tag del workspace (chip + popover estilo inbox v3), conservando las 4 tabs/chips de acceso rápido (Todos/Clientes/Leads/Mayoristas).
- **C-3** — Acceso al gestor de etiquetas (TagManager) desde la toolbar v3.

Purpose: Sin estas tres, la tabla v3 no es operable a diario (toda edición/borrado exige entrar al detalle, y workspaces con tags propios no pueden filtrar). Es la Wave 1 de la auditoría de paridad v3.

Output: `contacts-table.tsx` con el branch `v3` ampliado de forma aditiva. Las ramas legacy y v2 intactas. El flag `ui_editorial_v3` sigue OFF en prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- Extraído del codebase. El executor usa esto directamente — no explorar. -->

Estado y handlers YA existentes en contacts-table.tsx (no recrear, reusar):
- `const [tagManagerOpen, setTagManagerOpen] = React.useState(false)`  (L66)
- `const [dialogOpen, setDialogOpen]`, `const [editingContact, setEditingContact]`  (L64-65)
- `handleTagSelectionChange(tagIds: string[])` → escribe `?tags=id,id` en la URL  (L99-102)
- `currentTagIds: string[]` (prop) — el filtro de tags YA es multi-select a nivel de estado
- En el memo `columns` (L122-144) están los 3 handlers de fila:
    onEdit:       setEditingContact(contact); setDialogOpen(true)
    onDelete:     confirm(...) → await deleteContact(contact.id) → toast + router.refresh()
    onViewDetail: router.push(`/crm/contactos/${contact.id}`)
  (El memo `columns` solo lo consume el DataTable legacy. Para v3 hay que invocar la MISMA lógica desde el dropdown de la fila — extraer a callbacks reutilizables o inline.)
- `<TagManager open={tagManagerOpen} onOpenChange={setTagManagerOpen} tags={tags} />` YA montado (L527-531) — solo falta disparar setTagManagerOpen(true).
- `deleteContact` ya importado desde `@/app/actions/contacts`; `toast` de sonner; `router` de next/navigation.

Patrón de tabla v3 (table.dict): filas en L461-487. La última `<th>` es "Última actividad"; añadir un `<th>` vacío al final para la columna de acciones.

Patrón portal-scoping para Radix bajo `.theme-editorial-v3` (clonar de conversation-list.tsx L60-99):
- `const themeContainerRef = useRef<HTMLElement | null>(null)`
- `useEffect(() => { themeContainerRef.current = document.querySelector('.theme-editorial-v3') as HTMLElement | null }, [])`
  (el `<main>` del dashboard layout lleva `.theme-editorial-v3` — layout.tsx L99-103; NO hay data-module en CRM, usar el selector de clase).
- DropdownMenuContent y PopoverContent aceptan `portalContainer={themeContainerRef.current ?? undefined}` (ya soportado en src/components/ui/dropdown-menu.tsx L37-49 y en el Popover usado por el inbox).

Patrón chip+popover de tags del inbox v3 (clonar idioma de conversation-list.tsx L329-378):
- `<Popover open={tagFilterOpen} onOpenChange={setTagFilterOpen}>`
- Trigger = `<button className={cn('chip', activo && 'on')}>` con icono `<Tag />` lucide
- Content `align="start"` + `portalContainer` + lista de tags (swatch de color + nombre) + opción "Quitar filtro".
</interfaces>
</context>

<design_decisions>
- **C-2 multi-select (paridad legacy):** El estado YA es multi-select (`currentTagIds: string[]`, `handleTagSelectionChange(string[])`). El popover hará toggle multi-select como el TagFilter legacy (clic en un tag lo añade/quita de la selección, "Quitar filtro" limpia todo). Las 4 tabs/chips rápidos se conservan (Todos/Clientes/Leads/Mayoristas) — son atajos a un solo tag; cuando la selección tiene >1 tag o un tag fuera de los 4, ningún chip rápido queda `on` (el popover refleja la selección real). El chip del popover muestra `on` cuando hay ≥1 tag activo.
- **C-1 reuso de handlers:** NO duplicar lógica. Extraer la lógica del memo `columns` (onEdit/onDelete/onViewDetail) a 3 callbacks con `React.useCallback` reutilizados tanto por el memo legacy como por el dropdown v3 (single source of truth). Evita drift entre ramas.
- **Idioma editorial:** Dropdown/Popover usan componentes shadcn EXISTENTES (DropdownMenu, Popover) re-rooteados al scope `.theme-editorial-v3` vía `portalContainer` (igual que el inbox v3). El trigger del dropdown de fila es un botón discreto (`MoreHorizontal`) estilado para encajar con `table.dict`; el trigger del filtro de tags es un `.chip`. NO se introducen shadcn buttons crudos visibles.
- **Regla 6:** Todo el cambio vive DENTRO del `if (v3) { ... }` (L236-539) y en estado/callbacks compartidos que no alteran el render legacy/v2. El branch legacy (L541+) y v2 (otro componente) quedan byte-idénticos en comportamiento.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Extraer handlers de fila y añadir dropdown de acciones a la tabla v3 (C-1)</name>
  <files>src/app/(dashboard)/crm/contactos/components/contacts-table.tsx</files>
  <action>
1. Extraer la lógica del memo `columns` (L122-144) a 3 callbacks estables con `React.useCallback`, declarados antes del memo:
   - `handleEditContact(contact)` → `setEditingContact(contact); setDialogOpen(true)`
   - `handleDeleteContact(contact)` → `if (!confirm(\`Eliminar contacto "${contact.name}"?\`)) return; const result = await deleteContact(contact.id); if ('error' in result) toast.error(result.error); else { toast.success('Contacto eliminado'); router.refresh() }`
   - `handleViewDetail(contact)` → `router.push(\`/crm/contactos/${contact.id}\`)`
   Luego hacer que el memo `columns` consuma estos 3 callbacks (pasarlos a `createColumns({ onEdit: handleEditContact, onDelete: handleDeleteContact, onViewDetail: handleViewDetail })`). El comportamiento legacy debe quedar idéntico.
2. Añadir refs/estado para portal-scoping (clonar de conversation-list.tsx L60-99): `const themeContainerRef = React.useRef<HTMLElement | null>(null)` + `React.useEffect(() => { themeContainerRef.current = document.querySelector('.theme-editorial-v3') as HTMLElement | null }, [])`. Importar `DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuTrigger` de `@/components/ui/dropdown-menu` y `MoreHorizontalIcon` de lucide-react.
3. En la tabla v3 (`<table className="dict">`): añadir un `<th style={{ width: 40 }} aria-hidden />` al final del `<thead>` (después de "Última actividad", L441) y en cada `<tr>` de fila (L462-485) añadir una `<td>` final con un DropdownMenu:
   - Trigger: `<button type="button" className="btn" aria-label="Acciones" style={{ padding: '2px 6px' }}><MoreHorizontalIcon width={14} height={14} /></button>` (estilo discreto acorde a table.dict; usar la clase `.btn` editorial existente, no shadcn Button crudo).
   - Content: `<DropdownMenuContent align="end" portalContainer={themeContainerRef.current ?? undefined}>` con items "Ver detalles" (`onSelect={() => handleViewDetail(contact)}`), "Editar" (`handleEditContact(contact)`), `<DropdownMenuSeparator />`, y "Eliminar" (`handleDeleteContact(contact)`, con `style={{ color: 'var(--viv-red, var(--ink-1))' }}` o clase destructiva editorial).
   - Actualizar el `colSpan={6}` del empty row (L448) a `colSpan={7}` para que cuadre con la columna nueva.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>tsc 0 errores. La tabla v3 muestra un menú por fila con Ver detalles/Editar/Eliminar que reusa los handlers existentes; el branch legacy sigue usando los mismos callbacks vía `createColumns` sin cambios de comportamiento.</done>
</task>

<task type="auto">
  <name>Task 2: Filtro dinámico de tags (chip+popover) + botón Gestionar etiquetas en la toolbar v3 (C-2, C-3)</name>
  <files>src/app/(dashboard)/crm/contactos/components/contacts-table.tsx</files>
  <action>
1. Importar `Popover, PopoverContent, PopoverTrigger` de `@/components/ui/popover`, `Tag as TagIcon, SettingsIcon` de lucide-react, y `cn` de `@/lib/utils`. Añadir estado `const [tagFilterOpen, setTagFilterOpen] = React.useState(false)`.
2. En la `.toolbar` del branch v3 (después de los 4 chips rápidos, L408), añadir un chip-popover de tags multi-select (clonar idioma del inbox v3, conversation-list.tsx L329-378):
   - Trigger: `<button type="button" className={cn('chip', currentTagIds.length > 0 && 'on')} ...><TagIcon width={12} height={12} aria-hidden /> {currentTagIds.length > 0 ? \`${currentTagIds.length} etiqueta(s)\` : 'Etiqueta'}</button>`.
   - `<PopoverContent className="w-[220px] p-2" align="start" portalContainer={themeContainerRef.current ?? undefined}>` con:
     - Opción "Quitar filtro" (visible si `currentTagIds.length > 0`) → `handleTagSelectionChange([])`.
     - `tags.map(tag => ...)` botón con swatch (`<span style={{ background: tag.color }} />`) + `tag.name`; clic hace TOGGLE multi-select: `const next = currentTagIds.includes(tag.id) ? currentTagIds.filter(id => id !== tag.id) : [...currentTagIds, tag.id]; handleTagSelectionChange(next)`. Marcar visualmente los tags ya seleccionados (`currentTagIds.includes(tag.id)`).
     - Si `tags.length === 0`: texto "Sin etiquetas".
   - Conservar los 4 chips rápidos existentes sin cambios.
3. Añadir el botón Gestionar etiquetas (C-3): en la `.toolbar` (al final, alineado a la derecha) un `<button type="button" className="btn" onClick={() => setTagManagerOpen(true)}><SettingsIcon width={14} height={14} /> Gestionar etiquetas</button>`. El `<TagManager>` ya está montado (L527-531) — esto solo cablea el trigger que faltaba.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>tsc 0 errores. La toolbar v3 tiene: 4 chips rápidos + un chip "Etiqueta" que abre un popover con todos los tags del workspace (multi-select, "Quitar filtro") cableado a `handleTagSelectionChange`, y un botón "Gestionar etiquetas" que abre el TagManager. Popover re-rooteado al scope `.theme-editorial-v3`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → server action (delete) | El operador dispara `deleteContact(id)` desde el dropdown de fila — input cruza al server action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w1a-01 | Tampering | `deleteContact(contact.id)` desde dropdown v3 | mitigate | Reusa el server action existente `@/app/actions/contacts` que valida auth + filtra por `workspace_id` (Regla 3). No se introduce nuevo path de mutación. Confirmación `confirm()` previa (igual que legacy). |
| T-w1a-02 | Information Disclosure | filtro de tags vía URL (`?tags=`) | accept | El listado de tags y contactos ya viene server-filtered por workspace (page.tsx). El popover solo refleja `tags` (prop ya scoped). Sin acceso cross-workspace. |
| T-w1a-03 | Elevation of Privilege | acciones de fila visibles | accept | Mismo nivel de permiso que el detalle/legacy; no hay roles finos en este módulo y la mutación pasa por el action con auth. Riesgo bajo, paridad con UI legacy. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` → 0 errores.
- Inspección manual del diff: todos los cambios viven dentro del `if (v3)` o en estado/callbacks compartidos que no alteran el JSX del branch legacy (L541+). El componente v2 (`ContactsViewV2`) no se toca.
- `git diff` confirma que el branch legacy de la tabla (`<DataTable columns={columns} ... />` y los dialogs legacy) sigue invocando los mismos handlers vía `createColumns`.
</verification>

<success_criteria>
- C-1: dropdown de fila Ver detalles/Editar/Eliminar funcional en la tabla v3, reusando handlers existentes (cero duplicación de lógica de mutación).
- C-2: filtro por cualquier tag del workspace vía chip+popover multi-select; 4 tabs rápidos conservados.
- C-3: botón "Gestionar etiquetas" abre el TagManager ya montado.
- Regla 6: legacy y v2 byte-idénticos en comportamiento; flag `ui_editorial_v3` OFF por defecto.
- `pnpm exec tsc --noEmit` con 0 errores.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-w1a-contactos-v3-crud-tags/260611-w1a-SUMMARY.md`
</output>
