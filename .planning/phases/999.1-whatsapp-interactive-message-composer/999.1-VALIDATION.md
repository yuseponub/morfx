---
phase: 999.1
slug: whatsapp-interactive-message-composer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-03
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | repo root (existing — `meta-whatsapp-sender.test.ts` & `messages-provider.test.ts` already run) |
| **Quick run command** | `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts src/lib/whatsapp/__tests__/interactive-limits.test.ts` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~5–15 seconds (quick), full suite per repo baseline |

---

## Sampling Rate

- **After every task commit:** Run quick run command above.
- **After every plan wave:** Run `npx vitest run` (full suite).
- **Before `/gsd-verify-work`:** Full suite must be green + manual smoke (see Manual-Only).
- **Max feedback latency:** ~15 seconds (quick run).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| W0 | — | 0 | D-05a | T-999.1-V5 | length/count/uniqueness validation server-checkable | unit | `npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts` | ❌ W0 | ⬜ pending |
| W0 | — | 0 | D-03/D-04/D-06 | T-999.1-V4 | provider branch + union payload shape | unit | `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts` | ❌ W0 | ⬜ pending |
| domain | — | — | D-03/D-04 | T-999.1-V4 | meta vs 360dialog branch; 360 list → clear error; creds from ctx.workspaceId | unit | `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts` | ❌ W0 | ⬜ pending |
| limits | — | — | D-05a | T-999.1-V5 | full Meta limit set incl. total-rows ≤10 across sections | unit | `npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts` | ❌ W0 | ⬜ pending |
| regression | — | — | Regla 6 | — | sender 3/3 + provider parity stay GREEN | unit | `npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts src/lib/domain/__tests__/messages-provider.test.ts` | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/whatsapp/interactive-limits.ts` — the validation helper under test (full Meta limit set: ≤3 buttons, button label ≤20, button id ≤256, ≤10 sections, section title ≤24, row title ≤24, row description ≤72, list button label ≤20, body ≤1024, header ≤60, footer ≤60, **total rows across ALL sections ≤10**)
- [ ] `src/lib/whatsapp/__tests__/interactive-limits.test.ts` — stubs for D-05a (esp. total-rows ≤10 boundary)
- [ ] `src/lib/domain/__tests__/messages-interactive-provider.test.ts` — stubs for D-03/D-04/D-06 (provider branch + union payload shape + 360 list error)

*Mirrors existing `meta-whatsapp-sender.test.ts` + `messages-provider.test.ts` — no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Outbound rich bubble render (buttons + list) | D-05c | Visual render in inbox — not unit-testable | Open a conversation, send buttons + list; confirm `message-bubble.tsx` `case 'interactive'` renders the buttons/list from `content` JSONB (not the placeholder) |
| meta_direct interactive send E2E | D-03/D-04 | Requires live Meta number + open 24h window | On the meta_direct test number with an open window: send buttons + send list; confirm delivery in WhatsApp |
| 360dialog buttons send + list-blocked | D-03 | Requires a live 360dialog workspace | On a 360dialog workspace: send buttons (delivers via legacy `sendButtonMessage`); attempt list → operator sees clear "lista no soportada en 360dialog (legacy)" error |
| 24h-window gating of composer button | D-02 | Visual/interaction state | With window closed → composer interactive button hidden/disabled; with window open → enabled |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (interactive-limits.ts + 2 test files)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
