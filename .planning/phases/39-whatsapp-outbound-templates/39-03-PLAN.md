---
phase: 39-whatsapp-outbound-templates
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/meta/media.ts
  - src/lib/meta/templates.ts
autonomous: true
requirements: [WA-06, WA-08]
must_haves:
  truths:
    - "Outbound media can be uploaded to Meta CDN (multipart) returning a media_id"
    - "Inbound Meta CDN media is downloaded (two-step, Bearer) and rehosted to Supabase Storage"
    - "Templates can be created, listed, deleted, and edited via Graph API"
    - "editTemplateMeta enforces the D-05 constraints (name/language immutable; status-gated edits)"
  artifacts:
    - path: "src/lib/meta/media.ts"
      provides: "uploadMedia (multipart→media_id), getMediaUrl, downloadAndRehostMedia"
      contains: "messaging_product"
    - path: "src/lib/meta/templates.ts"
      provides: "createTemplateMeta, listTemplatesMeta, deleteTemplateMeta, editTemplateMeta, syncTemplateStatusMeta, uploadHeaderHandleMeta"
      contains: "message_templates"
  key_links:
    - from: "src/lib/meta/media.ts"
      to: "Supabase Storage bucket whatsapp-media"
      via: "admin client upload (mirror downloadAndUploadMedia)"
      pattern: "whatsapp-media"
    - from: "src/lib/meta/templates.ts"
      to: "graph.facebook.com/v22.0/{waba_id}/message_templates"
      via: "metaRequest"
      pattern: "message_templates"
---

<objective>
Build the two new Meta service files: `meta/media.ts` (WA-06 CDN upload/download/rehost) and `meta/templates.ts` (WA-08 template CRUD + the D-05 edit-constraint guard). Both mirror proven 360dialog analogs, swapping base URL + auth.

Purpose: The domain template branch (Plan 04) and the inbound webhook media path (Plan 05) consume these. Parallel with Plan 02 — no file overlap.
Output: New `src/lib/meta/media.ts`, new `src/lib/meta/templates.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/lib/meta/api.ts
@src/lib/whatsapp/templates-api.ts
@src/lib/whatsapp/api.ts

<interfaces>
From src/lib/meta/api.ts:
```typescript
export async function metaRequest<T>(accessToken, endpoint, options?): Promise<T>  // forces Content-Type: application/json — do NOT use for multipart
```
From src/lib/meta/constants.ts:
```typescript
export const META_GRAPH_API_VERSION = 'v22.0'
export const META_BASE_URL = 'https://graph.facebook.com/v22.0'
```
360dialog analogs to mirror function-for-function (swap base URL + auth header):
- media download two-step: `src/lib/whatsapp/api.ts` `downloadMedia` (:263-296)
- rehost to Storage: `src/lib/whatsapp/webhook-handler.ts` `downloadAndUploadMedia` (:632-679) + `getExtensionFromMime` (:684-704)
- template CRUD: `src/lib/whatsapp/templates-api.ts` `createTemplate360` (:47), `listTemplates360` (:84), `deleteTemplate360` (:151), `syncTemplateStatus360` (:189), `uploadHeaderImage360` (:235 — resumable 2-step)
</interfaces>

D-05 MANDATORY edit table (RESEARCH "D-05 MANDATORY DELIVERABLE"):
- name + language NEVER editable (any status).
- edit allowed only when status ∈ {APPROVED, REJECTED, PAUSED}. PENDING/IN_REVIEW + DISABLED → not editable.
- after a successful edit, status flips to PENDING (re-review).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: meta/media.ts — CDN upload + inbound download/rehost (WA-06)</name>
  <files>src/lib/meta/media.ts</files>
  <behavior>
    - uploadMedia: multipart POST /{phoneNumberId}/media with messaging_product/type/file → { id }
    - uploadMedia uses a dedicated multipart fetch, NOT metaRequest (which forces JSON)
    - getMediaUrl: GET /{media_id} → { url, mime_type }
    - downloadAndRehostMedia: GET url with Authorization: Bearer (no hostname rewrite) → upload to Supabase Storage → return public URL
  </behavior>
  <action>
Create `src/lib/meta/media.ts` per RESEARCH Code Examples §6-7 + Pitfall 3 and 39-PATTERNS.md "NEW meta/media.ts":
- `uploadMedia(accessToken, phoneNumberId, fileBytes, mime)` → `POST {META_BASE_URL}/{phoneNumberId}/media` multipart/form-data (`messaging_product=whatsapp`, `type=<mime>`, `file=<binary>`) → `{ id }`. Use a dedicated `fetch` with `FormData` — NOT `metaRequest` (it forces `Content-Type: application/json`, RESEARCH §6). For chat media we send by `link` (Pattern 2); this upload is for template header media + large files.
- `getMediaUrl(accessToken, mediaId)` → `GET {META_BASE_URL}/{mediaId}` (via `metaRequest`) → `{ url, mime_type, file_size }`.
- `downloadAndRehostMedia(accessToken, mediaId, workspaceId, conversationId)` → call `getMediaUrl`, then `GET url` with `Authorization: Bearer <accessToken>` (NO hostname rewrite — Meta returns the real CDN URL, unlike 360dialog), then rehost to Supabase Storage by copying `downloadAndUploadMedia` (webhook-handler.ts:644-674): path `inbound/{ws}/{conv}/{ts}_{safeName}`, `getExtensionFromMime` map, `upsert:false`, return `getPublicUrl`. Download IMMEDIATELY (Pitfall 3 — CDN URL expires ~5min).
SSRF guard (T-39-07): before the binary GET, assert the `url` host is a Meta CDN domain (e.g. ends with `fbsbx.com` / `facebook.com`) — reject otherwise; cap downloaded size to the documented per-type limit (image 5MB, audio/video 16MB, document 100MB) to avoid resource exhaustion. Never log the Bearer token (T-39-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/meta/__tests__/media.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>media.test.ts GREEN: multipart upload (not metaRequest), two-step Bearer download, rehost to Storage; SSRF host check + size cap present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: meta/templates.ts — CRUD + D-05 edit guard (WA-08)</name>
  <files>src/lib/meta/templates.ts</files>
  <behavior>
    - createTemplateMeta → POST /{waba_id}/message_templates
    - listTemplatesMeta → GET …/message_templates?limit=250&fields=...
    - deleteTemplateMeta → DELETE …/message_templates?name=
    - editTemplateMeta → POST /{message_template_id} {category?, components?}; REJECTS name/language change; REJECTS when status ∉ {APPROVED, REJECTED, PAUSED}
    - uploadHeaderHandleMeta → resumable 2-step → { h }
  </behavior>
  <action>
Create `src/lib/meta/templates.ts` mirroring `src/lib/whatsapp/templates-api.ts` function-for-function (39-PATTERNS.md table), swapping base URL to `{META_BASE_URL}/{waba_id}/message_templates` + auth to Bearer via `metaRequest`:
- `createTemplateMeta(accessToken, wabaId, { name, language, category, components })` → POST (§9).
- `listTemplatesMeta(accessToken, wabaId)` → GET with `limit=250&fields=name,status,category,language,components,quality_score,rejected_reason`.
- `deleteTemplateMeta(accessToken, wabaId, name)` → DELETE `?name=<name>`.
- `syncTemplateStatusMeta(accessToken, wabaId)` → poll fallback (mirror `syncTemplateStatus360`).
- `editTemplateMeta(accessToken, messageTemplateId, { category?, components? }, currentStatus)` → POST `/v22.0/{message_template_id}`. ENFORCE D-05 (mandatory): throw/return error if caller attempts to change `name` or `language` (immutable, any status); throw/return error if `currentStatus ∉ {APPROVED, REJECTED, PAUSED}`. This is the D-05 deliverable — the guard lives here so every edit call-site inherits it.
- `uploadHeaderHandleMeta(accessToken, appId, fileBytes, mime)` → resumable 2-step (`POST /{app_id}/uploads?file_length&file_type` → session → `POST /{session_id}` `file_offset:0` → `{ h }`) — copy the structure of `uploadHeaderImage360` (templates-api.ts:235-291) pointing at Graph directly.
Use `metaRequest<T>` for all JSON calls (parses `MetaGraphApiError`). Do NOT modify `templates-api.ts` (Regla 6).
  </action>
  <verify>
    <automated>npx vitest run src/lib/meta/__tests__/templates.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>templates.test.ts GREEN incl. the D-05 edit guard (name/language immutable + status-gated); CRUD endpoint shapes match; templates-api.ts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta CDN → MorfX server | inbound media URL fetched server-side with Bearer; untrusted host |
| MorfX server → Meta Graph (management) | template CRUD with Bearer BISUAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Information Disclosure / DoS (SSRF) | media download | mitigate | Validate the CDN `url` host is a Meta domain before the Bearer GET; cap download size per type — prevents SSRF + resource exhaustion |
| T-39-01 | Information Disclosure | both files | mitigate | Bearer token only in fetch headers; never logged, never returned to client |
| T-39-08 | Tampering | editTemplateMeta | mitigate | D-05 guard rejects name/language change + non-editable statuses at the service layer (single enforcement point for all callers) |
</threat_model>

<verification>
- `npx vitest run src/lib/meta/__tests__/media.test.ts src/lib/meta/__tests__/templates.test.ts` GREEN.
- `npx tsc --noEmit` no new errors mentioning the two files.
- `git diff --stat` shows `templates-api.ts`, `whatsapp/api.ts`, `webhook-handler.ts` UNCHANGED (Regla 6 — copied not modified).
</verification>

<success_criteria>
WA-06 (media CDN upload + inbound download/rehost with SSRF + size guards) and WA-08 (template CRUD + the mandatory D-05 edit-constraint guard) are implemented as standalone Meta service files, with the 360dialog analogs untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-03-SUMMARY.md`
</output>
