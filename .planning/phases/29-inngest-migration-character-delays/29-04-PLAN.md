---
phase: 29-inngest-migration-character-delays
plan: 04
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/lib/agents/engine-adapters/production/messaging.ts
autonomous: true

must_haves:
  truths:
    - "Bot responses have typing delay proportional to character count instead of fixed delaySeconds"
    - "First message in a sequence also gets a character delay (more human-like)"
    - "Delay is multiplied by responseSpeed (0=instant, 0.2=rapido, 1.0=real)"
    - "When responseSpeed is 0 (instantaneo), no sleep occurs at all"
    - "The template.delaySeconds field is no longer used in the delay calculation"
  artifacts:
    - path: "src/lib/agents/engine-adapters/production/messaging.ts"
      provides: "ProductionMessagingAdapter with character-based delays"
      contains: "calculateCharDelay"
  key_links:
    - from: "src/lib/agents/engine-adapters/production/messaging.ts"
      to: "src/lib/agents/somnio/char-delay.ts"
      via: "import { calculateCharDelay }"
      pattern: "import.*calculateCharDelay.*char-delay"
---

<objective>
Replace the fixed template delay system in ProductionMessagingAdapter with the character-count-based delay curve from `calculateCharDelay()`.

Purpose: DELAY-01 and DELAY-02 requirements. Instead of each template having a hardcoded `delaySeconds` value, the delay is now calculated dynamically based on the number of characters in the message content. This produces more human-like typing behavior: short messages (~2s) feel "thought about", long messages (~12s cap) don't punish excessively. The existing `responseSpeed` multiplier continues to work with the same semantics (0=instant, 0.2=fast, 1.0=real).

Output: Updated ProductionMessagingAdapter.send() method using calculateCharDelay.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-inngest-migration-character-delays/29-RESEARCH.md
@src/lib/agents/engine-adapters/production/messaging.ts
@src/lib/agents/somnio/char-delay.ts (created in Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fixed delay with calculateCharDelay in messaging adapter</name>
  <files>src/lib/agents/engine-adapters/production/messaging.ts</files>
  <action>
    Modify `ProductionMessagingAdapter.send()` in `src/lib/agents/engine-adapters/production/messaging.ts`:

    1. **Add import** at top of file:
       ```typescript
       import { calculateCharDelay } from '@/lib/agents/somnio/char-delay'
       ```

    2. **Replace the delay logic in the for loop** (lines 99-105). The current code:
       ```typescript
       // Apply delay (skip for first message, skip if instantaneous)
       if (i > 0 && template.delaySeconds > 0 && this.responseSpeed > 0) {
         await sleep(template.delaySeconds * this.responseSpeed * 1000)
       }
       ```

       Replace with:
       ```typescript
       // Apply character-based delay (human-like typing simulation)
       // Skip entirely if responseSpeed is 0 (instantaneo preset)
       if (this.responseSpeed > 0) {
         const delayMs = calculateCharDelay(template.content.length) * this.responseSpeed
         await sleep(delayMs)
       }
       ```

    Key differences from the old code:
    - **First message also gets a delay** (removed the `i > 0` condition). This is more human-like -- the bot appears to "read" the customer message before responding. DISCUSSION.md resolved this: YES, add delay for first message.
    - **Character-based instead of fixed**: `calculateCharDelay(template.content.length)` replaces `template.delaySeconds * 1000`. A 50-char message gets ~3.5s, a 200-char message gets ~10s.
    - **Same multiplier semantics**: `* this.responseSpeed` still works identically. Instantaneo (0.0) = skip sleep entirely. Rapido (0.2) = 20% of real delay. Real (1.0) = full delay.
    - **template.delaySeconds is now ignored** in the delay calculation. The field still exists on the template type but is not used. No type changes needed.

    3. **Update the comment** at the top of the file (lines 1-8) to mention character-based delays instead of "delays between messages."

    4. Do NOT change any other logic in the send() method. The message sending, error handling, and logging remain exactly the same.

    5. Do NOT remove `delaySeconds` from the template type definition (line 72) -- it's still present in the database and may be referenced elsewhere. It's simply no longer used for delay calculation.
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - Grep for `calculateCharDelay` in messaging.ts confirms import and usage
    - Grep for `template.delaySeconds` in messaging.ts confirms it's no longer used in the delay logic (it may still exist in the type)
    - Grep for `i > 0` in messaging.ts confirms the condition is removed from delay logic
  </verify>
  <done>ProductionMessagingAdapter uses calculateCharDelay(content.length) * responseSpeed for typing delays. First message gets a delay too. template.delaySeconds is no longer used. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `calculateCharDelay` is imported from `@/lib/agents/somnio/char-delay`
- Delay calculation uses character count, not fixed delaySeconds
- First message in sequence gets a delay (no `i > 0` gate)
- responseSpeed = 0 skips sleep entirely
- No changes to template types or other adapter logic
</verification>

<success_criteria>
- A 50-char message with responseSpeed=1.0 delays ~3.5s (not the old fixed value)
- A 200-char message with responseSpeed=1.0 delays ~10s
- responseSpeed=0.0 (instantaneo) skips all delays
- responseSpeed=0.2 (rapido) gives 20% of the character delay
- First message in a sequence has a delay (unlike the old behavior)
- template.delaySeconds field is preserved in type but not used
</success_criteria>

<output>
After completion, create `.planning/phases/29-inngest-migration-character-delays/29-04-SUMMARY.md`
</output>
