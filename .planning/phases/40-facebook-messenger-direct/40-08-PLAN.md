---
phase: 40-facebook-messenger-direct
plan: 08
type: execute
wave: 5
depends_on: [40-00, 40-02, 40-03, 40-04, 40-05, 40-06, 40-07]
files_modified: []
autonomous: false
requirements: [SIGNUP-04, FB-01, FB-02, FB-03, FB-04, MIG-02]
must_haves:
  truths:
    - "Full suite is green and the Regla 6 byte-identical diff check passes before cutover"
    - "Exactly ONE test workspace is flipped to messenger_provider='meta_direct' via manual SQL"
    - "A real Messenger message to the connected Page appears in the MorfX inbox with the Messenger indicator (FB-01/FB-04)"
    - "A human agent can reply text + image from the inbox inside 24h (FB-02)"
    - "An outside-24h send is either tagged HUMAN_AGENT (if granted) or BLOCKED with a clear message (D-09)"
    - "Somnio + godentist-fb-ig + all other workspaces stay on manychat (byte-identical, Regla 6)"
  artifacts: []
  key_links:
    - from: "operator"
      to: "workspaces.messenger_provider"
      via: "manual SQL flip of one test workspace"
      pattern: "meta_direct"
---

<objective>
The FINAL gated cutover for Phase 40. Verify the full suite + Regla 6 byte-identical diff, push to main (Vercel), then PAUSE for the operator to: (1) connect a real Facebook Page via the new UI, (2) manually flip ONE test workspace to `messenger_provider='meta_direct'`, and (3) run the live Messenger smoke (inbound receive + outbound text/image + 24h/HUMAN_AGENT window). Somnio, godentist-fb-ig, and all other workspaces stay on manychat (Regla 6).

Purpose: Mirrors the Phase 39 gated-cutover plan (manual SQL flip + live smoke on a test number, checkpoints not auto). No new code — this plan wires the shipped pieces into a live, verified path on a single test workspace before any broad rollout.
Output: A live-verified Messenger path on one test workspace + a cutover record.

Depends on ALL prior plans (00 migration applied, 02 sender, 03 connect, 04 chokepoint, 05 inbound, 06 window gate, 07 UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/40-facebook-messenger-direct/40-RESEARCH.md
@.planning/phases/40-facebook-messenger-direct/40-VALIDATION.md
@.planning/phases/40-facebook-messenger-direct/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green + Regla 6 byte-identical diff + push to main</name>
  <read_first>
    - .planning/phases/40-facebook-messenger-direct/40-VALIDATION.md (phase gate)
    - CLAUDE.md §Regla 1 (push to Vercel before asking the user to test) + §Regla 6
  </read_first>
  <files></files>
  <action>
1. Run the full suite: `npx vitest run`. All six Phase 40 tests (from Plan 01, now GREEN) must pass. Pre-existing unrelated failures documented in prior STATE entries (e.g. somnio-v4 few-shots wording) are NOT regressions — confirm none of the failing files are Phase 40 files.
2. Regla 6 byte-identical gate: `git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts` MUST be EMPTY. Also confirm `git status --porcelain src/lib/agents/godentist-fb-ig/` is empty (godentist-fb-ig NOT migrated — D-11).
3. Commit any uncommitted Phase 40 work with atomic Spanish messages, then push to main (Vercel deploy) per Regla 1 — REQUIRED before asking the operator to run the live smoke.
   NOTE (Regla 5): the `messenger_provider` migration (Plan 00) MUST already be applied in prod (confirmed in Plan 00's checkpoint) — this push references the column.
  </action>
  <verify>
    <automated>git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts | tee /dev/stderr | wc -l | grep -qx 0 && npx vitest run src/lib/meta/__tests__/messenger-api.test.ts src/lib/channels/__tests__/meta-facebook-sender.test.ts src/lib/domain/__tests__/messenger-provider.test.ts src/lib/messenger/__tests__/webhook-handler.test.ts src/app/actions/__tests__/connect-facebook.test.ts src/app/actions/__tests__/messenger-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts` returns EMPTY (Regla 6)
    - `git status --porcelain src/lib/agents/godentist-fb-ig/` returns EMPTY (D-11 — not migrated)
    - the six Phase 40 test files all exit 0
    - Phase 40 work pushed to main (Vercel) — `git log origin/main -1` includes the Phase 40 cutover commit
  </acceptance_criteria>
  <done>Full Phase 40 suite green, Regla 6 + godentist-fb-ig byte-identical, code pushed to main (Vercel) before the live smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Connect a real Page + manual flip of ONE test workspace + live Messenger smoke</name>
  <what-built>
The full meta_direct Messenger surface is shipped and deployed to Vercel: the "Conectar Facebook" UI (Plan 07), the connect chain + Page subscribe (Plan 03), the provider chokepoint (Plan 04), the inbound `object==='page'` webhook + PSID handler (Plan 05), the send edge (Plan 02), and the 24h/HUMAN_AGENT window gate (Plan 06). The `messenger_provider` column is in prod (Plan 00). NO workspace is flipped yet — everything still runs on manychat.
  </what-built>
  <how-to-verify>
**Step A — Connect (SIGNUP-04):**
1. As the OWNER of a TEST workspace (NOT Somnio, NOT the godentist-fb-ig workspace), open Configuración → Integraciones.
2. Click "Conectar Facebook", authorize a real Facebook Page (grant `pages_messaging`; IG scope optional).
3. Confirm a `workspace_meta_accounts` row exists with `channel='facebook'`, a `page_id`, and a token:
   ```sql
   SELECT workspace_id, channel, page_id, is_active FROM workspace_meta_accounts WHERE channel='facebook' ORDER BY created_at DESC LIMIT 3;
   ```

**Step B — Flip ONE workspace to meta_direct (manual, Regla 6):**
4. Flip ONLY the test workspace:
   ```sql
   UPDATE workspaces SET messenger_provider='meta_direct' WHERE id='<TEST_WORKSPACE_UUID>';
   -- verify Somnio + godentist-fb-ig workspace + all others still 'manychat':
   SELECT messenger_provider, count(*) FROM workspaces GROUP BY messenger_provider;
   ```
   Expected: exactly 1 row under `meta_direct`; everything else `manychat`.

**Step C — Inbound (FB-01 / FB-03 / FB-04):**
5. From a personal Facebook account, send a Messenger message to the connected Page.
6. Confirm it appears in the MorfX inbox as a `channel='facebook'` conversation with the Messenger (FB) channel icon, the sender's display name, and a contact created by `(page_id, PSID)`.

**Step D — Outbound text + image inside 24h (FB-02):**
7. Reply with text from the inbox → confirm it arrives on Messenger.
8. Reply with an image (+ caption) → confirm the image arrives, then the caption as a follow-up text.

**Step E — Outside-24h window (D-09):**
9. On a thread where the last inbound was >24h ago (or simulate by adjusting `last_customer_message_at` on a test conversation), attempt a send:
   - If `META_HUMAN_AGENT_ENABLED=true` AND the Human Agent feature is granted → confirm the send succeeds with the HUMAN_AGENT tag.
   - Otherwise → confirm the send is BLOCKED with the clear Spanish message ("Ventana de 24h cerrada. Activa el permiso Human Agent…").

**Step F — Regla 6 sanity:**
10. Confirm a normal manychat-based FB/IG agent (e.g. godentist-fb-ig) still works unchanged on its workspace (no behavior change).

Record the results (PASS/FAIL per step) in a `40-HUMAN-UAT.md` if any step is time-gated (e.g. the Human Agent feature pending App Review, like P39's WA-09).
  </how-to-verify>
  <resume-signal>Type "approved" once inbound + outbound text/image work on the flipped test workspace and Somnio/godentist-fb-ig are confirmed unaffected. Note any time-gated items (e.g. Human Agent feature pending App Review) as deferred UAT rather than failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod DB | Manual provider flip of one workspace |
| live Messenger ↔ MorfX | Real inbound/outbound crossing the deployed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08-01 | Tampering | flipping more than the test workspace (broad rollout by accident) | mitigate | Single-workspace `UPDATE ... WHERE id='<TEST_UUID>'`; verification query confirms exactly 1 meta_direct row; Somnio + godentist-fb-ig stay manychat |
| T-40-08-02 | Repudiation | outside-window send violating policy in the live smoke | mitigate | Step E exercises the D-09 gate; HUMAN_AGENT only when feature-flag granted, else BLOCK |
| T-40-08-03 | Information Disclosure | Page token exposure during live connect | mitigate | Token encrypted at rest (Plan 03); connect UI only handles a code (Plan 07); never logged |
</threat_model>

<verification>
- Full Phase 40 suite green; Regla 6 + godentist-fb-ig byte-identical.
- Operator confirms: connect stores the Page row, one workspace flipped, inbound appears with the Messenger indicator, text + image send inside 24h, window gate behaves per D-09, and manychat agents unaffected.
</verification>

<success_criteria>
- All five ROADMAP Phase 40 success criteria verified live on one test workspace.
- Regla 6 confirmed live (Somnio + godentist-fb-ig untouched).
- Any time-gated item (Human Agent App Review) recorded as deferred UAT, not a failure.
</success_criteria>

<output>
After completion, create `.planning/phases/40-facebook-messenger-direct/40-08-SUMMARY.md` (and a `40-HUMAN-UAT.md` if any live step is time-gated).
</output>
