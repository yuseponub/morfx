---
phase: 999.1-whatsapp-interactive-message-composer
plan: 05
subsystem: whatsapp
tags: [ui, frontend, interactive-messages, outbound-bubble, message-bubble, wave-4, regla-6, xss-mitigation, d-05c, single-source-of-truth]
requires:
  - "src/app/(dashboard)/whatsapp/components/interactive-bubble.tsx — InteractiveBubble + InteractiveContent (render puro compartido, Plan 04 commit b603f299)"
  - "domain sendInteractiveMessage guarda la estructura interactiva completa como messages.content JSONB type='interactive' (Plan 02 D-04, commit b613d0e9)"
provides:
  - "src/app/(dashboard)/whatsapp/components/message-bubble.tsx case 'interactive' — render rico de la burbuja saliente via InteractiveBubble consumiendo el content JSONB (D-05c)"
affects: []
tech-stack:
  added: []
  patterns:
    - "Single source of truth: el MISMO componente puro InteractiveBubble renderiza el preview en vivo del composer (Plan 04) Y la burbuja outbound (este plan) → preview y mensaje enviado pixel-identicos"
    - "content-narrowing en el switch del bubble (const interactiveContent = content as unknown as InteractiveContent) espejando el patron del case 'template'"
    - "Cambio quirurgico: solo el inner JSX de UN case cambia; shell/timestamp/status-icon row intactos (git-diff gate)"
key-files:
  created: []
  modified:
    - "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"
decisions:
  - "D-05c: el case 'interactive' deja de mostrar el placeholder 'Mensaje interactivo / (Ver en WhatsApp)' y renderiza la estructura real (botones/lista) que el operador envio, consumiendo el content JSONB (D-04) via el InteractiveBubble compartido"
  - "Narrowing via `content as unknown as InteractiveContent`: el tipo Message.content del bubble es un union amplio; el doble-cast (unknown intermedio) es necesario porque InteractiveContent no se solapa estructuralmente con los otros miembros del union — mismo patron pragmatico que el case 'template' (`content as { body?: string }`)"
  - "T-999.1-XSS: cero nueva superficie de render — se reusa InteractiveBubble (auto-escaping de React, sin raw-HTML; grep gate == 0 verificado en Plan 04). No se introduce ningun render path nuevo"
metrics:
  duration: "~6 min"
  completed: "2026-06-04"
  tasks: 1
  files: 1
---

# Phase 999.1 Plan 05: Burbuja outbound interactiva rica Summary

Cierra la fase 999.1 (Wave 4, ultimo plan): la burbuja SALIENTE de mensaje interactivo en el inbox deja de mostrar el placeholder `Mensaje interactivo / (Ver en WhatsApp)` y renderiza los botones/lista que el operador realmente envio, consumiendo el `content` JSONB que el domain persiste (D-04). El render reusa el MISMO componente puro `InteractiveBubble` del Plan 04 — el que ya alimenta el preview en vivo del composer — garantizando que el preview y el mensaje enviado son pixel-identicos (single source of truth, D-05c). El cambio es quirurgico: solo el inner JSX del `case 'interactive'` cambia; el shell de la burbuja, el padding, el timestamp y el status-icon row (L201-220) quedan intactos.

## What Was Built

### Task 1 — `message-bubble.tsx` case 'interactive' rico — commit `1db99362`
- **Import agregado** junto al de `MediaPreview`: `import { InteractiveBubble, type InteractiveContent } from './interactive-bubble'`.
- **`case 'interactive'` reemplazado** (solo el inner JSX): el placeholder `<div className="text-sm"><p className="font-medium">Mensaje interactivo</p><p>(Ver en WhatsApp)</p></div>` se sustituye por:
  ```tsx
  case 'interactive': {
    const interactiveContent = content as unknown as InteractiveContent
    return <InteractiveBubble content={interactiveContent} />
  }
  ```
  Espejando el patron de content-narrowing del `case 'template'` (L118). El doble-cast (`as unknown as`) es necesario porque `InteractiveContent` no se solapa estructuralmente con los demas miembros del union `Message.content` — mismo patron pragmatico que ya usa el case 'template'.
- **Shell intacto:** el `git diff` confirma que SOLO cambian el nuevo import (L6) + el bloque del `case 'interactive'` (L133-135). El contenedor editorial `max-w-[70%]` (L183), el shell de la burbuja con verdes/ink tokens (L201-220), el timestamp + status-icon row (L226-250) y el error-message block quedan byte-identicos.

## Verification Results

| Check | Expected | Actual |
| ----- | -------- | ------ |
| `tsc --noEmit` errores en message-bubble.tsx | 0 | 0 |
| `grep -c "import { InteractiveBubble"` | ≥1 | 1 |
| `grep -c "<InteractiveBubble content="` | ≥1 | 1 |
| `grep -c "(Ver en WhatsApp)"` (placeholder removido) | 0 | 0 |
| `git diff` toca SOLO import + case 'interactive' (shell L201-220 intacto) | si | si (import L6 + case L133-135, nada cerca de L201-220) |
| Commit elimina archivos inesperados | no | no (1 file changed, 3+/6-) |
| Suite fase (`interactive-limits` 17 + `meta-whatsapp-sender` 3 + `messages-provider` 5 + `messages-interactive-provider` 4) | GREEN | 29/29 GREEN |

## Deviations from Plan

None — plan ejecutado exactamente como fue escrito. El plan especifica el narrowing como `content as unknown as InteractiveContent` (doble-cast); se aplico verbatim.

## Threat Surface

Sin nueva superficie de seguridad fuera del `<threat_model>` del plan. **T-999.1-XSS (mitigate):** no se introduce ningun render path nuevo — se reusa `InteractiveBubble` (render plain-text via auto-escaping de React, sin `dangerouslySetInnerHTML`, grep gate == 0 verificado en Plan 04). **T-999.1-R6 (accept→verify):** solo el inner JSX de UN `case` cambia; el shell/timestamp/status del bubble switch quedan intactos (git-diff gate) y la suite de la fase queda 29/29 GREEN.

## Manual Phase Gate (per VALIDATION manual-only)

Pendiente de prueba manual del operador (no automatizable sin browser + ventana 24h abierta + envio real): abrir una conversacion, enviar botones + lista desde el composer, y confirmar que la burbuja saliente renderiza los botones/lista reales (no el placeholder). El render es identico al preview del composer (single source of truth).

## Pre-existing Working-Tree Changes (NOT touched)

Per la mandate de ejecucion, los cambios pre-existentes bajo `.planning/standalone/somnio-v4-rag-generative/` (SMOKE-A-RESULTS.md, SMOKE-B-RESULTS.md) y los archivos untracked sueltos NO se stagearon ni committearon. Solo `message-bubble.tsx` se stageo individualmente (nunca `git add -A`). Verificado: el commit `1db99362` = 1 file changed, sin eliminaciones, sin archivos de somnio-v4-rag.

## Self-Check: PASSED

- FOUND (modificado): src/app/(dashboard)/whatsapp/components/message-bubble.tsx
- FOUND commit: 1db99362 (Task 1 — burbuja interactiva saliente rica)
- tsc: 0 errores en message-bubble.tsx
- placeholder removido: grep "(Ver en WhatsApp)" == 0
- shell intacto: git diff toca solo import + case 'interactive'
- Regla 6: suite fase 29/29 GREEN
