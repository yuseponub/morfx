---
phase: 38-embedded-signup-wa-inbound
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/components/settings/connect-whatsapp.tsx
  - src/app/(dashboard)/configuracion/integraciones/page.tsx
autonomous: false
requirements: [SIGNUP-01]
must_haves:
  truths:
    - "A 'Conectar WhatsApp' button in workspace settings loads the Facebook JS SDK and opens the Embedded Signup v4 popup via FB.login with config_id"
    - "The popup returns the auth code via FB.login callback AND waba_id/phone_number_id via a window 'message' WA_EMBEDDED_SIGNUP listener"
    - "Once both channels fire, the client calls connectWhatsAppNumber({ code, wabaId, phoneNumberId }) and shows a Spanish success/error toast"
    - "META_APP_SECRET is never referenced in the client component"
  artifacts:
    - path: "src/components/settings/connect-whatsapp.tsx"
      provides: "client component — FB SDK load + FB.login popup + message listener + server action call"
      contains: "WA_EMBEDDED_SIGNUP"
    - path: "src/app/(dashboard)/configuracion/integraciones/page.tsx"
      provides: "renders ConnectWhatsApp in the integrations settings surface"
      contains: "ConnectWhatsApp"
  key_links:
    - from: "src/components/settings/connect-whatsapp.tsx"
      to: "src/app/actions/meta-onboarding.ts (connectWhatsAppNumber)"
      via: "server action call once code + session info captured"
      pattern: "connectWhatsAppNumber"
    - from: "src/components/settings/connect-whatsapp.tsx"
      to: "Facebook JS SDK"
      via: "<script connect.facebook.net/en_US/sdk.js> + FB.init + FB.login"
      pattern: "FB.login|connect.facebook.net"
---

<objective>
Ship the DELIVERABLE 2 frontend: the "Conectar WhatsApp" client component that loads the Facebook JS SDK, launches the Embedded Signup v4 popup (`FB.login` with `config_id`), captures both return channels (auth `code` via callback + `waba_id`/`phone_number_id` via the `WA_EMBEDDED_SIGNUP` window message), and calls the `connectWhatsAppNumber` server action — surfaced in the integrations settings page. This completes SIGNUP-01 end-to-end with the Plan 04 backend.

Purpose: Replace the manual SQL token insert (deliverable 1's throwaway) with true self-service onboarding.
Output: `src/components/settings/connect-whatsapp.tsx`, wired into the integrations settings page, pushed to Vercel, validated by a live popup smoke.

NOTE (MEDIUM confidence — RESEARCH Pattern 4 / Pitfall 8): `config_id`, `featureType`, and exact `extras` keys are Meta-version-sensitive. The executor MUST read the live `config_id` from the Meta App dashboard (Embedded Signup product) and validate the popup options in the browser before declaring done. Treat the research snippet as a starting template, not gospel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-embedded-signup-wa-inbound/38-RESEARCH.md
@.planning/phases/38-embedded-signup-wa-inbound/38-PATTERNS.md

<interfaces>
<!-- The server action this component calls (Plan 04). -->
connectWhatsAppNumber (src/app/actions/meta-onboarding.ts — Plan 04):
```typescript
connectWhatsAppNumber(input: { code: string; wabaId: string; phoneNumberId: string }):
  Promise<{ success: true } | { success: false; error: string }>
```

Client-shell analog (src/app/(dashboard)/configuracion/integraciones/components/shopify-form.tsx:136-157):
  'use client' + useState/useTransition + toast (sonner) + Button — COPY the shell, swap the connect mechanism to FB.login.

FB.login config (RESEARCH Pattern 4 lines 259-293, MEDIUM — validate live):
  FB.init({ appId: '1457229738955828', version: 'v22.0' })   // version MUST match META_GRAPH_API_VERSION
  FB.login(cb, { config_id: <READ FROM DASHBOARD>, response_type: 'code',
                 override_default_response_type: true, extras: { sessionInfoVersion: '3' } })
  window 'message' listener: data.type === 'WA_EMBEDDED_SIGNUP', data.event === 'FINISH'
    → data.data = { phone_number_id, waba_id, business_id }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: connect-whatsapp.tsx client component (FB.login + dual-channel capture)</name>
  <files>src/components/settings/connect-whatsapp.tsx</files>
  <read_first>
    - src/app/(dashboard)/configuracion/integraciones/components/shopify-form.tsx (lines 136-190 DisconnectedBranch — the 'use client' + useTransition + toast shell to clone)
    - src/app/actions/meta-onboarding.ts (Plan 04 — connectWhatsAppNumber signature)
    - .planning/phases/38-embedded-signup-wa-inbound/38-PATTERNS.md (lines 197-239 connect-whatsapp.tsx pattern; lines 295-301 No-Analog note for FB SDK)
    - .planning/phases/38-embedded-signup-wa-inbound/38-RESEARCH.md (Pattern 4 lines 259-293; Pitfall 8 line 391; Pitfall 9 line 395)
  </read_first>
  <action>
    Create `src/components/settings/connect-whatsapp.tsx` ('use client'). Clone the shopify-form shell (useState + useTransition + sonner toast + Button), swap the connect mechanism to FB Embedded Signup.

    SDK load: inject `<script src="https://connect.facebook.net/en_US/sdk.js" async defer>` once on mount (guard against double-inject via a module-level flag or `document.getElementById('facebook-jssdk')`). On load, `window.FB.init({ appId: '1457229738955828', version: 'v22.0', xfbml: false })`. The `appId` + version MUST match `META_GRAPH_API_VERSION` (constants.ts:7 = v22.0). Read the config_id from `process.env.NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID` (a NEW public env var — the config_id is NOT a secret; document it must be set in Vercel, read from the Meta App dashboard per Pitfall 8). If the env var is absent, disable the button + show a hint ("Falta configurar NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID").

    Dual-channel capture (RESEARCH Pattern 4 — two return paths that must BOTH fire before calling the action):
    - Channel 1 (auth code): in the `FB.login` callback, capture `response.authResponse?.code` into a ref.
    - Channel 2 (session info): a `window.addEventListener('message', ...)` registered on mount that parses `event.data`, guards `event.origin.endsWith('facebook.com')`, and on `data.type === 'WA_EMBEDDED_SIGNUP' && data.event === 'FINISH'` captures `data.data.waba_id` + `data.data.phone_number_id` into a ref. Remove the listener on unmount.
    - When BOTH the code and {waba_id, phone_number_id} are present, call `startTransition(async () => { const r = await connectWhatsAppNumber({ code, wabaId, phoneNumberId }); r.success ? toast.success('WhatsApp conectado') : toast.error(r.error) })`. Reset the refs after.
    - Handle `data.event === 'CANCEL' | 'ERROR'` → `toast.error('Conexión cancelada')` (Pitfall — popup can be cancelled).

    FB.login call (on button click — MEDIUM confidence, validate live):
    ```typescript
    window.FB.login((response: any) => {
      if (response.authResponse?.code) { codeRef.current = response.authResponse.code; tryComplete() }
    }, {
      config_id: process.env.NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID,
      response_type: 'code',
      override_default_response_type: true,
      extras: { sessionInfoVersion: '3' },
    })
    ```
    `tryComplete()` checks both refs are set, then fires the action. The component MUST NOT reference `META_APP_SECRET` or any non-`NEXT_PUBLIC_` secret (it runs in the browser).

    Add a minimal `declare global { interface Window { FB: any } }` (or a local `// @ts-expect-error`) for the SDK global. Render: a `<Button onClick={launch} disabled={!configReady || isPending}>Conectar WhatsApp</Button>` with a short description.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "connect-whatsapp" ; grep -c "META_APP_SECRET" src/components/settings/connect-whatsapp.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/settings/connect-whatsapp.tsx` exists and starts with `'use client'`
    - `grep -c "FB.login" src/components/settings/connect-whatsapp.tsx` returns >=1
    - `grep -c "WA_EMBEDDED_SIGNUP" src/components/settings/connect-whatsapp.tsx` returns >=1 (channel 2 listener)
    - `grep -c "connectWhatsAppNumber" src/components/settings/connect-whatsapp.tsx` returns >=1 (calls Plan 04 action)
    - `grep -c "connect.facebook.net" src/components/settings/connect-whatsapp.tsx` returns >=1 (SDK injection)
    - `grep -c "endsWith('facebook.com')\|facebook.com" src/components/settings/connect-whatsapp.tsx` returns >=1 (origin guard on message listener)
    - `grep -c "META_APP_SECRET" src/components/settings/connect-whatsapp.tsx` returns 0 (no secret in client bundle)
    - `npx tsc --noEmit` reports no NEW errors mentioning `connect-whatsapp`
  </acceptance_criteria>
  <done>Client component exists, loads SDK, launches FB.login, captures both channels with origin guard, calls the action, no secret referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ConnectWhatsApp into the integrations settings page</name>
  <files>src/app/(dashboard)/configuracion/integraciones/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/configuracion/integraciones/page.tsx (current structure — where Shopify/BOLD tabs render; find the additive insertion point)
    - src/components/settings/connect-whatsapp.tsx (Task 1 — the component to mount)
    - .claude/rules/agent-scope.md + CLAUDE.md Regla 6 (additive, no behavior change for existing surfaces)
  </read_first>
  <action>
    Render `<ConnectWhatsApp />` in the integrations settings page additively — mirror how the existing Shopify/BOLD connect surfaces are placed (a new section/tab "WhatsApp (Meta directo)"). Do NOT restructure or alter existing integration tabs (Regla 6 — additive only). Import the component and mount it in the appropriate section. If the page is a Server Component, `ConnectWhatsApp` ('use client') mounts fine as a child. Keep the change purely additive: a new section block + one import.
  </action>
  <verify>
    <automated>grep -c "ConnectWhatsApp" src/app/(dashboard)/configuracion/integraciones/page.tsx ; npx tsc --noEmit 2>&1 | grep -c "integraciones/page"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ConnectWhatsApp" "src/app/(dashboard)/configuracion/integraciones/page.tsx"` returns >=2 (import + usage)
    - `git diff src/app/(dashboard)/configuracion/integraciones/page.tsx` shows only additive lines (new import + new section) — no existing tab/section removed or restructured
    - `npx tsc --noEmit` reports no NEW errors mentioning `integraciones/page`
  </acceptance_criteria>
  <done>ConnectWhatsApp mounted additively in the integrations page; existing surfaces untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Source audit, tsc gate, Vercel push</name>
  <files>src/components/settings/connect-whatsapp.tsx</files>
  <read_first>
    - CLAUDE.md Regla 1 (push to Vercel before asking user to test)
    - .planning/phases/38-embedded-signup-wa-inbound/38-VALIDATION.md (full per-req map)
  </read_first>
  <action>
    Final gate before the live popup smoke:
    1. `npx tsc --noEmit` — no new errors across the phase's files.
    2. `npx vitest run src/app/api/webhooks/meta/ src/lib/meta/` — all phase unit tests GREEN (HOOK-01/02, SIGNUP-02/03).
    3. Regla 6 re-check: `git diff --stat HEAD -- src/lib/whatsapp/webhook-handler.ts src/app/api/webhooks/whatsapp/route.ts` = 0 changed.
    4. Document the new public env var requirement: `NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID` must be set in Vercel (read from Meta App dashboard → Embedded Signup config). Add a note in the SUMMARY.
    5. Commit (atomic, Spanish, Co-Authored-By Claude) + push to Vercel (Regla 1).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/webhooks/meta/ src/lib/meta/ 2>&1 | tail -6 ; git diff --stat HEAD -- src/lib/whatsapp/webhook-handler.ts | grep -c changed</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/webhooks/meta/ src/lib/meta/` exits 0 (all HOOK-01/02 + SIGNUP-02/03 tests green)
    - `git diff --stat HEAD -- src/lib/whatsapp/webhook-handler.ts src/app/api/webhooks/whatsapp/route.ts` shows 0 changed files (Regla 6)
    - `npx tsc --noEmit` shows no new phase errors
    - Commit pushed — `git log --oneline -1` shows the new commit
  </acceptance_criteria>
  <done>All phase unit tests green, Regla 6 intact, env var documented, pushed to Vercel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Embedded Signup popup smoke (SIGNUP-01/02/03 end-to-end)</name>
  <action>HUMAN CHECKPOINT (Regla 1 / SIGNUP-01) — code is already pushed to Vercel by Task 3; the user sets NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID in Vercel, runs the live popup smoke (popup → encrypted row → active subscription + owner gate + provider still 360dialog). No autonomous code action; see how-to-verify.</action>
  <what-built>The "Conectar WhatsApp" button in /configuracion/integraciones loads the FB SDK, opens the Embedded Signup v4 popup, captures the auth code + waba_id/phone_number_id, and calls the owner-gated `connectWhatsAppNumber` action which exchanges code→BISUAT, encrypts, persists via `upsertMetaAccount`, and auto-subscribes the WABA. Pushed to Vercel.</what-built>
  <how-to-verify>
    Pre-reqs (RESEARCH Environment Availability):
    - Set `NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID` in Vercel = the config_id read from Meta App `1457229738955828` → Embedded Signup product (Pitfall 8 — validate live).
    - Confirm `whatsapp_business_messaging` + `whatsapp_business_management` have **Advanced Access** for the onboarded-customer webhook path (Pitfall 5 — needed for multi-tenant, not just own-portfolio).
    Steps:
    1. As a workspace **owner**, open `https://morfx.app/configuracion/integraciones` → WhatsApp (Meta directo) section. Confirm the "Conectar WhatsApp" button is enabled (config_id present).
    2. Click it → the Meta Embedded Signup popup opens. Authorize a WABA + phone number.
    3. On FINISH, confirm a Spanish success toast ("WhatsApp conectado").
    4. Confirm the encrypted row exists:
       ```sql
       SELECT workspace_id, channel, phone_number_id, waba_id, is_active,
              left(access_token_encrypted, 12) AS token_prefix
       FROM workspace_meta_accounts
       WHERE workspace_id = '<your test workspace>' AND channel = 'whatsapp';
       -- Expected: one row, is_active=true, access_token_encrypted populated (encrypted, not plaintext)
       ```
    5. Confirm the subscription is active:
       ```
       GET https://graph.facebook.com/v22.0/{waba_id}/subscribed_apps  (Bearer = the BISUAT)
       → data array includes your app
       ```
    6. Negative test: as a **non-owner** member, confirm the action returns "Solo el Owner puede conectar WhatsApp" (V4 access control).
    7. Confirm `whatsapp_provider` for that workspace is STILL `'360dialog'` (connecting ≠ activating traffic — D-04/D-06):
       ```sql
       SELECT whatsapp_provider FROM workspaces WHERE id = '<your test workspace>';
       -- Expected: '360dialog'
       ```
  </how-to-verify>
  <verify>Manual — user reports pass/fail per how-to-verify (live smoke; no automated command).</verify>
  <done>All listed verification criteria pass against the live deployment.</done>
  <resume-signal>Type "signup OK" if popup → encrypted row → subscription active + owner gate + provider still 360dialog, or describe which step failed (config_id/popup/exchange/persist/subscribe/owner-gate).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Meta popup | FB.login session Meta-managed; only config_id (non-secret) in client; secret stays server |
| window 'message' events | Untrusted; origin guarded to *.facebook.com before parsing |
| Client → server action | Owner-gated server-side (Plan 04); client cannot bypass the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-17 | Spoofing | window 'message' listener | mitigate | `event.origin.endsWith('facebook.com')` guard + JSON.parse try/catch before trusting WA_EMBEDDED_SIGNUP data. |
| T-38-18 | Info Disclosure | client bundle | mitigate | Only `NEXT_PUBLIC_META_EMBEDDED_SIGNUP_CONFIG_ID` (non-secret) in client; META_APP_SECRET never referenced (grep gate = 0). |
| T-38-19 | Elevation | non-owner connect attempt | mitigate | Server action (Plan 04) enforces owner; client UI gating is cosmetic only — the real gate is server-side. Verified in smoke step 6. |
| T-38-20 | Tampering | accidental traffic flip on connect | mitigate | Component + action never touch whatsapp_provider; smoke step 7 confirms it stays '360dialog' (D-06). |
</threat_model>

<verification>
- All phase unit tests green (Task 3).
- Regla 6: shared 360dialog files unchanged.
- Manual SIGNUP-01/02/03 smoke: popup → encrypted row → active subscription + owner gate + provider unchanged.
</verification>

<success_criteria>
- "Conectar WhatsApp" popup completes self-service onboarding end-to-end: token exchanged, encrypted, stored, WABA auto-subscribed.
- Owner-only; workspaceId session-derived; provider not flipped by connecting.
- META_APP_SECRET never in the client bundle.
</success_criteria>

<output>
After completion, create `.planning/phases/38-embedded-signup-wa-inbound/38-05-SUMMARY.md`
</output>
