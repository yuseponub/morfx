# CONTINUE-HERE — somnio-v4-crm-subloop (handoff post-/clear → plan-phase)

**Creado:** 2026-05-29 · **Rama:** `exec/debounce-v2-wave6` · **origin/main HEAD esperado:** `eaee7496` (turn-ledger ya mergeado)

## Cómo retomar

Esto es un **standalone** en `.planning/standalone/somnio-v4-crm-subloop/` — **NO lo descubre
`gsd-tools init`** (devuelve `phase_found: false`). Orquestar a MANO igual que el turn-ledger:
leer el workflow de referencia + los artefactos, y spawnear `gsd-planner`.

- Tooling real: `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs` (NO `gsd-sdk`).
- Branch flow: ver memoria `git_branch_flow` — `origin/main` es deploy (al día), `exec/debounce-v2-wave6`
  es la rama de integración (ff a main). SIEMPRE `git fetch` + comparar contra `origin/main`, NO el main local (stale).

## Estado: discuss ✅ + research ✅ — LISTO PARA PLAN

| Artefacto | Commit |
|-----------|--------|
| CONTEXT.md (D-01..D-26) | `98943933`, `bf5fb1ac`, `c82794b4` |
| RESEARCH.md (894 líneas + suplemento) | `bf5fb1ac`, `c82794b4` |
| DISCUSSION-LOG.md | `98943933` |

**Leer primero:** `CONTEXT.md` (las 26 decisiones + Riesgos + canonical_refs) y `RESEARCH.md`
(integración, blockers resueltos, y el `## SUPLEMENTO` del rediseño lifecycle).

## Qué se va a planear (resumen — el detalle está en CONTEXT/RESEARCH)

Consolidar TODO el CRM de v4 al **sub-loop grounded** + **rediseño del lifecycle del pedido**:
- Gate post-sales-track amplio → sub-loop LLM grounded decide+ejecuta → guards red final. ADITIVO (templates siguen).
- Big-bang: eliminar `executeInvocations` inline + `createOrder` del runner.
- **Lifecycle:** createOrder cascarón en datos-críticos (NUEVO PEDIDO `6be952b0`) → `updateOrder` con pack →
  `confirmar`→`moveOrderToStage(CONFIRMADO `4770a36e`)`. Timers L3/L4 → solo template `recordar_*`, ya no crean.
- Grounding 2 vistas (DB query-tools + ledger crmActions). Cache `_v4` + fresh antes de createOrder + CAS.
- Guards: createOrder rechaza+devuelve existente (idempotency `crm_mutation_idempotency_keys`); moveOrderToStage
  whitelist →CONFIRMADO + CAS (`crm_stage_integrity_cas_enabled`).
- Ledger: sub-loop puebla `crmActions[]` (origen:'rag') derivando de `rawResult.steps[].toolResults`.

## Toca módulos COMPARTIDOS (aprobado por usuario, Regla-6-safe por aditividad / 0 consumidores prod)
- `crm-mutation-tools.updateOrder` += `items[]` opcional (D-25).
- Helper `resolveOrCreateContact` (compone domain `searchContacts`+`createContact`) (D-24).
- El plan DEBE incluir greps de no-regresión Regla 6 (v3/godentist/godentist-fb-ig/recompra/pw-confirmation).

## Pendiente planner-internal (sin decisión usuario)
- Query `information_schema` del default de `orders.total_value` (display $0-vs-null).
- Naming final de símbolos `recordar_promo`/`recordar_confirmacion` + `confirmar_orden` (D-18); ¿entran a `SIGNIFICANT_ACTIONS`?

## Restricción transversal
v4 DORMANT (0 workspaces) → Regla 6. Cambios v4-specific + los 2 toques compartidos aditivos. Sin feature flag (D-16).
Sandbox simula CRM (flag en `buildSubLoopTools`). Migración(es) DB → Regla 5 (aplicar en prod ANTES de pushear).

## Next
`/gsd-plan-phase somnio-v4-crm-subloop` (orquestar a mano). Probablemente multi-wave (state-machine + sub-loop +
grounding + guards + ledger + el toque a updateOrder).
