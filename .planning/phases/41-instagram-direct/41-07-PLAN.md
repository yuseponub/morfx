---
phase: 41-instagram-direct
plan: 07
type: execute
wave: 5
depends_on: [41-00, 41-01, 41-02, 41-03, 41-04, 41-05, 41-06]
files_modified: []
autonomous: false
requirements: [IG-01, IG-02, IG-03, IG-04, IG-05, MIG-02]
must_haves:
  truths:
    - "Full suite is green and the Regla 6 byte-identical diff (registry + manychat-sender + godentist-fb-ig) passes before cutover"
    - "The IG-05 action-layer window gate (wired in 41-04) is VERIFIED present in actions/messages.ts via grep — no code change in this plan"
    - "Exactly ONE test workspace is flipped to instagram_provider='meta_direct' via manual SQL"
    - "A1 linchpin verified: the webhook entry.id equals the stored ig_account_id (routing matches)"
    - "A2 linchpin verified: an IG DM fires the webhook via the existing Page subscription (or a per-account IG subscribe is added if not)"
    - "A real IG DM appears in the inbox with the Instagram indicator (IG-01/IG-03/IG-04); a human reply text+image is delivered (IG-02)"
    - "An outside-24h send is BLOCKED with the clear Spanish window-closed message (IG-05, window-gate reused)"
    - "Somnio + godentist-fb-ig + all other workspaces stay on manychat (byte-identical, Regla 6)"
  artifacts: []
  key_links:
    - from: "operator"
      to: "workspaces.instagram_provider"
      via: "manual SQL flip of one test workspace"
      pattern: "meta_direct"
---

<objective>
The FINAL gated cutover for Phase 41. Verify the full suite + the Regla 6 byte-identical diff + the IG-05 action-layer gate (grep-verify only — the gate was WIRED in 41-04, this plan does NOT change code), push to main (Vercel), then PAUSE for the operator to: (1) connect a real IG account via the new UI, (2) verify the two MEDIUM-confidence smoke linchpins A1 (`entry.id` == stored `ig_account_id`) and A2 (IG DM delivery via the existing Page subscription), (3) manually flip ONE test workspace to `instagram_provider='meta_direct'`, and (4) run the live IG smoke (inbound receive + outbound text/image + the outside-24h block). Somnio, godentist-fb-ig, and all other workspaces stay on manychat (Regla 6).

Purpose: Mirrors Phase 40 Plan 40-08 (gated cutover, manual SQL flip + live smoke, checkpoints not auto). NO feature code — this plan is a PURE verify/cutover. The IG-05 outside-24h block is enforced by the action-layer window gate that was already WIRED in Plan 41-04 (the reused FB `window-gate.ts` helper — D-IG-09, no IG sibling). This plan VERIFIES that gate is present (grep, matching the FB precedent which is grep+live-smoke verified) and EXERCISES it live in Step F. It wires the shipped pieces into a live, verified path on a single test workspace before any broad rollout, AND resolves the two smoke-verify items (A1/A2) flagged MEDIUM-confidence in RESEARCH.
Output: A live-verified IG path on one test workspace + a cutover record; A1/A2 resolved. NO code change (files_modified empty).

Depends on ALL prior plans (00 migration applied, 02 sender, 03 connect, 04 chokepoint + action-layer IG-05 gate, 05 inbound, 06 UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-instagram-direct/41-RESEARCH.md
@.planning/phases/41-instagram-direct/41-VALIDATION.md
@.planning/phases/41-instagram-direct/41-CONTEXT.md
@.planning/phases/41-instagram-direct/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green + Regla 6 byte-identical diff + grep-VERIFY the IG-05 action-gate (no code change) + push to main</name>
  <read_first>
    - .planning/phases/41-instagram-direct/41-VALIDATION.md (phase gate)
    - src/app/actions/messages.ts (CONFIRM the instagram window-gate arm wired in Plan 41-04 is present — grep-verify only; this plan does NOT edit this file)
    - CLAUDE.md §Regla 1 (push to Vercel before asking the user to test) + §Regla 6
  </read_first>
  <files></files>
  <action>
1. Run the full suite: `npx vitest run`. The five Phase 41 test files (from Plan 41-01, now GREEN) must pass. Pre-existing unrelated failures documented in prior STATE entries (e.g. somnio-v4 wording) are NOT regressions — confirm none of the failing files are Phase 41 files.
2. **IG-05 action-layer gate — grep VERIFY ONLY (the gate was WIRED in Plan 41-04; this plan does NOT change code):** confirm `src/app/actions/messages.ts` already applies the window gate for `channel === 'instagram' && instagram_provider === 'meta_direct'` at BOTH gate sites, calling the REUSED `resolveMessengerWindowSend` and returning the Spanish `decision.error` on `blocked` (D-IG-09 — same helper as FB, no IG sibling). This is a VERIFY step — if the grep checks below fail, that is a 41-04 regression to fix in 41-04, NOT a code change here.
   - `grep -c "channel === 'instagram'" src/app/actions/messages.ts` ≥ 2
   - `grep -c "resolveMessengerWindowSend" src/app/actions/messages.ts` ≥ 4 (2 FB + 2 IG)
   - `grep -c "instagram_provider" src/app/actions/messages.ts` ≥ 2 (the two `.select(...)`)
3. Regla 6 byte-identical gate: `git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts` MUST be EMPTY. `git status --porcelain src/lib/agents/godentist-fb-ig/` MUST be empty (godentist-fb-ig NOT migrated — D-IG-03). `grep -c "metaInstagramSender" src/lib/channels/registry.ts` MUST be 0.
4. Commit any uncommitted Phase 41 work with atomic Spanish messages, then push to main (Vercel deploy) per Regla 1 — REQUIRED before the operator runs the live smoke.
   NOTE (Regla 5): the `instagram_provider` migration (Plan 41-00) MUST already be applied in prod (confirmed in Plan 00's checkpoint) — this push references the column.
  </action>
  <verify>
    <automated>[ "$(grep -c "channel === 'instagram'" src/app/actions/messages.ts)" -ge 2 ] && [ "$(grep -c resolveMessengerWindowSend src/app/actions/messages.ts)" -ge 4 ] && [ -z "$(git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts)" ] && [ "$(grep -c metaInstagramSender src/lib/channels/registry.ts)" -eq 0 ] && [ -z "$(git status --porcelain src/lib/agents/godentist-fb-ig/)" ] && npx vitest run src/lib/meta/__tests__/instagram-api.test.ts src/lib/channels/__tests__/meta-instagram-sender.test.ts src/lib/domain/__tests__/messages-instagram.test.ts src/lib/instagram/__tests__/webhook-handler.test.ts src/app/api/webhooks/meta/__tests__/instagram-branch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat src/lib/channels/manychat-sender.ts src/lib/channels/registry.ts` returns EMPTY (Regla 6)
    - `grep -c "metaInstagramSender" src/lib/channels/registry.ts` returns 0 (Pitfall 4)
    - `git status --porcelain src/lib/agents/godentist-fb-ig/` returns EMPTY (D-IG-03 — not migrated)
    - the five Phase 41 test files all exit 0
    - IG-05 VERIFY (no code change here): `grep -c "channel === 'instagram'" src/app/actions/messages.ts` ≥ 2 AND `grep -c "resolveMessengerWindowSend" src/app/actions/messages.ts` ≥ 4 (the instagram arm wired in 41-04 using the reused helper is present)
    - `actions/messages.ts` is NOT in this plan's git changes (the IG gate lives in 41-04) — this plan's only commits are the cutover push, not a `messages.ts` edit
    - Phase 41 work pushed to main (Vercel) — `git log origin/main -1` includes the Phase 41 cutover commit
  </acceptance_criteria>
  <done>Full Phase 41 suite green, Regla 6 + godentist-fb-ig byte-identical, IG-05 action-layer window gate VERIFIED present (wired in 41-04, no code change here), code pushed to main (Vercel) before the live smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Connect real IG + A1/A2 linchpins + manual flip of ONE test workspace + live IG smoke</name>
  <what-built>
The full meta_direct Instagram surface is shipped and deployed to Vercel: the "Conectar Instagram" UI (Plan 06), the connect chain + IG-account resolve (Plan 03), the provider chokepoint + IG-05 action-layer window gate (Plan 04), the inbound `object==='instagram'` webhook + IGSID handler (Plan 05), the send edge (Plan 02), and the 24h/HUMAN_AGENT window gate (reused FB window-gate, wired at the action layer in Plan 04). The `instagram_provider` column is in prod (Plan 00). NO workspace is flipped yet — everything still runs on manychat.
  </what-built>
  <how-to-verify>
**Step A — Connect (IG-03):**
1. As the OWNER of a TEST workspace (NOT Somnio, NOT the godentist-fb-ig workspace `f0241182-...`) whose Facebook Page already has a linked IG Professional account, open Configuración → Integraciones → Instagram.
2. Click "Conectar Instagram". Confirm a `workspace_meta_accounts` row exists with `channel='instagram'`, a non-null `ig_account_id`, `ig_username`, and `page_id`:
   ```sql
   SELECT workspace_id, channel, ig_account_id, ig_username, page_id, is_active
   FROM workspace_meta_accounts WHERE channel='instagram' ORDER BY created_at DESC LIMIT 3;
   ```
3. Negative case: if the connected Page has NO linked IG account, confirm the clear Spanish error "vincula una cuenta de Instagram Profesional a tu página de Facebook".

**Step B — A1 + A2 linchpins (the two MEDIUM-confidence smoke-verify items — RESEARCH Open Q1/Q2):**
4. **App Dashboard one-time setup (A2 prerequisite):** in the Meta App Dashboard, confirm the `instagram` webhook field `messages` is subscribed at the www callback `https://www.morfx.app/api/webhooks/meta`. If not, subscribe it.
5. From a personal IG account, send a DM to the test IG Professional account.
6. **A1:** in the server logs (or a temporary log of `entry.id` in the IG branch), confirm the inbound `entry.id` EXACTLY equals the `ig_account_id` stored in Step A2. If they differ, routing (`resolveByIgAccountId(entry.id)`) will miss — STOP and report the namespacing mismatch (A1 is the single linchpin).
7. **A2:** confirm the DM actually fired the webhook (it appeared / was logged). If NO webhook fired, the existing Page `subscribed_apps` is insufficient → add a per-account IG subscribe (mirror `subscribeMessengerPage`) in `connectInstagramAccount` and re-test. Record which subscription path was needed.

**Step C — Flip ONE workspace to meta_direct (manual, Regla 6):**
8. Flip ONLY the test workspace:
   ```sql
   UPDATE workspaces SET instagram_provider='meta_direct' WHERE id='<TEST_WORKSPACE_UUID>';
   -- verify Somnio + godentist-fb-ig workspace + all others still 'manychat':
   SELECT instagram_provider, count(*) FROM workspaces GROUP BY instagram_provider;
   ```
   Expected: exactly 1 row under `meta_direct`; everything else `manychat`. The godentist-fb-ig workspace (`f0241182-...`) MUST read `manychat`.

**Step D — Inbound (IG-01 / IG-03 / IG-04):**
9. Send another DM from the personal IG account. Confirm it appears in the MorfX inbox as a `channel='instagram'` conversation with the "Instagram" channel indicator, the sender's display name (or @username, self-healed if the first message raced), and a contact created by `ig-${igsid}`.

**Step E — Outbound text + image inside 24h (IG-02):**
10. Reply with text from the inbox → confirm it arrives in the IG DM thread.
11. Reply with an image (+ caption) → confirm the image arrives, then the caption as a follow-up text.

**Step F — Outside-24h window block (IG-05, the action-layer gate wired in 41-04):**
12. On a thread where the last inbound was >24h ago (or simulate by backdating `last_customer_message_at` to −25h via SQL on a test conversation), attempt a send:
    - With `META_HUMAN_AGENT_ENABLED` OFF (default) → confirm the send is BLOCKED with the clear Spanish message ("Ventana de 24h cerrada. Activa el permiso Human Agent o espera a que el cliente escriba."). This exercises the `actions/messages.ts` instagram window-gate arm wired in Plan 41-04 (reused `resolveMessengerWindowSend`). IG has NO templates — block-only is correct (Pitfall 6).

**Step G — Regla 6 sanity:**
13. Confirm a normal manychat-based IG agent (e.g. godentist-fb-ig on its workspace) still works unchanged (no behavior change).

Record the results (PASS/FAIL per step) in a `41-HUMAN-UAT.md` if any step is time-gated (e.g. the IG account not yet linkable, or A1/A2 needing a follow-up subscribe).
  </how-to-verify>
  <resume-signal>Type "approved" once: A1 (entry.id == ig_account_id) and A2 (webhook delivery) are confirmed, inbound + outbound text/image work on the flipped test workspace, the outside-24h send blocks with the Spanish message (the 41-04 action-layer gate), and Somnio/godentist-fb-ig are confirmed unaffected. Note any time-gated items (e.g. a needed per-account IG subscribe, or the Human Agent feature pending App Review) as deferred UAT rather than failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod DB | Manual provider flip of one workspace |
| live IG ↔ MorfX | Real inbound/outbound crossing the deployed path |
| Meta App Dashboard → www callback | One-time `instagram`/`messages` field subscription |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Tampering | flipping more than the test workspace (broad rollout by accident) | mitigate | Single-workspace `UPDATE ... WHERE id='<TEST_UUID>'`; GROUP BY confirms exactly 1 meta_direct row; Somnio + godentist-fb-ig stay manychat |
| T-41-07-02 | Information Disclosure / Tampering | A1 mismatch → cross-tenant routing if entry.id ≠ ig_account_id | mitigate | Step B6 explicitly asserts entry.id == stored ig_account_id BEFORE trusting routing; mismatch → STOP and report |
| T-41-07-03 | Repudiation | outside-window send violating policy in the live smoke | mitigate | Step F exercises the IG-05 gate WIRED in 41-04 (reused resolveMessengerWindowSend); HUMAN_AGENT only when feature-flag granted, else BLOCK |
| T-41-07-04 | Information Disclosure | Page token exposure during live connect | mitigate | Token encrypted at rest (Plan 03); connect UI never sees the token (no-popup path); never logged |
| T-41-07-05 | Denial of Service | wrong subscription path → zero inbound (A2) | mitigate | Step B7 verifies delivery; if the Page subscription is insufficient, add a per-account IG subscribe and re-test |
</threat_model>

<verification>
- Full Phase 41 suite green; Regla 6 (registry + manychat-sender + godentist-fb-ig) byte-identical; metaInstagramSender NOT in the map.
- IG-05 action-layer window gate VERIFIED present in actions/messages.ts (wired in 41-04; grep-verify, no code change here) and EXERCISED live in Step F.
- A1 (entry.id == ig_account_id) + A2 (webhook delivery) resolved in the live smoke.
- Operator confirms: connect stores the IG row, one workspace flipped, inbound appears with the Instagram indicator, text + image send inside 24h, outside-24h blocks with the Spanish message, manychat agents unaffected.
</verification>

<success_criteria>
- ROADMAP Phase 41 criteria #1/#2/#3 verified live on one test workspace; criterion #4 satisfied as block-only (no countdown — D-IG-09); criterion #5 (agents) deferred (D-IG-01).
- IG-05 verified (grep) + smoked (Step F) — the action-layer gate itself ships in 41-04, not here.
- A1/A2 MEDIUM-confidence linchpins resolved.
- Regla 6 confirmed live (Somnio + godentist-fb-ig untouched).
- Any time-gated item (per-account IG subscribe, Human Agent App Review) recorded as deferred UAT, not a failure.
</success_criteria>

<output>
After completion, create `.planning/phases/41-instagram-direct/41-07-SUMMARY.md` (and a `41-HUMAN-UAT.md` if any live step is time-gated).
</output>
