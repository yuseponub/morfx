---
phase: 39-whatsapp-outbound-templates
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/app/actions/messages.ts
  - src/lib/automations/action-executor.ts
  - src/lib/domain/contact-reviews.ts
autonomous: true
requirements: [WA-03, WA-07, MIG-03]
must_haves:
  truths:
    - "Read receipts route via Cloud API for meta_direct workspaces and 360dialog for the rest"
    - "The two direct send360Template bypass sites route through the domain provider branch"
    - "No send/receipt surface bypasses the domain chokepoint (the full 131047 blast-radius is closed)"
  artifacts:
    - path: "src/app/actions/messages.ts"
      provides: "markMessageAsRead Meta arm (D-07)"
      contains: "whatsapp_provider"
    - path: "src/lib/automations/action-executor.ts"
      provides: "host-notification template send routed through domain"
    - path: "src/lib/domain/contact-reviews.ts"
      provides: "review template send routed through domain"
  key_links:
    - from: "src/lib/automations/action-executor.ts"
      to: "src/lib/domain/messages.ts sendTemplateMessage"
      via: "domain call (no direct whatsapp/api import)"
      pattern: "sendTemplateMessage"
    - from: "src/lib/domain/contact-reviews.ts"
      to: "src/lib/domain/messages.ts sendTemplateMessage"
      via: "in-process domain call"
      pattern: "sendTemplateMessage"
---

<objective>
Close the full 131047 blast radius and add read receipts (D-07). Rewire the two direct `send360Template` bypass sites to go through the domain provider branch, and add a Meta arm to `markMessageAsRead`. After this plan, NO send/receipt surface bypasses the domain chokepoint.

Purpose: Pitfall 1 — surfaces that bypass domain send out the wrong provider's number. The provider branch (Plan 04) only helps surfaces that call domain; these three are the remaining ones that resolve apiKey themselves or import 360dialog directly.
Output: Modified `actions/messages.ts` (read receipts), `action-executor.ts` (rewire), `contact-reviews.ts` (rewire).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/app/actions/messages.ts
@src/lib/automations/action-executor.ts
@src/lib/domain/contact-reviews.ts

<interfaces>
From src/lib/meta/api.ts (Plan 02):
```typescript
export async function markWhatsAppRead(accessToken, phoneNumberId, wamid): Promise<...>
```
From src/lib/meta/credentials.ts:
```typescript
export async function resolveByWorkspace(workspaceId, 'whatsapp'): Promise<MetaCredentials | null>
```
From src/lib/domain/messages.ts (now provider-aware after Plan 04):
```typescript
export async function sendTemplateMessage(ctx, params): Promise<{ success, error?, ... }>  // requires conversationId
```
Bypass sites (grep-verified):
- `action-executor.ts:1279-1286` — host-notification `send360Template(workspace.whatsapp_api_key, hostPhone, 'informacion_general', 'es', [...])`
- `contact-reviews.ts:437-444` — `send360Template(workspace.whatsapp_api_key, contact.phone, template.templateName, template.language, components)`
- `actions/messages.ts:331-365` — `markMessageAsRead` (dynamic import `markRead360`)
</interfaces>

D-04: the 24h-window check + recipient resolution in `sendMessage`/`sendMediaMessage` are provider-agnostic and inherited for free — DO NOT touch them.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: markMessageAsRead Meta arm (D-07 read receipts)</name>
  <files>src/app/actions/messages.ts</files>
  <behavior>
    - flag=meta_direct: resolveByWorkspace then markWhatsAppRead(accessToken, phoneNumberId, wamid)
    - flag=360dialog (default): existing markRead360(apiKey, wamid) unchanged
    - 24h-window + recipient-resolution code untouched (D-04)
  </behavior>
  <action>
In `markMessageAsRead` (messages.ts:331-372), before resolving the 360dialog `apiKey`, read `workspaces.whatsapp_provider` for the conversation's workspace. Per RESEARCH Open Question 2 + 39-PATTERNS.md "MODIFY actions/messages.ts":
- `meta_direct`: `resolveByWorkspace(workspaceId, 'whatsapp')` then if creds present, `markWhatsAppRead(creds.accessToken, creds.phoneNumberId, message.wamid)`.
- `360dialog` (default): keep the existing `markRead360(apiKey, message.wamid)` arm BYTE-IDENTICAL.
Trigger point stays the existing conversation-open behavior (D-07, A5 — no new trigger). DO NOT touch the 24h-window check or recipient resolution in `sendMessage`/`sendMediaMessage` (D-04 — inherited for free). Never log the resolved token (T-39-01).
  </action>
  <verify>
    <automated>grep -nE "whatsapp_provider|markWhatsAppRead|markRead360" src/app/actions/messages.ts && (npx tsc --noEmit 2>&1 | grep -E "actions/messages" || echo "no new tsc errors")</automated>
  </verify>
  <done>markMessageAsRead has a meta_direct arm calling markWhatsAppRead and an unchanged 360dialog arm; 24h-window code untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the two send360Template bypass sites through domain</name>
  <files>src/lib/automations/action-executor.ts, src/lib/domain/contact-reviews.ts</files>
  <action>
Rewire both direct `send360Template` imports to call the now-provider-aware domain `sendTemplateMessage` (39-PATTERNS.md rewire sites + RESEARCH Pitfall 1):

**action-executor.ts (~1279):** replace the `await import('@/lib/whatsapp/api')` + `send360Template(workspace.whatsapp_api_key, hostPhone, 'informacion_general', 'es', [...])` with a `domain/messages.ts::sendTemplateMessage(ctx, {...})` call so it inherits the provider branch. The components shape it passes (`{type:'body', parameters:[{type:'text', text}]}`) already matches `SendTemplateMessageParams.components`. NOTE: domain `sendTemplateMessage` requires a `conversationId`; this is a host-notification to a fixed phone. Resolve/create the conversation for `hostPhone` in the workspace (reuse the existing recipient/conversation-resolution helper the domain send path uses) and pass its id. Do NOT reintroduce a direct 360dialog import as a fallback.

**contact-reviews.ts (~437):** since this file is itself in the domain layer, call `sendTemplateMessage` in-process. The `components` array it builds (body params + optional `{type:'header', parameters:[{type:'image', image:{link}}]}`) is already domain-compatible. Pass the contact's conversation id (resolve via the existing contact-to-conversation lookup used elsewhere in the domain).

After both rewires, no surface imports the 360dialog send directly. The 360dialog behavior is preserved for 360dialog workspaces because the domain branch defaults to it (Regla 6).
  </action>
  <verify>
    <automated>(grep -rnE "send360Template" src/lib/automations/action-executor.ts src/lib/domain/contact-reviews.ts | grep -vE "^\s*//" || echo "0 direct bypass imports remain") && npx vitest run src/lib/domain/__tests__/messages-provider.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>Zero `send360Template` direct imports remain in either file; both route through domain `sendTemplateMessage`; messages-provider test still GREEN; full suite still green for these surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automation/review trigger → domain | template send args caller-controlled; workspaceId from ctx |
| inbox action → Meta read-receipt | wamid is an inbound message id; creds from workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | Tampering (wrong number / 131047) | bypass rewires | mitigate | Both bypass sites now go through the single domain chokepoint; no per-call-site apiKey resolution remains — the literal 131047 fix completed |
| T-39-01 | Information Disclosure | read-receipt Meta arm | mitigate | Resolved token only passed to markWhatsAppRead; never logged |
| T-39-09 | DoS (prod regression) | 360dialog arms | mitigate | 360dialog arms byte-identical; domain default keeps Somnio + 4 prod workspaces unchanged |
</threat_model>

<verification>
- `grep -rn "send360Template" src/lib/automations/action-executor.ts src/lib/domain/contact-reviews.ts` returns 0 (non-comment).
- `grep -n "whatsapp_provider" src/app/actions/messages.ts` shows the read-receipt branch read.
- `npx vitest run` full suite green (no regression on automations / contact-reviews / inbox).
- `git diff` shows the 24h-window check in `sendMessage`/`sendMediaMessage` untouched (D-04).
</verification>

<success_criteria>
Every WhatsApp send/receipt surface routes through the provider-aware domain layer — the full 131047 blast radius is closed — and read receipts (D-07) work for meta_direct while 360dialog stays byte-identical (Regla 6).
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-05-SUMMARY.md`
</output>
