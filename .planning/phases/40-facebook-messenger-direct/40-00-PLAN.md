---
phase: 40-facebook-messenger-direct
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260604120000_add_messenger_provider.sql
autonomous: false
requirements: [MIG-02]
user_setup:
  - service: supabase-prod
    why: "Regla 5 — the messenger_provider column must exist in PROD before any code that reads it deploys"
    dashboard_config:
      - task: "Apply the new migration SQL in the Supabase SQL editor (production project)"
        location: "Supabase Dashboard → SQL Editor → run the contents of supabase/migrations/20260604120000_add_messenger_provider.sql"
must_haves:
  truths:
    - "A messenger_provider column exists on workspaces with default 'manychat'"
    - "Every existing workspace (incl. godentist-fb-ig) reads as 'manychat' (zero backfill)"
    - "The migration is applied to PRODUCTION and confirmed by the user BEFORE any provider-reading code deploys"
  artifacts:
    - path: "supabase/migrations/20260604120000_add_messenger_provider.sql"
      provides: "messenger_provider column (Regla 5)"
      contains: "messenger_provider"
  key_links:
    - from: "supabase/migrations/20260604120000_add_messenger_provider.sql"
      to: "workspaces.messenger_provider"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "messenger_provider"
---

<objective>
Create the `messenger_provider` migration (MIG-02 / D-10) and PAUSE for the user to apply it in PRODUCTION before any provider-reading code ships. This is the Regla 5 gate: the column MUST exist in prod before Plan 04 (the domain chokepoint that reads it) or Plan 06/07 deploy.

Purpose: Mirrors the Phase 39 `whatsapp_provider` migration. Default `'manychat'` keeps every existing workspace — including the protected `godentist-fb-ig` agent — on the ManyChat path with zero backfill (Regla 6).
Output: One migration file + an applied-in-prod confirmation checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/40-facebook-messenger-direct/40-RESEARCH.md
@.planning/phases/40-facebook-messenger-direct/40-PATTERNS.md

<interfaces>
<!-- The P39 migration template to clone verbatim (swap names). -->

From supabase/migrations/20260602120000_add_whatsapp_provider.sql (the analog):
```sql
ALTER TABLE workspaces
  ADD COLUMN whatsapp_provider TEXT NOT NULL DEFAULT '360dialog'
  CHECK (whatsapp_provider IN ('360dialog', 'meta_direct'));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the messenger_provider migration file</name>
  <read_first>
    - supabase/migrations/20260602120000_add_whatsapp_provider.sql (the EXACT template to clone — copy the header-comment style, swap names)
    - .planning/phases/40-facebook-messenger-direct/40-PATTERNS.md §`supabase/migrations/*_add_messenger_provider.sql`
    - CLAUDE.md §Regla 5 (migration before deploy)
  </read_first>
  <files>supabase/migrations/20260604120000_add_messenger_provider.sql</files>
  <action>
Create `supabase/migrations/20260604120000_add_messenger_provider.sql`. Clone the P39 `add_whatsapp_provider.sql` structure verbatim, swapping the column name to `messenger_provider`, the default to `'manychat'`, and the CHECK set to `('manychat', 'meta_direct')`.

The file MUST contain a header comment block (mirror the P39 header) stating: purpose (per-workspace Messenger provider flag, MIG-02 / D-10), phase (40-facebook-messenger-direct), and the Regla 5 warning "APPLY IN PROD BEFORE pushing any code that references workspaces.messenger_provider".

The SQL body (exact):
```sql
ALTER TABLE workspaces
  ADD COLUMN messenger_provider TEXT NOT NULL DEFAULT 'manychat'
  CHECK (messenger_provider IN ('manychat', 'meta_direct'));
```

Default `'manychat'` (Regla 6 — every existing workspace incl. godentist-fb-ig stays on ManyChat). NO backfill statement. NO index. Do NOT run `supabase db push` — the apply step is the human checkpoint in Task 2.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260604120000_add_messenger_provider.sql && grep -q "messenger_provider TEXT NOT NULL DEFAULT 'manychat'" supabase/migrations/20260604120000_add_messenger_provider.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260604120000_add_messenger_provider.sql` exists
    - `grep "ADD COLUMN messenger_provider TEXT NOT NULL DEFAULT 'manychat'"` returns a match
    - `grep "CHECK (messenger_provider IN ('manychat', 'meta_direct'))"` returns a match
    - `grep -i "regla 5\|APPLY IN PROD\|before pushing"` returns a match (header warning present)
    - `grep -c "supabase db push" supabase/migrations/20260604120000_add_messenger_provider.sql` returns 0
  </acceptance_criteria>
  <done>Migration file exists with `messenger_provider` default `'manychat'`, CHECK constraint, and Regla 5 header warning. No auto-apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply the migration in PRODUCTION (Regla 5 gate)</name>
  <what-built>The `supabase/migrations/20260604120000_add_messenger_provider.sql` migration file (Task 1). It is NOT yet applied to production.</what-built>
  <how-to-verify>
1. Open the Supabase Dashboard for the PRODUCTION project → SQL Editor.
2. Paste and run the body of `supabase/migrations/20260604120000_add_messenger_provider.sql`:
   ```sql
   ALTER TABLE workspaces
     ADD COLUMN messenger_provider TEXT NOT NULL DEFAULT 'manychat'
     CHECK (messenger_provider IN ('manychat', 'meta_direct'));
   ```
3. Confirm success — verify the column exists and every workspace defaults to `'manychat'`:
   ```sql
   SELECT messenger_provider, count(*) FROM workspaces GROUP BY messenger_provider;
   ```
   Expected: all rows under `manychat` (zero `meta_direct`).
4. Do NOT flip any workspace to `meta_direct` yet — that is the manual cutover step in Plan 08.
  </how-to-verify>
  <resume-signal>Type "applied" once the column exists in prod and all workspaces read `manychat`. This UNBLOCKS Plans 04, 06, 07 (the provider-reading code).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → prod DB | Schema change applied manually before code that depends on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-00-01 | Denial of Service | provider-reading code deployed before column exists | mitigate | Regla 5 checkpoint: migration applied + confirmed in prod BEFORE Plans 04/06/07 deploy (this plan blocks the others via depends_on) |
| T-40-00-02 | Tampering | accidental backfill flipping workspaces to meta_direct | accept | No backfill in the migration; default `'manychat'` keeps all existing workspaces (incl. godentist-fb-ig) on the untouched path (Regla 6) |
</threat_model>

<verification>
- `supabase/migrations/20260604120000_add_messenger_provider.sql` exists with default `'manychat'` + CHECK.
- User has confirmed the column exists in prod and all workspaces read `manychat`.
</verification>

<success_criteria>
- Migration file created with the correct column, default, and CHECK.
- Regla 5 honored: column applied to prod and confirmed BEFORE any provider-reading plan deploys.
</success_criteria>

<output>
After completion, create `.planning/phases/40-facebook-messenger-direct/40-00-SUMMARY.md`
</output>
