---
phase: 999.1-whatsapp-interactive-message-composer
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/app/actions/messages.ts
autonomous: true
requirements: [D-02, D-04]
must_haves:
  truths:
    - "A new server action sendInteractiveMessage mirrors sendMessage: auth (getRequestAuth) → load conversation scoped by workspace_id → 24h window check → resolve apiKey → build ctx → call domain → revalidatePath('/whatsapp')"
    - "Auth is required: no auth → { error:'No autenticado' } (V2)"
    - "Conversation loaded with .eq('workspace_id', workspaceId); channel !== 'whatsapp' rejected (FB/IG out of scope)"
    - "24h window re-checked server-side (defense-in-depth, even though UI gates it — D-02): closed window → clear error, interactive is a session message"
    - "Provider is NEVER read or branched in the action (Regla 3 — domain decides)"
  artifacts:
    - path: "src/app/actions/messages.ts"
      provides: "sendInteractiveMessage server action + domainSendInteractiveMessage import"
      contains: "export async function sendInteractiveMessage"
  key_links:
    - from: "src/app/actions/messages.ts sendInteractiveMessage"
      to: "getRequestAuth"
      via: "auth gate"
      pattern: "getRequestAuth\\(\\)"
    - from: "src/app/actions/messages.ts sendInteractiveMessage"
      to: "domain sendInteractiveMessage"
      via: "delegate after window check"
      pattern: "domainSendInteractiveMessage\\(ctx"
    - from: "src/app/actions/messages.ts sendInteractiveMessage"
      to: "conversations table"
      via: "workspace-scoped load"
      pattern: "\\.eq\\('workspace_id', workspaceId\\)"
---

<objective>
Add the `sendInteractiveMessage` server action to `src/app/actions/messages.ts`, mirroring `sendMessage` EXACTLY: auth → load conversation (workspace-scoped) → 24h-window check → resolve apiKey → build `DomainContext` → call `domainSendInteractiveMessage` → `revalidatePath('/whatsapp')`. This is the chokepoint the composer modal (Plan 04) calls.

Purpose: Adapter-layer concerns (auth, window gating defense-in-depth, cred resolution, revalidation) live here, NOT in the domain. The action NEVER reads `whatsapp_provider` (Regla 3 — the domain decides). Window check is re-applied server-side even though the UI gates it (D-02).
Output: `sendInteractiveMessage(conversationId, payload)` exported from `app/actions/messages.ts`, returning `ActionResult<{ messageId: string }>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-CONTEXT.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-RESEARCH.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md

<interfaces>
<!-- Verified anchors in src/app/actions/messages.ts (this session): -->
<!-- L4: import { revalidatePath } from 'next/cache' -->
<!-- L5: import { getRequestAuth } from '@/lib/auth/request-auth' -->
<!-- L6: import { differenceInHours } from 'date-fns' -->
<!-- L9-11: domainSendTextMessage/Media/Template import aliases from '@/lib/domain/messages' -->
<!-- L16: ActionResult type (reuse) -->
<!-- L88-178: sendMessage (the EXACT sibling skeleton to mirror) -->
<!--   L92-95: auth gate; L100-109: load conversation; L114-127: 24h window check -->
<!--   L130-147: resolve apiKey from workspaces.settings; L155-162: ctx + delegate -->
<!--   L164-177: result handling + unarchive + revalidatePath -->

<!-- domain function added by Plan 02: -->
sendInteractiveMessage(ctx, { conversationId, contactPhone, apiKey, interactiveType, body, header?, footer?, buttons?, buttonLabel?, sections? }): Promise<DomainResult<SendMessageResult>>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add sendInteractiveMessage server action mirroring sendMessage</name>
  <read_first>
    - src/app/actions/messages.ts L88-178 (sendMessage — copy structure verbatim) + L8-16 (import block + ActionResult)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md §app/actions/messages.ts (lines 120-152 — the 6-step skeleton)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-RESEARCH.md §Pattern 2 (lines 181-201)
    - src/lib/domain/messages.ts (the sendInteractiveMessage signature added in Plan 02)
  </read_first>
  <action>
    In `src/app/actions/messages.ts`:

    (a) Extend the existing domain import block (L9-11) by ADDING `sendInteractiveMessage as domainSendInteractiveMessage,` to the import from `@/lib/domain/messages`.

    (b) Add the action (copy the `sendMessage` skeleton L88-178 verbatim, adapting the payload):
    ```typescript
    export async function sendInteractiveMessage(
      conversationId: string,
      payload: {
        interactiveType: 'buttons' | 'list'
        body: string
        header?: string
        footer?: string
        buttons?: { id: string; title: string }[]
        buttonLabel?: string
        sections?: { title: string; rows: { id: string; title: string; description?: string }[] }[]
      }
    ): Promise<ActionResult<{ messageId: string }>> {
      const auth = await getRequestAuth()
      if (!auth) return { error: 'No autenticado' }
      const workspaceId = auth.workspaceId
      // ... load conversation, window check, apiKey, ctx, delegate, revalidate (below)
    }
    ```

    Steps inside (mirror sendMessage exactly):
    1. AUTH (L92-95): `const auth = await getRequestAuth(); if (!auth) return { error: 'No autenticado' }`; `const workspaceId = auth.workspaceId`.
    2. LOAD CONVERSATION (L100-109): `.from('conversations').select('id, phone, last_customer_message_at, status, channel').eq('id', conversationId).eq('workspace_id', workspaceId).single()`. Guard not-found → `{ error: 'Conversación no encontrada' }`. ADD a channel guard: `if (conversation.channel !== 'whatsapp') return { error: 'Interactivos solo disponibles en WhatsApp' }` (FB/IG out of scope — RESEARCH Pattern 2 channel note).
    3. 24h WINDOW CHECK (copy verbatim L114-127 — interactive is a session message, window MUST be open):
       ```typescript
       if (!conversation.last_customer_message_at) {
         return { error: 'Ventana de 24h cerrada. Usa un template.' }
       }
       const hoursSinceCustomerMessage = differenceInHours(new Date(), new Date(conversation.last_customer_message_at))
       if (hoursSinceCustomerMessage >= 24) {
         return { error: 'Ventana de 24h cerrada. Usa un template.' }
       }
       ```
    4. RESOLVE apiKey (copy L130-147 whatsapp arm): load `workspaces.settings`, `const apiKey = workspaceSettings?.settings?.whatsapp_api_key || process.env.WHATSAPP_API_KEY`. (Meta arm ignores apiKey; the domain resolves Meta creds itself — do NOT read whatsapp_provider here.)
    5. BUILD ctx + DELEGATE (L155-162): `const ctx: DomainContext = { workspaceId, source: 'server-action' }`; `const result = await domainSendInteractiveMessage(ctx, { conversationId, contactPhone: conversation.phone, apiKey, ...payload })`.
    6. RESULT + revalidate (L164-177): `if (!result.success) return { error: result.error || 'Error al enviar mensaje interactivo' }`; unarchive if `conversation.status === 'archived'` (copy L169-174); `revalidatePath('/whatsapp')`; `return { success: true, data: { messageId: result.data!.messageId } }`.

    DO NOT read or branch on `whatsapp_provider` anywhere in this action (Regla 3 — the domain owns the provider decision).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "src/app/actions/messages.ts" | grep -c "error" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function sendInteractiveMessage" src/app/actions/messages.ts` >= 1
    - `grep -c "sendInteractiveMessage as domainSendInteractiveMessage" src/app/actions/messages.ts` >= 1
    - `grep -c "Interactivos solo disponibles en WhatsApp" src/app/actions/messages.ts` >= 1
    - the action contains the 24h check: `grep -c "Ventana de 24h cerrada" src/app/actions/messages.ts` increased by >=1 vs the sendMessage baseline
    - REGLA 3: `whatsapp_provider` is NOT referenced in the new action — `grep -c "whatsapp_provider" src/app/actions/messages.ts` == 0
    - the action delegates to the domain: `grep -c "domainSendInteractiveMessage(ctx" src/app/actions/messages.ts` >= 1
    - workspace scoping present: the new action loads conversation with `.eq('workspace_id', workspaceId)` (mirror sendMessage)
    - `npx tsc --noEmit` reports 0 errors for src/app/actions/messages.ts
  </acceptance_criteria>
  <done>sendInteractiveMessage server action mirrors sendMessage (auth, workspace-scoped load, 24h window, apiKey, ctx, delegate, revalidate); no provider read; channel guarded to whatsapp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (composer modal) → server action | Untrusted `conversationId` + interactive payload from the browser. Auth + workspace scoping + window gating enforced here. |
| server action → domain | Trusted-after-auth call; apiKey resolved from workspace settings, not from the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-V2 | Spoofing (unauth send) | action entry | mitigate | `getRequestAuth()` gate; no auth → `{ error:'No autenticado' }` (mirror sendMessage). |
| T-999.1-V4 | Tampering / EoP (cross-tenant conversation) | conversation load | mitigate | `.eq('workspace_id', workspaceId)` with workspaceId from `auth`, never from the client payload (Regla 3 / Phase 39 CR-01 lesson). |
| T-999.1-D2 | (Meta policy violation — send outside 24h) | window check | mitigate | Server-side `differenceInHours(...) >= 24` re-check (defense-in-depth behind the UI gate D-02); interactive is a session message. |
| T-999.1-D3 | Tampering (provider bypass at call-site) | action body | mitigate | Action NEVER reads/branches `whatsapp_provider` (Regla 3 grep gate == 0); domain owns the decision. |
| T-39-01 | Information Disclosure (token in logs) | apiKey handling | mitigate | apiKey passed to domain only; never logged. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors for actions/messages.ts
- `grep -c "whatsapp_provider" src/app/actions/messages.ts` → 0 (Regla 3)
- Auth + workspace + window guards present (acceptance criteria greps)
</verification>

<success_criteria>
- sendInteractiveMessage action exists, mirrors sendMessage, gates auth + workspace + 24h window + channel, never reads provider, delegates to domain.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-03-SUMMARY.md`
</output>
