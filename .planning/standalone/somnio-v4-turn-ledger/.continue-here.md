# CONTINUE-HERE — somnio-v4-turn-ledger (handoff post-/clear)

**Creado:** 2026-05-28 · **Rama:** `exec/debounce-v2-wave6` · **HEAD esperado:** `d9de2bb7`

## Cómo retomar

Este es un standalone en `.planning/standalone/somnio-v4-turn-ledger/` — **NO lo descubre
`gsd-tools init execute-phase`** (devuelve `phase_found: false`). Hay que orquestar a mano:
leer los 5 PLAN.md, ejecutar por waves. Workflow de referencia ya leído:
`$HOME/.claude/get-shit-done/workflows/execute-phase.md` (tooling real:
`node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs`, NO `gsd-sdk`).

## Directiva del usuario

**Correr las waves restantes DE CORRIDO, sin pausas entre ellas.** El único checkpoint
humano (Wave 2 = migración) YA está resuelto. Planes 01/03/04/05 son `autonomous: true`.

## Estado YA hecho (NO repetir)

| Commit | Qué |
|--------|-----|
| `c78fde1e` | docs(v4): correcciones de comentarios Haiku/Flash-Lite → Gemini 2.5 Flash (8 archivos, higiene previa) |
| `415aad6c` | **Wave 2 / Plan 02**: migración `20260528000000_v4_turn_ledger_dims_column.sql` (ADD COLUMN idempotente) |
| `d9de2bb7` | docs: 02-SUMMARY |

- **Migración APLICADA en prod por el usuario** (Supabase SQL editor, confirmado "ok ya").
  Regla 5 satisfecha → el código que usa la columna YA se puede pushear.
- `02-SUMMARY.md` existe → **saltar Wave 2**.

## Waves pendientes (ejecutar en este orden, secuencial en main, SIN worktree)

Cada wave = 1 plan. Spawn `gsd-executor` (model opus) por plan, en secuencia (no paralelo;
no hay paralelismo que ganar). Capturar `EXPECTED_BASE` antes; el executor commitea atómico
con hooks (commit normal, NO --no-verify, ya que es secuencial en main). Cada plan crea su
`0N-SUMMARY.md` en este dir.

- [ ] **Wave 1 → Plan 01** — tipos `TurnLedger`/`TurnLedgerDims` + `commitTurn` + serialize/deserialize. Files: `somnio-v4/types.ts`, `somnio-v4/state.ts`, `agents/types.ts`, `__tests__/state.test.ts`.
- [ ] **Wave 3 → Plan 03** — 7 commit-paths del agente construyen TurnLedger COMPLETO + `commitTurn`; rama RAG registra `kb_topic` (cierra bug central D-05). Files: `somnio-v4-agent.ts`, `__tests__/somnio-v4-agent.test.ts`.
- [ ] **Wave 4 → Plan 04** — runner + sandbox engine restore/persist/carryState dims + observability. Files: `engine/v4-production-runner.ts`, `somnio-v4/engine-v4.ts`, `sandbox/types.ts`.
- [ ] **Wave 5 → Plan 05** — UI state-tab (KB Topics + CRM Actions), `ARCHITECTURE.md` (doc ledger + corrige crm_mutation), tests Regla 6. Files: `debug-panel/state-tab.tsx`, `__tests__/state.test.ts`, `somnio-v4/ARCHITECTURE.md`.

## Después de las waves

1. Verificación de fase (spawn `gsd-verifier`, model sonnet) → VERIFICATION.md.
2. **Push a Vercel** (Regla 1): `git push origin exec/debounce-v2-wave6` (o la rama vigente).
3. Greps Regla 6 (0 impacto no-v4) — están en el must_haves de Plan 05.
4. Actualizar memoria `somnio_v4_architecture_roadmap` (standalone #1 de 3 cerrado).

## Notas

- `src/lib/agents/somnio-v4/ARCHITECTURE.md` está **untracked** — Plan 05 lo edita; quedará trackeado en ese commit.
- v4 DORMANT (0 workspaces) — Regla 6: cambios solo en archivos somnio-v4-specific.
