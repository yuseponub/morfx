---
phase: 37.5-meta-verification-website
plan: 05
type: execute
wave: 4
depends_on: ["37.5-03", "37.5-04"]
files_modified:
  - docs/analysis/04-estado-actual-plataforma.md
  - .planning/phases/37.5-meta-verification-website/37.5-LEARNINGS.md
  - .planning/phases/37.5-meta-verification-website/META-VERIFICATION-CHECKLIST.md
autonomous: false

must_haves:
  truths:
    - "Full regression test suite (build + key routes) passes locally before push"
    - "Site deployed to Vercel at https://morfx.app with all marketing and auth routes responding 200"
    - "Meta Business Manager Domain Verification TXT record checklist exists and user has executed it"
    - "Business Verification has been resubmitted in Meta Business Manager by the user"
    - "Documentation updated (LEARNINGS + estado-actual-plataforma + memory)"
  artifacts:
    - path: ".planning/phases/37.5-meta-verification-website/META-VERIFICATION-CHECKLIST.md"
      provides: "Step-by-step manual checklist for user to verify domain in Meta and resubmit Business Verification"
      min_lines: 30
    - path: ".planning/phases/37.5-meta-verification-website/37.5-LEARNINGS.md"
      provides: "Phase learnings: middleware whitelist pattern, next-intl with Next 16, locale-prefix as-needed quirks, og-image sizing"
  key_links:
    - from: "https://morfx.app"
      to: "Meta Business Manager Domain Verification"
      via: "DNS TXT record on Porkbun"
      pattern: "facebook-domain-verification"
---

<objective>
Close out Phase 37.5: run full regression, deploy to Vercel, produce a manual checklist for the user to (a) verify the morfx.app domain in Meta Business Manager via DNS TXT and (b) resubmit Business Verification with the now-public landing as evidence, then update documentation.

Purpose: The implementation work is done in Plans 01-04. This plan turns that into a deployed, reviewer-ready site plus the explicit manual steps only the user can perform (Meta dashboard, Porkbun DNS).

Output: Production deploy at morfx.app, checklist the user follows to close the Meta Business Verification loop, and updated project docs.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.5-meta-verification-website/37.5-CONTEXT.md
@.planning/phases/37.5-meta-verification-website/37.5-01-SUMMARY.md
@.planning/phases/37.5-meta-verification-website/37.5-02-SUMMARY.md
@.planning/phases/37.5-meta-verification-website/37.5-03-SUMMARY.md
@.planning/phases/37.5-meta-verification-website/37.5-04-SUMMARY.md
@docs/analysis/04-estado-actual-plataforma.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local regression test — build + route smoke + responsive</name>
  <files>(none — validation only)</files>
  <action>
  Run the full local regression before push:

  1. Type check: `npx tsc --noEmit` — must pass with 0 errors
  2. Build: `npm run build` — must succeed; capture any warnings about next-intl plugin or middleware edge runtime
  3. Start prod locally: `npm start` (port 3020) in background
  4. Smoke test each route with curl and expect 200 (or 307 for login-redirect):

     # Marketing (must be 200, no redirect)
     curl -sI http://localhost:3020/         | head -1
     curl -sI http://localhost:3020/en       | head -1
     curl -sI http://localhost:3020/privacy  | head -1
     curl -sI http://localhost:3020/en/privacy | head -1
     curl -sI http://localhost:3020/terms    | head -1
     curl -sI http://localhost:3020/en/terms | head -1

     # Auth (must still respond — login shows form, others redirect)
     curl -sI http://localhost:3020/login         | head -1
     curl -sI http://localhost:3020/signup        | head -1
     curl -sI http://localhost:3020/forgot-password | head -1

     # Protected (must redirect to /login when unauthenticated)
     curl -sI http://localhost:3020/crm | head -1  # expect 307 → /login

  5. Content grep: confirm required strings appear in HTML
     curl -s http://localhost:3020/ | grep -E "MORFX S.A.S.|902052328-5|Carrera 38|\+57 313 754 9286|morfx.colombia@gmail.com" | wc -l  # expect >= 5

  6. Responsive visual check (manual browser): resize devtools to 375px, 768px, 1280px — no horizontal scroll, no broken layout on /, /en, /privacy, /terms

  If ANY check fails, STOP and diagnose before proceeding to Task 2.
  </action>
  <verify>
  All six marketing routes return 200; /login returns 200; /crm returns 307; content grep finds legal info strings; no build errors; responsive check clean.
  </verify>
  <done>Local build and all routes verified; safe to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Commit, push to main, and confirm Vercel deploy</name>
  <files>(git operations — no direct file modifications)</files>
  <action>
  Per Regla 1 (push to Vercel after every plan), this is the deploy task.

  1. Confirm all previous plans' commits are already pushed. If not, run:
       git status
       git log origin/main..HEAD --oneline
     and push any pending commits.

  2. If there are any uncommitted changes from this plan's Task 1 (e.g., fixes discovered during regression), commit them atomically:
       git add <files>
       git commit -m "fix(37.5): regression fix — <description>

       Co-authored-by: Claude <noreply@anthropic.com>"
       git push origin main

  3. Wait for Vercel build to complete (~2-3 min). Monitor with:
       # If vercel CLI authenticated:
       vercel ls --prod | head -5
     Otherwise poll the production URL:
       until curl -sI https://morfx.app/ | grep -q "HTTP/2 200"; do sleep 10; done

  4. Smoke test production:
       curl -sI https://morfx.app/         | head -1   # 200
       curl -sI https://morfx.app/en       | head -1   # 200
       curl -sI https://morfx.app/privacy  | head -1   # 200
       curl -sI https://morfx.app/en/privacy | head -1 # 200
       curl -sI https://morfx.app/terms    | head -1   # 200
       curl -sI https://morfx.app/en/terms | head -1   # 200
       curl -sI https://morfx.app/login    | head -1   # 200
       curl -s  https://morfx.app/ | grep -c "MORFX S.A.S."  # >= 1

  5. If any production route fails, revert via `git revert` the offending commit, push, and diagnose before continuing.
  </action>
  <verify>
  All production routes return 200; legal info strings present in production HTML at https://morfx.app/.
  </verify>
  <done>Production deployed to morfx.app; all marketing + auth routes verified live.</done>
</task>

<task type="auto">
  <name>Task 3: Write META-VERIFICATION-CHECKLIST.md for user to execute manual Meta steps</name>
  <files>.planning/phases/37.5-meta-verification-website/META-VERIFICATION-CHECKLIST.md</files>
  <action>
  Create a detailed step-by-step checklist the user will follow manually. Include:

  # Meta Business Verification Checklist

  ## Prerequisites (already done by Claude)
  - [x] https://morfx.app loads with public landing
  - [x] /privacy and /terms return 200 with substantive content
  - [x] Legal business info (MORFX S.A.S., NIT, address, phone, email) visible in footer

  ## Part A — Domain Verification in Meta Business Manager

  1. Open https://business.facebook.com/settings/
  2. Go to **Brand Safety → Domains**
  3. Click **Add domain** → enter `morfx.app`
  4. Meta will show a verification method; choose **DNS TXT record**
  5. Copy the TXT value (looks like `facebook-domain-verification=XXXXXXXXXXXX`)
  6. Open Porkbun (https://porkbun.com/account/domainsSpeedy) → `morfx.app` → **DNS Records**
  7. Add a new record:
     - Type: `TXT`
     - Host: `@` (or leave blank)
     - Answer: `facebook-domain-verification=XXXXXXXXXXXX` (exact value from Meta)
     - TTL: 600 (default fine)
  8. Save
  9. Wait 5-15 minutes for DNS propagation. Check with:
       dig TXT morfx.app +short
     Expected output: includes `"facebook-domain-verification=..."`
  10. Return to Meta Business Manager → Domains → click **Verify** next to morfx.app
  11. Confirm domain shows as **Verified** (green check)

  ## Part B — Resubmit Business Verification

  1. Open https://business.facebook.com/settings/security
  2. Go to **Business Verification** (or Security Center → Business Verification)
  3. Click **Start verification** (or **Resubmit**)
  4. Confirm business details:
     - Legal name: **MORFX S.A.S.**
     - Address: Carrera 38 # 42-17 Apartamento 1601B, Bucaramanga, Santander, Colombia
     - Phone: +57 313 754 9286
     - Website: https://morfx.app
     - Business email: morfx.colombia@gmail.com
  5. Upload documents again (these were already accepted but Meta asks on each resubmission):
     - Certificado de Existencia y Representación Legal (cert-12614394-1-0.pdf)
     - Certificado de Matrícula Mercantil (cert-12614393-0-0.pdf)
     - RUT DIAN (RUT MORFX SAS - MARZO 2026 (4).pdf)
  6. Pick verification method: **Email OTP** (fastest) to morfx.colombia@gmail.com
  7. Submit
  8. Expected SLA: 2-5 business days
  9. Monitor status at https://business.facebook.com/settings/security

  ## Part C — If Meta rejects again

  Capture the exact rejection message (screenshot). Common causes:
  - Address on website does NOT match address on legal docs → fix footer and resubmit
  - Privacy/Terms missing specific clauses Meta flags → update and resubmit
  - Website still has "under construction" or placeholder text → add real content

  Escalation: open a support case via Meta Business Help Center referencing the morfx.app domain and attach landing screenshots.

  ## Part D — After approval

  - [ ] Confirm Business Verification status = "Verified" in Business Manager
  - [ ] Update `meta_integration_setup.md` in memory with verification date
  - [ ] Unblock Phase 38 (Embedded Signup)

  ---

  **Prepared:** 2026-04-14
  **Executor:** User (Jose) — manual actions in Meta + Porkbun dashboards
  </action>
  <verify>
  `.planning/phases/37.5-meta-verification-website/META-VERIFICATION-CHECKLIST.md` exists and contains Parts A, B, C, D.
  </verify>
  <done>Checklist ready for user to execute Meta Business Manager steps.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
  Everything automatable is done: morfx.app is live with landing, privacy, terms in ES/EN; legal info visible; login still works.
  </what-built>
  <action>
  Execute the manual steps in `.planning/phases/37.5-meta-verification-website/META-VERIFICATION-CHECKLIST.md`:
    - Part A: Add DNS TXT on Porkbun, verify domain in Meta
    - Part B: Resubmit Business Verification in Meta Business Manager

  These require logging into Porkbun and Meta Business Manager — only you can do this.
  </action>
  <how-to-verify>
  1. After Part A: `dig TXT morfx.app +short` includes `facebook-domain-verification=...`
  2. After Part A: Domain shows "Verified" in Meta Business Manager → Domains
  3. After Part B: Business Verification status shows "In review" or "Verified" (not rejected)
  </how-to-verify>
  <resume-signal>
  Reply with:
    - "Domain verified + BV resubmitted" (happy path)
    - "Domain verified but BV failed — reason: <...>" (iterate on landing/legal)
    - "Blocked on <...>" (we help troubleshoot)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update documentation — estado-actual-plataforma.md, LEARNINGS.md, user memory</name>
  <files>docs/analysis/04-estado-actual-plataforma.md, .planning/phases/37.5-meta-verification-website/37.5-LEARNINGS.md</files>
  <action>
  Per Regla 4 (documentation always updated):

  1. **Update `docs/analysis/04-estado-actual-plataforma.md`:**
     - Find the section on website/public presence (or Meta integration status if it exists)
     - Add entry: "Phase 37.5 complete — morfx.app now serves public bilingual landing with privacy and terms; Meta Business Verification resubmitted on YYYY-MM-DD"
     - Update v5.0 milestone progress (was 10%, now ~20% with Phase 37.5 done)
     - Remove any reference to "morfx.app only has /login" in technical debt sections

  2. **Create `.planning/phases/37.5-meta-verification-website/37.5-LEARNINGS.md`:**
     - Bugs found: (document any regressions discovered during plan execution)
     - Patterns learned:
       * `localePrefix: 'as-needed'` + route group `(marketing)/[locale]` renders `/` as ES and `/en` as EN cleanly
       * Next 16 async params (`params: Promise<{locale}>`) requires `await` before use
       * Supabase middleware whitelist must be composed with next-intl middleware — order matters (intl first for marketing, supabase for everything else)
       * `/login`, `/signup`, etc. stay OUTSIDE the locale segment (auth has no locale variants)
       * `messages/` folder lives at repo root, not under src/
       * Hand-writing privacy/terms avoids dependency on external legal template services (Termly, iubenda) and keeps content aligned with actual stack (Supabase, Vercel, Meta, Alegra explicitly named as processors)
     - Decisions: ES as default, EN as /en prefix, single OG image for both locales, v1.0 pending-legal-review banner on both docs
     - Open questions: Final branded OG image; professional legal review of privacy/terms before major launch

  3. **Update user memory file `meta_integration_setup.md`** (via Bash append or manual edit — this lives in Claude memory):
     - Add: "Phase 37.5 complete 2026-04-XX — morfx.app live with public bilingual landing + legal pages. Domain TXT verified in Meta Business Manager. Business Verification resubmitted."
  </action>
  <verify>
  - `grep -c "Phase 37.5" docs/analysis/04-estado-actual-plataforma.md` >= 1
  - `ls .planning/phases/37.5-meta-verification-website/37.5-LEARNINGS.md` exists
  - Memory file references Phase 37.5 completion
  </verify>
  <done>Documentation synced with reality: platform status, learnings captured, user memory updated.</done>
</task>

</tasks>

<verification>
- Production site live at https://morfx.app with all required routes returning 200
- DNS TXT for Meta domain verification in place (user-confirmed)
- Business Verification resubmitted (user-confirmed)
- Documentation updated (estado-actual, LEARNINGS, memory)
</verification>

<success_criteria>
Phase 37.5 complete: Meta reviewers can now visit morfx.app and verify MORFX S.A.S. is a real business with a legitimate SaaS product. Domain is verified in Business Manager. Business Verification is resubmitted and tracking. Phase 38 (Embedded Signup) is unblocked pending Meta's review response.
</success_criteria>

<output>
After completion, create `.planning/phases/37.5-meta-verification-website/37.5-05-SUMMARY.md` with:
- Production deploy URL and verification timestamps
- DNS TXT value used (for future reference)
- Date BV was resubmitted
- Any user-reported issues from the Meta checklist
- Link to LEARNINGS.md
</output>
