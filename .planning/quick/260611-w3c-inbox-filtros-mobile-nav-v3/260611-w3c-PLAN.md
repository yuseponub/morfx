---
phase: 260611-w3c-inbox-filtros-mobile-nav-v3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/nav-items.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/mobile-nav.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/whatsapp/components/conversation-list.tsx
autonomous: true
requirements: [C-6, C-7, M-1]
must_haves:
  truths:
    - "En inbox v3, el usuario puede filtrar conversaciones por 'Sin asignar' y 'Sin respuesta'"
    - "En inbox v3, el usuario puede alternar el orden de la lista entre última interacción del cliente y último mensaje"
    - "En móvil v3, el menú lateral lista los 14 módulos del sidebar (no 5), respetando admin/settings/hidden filters"
    - "El sidebar desktop v3/v2/legacy renderiza idéntico tras el refactor de navCategoriesV2 (verificable por diff de comportamiento)"
    - "El chip de filtro por etiqueta (commit 5c358db1) sigue funcionando intacto en inbox v3"
    - "El MobileNav del header de marketing (header.tsx) sigue byte-frozen (v3=false)"
  artifacts:
    - path: "src/components/layout/nav-items.ts"
      provides: "navCategoriesV2 + NavItem type + CAT_SLUG + filterNavItem helper compartidos"
      contains: "navCategoriesV2"
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Rama v3 derivada de navCategoriesV2 con categorías y filtros"
      contains: "navCategoriesV2"
    - path: "src/app/(dashboard)/whatsapp/components/conversation-list.tsx"
      provides: "Chips 'Sin asignar' + 'Sin respuesta' + toggle de ordenamiento en rama v3"
      contains: "Sin asignar"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "src/components/layout/nav-items.ts"
      via: "import { navCategoriesV2, NavItem, CAT_SLUG }"
      pattern: "from './nav-items'"
    - from: "src/components/layout/mobile-nav.tsx"
      to: "src/components/layout/nav-items.ts"
      via: "import navCategoriesV2 + filterNavItem"
      pattern: "from './nav-items'"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "<MobileNav v3 currentWorkspace={currentWorkspace} />"
      pattern: "MobileNav v3"
---

<objective>
Restaurar tres capacidades que el rediseño editorial v3 dejó sin punto de entrada en el UI (gaps C-6, C-7, M-1 de `.planning/standalone/ui-v3-parity-audit/AUDIT.md`):

- **C-7 (Inbox):** chips "Sin asignar" y "Sin respuesta" — la lógica de filtro YA existe (`ConversationFilter` incluye `'unassigned'` y `'unanswered'`; el hook ya las aplica). Solo falta cablear 2 chips en la rama v3.
- **M-1 (Inbox):** control UI para el toggle de ordenamiento — `sortMode`/`setSortMode` YA existen y se aplican en v3, pero no hay control visible. Falta un chip/control en la fila de filtros v3.
- **C-6 (Shell móvil):** el `mobile-nav.tsx` v3 hardcodea 5 items; el sidebar v3 enlaza 14 vía `navCategoriesV2`. Derivar el mobile-nav v3 de `navCategoriesV2` aplicando los MISMOS filtros (adminOnly / settingsKey / hidden_modules).

Purpose: Paridad funcional del shell editorial v3 con la UI legacy/v2 antes de cualquier activación del flag `ui_editorial_v3` (hoy OFF en prod).
Output: nav-items.ts compartido + sidebar refactorizado (puro) + mobile-nav v3 completo + 3 controles nuevos en inbox v3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/standalone/ui-v3-parity-audit/AUDIT.md
@./CLAUDE.md

<constraints_recordatorio>
- **Regla 6 (CRÍTICA):** todos los cambios SOLO aditivos a las ramas v3. legacy/v2 y el `<MobileNav />` del header de marketing quedan INTACTOS. El flag `ui_editorial_v3` sigue OFF en prod.
- **No tocar el chip de tag filter** (commit 5c358db1, conversation-list.tsx ~L329-378). Los chips nuevos se AÑADEN sin reescribir ese bloque.
- **El refactor de `navCategoriesV2` a módulo compartido debe ser PURO:** el sidebar importa el array desde `nav-items.ts` sin cambio de comportamiento. La rama v3, la rama v2 y la legacy del sidebar deben seguir renderizando idéntico.
- **Verify final:** `pnpm exec tsc --noEmit` con 0 errores.
</constraints_recordatorio>

<interfaces>
<!-- Contratos clave extraídos del codebase — el ejecutor los usa directo, sin explorar. -->

De src/hooks/use-conversations.ts (YA existe, no modificar):
```typescript
export type ConversationFilter = 'all' | 'unread' | 'mine' | 'unassigned' | 'archived' | 'unanswered'
// El hook expone, entre otros:
//   filter: ConversationFilter, setFilter: (f: ConversationFilter) => void
//   agentFilter / setAgentFilter
//   sortMode: ConversationSort, setSortMode: React.Dispatch<SetStateAction<ConversationSort>>
// ConversationSort = 'last_customer_message' | 'last_message'  (default 'last_customer_message')
```

De src/components/layout/sidebar.tsx (a EXTRAER verbatim a nav-items.ts):
```typescript
type NavItem = {
  href: string
  label: string
  icon: typeof Building2
  badgeType?: 'tasks' | 'automations'
  adminOnly?: boolean
  settingsKey?: string   // '<ns>.<key>' contra workspace.settings JSONB
  subLink?: { href: string; label: string; icon: typeof Building2 }
}
type SidebarCategoryV2 = { label: string; items: NavItem[] }
const CAT_SLUG: Record<string, string> = {
  'Operación': 'op', 'Automatización': 'auto', 'Análisis': 'ana', 'Admin': 'adm',
}
const navCategoriesV2: SidebarCategoryV2[] = [ /* 4 categorías, 14 items — verbatim */ ]
// El filtro (idéntico en ramas v3 y v2 del sidebar):
//   const filterItem = (item: NavItem): boolean => {
//     if (item.adminOnly && !isManager) return false
//     if (hiddenModules?.includes(item.href)) return false
//     if (item.settingsKey) { const [ns,key]=item.settingsKey.split('.'); ... }
//     return true
//   }
// isManager = role === 'owner' || role === 'admin'
// hiddenModules = (workspace.settings?.hidden_modules as string[] | undefined)
```

De src/lib/types/database.ts:
```typescript
export interface WorkspaceWithRole extends Workspace {
  role: WorkspaceRole          // 'owner' | 'admin' | 'agent' | ...
  settings: Record<string, unknown> | null
}
```

De src/components/layout/mobile-nav.tsx (firma actual — extender de forma aditiva):
```typescript
export function MobileNav({ v3 = false }: { v3?: boolean } = {}): JSX.Element
// Consumidores: header.tsx -> <MobileNav />  (marketing, v3=false, NO TOCAR)
//               (dashboard)/layout.tsx -> <MobileNav v3 />  (extender aquí)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer navCategoriesV2 a módulo compartido (refactor puro del sidebar)</name>
  <files>src/components/layout/nav-items.ts, src/components/layout/sidebar.tsx</files>
  <action>
Crear `src/components/layout/nav-items.ts` (nuevo módulo, sin 'use client' — es data + tipos + helper puro) y mover ALLÍ, VERBATIM, las definiciones que hoy viven en sidebar.tsx:

1. El tipo `NavItem` (sidebar.tsx L33-51) → exportarlo.
2. El tipo `SidebarCategoryV2` (L144-147) → exportarlo.
3. La constante `CAT_SLUG` (L154-156) → exportarla.
4. La constante `navCategoriesV2` (L158-192) → exportarla. Mantener los imports de iconos lucide necesarios (Building2, MessageSquare, ListTodo, CalendarCheck, MessageSquareText, Zap, Bot, Terminal, BarChart3, TrendingUp, FlaskConical, Users, Settings) en el nuevo archivo.
5. Añadir un helper puro EXPORTADO `filterNavItem(item, ctx)` que encapsule la lógica de `filterItem` duplicada hoy en sidebar.tsx (L254-263 rama v3 y L430-439 rama v2). Firma:
   ```typescript
   export function filterNavItem(
     item: NavItem,
     ctx: { isManager: boolean; hiddenModules?: string[]; settings?: Record<string, unknown> | null }
   ): boolean
   ```
   con el cuerpo idéntico (adminOnly → !isManager; hidden_modules; settingsKey ns.key contra settings).

Luego en `sidebar.tsx`:
- Importar `{ navCategoriesV2, CAT_SLUG, filterNavItem, type NavItem, type SidebarCategoryV2 }` desde `'./nav-items'`.
- BORRAR las definiciones locales movidas (el tipo `NavItem`, `SidebarCategoryV2`, `CAT_SLUG`, `navCategoriesV2`). DEJAR INTACTO el array `navItems` (flat legacy, L53-131) que sigue usando la rama legacy — NO se mueve.
- Reemplazar las DOS definiciones inline de `const filterItem = ...` (ramas v3 y v2) por uso de `filterNavItem(item, { isManager, hiddenModules, settings })`. El `filterItem` que usa la rama legacy (`filteredNavItems`, L233-242) PUEDE quedarse inline o usar el helper — preferir usar el helper para consistencia, pero verificar que el comportamiento es idéntico.
- Quitar de los imports de lucide en sidebar.tsx SOLO los iconos que ya NO se usen tras mover navCategoriesV2 (los que sigan usándose en `navItems` flat o en la rama legacy deben permanecer). Verificar con grep que no quede import sin usar (tsc lo marcará).

REGLA 6: este es un refactor PURO. Las 3 ramas del sidebar (v3, v2, legacy) deben renderizar idéntico. Cero cambio de markup, clases, estilos ni orden de items.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "sidebar|nav-items" || echo "OK sin errores en sidebar/nav-items"</automated>
  </verify>
  <done>nav-items.ts existe y exporta navCategoriesV2 + NavItem + SidebarCategoryV2 + CAT_SLUG + filterNavItem. sidebar.tsx los importa, ya no define navCategoriesV2 local, mantiene navItems flat. tsc sin errores nuevos. Diff del sidebar = solo import + borrado de defs + sustitución de filterItem inline por helper (sin cambio de markup).</done>
</task>

<task type="auto">
  <name>Task 2: Derivar mobile-nav v3 de navCategoriesV2 con filtros y categorías</name>
  <files>src/components/layout/mobile-nav.tsx, src/app/(dashboard)/layout.tsx</files>
  <action>
Objetivo C-6: la rama v3 del mobile-nav (mobile-nav.tsx L69-111) debe listar los 14 módulos agrupados por categoría, aplicando los MISMOS filtros que el sidebar (admin / settingsKey / hidden_modules).

1. **Threading de datos (aditivo, no rompe marketing):** extender la firma de `MobileNav` para aceptar `currentWorkspace` opcional:
   ```typescript
   export function MobileNav(
     { v3 = false, currentWorkspace = null }:
     { v3?: boolean; currentWorkspace?: WorkspaceWithRole | null } = {}
   )
   ```
   Importar `WorkspaceWithRole` de `@/lib/types/database`. El `<MobileNav />` del header de marketing (header.tsx) NO pasa props → `currentWorkspace` queda `null` y NUNCA entra a la rama v3 (v3=false). NO modificar header.tsx.

2. En `(dashboard)/layout.tsx` L89, pasar el workspace que ya tiene el layout:
   `<MobileNav v3 currentWorkspace={currentWorkspace} />`
   (el layout ya tiene `currentWorkspace` en scope — es el mismo que pasa al `<Sidebar>`).

3. **Badges:** importar `useTaskBadge` y `useAutomationBadge` (mismos hooks que el sidebar, `@/hooks/use-task-badge` y `@/hooks/use-automation-badge`). Llamarlos SIEMPRE en el top del componente (reglas de hooks — no condicionar por v3). En la rama no-v3 simplemente no se usan los valores (el marketing header monta MobileNav sin WorkspaceProvider, pero estos hooks NO dependen de WorkspaceProvider — verificar; si alguno lanza sin provider, envolver su uso solo en la rama v3 moviendo la llamada del hook a un sub-componente `<MobileNavV3>` que solo se renderiza cuando v3. Preferir el sub-componente para aislar los hooks del path de marketing y evitar regresión).

   **Decisión recomendada (más segura para Regla 6):** extraer la rama v3 a un componente interno `function MobileNavV3({ currentWorkspace }: { currentWorkspace: WorkspaceWithRole | null })` en el mismo archivo. Ese sub-componente usa `useState(open)`, `usePathname`, `useTaskBadge`, `useAutomationBadge`. `MobileNav` hace `if (v3) return <MobileNavV3 currentWorkspace={currentWorkspace} />` ANTES del return legacy. Así los hooks de badge solo corren en el path dashboard (que sí tiene los providers), y el path marketing queda byte-frozen.

4. **Render v3 derivado de navCategoriesV2:** dentro de `MobileNavV3`, importar `{ navCategoriesV2, CAT_SLUG, filterNavItem, type NavItem }` de `'./nav-items'`. Calcular:
   ```typescript
   const isManager = currentWorkspace?.role === 'owner' || currentWorkspace?.role === 'admin'
   const settings = currentWorkspace?.settings as Record<string, unknown> | null | undefined
   const hiddenModules = settings?.hidden_modules as string[] | undefined
   ```
   Reemplazar el `<nav className="sb-nav">` actual (que mapea `navItems` hardcodeado con una sola `.cat` "Navegacion") por un map sobre `navCategoriesV2`, idéntico en estructura al sidebar v3 (L289-335):
   - Por categoría: `const visible = category.items.filter(it => filterNavItem(it, { isManager, hiddenModules, settings }))`; si `visible.length === 0` → `return null`.
   - Wrapper `<div className={cn('sb-sec', CAT_SLUG[category.label])}>` + `<div className="cat">{category.label}</div>` + `<ul>`.
   - Por item: `<li><Link href onClick={() => setOpen(false)} className={isActive ? 'active' : ''}>` con `isActive = pathname === item.href || pathname.startsWith(item.href + '/')` (mismo criterio que el sidebar v3, NO el `startsWith` simple que usa hoy el mobile-nav).
   - Icono `<Icon width={16} height={16} />` + `<span style={{ flex: 1 }}>{item.label}</span>`.
   - Badge: igual que el sidebar v3 (L311-326): si `item.badgeType==='tasks'` → taskBadgeCount; `'automations'` → automationFailureCount; render el `<span>` con los mismos estilos inline (`--viv-red`, font-mono 10px, etc.) cuando `badgeCount > 0`.
   - Mantener el `<SheetContent className="theme-editorial-v3 sb w-64 p-0">` + `<SheetHeader className="brand">` + wordmark `morf·x` que ya existe (L78-85). NO tocar esa cabecera.
   - El `subLink` de Automatizaciones (AI Builder) NO se renderiza en móvil (el sidebar lo muestra como icono adyacente; en móvil se omite por simplicidad — documentar como decisión, no es gap nuevo: el módulo `/automatizaciones` sí queda accesible).

5. Tras el cambio, el array hardcodeado `navItems` (L18-44 del mobile-nav) y su import de iconos (`Building2, MessageSquare, Settings, Bot, Zap`) quedan SIN USO en la rama v3. La rama legacy (no-v3) TODAVÍA usa `navItems` (L130). Por tanto NO borrar `navItems` ni esos iconos — la rama legacy del marketing los necesita. Confirmar que la rama legacy queda byte-idéntica.

REGLA 6: header.tsx intacto; rama no-v3 del mobile-nav byte-frozen; solo la rama v3 cambia.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "mobile-nav|layout" || echo "OK sin errores en mobile-nav/layout"</automated>
  </verify>
  <done>MobileNav acepta currentWorkspace opcional; (dashboard)/layout.tsx lo pasa. La rama v3 (sub-componente MobileNavV3) deriva sus items de navCategoriesV2 con filterNavItem, agrupados por categoría con headers .cat y badges tasks/automations. header.tsx sin cambios; rama legacy del mobile-nav byte-idéntica. tsc sin errores nuevos.</done>
</task>

<task type="auto">
  <name>Task 3: Chips "Sin asignar" + "Sin respuesta" + toggle de orden en inbox v3</name>
  <files>src/app/(dashboard)/whatsapp/components/conversation-list.tsx</files>
  <action>
Objetivo C-7 + M-1. SOLO en la rama v3 (`if (v3)`, conversation-list.tsx L268-423). La lógica de filtro y orden YA existe — solo cablear controles. NO TOCAR el bloque del chip de tag filter (Popover, L329-378, commit 5c358db1) — los nuevos chips se añaden ANTES de ese Popover dentro del mismo `<div className="conv-filters">`.

1. **C-7 — 2 chips nuevos.** En el bloque de helpers de estado activo de la rama v3 (L269-274), añadir:
   ```typescript
   const isSinAsignar = filter === 'unassigned'
   const isSinRespuesta = filter === 'unanswered'
   ```
   Dentro de `<div className="conv-filters">`, AÑADIR dos `<button className={cn('chip', ...)}>` nuevos, ubicados de forma coherente con el orden legacy (Todas / Sin leer / Mías / Sin asignar / Sin respuesta / Agente IA / Cerradas). Sugerencia: insertar "Sin asignar" y "Sin respuesta" justo después del chip "Mías" (tras L314) y antes de "Agente IA":
   ```tsx
   <button type="button" className={cn('chip', isSinAsignar && 'on')}
     onClick={() => setFilter('unassigned')}>Sin asignar</button>
   <button type="button" className={cn('chip', isSinRespuesta && 'on')}
     onClick={() => setFilter('unanswered')}>Sin respuesta</button>
   ```
   Mismo patrón visual que los chips existentes (clase `chip` + `on`). No tocar `isTodas`/`isSinLeer`/`isMias`/`isAgenteIA`/`isCerradas`.

2. **M-1 — toggle de ordenamiento.** `sortMode`/`setSortMode` ya están destructurados del hook (L84-85) y disponibles en la rama v3. Añadir un control en la fila de filtros (o en la `conv-head` junto a la búsqueda — preferir dentro de `conv-filters` para consistencia con el shell de chips). Reusar el patrón del legacy (L505-517): un chip-toggle con icono `UserRoundSearch` (ya importado, L4) que alterna `last_customer_message ↔ last_message`:
   ```tsx
   <button
     type="button"
     className={cn('chip', sortMode === 'last_message' && 'on')}
     onClick={() => setSortMode(prev =>
       prev === 'last_customer_message' ? 'last_message' : 'last_customer_message')}
     title={sortMode === 'last_message'
       ? 'Ordenando por última interacción'
       : 'Ordenar por última interacción'}
     style={{ display: 'inline-flex', alignItems: 'center', gap: '4px' }}
   >
     <UserRoundSearch style={{ width: 12, height: 12 }} aria-hidden />
     Orden
   </button>
   ```
   Ubicarlo al final de la fila de chips (después del Popover de tag filter, o antes — elegir después para no alterar la posición del tag chip). Verificar que `cn` ya está importado (L13) y que el icono no rompe el layout del chip (usar tamaño 12×12 como el icono Tag del tag-filter chip para consistencia, L339).

3. Confirmar que el empty-state filtrado (`isFiltered`, L235-239) ya cubre los nuevos filtros: incluye `filter !== 'all'`, así que `unassigned`/`unanswered` ya disparan el estado "Nada coincide con los filtros activos" + "Limpiar filtros". No requiere cambio. El botón "Limpiar filtros" (L391-403) ya hace `setFilter('all')` → cubre los nuevos chips. (El sortMode NO se resetea en "Limpiar filtros" — está bien, el orden no es un filtro.)

REGLA 6: solo la rama v3. v2 (L425+) y legacy intactas. NO reescribir el Popover del tag filter.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "conversation-list" || echo "OK sin errores en conversation-list"</automated>
  </verify>
  <done>La rama v3 del inbox muestra chips "Sin asignar" (filter=unassigned), "Sin respuesta" (filter=unanswered) y un toggle de orden (UserRoundSearch, alterna sortMode). El chip de tag filter (5c358db1) sigue intacto. v2/legacy sin cambios. tsc sin errores nuevos.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` → 0 errores en todo el proyecto (verify global final).
- Diff de sidebar.tsx: solo import desde './nav-items' + borrado de defs movidas + sustitución de filterItem inline por filterNavItem. Sin cambio de markup/clases.
- header.tsx sin cambios (grep diff vacío).
- Rama no-v3 de mobile-nav byte-idéntica (navItems flat + iconos legacy intactos).
- En conversation-list.tsx: el bloque del Popover de tag filter (L329-378 aprox) sin modificar.
</verification>

<success_criteria>
- C-7: inbox v3 expone "Sin asignar" y "Sin respuesta" cableados a `setFilter('unassigned'|'unanswered')`.
- M-1: inbox v3 expone un control que alterna `sortMode` entre `last_customer_message` y `last_message`.
- C-6: mobile-nav v3 lista los 14 módulos agrupados por categoría, filtrados por admin/settingsKey/hidden_modules igual que el sidebar; antes solo 5.
- Regla 6: legacy/v2 del sidebar, marketing MobileNav, v2/legacy del inbox y tag-filter chip — todos intactos. Flag `ui_editorial_v3` sigue OFF.
- `pnpm exec tsc --noEmit` = 0 errores.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-w3c-inbox-filtros-mobile-nav-v3/260611-w3c-SUMMARY.md`
</output>
