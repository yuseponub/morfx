---
phase: 260611-w2b-pedidos-kanban-v3-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/crm/pedidos/components/kanban-card.tsx
  - src/app/(dashboard)/crm/pedidos/components/kanban-column.tsx
  - src/app/(dashboard)/crm/pedidos/components/orders-view.tsx
  - src/app/globals.css
autonomous: true
requirements: [C-4, C-5, M-4, M-5, M-7]

must_haves:
  truths:
    - "Con el flag ui_editorial_v3 ON, una card v3 de un pedido marcado con error de duplicado muestra un badge 'Sin productos' que abre un popover con detalle y botón 'Marcar resuelto' funcional"
    - "Una columna v3 con wip_limit configurado muestra el contador como {count}/{wipLimit} y aplica clase de warning cuando está at/over limit"
    - "La cabecera de columna v3 muestra una affordance visual de drag (grip + cursor grab)"
    - "Una card v3 con tracking_number muestra el número de guía (+ carrier) con marca editorial"
    - "Con 0 pedidos y el flag v3 ON, /crm/pedidos renderiza un empty state con idioma editorial v3 (no el markup legacy shadcn)"
    - "Con el flag OFF, el render legacy y v2 son byte-idénticos a antes (Regla 6)"
  artifacts:
    - path: "src/app/(dashboard)/crm/pedidos/components/kanban-card.tsx"
      provides: "Badge de error duplicado + tracking en la rama v3 de la card"
      contains: "duplicateError"
    - path: "src/app/(dashboard)/crm/pedidos/components/kanban-column.tsx"
      provides: "Contador WIP {count}/{wipLimit} + warning + grip en la rama v3 de la columna"
      contains: "wipLimit"
    - path: "src/app/(dashboard)/crm/pedidos/components/orders-view.tsx"
      provides: "Empty state v3 propio dentro de la rama v3 (condición pasa de v3 && !isEmpty a v3)"
      contains: "v3 &&"
    - path: "src/app/globals.css"
      provides: "Clases v3 nuevas: .kcard .err, .kcol-head .c.warn, .kcol-head .grip, .kempty-v3 CTA"
      contains: ".theme-editorial-v3 .kcard .err"
  key_links:
    - from: "kanban-card.tsx rama v3"
      to: "getDuplicateError + clearOrderDuplicateError"
      via: "reutiliza la misma lógica/estado que la rama legacy (ya importados en el archivo)"
      pattern: "getDuplicateError|clearOrderDuplicateError"
    - from: "orders-view.tsx rama v3"
      to: "setFormSheetOpen (CTA Crear pedido)"
      via: "empty state v3 con botón que abre el mismo sheet de crear pedido"
      pattern: "setFormSheetOpen\\(true\\)"
---

<objective>
Restaurar en la rama editorial v3 de CRM Pedidos cinco capacidades que existen en la card/columna legacy pero se omitieron al reescribir el markup v3 (gaps de la auditoría `ui-v3-parity-audit/AUDIT.md`):

- **C-4** — Badge de error de duplicado-de-productos en la card v3 (feature del standalone `crm-duplicate-order-products-integrity`, caso Doralba). Hoy invisible en v3 → pedidos con total incorrecto pasan desapercibidos.
- **C-5** — Avisos de límite WIP en la columna v3 (contador `{count}/{wipLimit}` + warning visual).
- **M-4** — Affordance visual de drag para reordenar etapas en la cabecera de columna v3 (los listeners existen pero no hay grip/cursor).
- **M-5** — Tracking number en la card v3 (hoy solo visible en el sheet).
- **M-7** — Empty state v3 propio: hoy `if (v3 && !isEmpty)` cae al markup legacy shadcn con 0 pedidos.

Purpose: paridad funcional de la vista Pedidos bajo el rediseño editorial sin tocar el comportamiento legacy/v2.
Output: 4 archivos modificados (3 componentes + globals.css), todos cambios ADITIVOS dentro de la rama v3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/standalone/ui-v3-parity-audit/AUDIT.md

<constraints>
- **Regla 6 / flag OFF:** `ui_editorial_v3` está OFF por defecto en prod. TODOS los cambios van DENTRO de la rama `if (v3)` o en clases CSS scopeadas a `.theme-editorial-v3`. El render legacy (rama `else`) y v2 deben quedar byte-idénticos en comportamiento. NO tocar el código fuera de las ramas v3.
- **Idioma editorial v3:** reutilizar la LÓGICA/handlers/popovers del legacy pero con estilo v3 (clases `.kcard`/`.kcol`/tokens `--ink-*`/`--paper-*`/`--stage-c`/`--semantic-*`). Antes de inventar clases CSS, revisar las existentes en globals.css (ya leídas abajo).
- **C-4 funcionalidad completa:** el badge v3 debe conservar TODO el comportamiento del legacy — popover con detalle de productos esperados + link a pedido origen + `AlertDialog` "Marcar resuelto" que llama `clearOrderDuplicateError`. No solo el ícono.
- **stopPropagation:** todo elemento interactivo dentro de la card (badge, popover, botones) DEBE tener `onClick`/`onPointerDown` con `stopPropagation` para no disparar el drag (patrón P-8/P-9 ya usado en el legacy líneas 322-445).
- **Verify:** `pnpm exec tsc --noEmit` con 0 errores.
</constraints>

<interfaces>
<!-- Estado/lógica ya disponible en kanban-card.tsx (NO re-importar, ya está) -->
Importado en kanban-card.tsx:
  import { getDuplicateError } from '@/lib/orders/types'
  import { clearOrderDuplicateError } from '@/app/actions/orders'
  // Popover, AlertDialog, Button, TruckIcon, AlertTriangleIcon, toast, useRouter — TODO ya importado.

Estado ya definido en el componente KanbanCard (compartido por ambas ramas, líneas 120-140):
  const duplicateError = React.useMemo(() => getDuplicateError(order), [order])  // null | DuplicateError
  const [isClearing, setIsClearing] = React.useState(false)
  async function handleResolveDuplicateError() { ... }  // ya existe
  // order.tracking_number: string | null ; order.carrier: string | null

Shape de duplicateError (usado en el popover legacy 345-441):
  { failedAt, errorCode, errorMessage, attemptedProducts: {sku,title,quantity,unit_price}[], sourceOrderId }

<!-- kanban-column.tsx — WIP ya calculado (compartido, líneas 175-179) -->
  const orderCount = orders.length
  const wipLimit = stage.wip_limit            // number | null
  const isAtLimit  = wipLimit !== null && orderCount >= wipLimit
  const isOverLimit = wipLimit !== null && orderCount > wipLimit
  // v3 head ya renderiza: <span className="c">{totalCount ?? orderCount}</span> (línea 204)
  // attributes/listeners de useSortable ya aplicados al <div className="kcol-head"> (línea 201)

<!-- orders-view.tsx -->
  const isEmpty = orders.length === 0               // línea 743
  if (v3 && !isEmpty) { ... return <>topbar + page + ...</> }  // línea 920 — rama v3 actual
  // CTA crear pedido: onClick={() => setFormSheetOpen(true)}  (ya usado en línea 936)

<!-- globals.css — clases v3 EXISTENTES (reutilizar, no duplicar) -->
  .theme-editorial-v3 .kcol { ... border-left; padding:0 14px; --stage-c }
  .theme-editorial-v3 .kcard { background:var(--paper-0); border; radius; padding:11px 12px }
  .theme-editorial-v3 .kcard .foot { ...; border-top:1px solid var(--border) }
  .theme-editorial-v3 .kcol-head { display:flex; gap:8px; padding-top:9px; border-top:2px solid var(--stage-c) }
  .theme-editorial-v3 .kcol-head .grip { display:none }   /* ← existe pero oculto; M-4 lo activa */
  .theme-editorial-v3 .kcol-head .c { color:var(--stage-c); font-weight:600; margin-left:auto }
  .theme-editorial-v3 .kempty { font-serif italic; color:var(--ink-4); padding:24px 0 }
  .theme-editorial-v3 .topbar / .eye / h1 / .btn.pri / .page  → idioma del empty state
  Tokens: --semantic-warning, --semantic-danger(=--rubric-2), --viv-red, --ink-1..4, --paper-0..3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card v3 — badge de error duplicado (C-4) + tracking (M-5)</name>
  <files>src/app/(dashboard)/crm/pedidos/components/kanban-card.tsx, src/app/globals.css</files>
  <action>
    SOLO dentro de la rama `if (v3)` (líneas 156-251). NO tocar la rama legacy.

    **C-4 — badge de error duplicado:** Insertar el bloque del badge ANTES del bloque `{productLine && (...)}` (es decir, justo después del `<div className="top">…</div>`). Reutilizar la MISMA lógica del legacy (líneas 323-445): el estado `duplicateError`, `isClearing` y `handleResolveDuplicateError` YA existen en el componente (líneas 120-140) y son compartidos por ambas ramas — NO redefinir. Portar el JSX del Popover + PopoverTrigger (botón con AlertTriangleIcon + "Sin productos") + PopoverContent (header, errorCode/errorMessage, lista de attemptedProducts, link "Ver pedido origen", AlertDialog "Marcar resuelto") VERBATIM en estructura, cambiando SOLO las clases a idioma v3:
      - Wrapper: `<div className="err" onClick={stopProp} onPointerDown={stopProp}>` (clase nueva v3, ver CSS abajo).
      - El trigger button usa una clase v3 (no las clases tailwind `bg-destructive/10`). El PopoverContent puede conservar las clases shadcn de layout interno (`w-80 p-0`, `border-b`, etc.) pero el badge trigger debe verse editorial.
      - Conservar TODOS los `stopPropagation` en wrapper, PopoverContent, link, AlertDialogTrigger/Content/Cancel/Action (patrón P-8/P-9).
      - El AlertDialogAction sigue llamando `handleResolveDuplicateError()`.

    **M-5 — tracking:** Insertar dentro del `<div className="foot">` (líneas 216-248), o como una fila propia entre `.tags` y `.foot`, un bloque condicional `{order.tracking_number && (...)}` con marca editorial: `<div className="track"><TruckIcon className="h-3 w-3" /><span className="mono">{order.tracking_number}</span>{order.carrier && <span className="car">{order.carrier}</span>}</div>`. TruckIcon ya está importado. Preferir fila propia `.track` antes de `.foot` para no romper el layout de la hairline row.

    **CSS (globals.css):** Añadir al bloque `.theme-editorial-v3` (cerca de las reglas `.kcard` existentes, ~línea 1265) clases nuevas:
      - `.theme-editorial-v3 .kcard .err{margin-top:8px}` y un trigger pill editorial: `.theme-editorial-v3 .kcard .err-trigger{display:inline-flex;align-items:center;gap:4px;padding:2px 7px;border-radius:var(--radius-2);font-family:var(--font-sans);font-size:10px;font-weight:600;background:color-mix(in oklch,var(--semantic-danger) 12%,var(--paper-0));color:var(--semantic-danger);border:1px solid color-mix(in oklch,var(--semantic-danger) 35%,var(--paper-0));cursor:pointer}` (+ `:hover`).
      - `.theme-editorial-v3 .kcard .track{display:flex;align-items:center;gap:6px;margin-top:8px;font-family:var(--font-mono);font-size:10px;color:var(--ink-3)}` y `.theme-editorial-v3 .kcard .track .car{font-family:var(--font-sans);text-transform:uppercase;font-size:9px;color:var(--ink-4)}`.
      Reutilizar tokens existentes; NO inventar colores hardcoded.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>La rama v3 de KanbanCard renderiza el badge "Sin productos" (con popover completo + Marcar resuelto) cuando `duplicateError` no es null, y la guía de tracking cuando `order.tracking_number` existe. Rama legacy intacta. tsc 0 errores.</done>
</task>

<task type="auto">
  <name>Task 2: Columna v3 — WIP {count}/{wipLimit} + warning (C-5) + grip de drag (M-4)</name>
  <files>src/app/(dashboard)/crm/pedidos/components/kanban-column.tsx, src/app/globals.css</files>
  <action>
    SOLO dentro de la rama `if (v3)` (líneas 190-271). NO tocar la rama legacy.

    **C-5 — WIP:** En el `<span className="c">` (línea 204) que muestra el contador, añadir el sufijo del límite cuando exista: cuando `wipLimit !== null`, renderizar `{count}/{wipLimit}` (usar `totalCount ?? orderCount` como `count`). Aplicar clase de warning condicional: `className={cn('c', isOverLimit && 'over', isAtLimit && !isOverLimit && 'warn')}`. `wipLimit`, `isAtLimit`, `isOverLimit` ya están calculados arriba (líneas 175-179). NO añadir el banner rojo separado del legacy (en v3 el color del contador comunica el estado — más limpio para el idioma editorial); el sufijo `/N` + color del contador es suficiente.

    **M-4 — grip:** La CSS ya tiene `.theme-editorial-v3 .kcol-head .grip{display:none}` (línea 1246). Añadir el elemento grip en el `<div className="kcol-head">` (línea 201) como PRIMER hijo, antes del `<span className="dot">`: `<span className="grip" aria-hidden>⋮⋮</span>` (o usar GripVerticalIcon ya importado con className="grip"). Como `.kcol-head` ya tiene los listeners del sortable, el grip es puramente visual (NO necesita listeners propios). Cambiar el CSS `.grip` de `display:none` a visible con cursor grab.

    **CSS (globals.css):**
      - Cambiar `.theme-editorial-v3 .kcol-head .grip` de `display:none` a: `display:inline-flex;align-items:center;color:var(--ink-4);cursor:grab;font-size:10px;line-height:0;opacity:0;transition:opacity .12s;flex:none}` y añadir `.theme-editorial-v3 .kcol-head:hover .grip{opacity:.55}` + `.theme-editorial-v3 .kcol-head:active .grip{cursor:grabbing}`. Si se usa GripVerticalIcon, dimensionar con `width:12px;height:12px`.
      - Para el contador WIP: `.theme-editorial-v3 .kcol-head .c.warn{color:var(--semantic-warning)}` y `.theme-editorial-v3 .kcol-head .c.over{color:var(--semantic-danger);font-weight:700}`. Colocar DESPUÉS de la regla existente `.theme-editorial-v3 .kcol-head .c{color:var(--stage-c);font-weight:600}` (~línea 1630) para ganar especificidad por orden.

    Verificar que el grip NO empuja el `.dot`/título fuera de layout (el head es flex con gap:8px).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>La rama v3 de KanbanColumn muestra el contador como `N/M` cuando hay wip_limit, con color warning/over según estado, y un grip visible al hover en la cabecera. Rama legacy intacta. tsc 0 errores.</done>
</task>

<task type="auto">
  <name>Task 3: Empty state v3 propio (M-7)</name>
  <files>src/app/(dashboard)/crm/pedidos/components/orders-view.tsx, src/app/globals.css</files>
  <action>
    Hoy `if (v3 && !isEmpty)` (línea 920) hace que con 0 pedidos caiga al markup legacy shadcn (líneas 1168-1181). Objetivo: que con flag v3 ON y 0 pedidos se renderice idioma editorial v3.

    **Enfoque (mínimo y seguro):** Cambiar la condición de la rama v3 de `if (v3 && !isEmpty)` a `if (v3)`. Dentro de la rama v3, en el bloque de contenido (donde hoy está el `viewMode === 'kanban' ? <KanbanBoard/> : <table.dict>`), envolver con un condicional: cuando `isEmpty`, renderizar un empty state editorial v3 en lugar del board/tabla. El resto del shell v3 (topbar, CrmTabs, toolbar, PipelineTabs, sharedOverlays, sheet de crear) se conserva tal cual — así el empty state queda DENTRO del shell editorial (consistencia visual, que es justo el gap M-7).

    El empty state v3 (reemplaza SOLO el contenido del `<div className="flex-1 min-h-0 overflow-auto">`, líneas 1052-1123, cuando isEmpty):
      ```
      <div className="kempty-v3">
        <div className="ic" aria-hidden>◷</div>   {/* o ShoppingCartIcon con clase */}
        <h3>Sin pedidos</h3>
        <p>Crea tu primer pedido para comenzar a gestionar tus ventas.</p>
        <button type="button" className="btn pri" onClick={() => setFormSheetOpen(true)}>Crear pedido</button>
      </div>
      ```
    NO duplicar topbar ni toolbar — solo el contenido central cambia según isEmpty. Mantener `{viewMode === 'kanban' ? ... : <table>}` para el caso no-vacío.

    Cuidado: la rama legacy (`return (<> {isEmpty ? <legacy empty> : <legacy board>} </>)`, líneas 1166+) debe quedar INTACTA — solo se alcanza cuando `!v3`. Verificar que el cambio de `v3 && !isEmpty` a `v3` no rompa el flujo: con `v3` true SIEMPRE entra a la rama v3 (vacía o no); con `v3` false sigue al return legacy. byte-idéntico para legacy.

    **CSS (globals.css):** Añadir `.theme-editorial-v3 .kempty-v3{display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:64px 24px;gap:10px}` + `.theme-editorial-v3 .kempty-v3 .ic{font-size:32px;color:var(--ink-4)}` + `.theme-editorial-v3 .kempty-v3 h3{font-family:var(--font-sans);font-weight:700;font-size:17px;color:var(--ink-1);margin:0}` + `.theme-editorial-v3 .kempty-v3 p{font-family:var(--font-serif);font-style:italic;font-size:13px;color:var(--ink-3);max-width:340px;margin:0}` + `.theme-editorial-v3 .kempty-v3 .btn.pri{margin-top:6px}`. Reutilizar `.btn.pri` v3 existente para el CTA.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Con `v3` true y 0 pedidos, /crm/pedidos renderiza el empty state editorial (`.kempty-v3`) dentro del shell v3 (topbar + sub-nav + toolbar + pipeline bar), con CTA que abre el sheet de crear pedido. La rama legacy queda byte-idéntica. tsc 0 errores.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` → 0 errores (gate de todas las tareas).
- Inspección de diff: confirmar que NINGÚN cambio toca código fuera de ramas `if (v3)` en los .tsx, ni clases CSS fuera de `.theme-editorial-v3`. Las ramas legacy/v2 deben quedar byte-idénticas (Regla 6).
- `grep -n "duplicateError" src/app/(dashboard)/crm/pedidos/components/kanban-card.tsx` debe aparecer ahora también dentro de la rama v3 (líneas 156-251).
- `grep -n "wipLimit" src/app/(dashboard)/crm/pedidos/components/kanban-column.tsx` debe aparecer dentro de la rama v3.
- `grep -n "if (v3)" src/app/(dashboard)/crm/pedidos/components/orders-view.tsx` (condición ahora sin `!isEmpty`).
</verification>

<success_criteria>
- C-4: badge de error duplicado completo (popover + Marcar resuelto) visible en card v3.
- C-5: contador WIP `{count}/{wipLimit}` con color warning/over en columna v3.
- M-4: grip de drag visible al hover en cabecera de columna v3.
- M-5: tracking number (+ carrier) visible en card v3.
- M-7: empty state v3 propio dentro del shell editorial (no markup legacy).
- Regla 6: flag OFF → legacy/v2 byte-idénticos. tsc 0 errores.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-w2b-pedidos-kanban-v3-parity/260611-w2b-SUMMARY.md`
</output>
