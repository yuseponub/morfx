# CONTINUE-HERE — v4-hybrid-template-rag-turn (handoff post-/clear → plan-phase)

**Creado:** 2026-05-30 · **Rama:** `exec/debounce-v2-wave6` · **HEAD esperado:** `1ef718f8` (research ya commiteado)

## Cómo retomar

Esto es un **standalone** en `.planning/standalone/v4-hybrid-template-rag-turn/` — **NO lo descubre
`gsd-tools init`** (devuelve `phase_found: false`). Orquestar a MANO igual que turn-ledger y crm-subloop:
leer el workflow de plan-phase + los artefactos, y spawnear `gsd-planner` (+ `gsd-plan-checker`).

- Tooling real: `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs` (NO `gsd-sdk` — no está instalado).
- Branch flow (memoria `git_branch_flow`): `origin/main` = deploy Vercel (al día, en `892f8522`);
  `exec/debounce-v2-wave6` = rama de integración (ff a main). SIEMPRE `git fetch` + comparar contra
  `origin/main`, NO el main local (stale).

## Estado: discuss ✅ + research ✅ — LISTO PARA PLAN

| Artefacto | Commit |
|-----------|--------|
| DISCUSSION-LOG.md (problema de fondo verificado + D-01..D-11) | `9fd422f0` |
| CONTEXT.md (11 decisiones + 6 riesgos + canonical refs file:line) | `9fd422f0` |
| RESEARCH.md (482 líneas, R1-R6, T-1..T-8, 8 pitfalls, no-regresión Regla 6) | `1ef718f8` |

**Leer primero:** `CONTEXT.md` (D-01..D-11 + canonical refs) y `RESEARCH.md` (las 8 decisiones técnicas
T-1..T-8 + el diagrama del flujo de slots de R2 + los 8 pitfalls).

## Qué se va a planear (resumen — detalle en CONTEXT/RESEARCH)

Híbrido **template+RAG en un turno**: reemplazar la palanca binaria (return temprano excluyente
`somnio-v4-agent.ts:243-314`, decidido SOLO por el primary) por **decisión de cobertura per-intent**
(matriz 4 casos: template+template / template+RAG / RAG+template / RAG+RAG).

Trabajo principal (de RESEARCH):
- **R3/D-01/D-04:** extender `MessageAnalysisSchema` con `secondary_confidence` + `secondary_query`
  (`.nullable()`, condicional a secondary≠ninguno) + few-shot calibration en comprehension-prompt.
- **R2 (corazón):** refactor del return temprano → **resolvedor de slots al FINAL, post-gate-CRM**
  (ortogonal al gate CRM #2 — reasons distintos). Preservar: captureUnknownCase, observability,
  mapOutcomeToAgentOutput, ledger, CKPT-1/2, guards R0/R1, sales-track, crm_mutation/cas_reject.
- **R1 (riesgo DESINFLADO):** handoff PARCIAL = poblar `output.templates` con el slot resuelto +
  `newMode='handoff'`. El runner YA envía templates (`v4-production-runner.ts:751`) ANTES del handoff
  (`:1123`). Precedente pw-confirmation. NO mecanismo nuevo.
- **R5/D-08:** RAG+RAG = 2 invocaciones secuenciales (una por sub-query). Matriz LLM calls 1/4/4/7.
- **R4/D-05/D-11:** inyectar el `responseText` del RAG como mensaje CORE en el block-composer, orden = orden de intents.
- **R6:** paridad runner↔engine-v4 automática (agente compartido); NO añadir nuevos `CheckpointId`.

## Restricciones
- v4 DORMANT (0 workspaces) → Regla 6. Cambios v4-specific. Sandbox simula. **Sin feature flag (D-10).**
- **Sin migración DB** (thresholds/handoff `status='handed_off'`/ledger ya existen — Regla 5 no aplica).
- El plan DEBE incluir greps/diff de no-regresión Regla 6 contra **baseline `9fd422f0`** (el commit del
  discuss del standalone — NO main; la rama está adelante con trabajo debounce-v2 ajeno). Ver la sección
  "no-regresión Regla 6" de RESEARCH.md (8 greps/diffs).

## Next
`/gsd-plan-phase v4-hybrid-template-rag-turn` (orquestar a mano). Probablemente multi-wave
(schema comprehension + resolvedor de slots + composición + handoff parcial + RAG+RAG + tests/paridad/Regla6).
