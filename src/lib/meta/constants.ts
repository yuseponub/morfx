// ============================================================================
// Meta Graph API Constants
// Pinned version — update here when Meta releases new minimum version
// Zero imports — prevents circular dependencies
// ============================================================================

export const META_GRAPH_API_VERSION = 'v22.0'

export const META_BASE_URL = `https://graph.facebook.com/${META_GRAPH_API_VERSION}`
