---
phase: 41-instagram-direct
plan: 03
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - src/lib/meta/instagram-connect.ts
  - src/lib/domain/meta-accounts.ts
  - src/app/actions/meta-onboarding.ts
autonomous: true
requirements: [IG-03]
must_haves:
  truths:
    - "resolveInstagramAccount(pageToken, pageId) returns {id,username} from GET /{pageId}?fields=instagram_business_account{id,username}, throwing a clear Spanish error if no IG linked"
    - "connectInstagramAccount is owner-gated (workspaceId session-derived, never input), resolves IG off the connected Page, and upserts channel='instagram' with ig_account_id reusing the SAME encrypted Page token"
    - "connectInstagramAccount NEVER flips instagram_provider (stays manychat — Regla 6)"
    - "upsertMetaAccount accepts igAccountId + igUsername and writes them in BOTH the UPDATE and INSERT column sets (sole write path — Regla 3)"
  artifacts:
    - path: "src/lib/meta/instagram-connect.ts"
      provides: "resolveInstagramAccount helper (the single genuinely-new Graph call)"
      contains: "instagram_business_account"
    - path: "src/app/actions/meta-onboarding.ts"
      provides: "connectInstagramAccount server action"
      contains: "connectInstagramAccount"
    - path: "src/lib/domain/meta-accounts.ts"
      provides: "upsertMetaAccount extended with igAccountId/igUsername"
      contains: "ig_account_id"
  key_links:
    - from: "src/app/actions/meta-onboarding.ts"
      to: "src/lib/meta/instagram-connect.ts"
      via: "resolveInstagramAccount(pageToken, pageId)"
      pattern: "resolveInstagramAccount"
    - from: "src/app/actions/meta-onboarding.ts"
      to: "src/lib/domain/meta-accounts.ts"
      via: "upsertMetaAccount({ channel:'instagram', igAccountId, igUsername })"
      pattern: "upsertMetaAccount"
---

<objective>
Build the IG connect chain (IG-03 identity persistence): the single genuinely-new Graph call `resolveInstagramAccount` (resolves the `instagram_business_account` off the workspace's already-connected Facebook Page), extend `upsertMetaAccount` to persist `igAccountId` + `igUsername`, and add the owner-gated `connectInstagramAccount` server action. IG has no independent OAuth — it rides on the connected Page and reuses the SAME encrypted Page token (D-IG-04).

Purpose: Mirrors Phase 40 Plan 40-03 (`connectFacebookPage` + `meta-accounts.ts` pageId extend). The connect chain (`exchangeForLongLivedUserToken`, `getPageToken`, `subscribeMessengerPage`) is REUSED verbatim from `messenger-connect.ts`; the only NEW code is the IG-account resolve. Per D-IG-04 (Claude's Discretion), prefer the no-popup path: `connectInstagramAccount` reads the workspace's existing connected-Page row, decrypts its Page token, and resolves IG off it — no fresh FB.login needed when a connected Page row already exists.
Output: One new file + two extended files. (No dedicated RED test for the connect action in 41-01 — this plan ships a focused connect chain verified by tsc + greps + the live smoke in 41-07.)

`depends_on: [41-01]`. Does NOT read `instagram_provider` → independent of the migration; runs in parallel with 41-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-instagram-direct/41-RESEARCH.md
@.planning/phases/41-instagram-direct/41-PATTERNS.md

<interfaces>
<!-- The FB analogs + reusables. -->
EXISTING — src/lib/meta/messenger-connect.ts: exchangeForLongLivedUserToken, getPageToken → {pageId,pageName,accessToken}, subscribeMessengerPage(pageToken, pageId) — REUSE verbatim.
EXISTING — src/lib/meta/api.ts metaRequest<T>(accessToken, path, init?).
EXISTING — src/lib/meta/token.ts encryptToken / decryptToken (AES-256-GCM).
EXISTING — src/lib/domain/meta-accounts.ts UpsertMetaAccountParams { workspaceId; channel: MetaChannel; wabaId; phoneNumberId; accessTokenEncrypted; phoneNumber?; businessId?; pageId?; isActive? } and upsertMetaAccount (sole write path, UPDATE+INSERT column sets, mapWriteError uq_meta_* Spanish strings).
EXISTING — src/app/actions/meta-onboarding.ts connectFacebookPage (lines 166-236): getRequestAuth → workspaceId=auth.workspaceId → owner check → exchange/getPageToken → encryptToken → upsertMetaAccount → subscribeMessengerPage.
EXISTING — src/lib/meta/credentials.ts resolveByWorkspace(workspaceId,'facebook') → MetaCredentials {workspaceId, pageId, accessToken (decrypted), ...} — REUSE to read the connected Page row + token for the no-popup path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: instagram-connect.ts — resolveInstagramAccount (the only new Graph call)</name>
  <read_first>
    - src/lib/meta/messenger-connect.ts (getPageToken + subscribeMessengerPage style — REUSE; do not duplicate)
    - .planning/phases/41-instagram-direct/41-PATTERNS.md §5 (resolveInstagramAccount + REUSE getPageToken/subscribeMessengerPage)
    - .planning/phases/41-instagram-direct/41-RESEARCH.md §Code Examples (resolve instagram_business_account off the Page)
  </read_first>
  <files>src/lib/meta/instagram-connect.ts</files>
  <action>
Create `src/lib/meta/instagram-connect.ts` with ONLY the new resolve helper (the rest of the chain is reused from `messenger-connect.ts`):

```typescript
import { metaRequest } from './api'

/**
 * Resolves the Instagram Professional account linked to a connected Facebook Page.
 * Reuses the existing Page token. IG rides on the connected Page (D-IG-04).
 * GET /{pageId}?fields=instagram_business_account{id,username}
 */
export async function resolveInstagramAccount(
  pageToken: string,
  pageId: string,
): Promise<{ id: string; username?: string }> {
  const res = await metaRequest<{ instagram_business_account?: { id: string; username?: string } }>(
    pageToken,
    `/${pageId}?fields=instagram_business_account{id,username}`,
  )
  const ig = res.instagram_business_account
  if (!ig?.id) {
    throw new Error('vincula una cuenta de Instagram Profesional a tu página de Facebook')   // D-IG-04 clear error
  }
  return ig
}
```

Do NOT re-implement `getPageToken` or `subscribeMessengerPage` — import them from `messenger-connect.ts` where needed (the action, Task 3). Never log the token.
  </action>
  <verify>
    <automated>test -f src/lib/meta/instagram-connect.ts && grep -q "instagram_business_account{id,username}" src/lib/meta/instagram-connect.ts && grep -q "vincula una cuenta de Instagram Profesional" src/lib/meta/instagram-connect.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "instagram_business_account{id,username}"` ≥ 1
    - `grep -c "vincula una cuenta de Instagram Profesional"` ≥ 1 (D-IG-04 clear Spanish error)
    - `grep -c "getPageToken\|subscribeMessengerPage" src/lib/meta/instagram-connect.ts` == 0 (reused, not re-implemented)
    - `grep -c "console.log" src/lib/meta/instagram-connect.ts` == 0
    - `npx tsc --noEmit` 0 errors mentioning instagram-connect.ts
  </acceptance_criteria>
  <done>resolveInstagramAccount built; throws the clear Spanish error when no IG linked; reuses (not duplicates) the rest of the chain.</done>
</task>

<task type="auto">
  <name>Task 2: Extend upsertMetaAccount with igAccountId + igUsername (Regla 3 sole write path)</name>
  <read_first>
    - src/lib/domain/meta-accounts.ts (UpsertMetaAccountParams lines 30-39; UPDATE column set lines 93-102; INSERT column set lines 114-124; mapWriteError uq_meta_* strings)
    - .planning/phases/41-instagram-direct/41-PATTERNS.md §10 (extend both UPDATE and INSERT)
  </read_first>
  <files>src/lib/domain/meta-accounts.ts</files>
  <action>
Extend `UpsertMetaAccountParams` with `igAccountId?: string | null` and `igUsername?: string | null` (siblings of `pageId`). Add `ig_account_id: params.igAccountId ?? null` and `ig_username: params.igUsername ?? null` to BOTH the `.update({...})` block AND the `.insert({...})` block (mirror exactly how `page_id: params.pageId ?? null` appears in both). Extend `mapWriteError` to map the `uq_meta_ig` UNIQUE conflict → a clear Spanish string (mirror the existing `uq_meta_page` → "Esta página ya está conectada en otro espacio de trabajo."): e.g. "Esta cuenta de Instagram ya está conectada en otro espacio de trabajo."

`(workspace_id, channel)` upsert key already isolates the IG row (channel='instagram') from the FB row. `ig_account_id` column + `uq_meta_ig` exist (Phase 37); `ig_username` exists (Plan 41-00 migration). This remains the SOLE write path into `workspace_meta_accounts` (Regla 3).
  </action>
  <verify>
    <automated>[ "$(grep -c ig_account_id src/lib/domain/meta-accounts.ts)" -ge 2 ] && grep -q "igAccountId" src/lib/domain/meta-accounts.ts && [ "$(grep -c ig_username src/lib/domain/meta-accounts.ts)" -ge 2 ] && grep -q "uq_meta_ig" src/lib/domain/meta-accounts.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ig_account_id" src/lib/domain/meta-accounts.ts` ≥ 2 (UPDATE + INSERT blocks)
    - `grep -c "ig_username" src/lib/domain/meta-accounts.ts` ≥ 2 (UPDATE + INSERT)
    - `grep -c "igAccountId" src/lib/domain/meta-accounts.ts` ≥ 1 (param)
    - `grep -c "uq_meta_ig" src/lib/domain/meta-accounts.ts` ≥ 1 (Spanish error mapping)
    - `npx tsc --noEmit` 0 errors mentioning meta-accounts.ts
  </acceptance_criteria>
  <done>upsertMetaAccount persists igAccountId + igUsername in both UPDATE and INSERT; uq_meta_ig conflict mapped to a Spanish error.</done>
</task>

<task type="auto">
  <name>Task 3: connectInstagramAccount server action (owner-gated, no provider flip)</name>
  <read_first>
    - src/app/actions/meta-onboarding.ts (connectFacebookPage lines 166-236 — auth gate, owner check, encrypt, upsert, subscribe)
    - src/lib/meta/credentials.ts resolveByWorkspace (read the connected Page row + decrypted token)
    - src/lib/meta/token.ts encryptToken/decryptToken
    - .planning/phases/41-instagram-direct/41-PATTERNS.md §9 (connectInstagramAccount flow), §RESEARCH Anti-Patterns (NO provider flip on connect)
    - CLAUDE.md §Regla 3 + §Regla 6
  </read_first>
  <files>src/app/actions/meta-onboarding.ts</files>
  <action>
Add `export async function connectInstagramAccount(): Promise<{ success: boolean; error?: string; igUsername?: string }>` cloning the `connectFacebookPage` auth gate verbatim:

```typescript
const auth = await getRequestAuth()
if (!auth) return { success: false, error: 'No autenticado' }
const workspaceId = auth.workspaceId                      // session-derived, NEVER input (Regla 3 / V4)
// owner check (copied verbatim from connectFacebookPage):
const { data: member } = await supabase.from('workspace_members')
  .select('role').eq('workspace_id', workspaceId).eq('user_id', auth.userId).single()
if (!member || member.role !== 'owner') return { success: false, error: 'Solo el Owner puede conectar Instagram' }
```

No-popup flow (D-IG-04 — prefer reusing the connected Page row):
1. `const fb = await resolveByWorkspace(workspaceId, 'facebook')` → if `!fb?.pageId || !fb.accessToken` return `{ success:false, error:'Primero conecta tu página de Facebook' }`. (`resolveByWorkspace` returns the DECRYPTED token.)
2. `const ig = await resolveInstagramAccount(fb.accessToken, fb.pageId)` (throws the Spanish error if no IG linked — wrap in try/catch and return `{ success:false, error: e.message }`).
3. `const accessTokenEncrypted = encryptToken(fb.accessToken)` (reuse the SAME Page token, re-encrypted for the IG row).
4. `await upsertMetaAccount({ workspaceId, channel:'instagram', wabaId:null, phoneNumberId:null, pageId: fb.pageId, igAccountId: ig.id, igUsername: ig.username ?? null, accessTokenEncrypted, isActive:true })`.
5. `await subscribeMessengerPage(fb.accessToken, fb.pageId)` (REUSE — IG events ride the same Page subscription; A2 verifies in smoke whether a separate IG subscribe is needed — if so, 41-07 adds it).
6. `return { success:true, igUsername: ig.username }`.

**Regla 6 CRITICAL:** This action MUST NOT touch `instagram_provider`. The workspace stays `manychat` until the manual SQL cutover (41-07). `grep -c "instagram_provider" src/app/actions/meta-onboarding.ts` must remain 0 (reword any explanatory comment to avoid the literal column name). Generic Spanish error on any unexpected failure; never log the token or return the plaintext token.
  </action>
  <verify>
    <automated>grep -q "export async function connectInstagramAccount" src/app/actions/meta-onboarding.ts && grep -qE "channel: ?'instagram'" src/app/actions/meta-onboarding.ts && [ "$(grep -c instagram_provider src/app/actions/meta-onboarding.ts)" -eq 0 ] && grep -q "Solo el Owner puede conectar" src/app/actions/meta-onboarding.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function connectInstagramAccount" src/app/actions/meta-onboarding.ts` == 1
    - `grep -cE "channel: ?'instagram'" src/app/actions/meta-onboarding.ts` ≥ 1
    - `grep -c "instagram_provider" src/app/actions/meta-onboarding.ts` == 0 (Regla 6 — NO provider flip)
    - `grep -c "resolveInstagramAccount" src/app/actions/meta-onboarding.ts` ≥ 1
    - owner gate present (`grep -c "Solo el Owner puede conectar"` ≥ 1)
    - `grep -c "console.log" ...` does not log a token; plaintext token never returned (`JSON.stringify(result)` excludes the token)
    - `npx tsc --noEmit` 0 errors mentioning meta-onboarding.ts
  </acceptance_criteria>
  <done>connectInstagramAccount built: owner-gated, resolves IG off the connected Page, upserts channel='instagram' with ig_account_id, reuses the Page token, NEVER flips instagram_provider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | Owner-gated connect; workspaceId from session, never input |
| MorfX → Meta Graph API | Resolve IG account + subscribe with the Page token |
| server action → prod DB | upsertMetaAccount sole write path (Regla 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Elevation of Privilege | non-owner connecting / workspaceId from input | mitigate | Owner gate copied verbatim from connectFacebookPage; workspaceId = auth.workspaceId (session-derived, never input) |
| T-41-03-02 | Information Disclosure | Page token leaked in logs or returned to client | mitigate | Token encrypted at rest (encryptToken); never logged; action returns only { success, igUsername }, never the plaintext token |
| T-41-03-03 | Elevation / Regla 6 | connect flips instagram_provider → silently migrates a workspace | mitigate | Action NEVER touches instagram_provider (grep == 0); flip is a separate manual SQL cutover (41-07) |
| T-41-03-04 | Tampering | cross-workspace IG account collision | mitigate | uq_meta_ig UNIQUE (Phase 37) → mapWriteError surfaces the Spanish conflict string |
</threat_model>

<verification>
- resolveInstagramAccount resolves the IG account off the Page, clear Spanish error if none linked.
- upsertMetaAccount persists ig_account_id + ig_username in both UPDATE and INSERT (sole write path).
- connectInstagramAccount owner-gated, reuses the Page token, NEVER flips instagram_provider.
</verification>

<success_criteria>
- IG-03 connect persistence: a `channel='instagram'` meta-account row with ig_account_id is created on connect.
- Regla 6: instagram_provider untouched on connect; workspace stays manychat until manual cutover.
- Regla 3: upsertMetaAccount remains the sole write path.
</success_criteria>

<output>
After completion, create `.planning/phases/41-instagram-direct/41-03-SUMMARY.md`.
</output>
