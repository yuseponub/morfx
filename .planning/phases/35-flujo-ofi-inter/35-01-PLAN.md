---
phase: 35-flujo-ofi-inter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/somnio/constants.ts
  - src/lib/agents/somnio/config.ts
  - src/lib/agents/somnio/data-extractor.ts
  - src/lib/agents/somnio/normalizers.ts
  - src/lib/agents/somnio/transition-validator.ts
autonomous: true

must_haves:
  truths:
    - "collecting_data_inter state exists in SOMNIO_STATES and has bidirectional transitions to/from collecting_data"
    - "OFI_INTER_CRITICAL_FIELDS and OFI_INTER_ADDITIONAL_FIELDS define the bifurcated field set"
    - "isCollectingDataMode helper returns true for both collecting_data and collecting_data_inter"
    - "hasCriticalDataInter checks ofi inter completion without requiring direccion/barrio"
    - "isDataComplete dispatches to correct completion check based on mode"
    - "isRemoteMunicipality identifies curated remote municipalities"
    - "detectOfiInterMention identifies ofi inter phrases in message text"
    - "CONFIRMATORY_MODES includes collecting_data_inter"
    - "Existing hasCriticalData and CRITICAL_FIELDS remain unchanged"
  artifacts:
    - path: "src/lib/agents/somnio/constants.ts"
      provides: "OFI_INTER constants, isCollectingDataMode, detectOfiInterMention, REMOTE_MUNICIPALITIES"
      contains: "OFI_INTER_CRITICAL_FIELDS"
    - path: "src/lib/agents/somnio/config.ts"
      provides: "collecting_data_inter state with transitions"
      contains: "collecting_data_inter"
    - path: "src/lib/agents/somnio/data-extractor.ts"
      provides: "hasCriticalDataInter, isDataComplete, cedula_recoge in ExtractedData"
      contains: "hasCriticalDataInter"
    - path: "src/lib/agents/somnio/normalizers.ts"
      provides: "isRemoteMunicipality helper function"
      contains: "isRemoteMunicipality"
    - path: "src/lib/agents/somnio/transition-validator.ts"
      provides: "Mode-aware auto-trigger and timer promo for ofi inter"
      contains: "isCollectingDataMode"
  key_links:
    - from: "src/lib/agents/somnio/data-extractor.ts"
      to: "src/lib/agents/somnio/constants.ts"
      via: "imports OFI_INTER_CRITICAL_FIELDS"
      pattern: "OFI_INTER_CRITICAL_FIELDS"
    - from: "src/lib/agents/somnio/config.ts"
      to: "SOMNIO_TRANSITIONS"
      via: "collecting_data_inter has bidirectional transitions"
      pattern: "collecting_data_inter.*collecting_data"
---

<objective>
Add ofi inter foundation: state machine, constants, field definitions, detection functions, and completion checks.

Purpose: Establish all the building blocks (types, constants, pure functions) that Plan 02 and 03 will wire into the agent pipeline. This plan makes NO behavioral changes to the existing flow -- all additions are dead code until wired.

Output: Updated constants.ts, config.ts, data-extractor.ts, normalizers.ts, transition-validator.ts with ofi inter support.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-flujo-ofi-inter/35-CONTEXT.md
@.planning/phases/35-flujo-ofi-inter/35-RESEARCH.md

@src/lib/agents/somnio/constants.ts
@src/lib/agents/somnio/config.ts
@src/lib/agents/somnio/data-extractor.ts
@src/lib/agents/somnio/normalizers.ts
@src/lib/agents/somnio/transition-validator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constants + Config + Detection Functions</name>
  <files>
    src/lib/agents/somnio/constants.ts
    src/lib/agents/somnio/config.ts
    src/lib/agents/somnio/normalizers.ts
  </files>
  <action>
**constants.ts** -- Add the following AFTER the existing Block Composition Constants section:

1. Ofi Inter field constants:
   - `OFI_INTER_CRITICAL_FIELDS = ['nombre', 'telefono', 'ciudad', 'departamento'] as const` (4 fields -- no direccion)
   - `OFI_INTER_ADDITIONAL_FIELDS = ['apellido', 'cedula_recoge', 'correo'] as const` (cedula optional)
   - `MIN_FIELDS_FOR_AUTO_PROMO_INTER = 6` (4 critical + at least 2 additional, since cedula is optional)

2. Ofi inter detection patterns:
   ```typescript
   export const OFI_INTER_PATTERNS: RegExp[] = [
     // Direct mentions
     /\bofi\s*inter\b/i,
     /\boficina\s*(de\s+)?inter(rapidisimo)?\b/i,
     /\breco[gj]o?\s*en\s*inter\b/i,
     /\brecoger?\s*en\s*inter\b/i,
     // Variations (from CONTEXT.md)
     /\bquiero\s+ir\s+a\s+recoger\b/i,
     /\bpuedo\s+pasar\s+a\s+buscar\b/i,
     /\bno\s+necesito\s+domicilio\b/i,
     /\benvi[ae]\s+a\s+la\s+oficina\b/i,
     /\brecoger\s+en\s+(la\s+)?oficina\b/i,
     /\brecojo\s+en\s+(la\s+)?oficina\b/i,
     /\brecoger\s+en\s+(la\s+)?transportadora\b/i,
   ]
   ```

3. `detectOfiInterMention(message: string): boolean` -- tests message against OFI_INTER_PATTERNS.

4. `isCollectingDataMode(mode: string): boolean` -- returns `mode === 'collecting_data' || mode === 'collecting_data_inter'`. This is the helper to replace ALL hardcoded `=== 'collecting_data'` checks in Plans 02/03.

5. Update `CONFIRMATORY_MODES` to include `'collecting_data_inter'` (so "ok"/"si" in ofi inter mode are RESPONDIBLE, not SILENCIOSO).

**config.ts** -- Update state machine:

1. Add `'collecting_data_inter'` to `SOMNIO_STATES` array (after `'collecting_data'`).
2. Add to `SOMNIO_TRANSITIONS`:
   - `bienvenida`: add `'collecting_data_inter'` to valid targets
   - `conversacion`: add `'collecting_data_inter'` to valid targets
   - `collecting_data`: add `'collecting_data_inter'` to valid targets (switch to ofi inter from normal)
   - `collecting_data_inter`: new entry `['collecting_data_inter', 'collecting_data', 'ofrecer_promos', 'handoff']` (bidirectional)
3. The `SomnioState` type auto-updates from `SOMNIO_STATES`.

**normalizers.ts** -- Add remote municipality detection:

1. `REMOTE_MUNICIPALITIES` Set with normalized lowercase entries (strip accents for lookup). Initial curated list based on remote department capitals:
   - leticia, mitu, inirida, puerto carreno, san jose del guaviare, mocoa, quibdo, florencia, yopal, arauca, riohacha
   - Include accent-stripped duplicates for matching.

2. `isRemoteMunicipality(city: string): boolean` -- normalizes input (strip accents, lowercase, trim) and checks against REMOTE_MUNICIPALITIES Set.

IMPORTANT: All these are pure additions. Do NOT modify any existing constants, fields, or functions. The existing `CRITICAL_FIELDS`, `CONFIRMATORY_MODES` Set constructor, and normalizer functions remain identical. Only ADD new entries to CONFIRMATORY_MODES and new exports.
  </action>
  <verify>
    `npx tsc --noEmit` passes. Grep for `OFI_INTER_CRITICAL_FIELDS` in constants.ts confirms export. Grep for `collecting_data_inter` in config.ts confirms state exists. Grep for `isRemoteMunicipality` in normalizers.ts confirms function exists.
  </verify>
  <done>
    - constants.ts exports OFI_INTER_CRITICAL_FIELDS, OFI_INTER_ADDITIONAL_FIELDS, MIN_FIELDS_FOR_AUTO_PROMO_INTER, OFI_INTER_PATTERNS, detectOfiInterMention, isCollectingDataMode
    - CONFIRMATORY_MODES includes 'collecting_data_inter'
    - config.ts has collecting_data_inter in SOMNIO_STATES with bidirectional transitions
    - normalizers.ts exports isRemoteMunicipality with curated REMOTE_MUNICIPALITIES list
  </done>
</task>

<task type="auto">
  <name>Task 2: Data Extractor + Transition Validator Ofi Inter Support</name>
  <files>
    src/lib/agents/somnio/data-extractor.ts
    src/lib/agents/somnio/transition-validator.ts
  </files>
  <action>
**data-extractor.ts** -- Add ofi inter completion logic and cedula field:

1. Add `cedula_recoge?: string` to `ExtractedData` interface (after `correo`).

2. Add `OFI_INTER_ADDITIONAL_FIELDS` import from constants.ts. Also import `OFI_INTER_CRITICAL_FIELDS`.

3. Add `hasCriticalDataInter(data: Record<string, string>): boolean` -- exported function:
   - Check all 4 OFI_INTER_CRITICAL_FIELDS are present and non-empty
   - Count OFI_INTER_ADDITIONAL_FIELDS that are present and non-empty
   - Return true when 4 critical fields present AND at least 2 additional fields present (6 total minimum)
   - cedula_recoge is optional so minimum is nombre, telefono, ciudad, departamento + apellido + correo

4. Add `isDataComplete(data: Record<string, string>, mode: string): boolean` -- exported function:
   - If mode === 'collecting_data_inter': return hasCriticalDataInter(data)
   - Otherwise: return hasCriticalData(data)
   - This is the single dispatch point. Plan 02 will update callers to use this instead of hasCriticalData directly.

5. Do NOT modify the existing `hasCriticalData`, `hasMinimumData`, `getFieldCounts`, `mergeExtractedData` functions. They remain identical for backward compatibility.

6. Do NOT change the `DATA_EXTRACTOR_PROMPT` yet -- that is Task 1 of Plan 03 (needs full agent context).

**transition-validator.ts** -- Add mode-aware support:

1. Import `isCollectingDataMode` and `MIN_FIELDS_FOR_AUTO_PROMO_INTER` from constants.ts.

2. Add `checkAutoTriggersForMode(intentsVistos, datosCapturados, mode: string): string | null` -- exported method on TransitionValidator:
   - If `isCollectingDataMode(mode)` is false, return null (auto-triggers only in collecting modes)
   - If ofrecer_promos already seen, return null
   - If mode === 'collecting_data_inter': check fieldCount >= MIN_FIELDS_FOR_AUTO_PROMO_INTER (6)
   - If mode === 'collecting_data': check fieldCount >= MIN_FIELDS_FOR_AUTO_PROMO (8) (existing behavior)
   - Return 'ofrecer_promos' if threshold met, null otherwise

3. Add `shouldTriggerTimerPromoForMode(intentsVistos, datosCapturados, mode: string): boolean` -- exported method:
   - Same as existing shouldTriggerTimerPromo but mode-aware
   - For collecting_data_inter: require OFI_INTER_CRITICAL_FIELDS instead of CRITICAL_FIELDS
   - For collecting_data: existing behavior

4. Do NOT modify existing `checkAutoTriggers` or `shouldTriggerTimerPromo` methods. The new methods are additions that Plan 02 will wire.
  </action>
  <verify>
    `npx tsc --noEmit` passes. Grep for `hasCriticalDataInter` in data-extractor.ts confirms export. Grep for `isDataComplete` in data-extractor.ts confirms export. Grep for `checkAutoTriggersForMode` in transition-validator.ts confirms method exists. Grep for `cedula_recoge` in data-extractor.ts confirms field in ExtractedData.
  </verify>
  <done>
    - data-extractor.ts exports hasCriticalDataInter, isDataComplete; ExtractedData has cedula_recoge
    - transition-validator.ts has checkAutoTriggersForMode and shouldTriggerTimerPromoForMode methods
    - Existing functions hasCriticalData, checkAutoTriggers, shouldTriggerTimerPromo remain unchanged
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes -- no type errors introduced
2. All new exports are importable from their respective modules
3. Existing exports and function signatures are unchanged
4. The `SomnioState` type includes 'collecting_data_inter'
5. `SOMNIO_TRANSITIONS['collecting_data_inter']` includes both 'collecting_data' and 'ofrecer_promos'
6. `CONFIRMATORY_MODES.has('collecting_data_inter')` returns true
</verification>

<success_criteria>
- All ofi inter constants, types, detection functions, and completion checks exist as importable exports
- State machine includes collecting_data_inter with bidirectional transitions
- No existing behavior is changed (all additions are dead code until wired in Plan 02)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/35-flujo-ofi-inter/35-01-SUMMARY.md`
</output>
