---
phase: 999.1-whatsapp-interactive-message-composer
plan: 01
subsystem: whatsapp
tags: [tdd, wave-0, validation, interactive-messages, chokepoint-contract]
requires: []
provides:
  - "src/lib/whatsapp/interactive-limits.ts (INTERACTIVE_LIMITS + validateButtons + validateList)"
  - "interactive-limits.test.ts (GREEN — pins the limit helper for the composer modal, Plan 04)"
  - "messages-interactive-provider.test.ts (RED contract — pins the domain chokepoint for Plan 02)"
affects:
  - "src/lib/domain/messages.ts (future sendInteractiveMessage — pinned by the RED test, Plan 02)"
  - "interactive-composer-modal.tsx (future consumer of validateButtons/validateList, Plan 04)"
tech-stack:
  added: []
  patterns:
    - "Pure zero-import validation module (mirrors src/lib/domain/types.ts)"
    - "vitest chokepoint mock scaffold (currentProvider toggle, mirror messages-provider.test.ts)"
    - "TDD Wave 0: GREEN helper + RED contract that turns GREEN when the implementation wave lands"
key-files:
  created:
    - "src/lib/whatsapp/interactive-limits.ts"
    - "src/lib/whatsapp/__tests__/interactive-limits.test.ts"
    - "src/lib/domain/__tests__/messages-interactive-provider.test.ts"
  modified: []
decisions:
  - "D-05a: validateButtons/validateList enforce the FULL Meta limit set incl. total-rows <=10 across ALL sections + id-uniqueness — the two checks the sender clamps silently miss (RESEARCH Pitfall 1/4)"
  - "D-03: messages-interactive-provider test pins 360dialog list → clear 'lista no soportada' error (no list function in legacy 360dialog); buttons reuse send360Buttons byte-identical (Regla 6)"
  - "Spanish error strings lifted verbatim from RESEARCH + UI-SPEC Copywriting Contract (LOCKED)"
metrics:
  duration: "~6 min"
  completed: "2026-06-04"
  tasks: 2
  files: 3
---

# Phase 999.1 Plan 01: Wave 0 — interactive-limits helper + chokepoint contracts Summary

Establishes the Nyquist sampling baseline for Phase 999.1: the one genuinely-new pure logic module (`interactive-limits.ts` — the COMPLETE Meta interactive limit set the sender clamps miss) plus the two test files that pin every downstream wave's contract. The limits helper is GREEN under its own unit test; the provider-branch contract test is RED (missing `sendInteractiveMessage` export) and will turn GREEN when Plan 02 lands the domain function.

## What Was Built

### Task 1 — `interactive-limits.ts` + unit test (TDD GREEN) — commit `dabefb44`
- **`src/lib/whatsapp/interactive-limits.ts`** — pure module, ZERO project imports (mirrors `src/lib/domain/types.ts`). Lifted verbatim from RESEARCH §Code Examples (lines 308-359): `INTERACTIVE_LIMITS` (all 13 Meta keys), `InteractiveValidationError`, `validateButtons`, `validateList`.
  - The two genuinely-new checks vs the `meta-whatsapp-sender.ts` clamps: **total-rows-across-all-sections ≤10** (`Máx 10 filas en total`) and **id-uniqueness** for both buttons (`IDs de botón duplicados`) and flattened row ids (`IDs de fila duplicados`).
  - Spanish strings LOCKED by UI-SPEC Copywriting Contract.
- **`src/lib/whatsapp/__tests__/interactive-limits.test.ts`** — 17 tests, no `vi.mock` (pure function). Includes the load-bearing headline case: 10 sections × 2 rows = 20 → reject. **GREEN (17/17).**

### Task 2 — `messages-interactive-provider.test.ts` (RED contract) — commit `cf84a055`
- **`src/lib/domain/__tests__/messages-interactive-provider.test.ts`** — mirrors `messages-provider.test.ts` verbatim with 3 mock deltas (`sendButtonMessage` on `@/lib/whatsapp/api`; `sendButtons`/`sendList` on `@/lib/channels/meta-whatsapp-sender`). 4 assertion blocks:
  - `360dialog + buttons` → `send360Buttons(apiKey, phone, body, buttons, header, footer)`; Meta path never touched (Regla 6 parity).
  - `360dialog + list` → `result.success === false`, error `/lista no soportada/i`; no sender called (D-03).
  - `meta_direct + buttons` → `resolveByWorkspace(WS_ID, 'whatsapp')` ONCE; `metaWhatsappSender.sendButtons(creds, ...)`.
  - `meta_direct + list` → `metaWhatsappSender.sendList(creds, body, buttonLabel, sections, ...)`.
  - **RED (expected)**: fails on `sendInteractiveMessage is not a function` (missing export, NOT a syntax/collection error). Turns GREEN when 999.1-02 lands.

## Verification Results

| Check | Expected | Actual |
| ----- | -------- | ------ |
| `interactive-limits.test.ts` | GREEN | GREEN (17/17, exit 0) |
| Regla 6 gate (`meta-whatsapp-sender` 3/3 + `messages-provider` 5/5) | GREEN | GREEN (8/8, exit 0) |
| `messages-interactive-provider.test.ts` | RED (missing export) | RED (exit 1, `sendInteractiveMessage is not a function`) |
| Zero imports in `interactive-limits.ts` | 0 | 0 |
| `Máx 10 filas en total` present | ≥1 | 1 |

## TDD Gate Compliance

This plan is `type: tdd`. Gate sequence honored:
- **Task 1 (tdd="true")**: GREEN gate — the limit helper and its passing test were committed together as a `feat` commit (`dabefb44`). The implementation is verbatim research lift (no prior failing-then-passing cycle is meaningful for a pure-data helper whose test and impl are authored together per the plan's `<action>`); the test was authored to assert the exact contract and passes 17/17.
- **Task 2**: the `test(...)` commit (`cf84a055`) lands the RED contract for the domain function that Plan 02 will implement (GREEN gate deferred to Plan 02 by design — Wave 0 pins the contract before the implementation wave).

The RED test was verified to fail on the missing implementation (export), not on a passing-by-accident or syntax error — satisfying the fail-fast rule.

## Deviations from Plan

None — plan executed exactly as written. The `Máx 10 filas en total` string was emitted via a template literal with the literal number `10` (verbatim per the LOCKED UI-SPEC string and the RESEARCH example), so the grep gate `grep -c "Máx 10 filas en total"` passes.

## Pre-existing Working-Tree Changes (NOT touched)

Per the execution mandate, the pre-existing unrelated changes under `.planning/standalone/somnio-v4-rag-generative/` were NOT staged or committed. Only the 3 files this plan creates were staged (individually, never `git add -A`).

## Self-Check: PASSED

- FOUND: src/lib/whatsapp/interactive-limits.ts
- FOUND: src/lib/whatsapp/__tests__/interactive-limits.test.ts
- FOUND: src/lib/domain/__tests__/messages-interactive-provider.test.ts
- FOUND commit: dabefb44 (Task 1)
- FOUND commit: cf84a055 (Task 2)
