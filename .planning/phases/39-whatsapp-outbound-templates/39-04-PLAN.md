---
phase: 39-whatsapp-outbound-templates
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/lib/domain/messages.ts
  - src/lib/domain/whatsapp-templates.ts
autonomous: true
requirements: [WA-01, WA-02, WA-03, WA-08, MIG-01, MIG-03]
must_haves:
  truths:
    - "A meta_direct workspace's text/media/template sends route to metaWhatsappSender; a 360dialog workspace's path is byte-identical to today"
    - "The provider decision is made ONCE in the domain layer, never per-call-site (the 131047 fix)"
    - "Template create routes to createTemplateMeta for meta_direct, createTemplate360 for 360dialog"
    - "Meta credentials resolve from ctx.workspaceId via resolveByWorkspace, never from input"
  artifacts:
    - path: "src/lib/domain/messages.ts"
      provides: "provider branch in sendTextMessage/sendMediaMessage/sendTemplateMessage"
      contains: "whatsapp_provider"
    - path: "src/lib/domain/whatsapp-templates.ts"
      provides: "provider branch at the template submit step"
      contains: "whatsapp_provider"
  key_links:
    - from: "src/lib/domain/messages.ts"
      to: "src/lib/channels/meta-whatsapp-sender.ts"
      via: "metaWhatsappSender.send* in the meta_direct branch"
      pattern: "metaWhatsappSender"
    - from: "src/lib/domain/messages.ts"
      to: "src/lib/meta/credentials.ts"
      via: "resolveByWorkspace(ctx.workspaceId, 'whatsapp')"
      pattern: "resolveByWorkspace"
---

<objective>
Wire the single provider-decision chokepoint (MIG-03 / D-02). Add a `whatsapp_provider` branch inside the three domain send functions and the domain template-create orchestration. The `meta_direct` arm resolves Meta creds inside the branch and calls `metaWhatsappSender` / `meta/templates.ts`; the `360dialog` arm stays byte-identical (Regla 6). This is the literal root-cause fix for the Phase 38 131047.

Purpose: Every send/template surface already funnels through these domain functions (Regla 3), so branching here once makes all of them provider-aware without per-call-site patches.
Output: Modified `domain/messages.ts` (3 fns) + `domain/whatsapp-templates.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/lib/domain/messages.ts
@src/lib/domain/whatsapp-templates.ts
@src/lib/channels/meta-whatsapp-sender.ts
@src/lib/meta/credentials.ts
@src/lib/meta/templates.ts

<interfaces>
From src/lib/channels/meta-whatsapp-sender.ts (Plan 02):
```typescript
export const metaWhatsappSender = {
  sendText(creds: {accessToken, phoneNumberId}, to, text): Promise<ChannelSendResult>
  sendMedia(creds, to, type, link, caption?, filename?): Promise<ChannelSendResult>
  sendTemplate(creds, to, name, language, components?): Promise<ChannelSendResult>
  // sendInteractive / sendRead also available
}
```
From src/lib/meta/credentials.ts:
```typescript
export async function resolveByWorkspace(workspaceId, 'whatsapp'): Promise<MetaCredentials | null>  // { accessToken (decrypted), phoneNumberId, wabaId }
```
From src/lib/meta/templates.ts (Plan 03):
```typescript
export async function createTemplateMeta(accessToken, wabaId, { name, language, category, components }): Promise<...>
export async function uploadHeaderHandleMeta(accessToken, appId, fileBytes, mime): Promise<{ h: string }>
```
</interfaces>

RESEARCH Pattern 1 + 39-PATTERNS.md "MODIFY messages.ts (THE CHOKEPOINT)" + "MODIFY whatsapp-templates.ts" give the exact branch shape and the lines to preserve verbatim. MIG-01 note: the `whatsapp_provider` column already exists in prod (Phase 38, total=4/dialog360=4) — this plan only READS it. NO migration.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider branch in domain/messages.ts (text/media/template sends)</name>
  <files>src/lib/domain/messages.ts</files>
  <behavior>
    - flag=360dialog (default): send360Text/Media/Template called with identical args; resolveByWorkspace + metaWhatsappSender NEVER called
    - flag=meta_direct: resolveByWorkspace(ctx.workspaceId,'whatsapp') called; metaWhatsappSender.send* called; missing creds → { success:false, error:'Credenciales Meta no configuradas' }
    - DB insert + conversation-update tail unchanged (provider-agnostic)
  </behavior>
  <action>
In each of `sendTextMessage`, `sendMediaMessage`, `sendTemplateMessage`, add the provider branch per 39-PATTERNS.md "THE CHOKEPOINT":
1. Read `workspaces.whatsapp_provider` for `ctx.workspaceId` (single `.select('whatsapp_provider').eq('id', ctx.workspaceId).single()`). MIG-01: column already exists — just read it.
2. `if (channel === 'whatsapp' && ws?.whatsapp_provider === 'meta_direct')`: `resolveByWorkspace(ctx.workspaceId, 'whatsapp')` → if no `accessToken`/`phoneNumberId` return `{ success:false, error:'Credenciales Meta no configuradas' }`; else call the matching `metaWhatsappSender.send*` and set `wamid = resp.externalMessageId`.
3. `else if (channel === 'whatsapp')`: keep the existing `send360Text/Media/Template` arm BYTE-IDENTICAL (Regla 6 — do not reformat, do not change args).
4. FB/IG `else` via `getChannelSender` — UNCHANGED.
Add imports: `resolveByWorkspace` from `@/lib/meta/credentials`, `metaWhatsappSender` from `@/lib/channels/meta-whatsapp-sender`. The DB insert + conversation-update tail of each fn is provider-agnostic — reuse verbatim (messages.ts text :145-186, media :247-293, template :331-371). Creds come ONLY from `ctx.workspaceId`, never from params/input (T-39-02, V4 access control). Never log the resolved token (T-39-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/domain/__tests__/messages-provider.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>messages-provider.test.ts GREEN incl. the Regla 6 parity assertion (flag=360dialog → 360dialog arm called, Meta path never touched) and the meta_direct routing assertion; existing 360dialog arms unchanged in `git diff`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider branch in domain/whatsapp-templates.ts (CRUD create)</name>
  <files>src/lib/domain/whatsapp-templates.ts</files>
  <behavior>
    - flag=360dialog: createTemplate360 called (unchanged); header_handle = public Supabase URL
    - flag=meta_direct: createTemplateMeta(creds, ...) called; header_handle = Meta resumable upload handle (uploadHeaderHandleMeta)
    - uniqueness check, local INSERT PENDING, submitted_at update, REJECTED-on-error audit all provider-agnostic (verbatim)
  </behavior>
  <action>
In `createTemplate` orchestration (whatsapp-templates.ts), wrap the 360dialog submit step (:167-173) in a `whatsapp_provider` branch per 39-PATTERNS.md "MODIFY whatsapp-templates.ts":
- `meta_direct`: `resolveByWorkspace(ctx.workspaceId, 'whatsapp')` → `createTemplateMeta(creds.accessToken, creds.wabaId, { name, language, category, components })`. For IMAGE/VIDEO/DOCUMENT headers, obtain the handle via `uploadHeaderHandleMeta` (Meta wants the resumable upload handle) instead of the public Supabase URL.
- `360dialog` (default): keep `createTemplate360(params.apiKey, ...)` BYTE-IDENTICAL (Regla 6); header step keeps the public Supabase URL in `header_handle` (the 360dialog nuance, PATTERNS.md lines 92-136).
The uniqueness check (:78-90), local INSERT with `status='PENDING'` (:139-164), `submitted_at` update (:176-180), and REJECTED-on-error audit (:186-196) are provider-agnostic — reuse verbatim. Do NOT add a migration (whatsapp_templates table already has the columns used). Note: `editTemplateMeta` (the D-05 capability) is exposed for the actions/UI layer via `meta/templates.ts`; wiring the edit UI is template-management UI work — this task covers create routing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/meta/__tests__/templates.test.ts 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | grep -E "whatsapp-templates" || echo "no new tsc errors"</automated>
  </verify>
  <done>Template create routes to createTemplateMeta for meta_direct and createTemplate360 (unchanged) for 360dialog; header-handle branch differs per provider; tsc clean; the 360dialog submit args are unchanged in `git diff`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (action/agent/automation) → domain | params are caller-controlled; workspaceId comes from ctx (execution context), not params |
| domain → Meta credential store | creds resolved by workspaceId only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | Spoofing/Tampering (cross-workspace send) | provider branch | mitigate | `resolveByWorkspace(ctx.workspaceId, ...)` — creds NEVER from params/input; the 131047 fix. Regla 6 parity test asserts default path untouched |
| T-39-01 | Information Disclosure | meta_direct branch | mitigate | Resolved BISUAT only passed to sender; never logged, never returned to client |
| T-39-09 | DoS (regression on prod) | 360dialog arm | mitigate | 360dialog arm byte-identical; Regla 6 parity test green = Somnio + 4 prod workspaces untouched |
</threat_model>

<verification>
- `npx vitest run src/lib/domain/__tests__/messages-provider.test.ts` GREEN (parity + meta_direct routing).
- `npx vitest run` full suite green (confirm no regression on Somnio/360dialog paths).
- `git diff` on the 360dialog arms = only the surrounding branch wrapper added; the existing `send360*`/`createTemplate360` calls unchanged.
- `grep -n "whatsapp_provider" src/lib/domain/messages.ts src/lib/domain/whatsapp-templates.ts` shows the read in each.
</verification>

<success_criteria>
The provider decision is made in exactly one layer (domain). A `meta_direct` workspace sends/creates-templates via Meta; a `360dialog` workspace (default — Somnio + all current clients) is byte-identical to today. MIG-03 satisfied; MIG-01 column read (no migration).
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-04-SUMMARY.md`
</output>
