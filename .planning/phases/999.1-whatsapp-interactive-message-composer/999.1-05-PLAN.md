---
phase: 999.1-whatsapp-interactive-message-composer
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/app/(dashboard)/whatsapp/components/message-bubble.tsx
autonomous: true
requirements: [D-05c]
must_haves:
  truths:
    - "The outbound message-bubble case 'interactive' renders the buttons/list actually sent (consuming the content JSONB from D-04), NOT the placeholder 'Mensaje interactivo / (Ver en WhatsApp)'"
    - "It reuses the SAME shared InteractiveBubble component as the composer preview (single source of truth — preview and sent bubble are pixel-identical)"
    - "The bubble container/shell/timestamp/status-icon row (L201-220) is UNCHANGED — only the inner content of case 'interactive' changes"
  artifacts:
    - path: "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"
      provides: "Rich outbound interactive render via InteractiveBubble"
      contains: "InteractiveBubble"
  key_links:
    - from: "message-bubble.tsx case 'interactive'"
      to: "interactive-bubble.tsx"
      via: "rich render consuming content JSONB"
      pattern: "InteractiveBubble content="
---

<objective>
Enrich the OUTBOUND bubble: replace the `case 'interactive'` placeholder in `message-bubble.tsx` with the shared `InteractiveBubble` (from Plan 04), consuming the `content` JSONB stored by the domain (D-04) so the operator sees the actual buttons/list they sent (D-05c).

Purpose: Single source of truth — the same component used for the composer's live preview renders the sent bubble, guaranteeing visual consistency. Only the inner content of the `case` changes; the bubble shell stays untouched.
Output: `message-bubble.tsx case 'interactive'` renders rich; this is the last wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-UI-SPEC.md

<interfaces>
<!-- message-bubble.tsx verified anchors (this session): -->
<!-- L120-130: case 'template' shows the content-narrowing pattern: const templateContent = content as { body?: string } -->
<!-- L133-140: case 'interactive' placeholder to REPLACE: -->
<!--   <div className="text-sm"><p className="font-medium">Mensaje interactivo</p><p className="text-xs text-muted-foreground">(Ver en WhatsApp)</p></div> -->
<!-- L201-220: bubble container/shell — DO NOT TOUCH -->

<!-- shared component (Plan 04): -->
import { InteractiveBubble, type InteractiveContent } from './interactive-bubble'
<!-- stored content JSONB shape: { interactiveType, body, header?, footer?, buttons? | buttonLabel?+sections? } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace case 'interactive' placeholder with InteractiveBubble</name>
  <read_first>
    - src/app/(dashboard)/whatsapp/components/message-bubble.tsx L118-145 (case 'template' narrowing pattern + case 'interactive' placeholder)
    - src/app/(dashboard)/whatsapp/components/interactive-bubble.tsx (the component to consume — InteractiveBubble + InteractiveContent, from Plan 04)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md §message-bubble.tsx (lines 344-359)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-UI-SPEC.md Area 3
  </read_first>
  <action>
    In `src/app/(dashboard)/whatsapp/components/message-bubble.tsx`:
    1. Add `import { InteractiveBubble, type InteractiveContent } from './interactive-bubble'` to the import block.
    2. Replace ONLY the inner JSX of `case 'interactive': { ... }` (L133-140) — the placeholder `<div className="text-sm"><p className="font-medium">Mensaje interactivo</p>...</div>` — with the rich render, mirroring the `case 'template'` content-narrowing pattern (L118):
       ```tsx
       case 'interactive': {
         const interactiveContent = content as unknown as InteractiveContent
         return <InteractiveBubble content={interactiveContent} />
       }
       ```
    Do NOT touch the bubble container/shell, padding, timestamp, or status-icon row (L201-220) — the rich content sits inside the existing `max-w-[70%]` editorial container unchanged (UI-SPEC Area 3). Only the inner content region of this one `case` changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "message-bubble.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { InteractiveBubble" "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"` >= 1
    - `grep -c "<InteractiveBubble content=" "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"` >= 1
    - placeholder removed: `grep -c "(Ver en WhatsApp)" "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"` == 0
    - shell untouched: `git diff "src/app/(dashboard)/whatsapp/components/message-bubble.tsx"` shows changes ONLY within the case 'interactive' block + the new import (no edits around L201-220)
    - `npx tsc --noEmit` reports 0 errors for message-bubble.tsx
    - `npx vitest run` exits 0 (full suite green — Regla 6 + provider + limits)
  </acceptance_criteria>
  <done>case 'interactive' renders the shared InteractiveBubble from the stored JSONB; placeholder gone; bubble shell unchanged; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored content JSONB → rendered bubble | The persisted interactive structure (operator-authored titles) renders as text in the outbound bubble. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-XSS | Tampering (stored-XSS via persisted titles) | message-bubble case 'interactive' | mitigate | Renders via the shared InteractiveBubble (React auto-escaping, NO dangerouslySetInnerHTML — verified in Plan 04). No new render path introduced. |
| T-999.1-R6 | (regression in the bubble switch) | message-bubble.tsx | accept→verify | Only the inner JSX of one `case` changes; container/timestamp/status untouched (git-diff gate); full suite green. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors for message-bubble.tsx
- `grep -c "(Ver en WhatsApp)"` → 0 (placeholder removed)
- `npx vitest run` → GREEN (full suite)
- MANUAL (phase gate, per VALIDATION manual-only): open a conversation, send buttons + list, confirm the outbound bubble renders the actual buttons/list (not the placeholder).
</verification>

<success_criteria>
- Outbound interactive bubble renders rich via the shared component; preview and sent bubble are identical; bubble shell unchanged; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-05-SUMMARY.md`
</output>
