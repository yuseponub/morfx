# CONTINUE-HERE — post /clear (2 trabajos en cola)

> **ACTUALIZACIÓN 2026-06-01 (b):** #3 `v4-media-audio-image` **WAVES 1-5 ✅ EJECUTADAS COMPLETAS.**
> - Wave 1 (Plan 02): `setMessageTranscription` domain + `Message.transcription`. Commits `5c59c15f..0cd248f1`.
> - Wave 2 (Plan 03): media-gate v4-gating + `image-classifier.ts` (Gemini 2.5 Flash) + `vision_respond` variant + `persist-transcription` step. Commits `28f04758..4c826d53`.
> - Wave 3 (Plan 04): rama de visión dedicada en `somnio-v4-agent.ts` (decisión Task1 = `wired-reuse`) + `visionContext` threaded prod+sandbox. Commits `4ec32d4b..6f4f34bc`.
> - Wave 4 (Plan 05): UI transcript bajo el `<audio>` player + A1 realtime confirmado. Commits `02a60098..de22c968`.
> - Wave 5 (Plan 06): REGLA6-EVIDENCE.md (G1-G8 todos PASS) + 06-SUMMARY.md + LEARNINGS.md. Commits `4adc3bd0..1b167cca`.
> - **Regla 6 verificado:** protected-list (5 agentes no-v4 + v3-runner + interruption-v2) = 0-diff vs `85092058`; CheckpointId=8; media-gate v4-gated. **Pusheado a `exec/debounce-v2-wave6` (`1b167cca`).**
> - **Test sweep:** 333 passed, 4 skipped. Fallos pre-existentes (NO regresión, del standalone `somnio-v4-rag-generative` sin cerrar): `few-shots > M1 probability framing` + `smoke-rag-b` 3 casos.
> - **PENDIENTE:** (a) decisión usuario: ff a main = deploy Vercel prod (v4 DORMANT → seguro pero outward-facing). (b) Smoke live diferido a activación v4 (canal 360dialog, D-11) — checklist en REGLA6-EVIDENCE.md.
>
> **ACTUALIZACIÓN 2026-06-01:** TAREA 1 ✅ SHIPPED · TAREA 2 → discuss-phase ✅ COMPLETO.
> - **#2 `v4-subloop-context-pass`** SHIPPED — commits `0dcd5a11` + `6335dcc3` + `8c19ee61`, pusheado.
>   stateContext (3 campos, tight — sin 4º) al path RAG. 22 tests, Regla 6 limpia vs `72ee8e95`, CheckpointId=8.
> - **#3 `v4-media-audio-image`** discuss ✅ + research ✅ (`662ce12a`) + plan-phase ✅ (`13b5cc01`, plan-checker
>   PASS 6/6) + **WAVE 0 ✅ (migración aplicada en prod 2026-06-01 — `messages.transcription TEXT NULL` confirmada;
>   pausa Regla 5 RESUELTA, ver `01-SUMMARY.md`)**. **NEXT: ejecutar Waves 1-5 COMPLETO (sin pausas — migración ya
>   en prod). Orquestar a mano:** spawnear `gsd-executor` (sonnet) por wave en orden 02→03→04→05→06 (depends_on en
>   frontmatter; Wave 4/Plan 05 solo depende de Plan 02). Plan 04 tiene 1 checkpoint:decision (re-verificar send
>   `rag:*` en v4-production-runner :751/:796/:839 — ya confirmado existente; outcome esperado `wired-reuse`).
>   Tras cada wave: greps Regla 6 vs baseline `85092058` (5 agentes no-v4 + v3-runner + interruption-v2 = 0-diff,
>   CheckpointId=8). Al final: push + (opcional) LEARNINGS. v4 DORMANT → push seguro.
>   Decisión arquitectónica clave del usuario: **respuesta de imagen RUTEADA POR EL ENGINE (opción b)** —
>   el send RAG vive en v4-production-runner (`:751`/`:796`/`:839`, emite `rag:*`), no en media-gate.
>   Rama de visión DENTRO del engine (`somnio-v4-agent.ts` branch en `input.visionContext`, salta
>   comprehension/templates=D-05 pero grounded vía runSubLoop) emite `rag:<topic>` → runner envía reusando
>   no-rep+interrupción+ledger. Audio E2E independiente (persist+UI+pipeline normal). Baseline Regla 6 `85092058`.

**Creado:** 2026-06-01 · **Rama:** `exec/debounce-v2-wave6` · **HEAD al crear:** `72ee8e95`
**origin/main** ya está al día (Vercel desplegado) en `89b1979a`; la rama va adelante con docs/planes nuevos.

> **Cómo retomar:** estos son **standalones** en `.planning/standalone/` — `gsd-tools init` NO los descubre
> (`phase_found:false`). Orquestar A MANO (leer el plan + artefactos, spawnear `gsd-executor`/agentes).
> Tooling real: `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs` (NO `gsd-sdk`).
> **Ejecución SECUENCIAL en main tree** (NO worktree — drift documentado en este WSL).

---

## CONTEXTO: dónde estamos

Acabamos de cerrar el standalone **`v4-hybrid-template-rag-turn`** (#3 del roadmap v4) — SHIPPED + en `origin/main`,
18/18 verificado, v4 DORMANT. Antes de simplificar v4, el usuario (Jose) pidió 3 cosas:

- **#1 (UI editor de templates + KBs por agente):** prompt para OTRA instancia → `.planning/PROMPT-agent-templates-kb-editor.md` (LISTO, ya entregado).
- **#2 (pasar contexto del state al sub-loop RAG):** mini-plan LISTO → ejecutar (TAREA 1 abajo).
- **#3 (audio + imagen):** standalone propio → arrancar discuss-phase (TAREA 2 abajo).

---

## TAREA 1 (PRIMERO): Ejecutar #2 — `v4-subloop-context-pass`

**Plan:** `.planning/standalone/v4-subloop-context-pass/01-PLAN.md` (APROBADO por el usuario, modulo el matiz de abajo).
**Baseline Regla 6 para diffs:** `72ee8e95` (HEAD al crear este handoff).

**Qué hace:** el path RAG del sub-loop (`somnio-v4-agent.ts:536`) hoy solo recibe `userMessage` + 4 vueltas crudas.
Le agregamos `stateContext` (datosCapturados + `atendido[]` del turno previo + recentBotMessages) e inyectamos
una sección "CONTEXTO DE LA CONVERSACIÓN (no lo repitas)" en `buildGenerationPrompt`. SOLO contexto + instrucción
ligera — NO sistema de no-repetición (futuro). v4-only, aditivo, sin migración, sin flag.

**Matiz que el usuario dejó abierto (decidir al ejecutar):** opcionalmente pasar también el **stage/lifecycle del
pedido** (ej. "ya tiene pedido CONFIRMADO") como 4º campo del stateContext, para que el RAG no contradiga el estado
de venta. Si `datosCapturados` no lo trae explícito y es fácil, agregarlo; si no, ejecutar los 3 campos del plan tal cual.
**Recomendación:** ejecutar tight (3 campos) salvo que el usuario pida el 4º.

**Cómo ejecutar:** spawnear `gsd-executor` (model sonnet) con el plan + CONTEXT/RESEARCH del hybrid (para entender
el sub-loop) + CLAUDE.md. Constraints: v4-only, correr tests (incl. anti-regresión sin stateContext = idéntico),
greps Regla 6 vs `72ee8e95` (siblings/v3-runner/interruption-v2 = 0-line, CheckpointId=8). Crear `01-SUMMARY.md`.
Deuda pre-existente a IGNORAR (no es regresión): `sub-loop/__tests__/few-shots.test.ts > M1 probability framing`
+ `smoke-rag-a/b` (live-LLM) — son del standalone `somnio-v4-rag-generative`, sin cerrar.

**Tras ejecutar:** spot-check (greps Regla 6 + suite sub-loop verde), commit, y si el usuario quiere → push + ff a main
(safe, v4 DORMANT). LEARNINGS opcional (es pequeño).

---

## TAREA 2 (DESPUÉS de #2): Arrancar #3 — audio + imagen (standalone nuevo)

**Acción:** `discuss-phase` a mano para un standalone nuevo (sugerencia de slug: `v4-media-audio-image`).
NO implementar inline — toca CÓDIGO COMPARTIDO de producción (Regla 6) + necesita migración (Regla 5).

**Lo que pidió el usuario (verbatim del original):**
- **Imágenes:** "podemos tener un contexto general (por lo general las personas envían fotos del producto, o de la
  pág o algo así)" — el agente debe poder responder a imágenes con ese contexto general.
- **Audio:** "agregar una herramienta que transcriba el audio correctamente a la UI del chat y que el agente también
  pueda responder a esto (muchas veces es bueno simplificar lo que dice la persona en un par de intents)".
- **Aclaración clave del usuario:** el audio "aún no está sirviendo" — aunque Whisper técnicamente transcribe, en la
  práctica no entrega valor (transcript no se ve en la UI, el agente no responde bien). Hay que hacerlo funcionar E2E.

**Hallazgos YA verificados en código (NO re-investigar — confirmado esta sesión):**
- **Audio YA transcribe** (Whisper `whisper-1`, es): `src/lib/agents/media/audio-transcriber.ts` +
  `src/lib/agents/media/media-gate.ts`. El transcript se le pasa al agente como texto. PERO:
  - NO se persiste: la tabla `messages` (mig `20260130000002_whatsapp_conversations.sql`) tiene `media_url`/
    `media_mime_type`/`media_filename` pero **NO columna `transcription`** → **REGLA 5 (migración)**.
  - NO se muestra en la UI del inbox: `src/app/(dashboard)/whatsapp/components/message-bubble.tsx` (audio → player,
    sin transcript) + `media-preview.tsx`.
- **Imagen → SIEMPRE handoff** hoy: `media-gate.ts:47`. Para contextualizar (visión) hay PRIOR ART listo:
  `src/lib/agents/media/sticker-interpreter.ts` (Claude Vision base64) + `src/lib/ocr/extract-guide-data.ts`
  (patrón canónico vision, image+pdf). Keys: `OPENAI_API_KEY` (Whisper), Anthropic (vision).
- **🚨 REGLA 6 — el media-gate es COMPARTIDO por TODOS los agentes**, incluido v3 que atiende clientes reales AHORA.
  `media-gate.ts` corre en el Inngest `agent-production.ts` para todos los workspaces. Cambiar imagen→handoff por
  imagen→visión, o cambiar el flujo de audio, AFECTA a v3 producción. La interrupción debounce-v2 se aisló con
  gating `resolvedAgentId === 'somnio-sales-v4'` (`webhook-handler.ts:339`) — usar el MISMO patrón de gating por
  agente para aislar los cambios de media, o feature flag. NO romper v3/godentist/recompra/pw-confirmation.
- **Comprehension toma `message: string`** (v3 y v4) → la conversión media→texto debe pasar ANTES del agente
  (ya pasa hoy en media-gate). La idea del usuario de "simplificar el audio a un par de intents" encaja como
  pre-procesamiento del transcript antes/dentro de comprehension.

**Gray areas para el discuss de #3:**
1. ¿Aislar media v4-only (gating por agente) o aplicar a todos? (Regla 6 — recomendado v4-only primero).
2. Imagen: ¿el agente responde la imagen, o solo la describe para handoff informado? ¿Qué hace si es foto de producto vs screenshot de la pág vs comprobante de pago?
3. Audio: ¿simplificar el transcript a intents antes de comprehension, o pasar transcript completo + que comprehension extraiga? ¿persistir transcript en `messages.transcription` (migración) y mostrarlo bajo el player?
4. ¿Provider de media? Hoy inbound media = 360dialog (`src/lib/whatsapp/api.ts:263`). Meta Direct / Onurix no sirven inbound media hoy — verificar al activar v4.

**Archivos clave #3:** `webhook-handler.ts`, `api.ts` (downloadMedia 360dialog), `media-gate.ts`, `audio-transcriber.ts`,
`sticker-interpreter.ts`, `extract-guide-data.ts`, `agent-production.ts`, `webhook-processor.ts`, `domain/messages.ts:391`,
`messages` migration, `message-bubble.tsx` + `media-preview.tsx` (UI).

---

## Después de #2 y #3
Volver a la conversación de **simplificar/unificar/optimizar v4** (puede llegar a 9 si se prueba + simplifica).
Secuencia propuesta: activación escalonada (1 workspace) para medir paths reales → standalone #4 simplify-unify.

## Anti-patterns (severity: blocking)
- **NO tocar el comportamiento de v3/godentist/recompra/pw-confirmation** (Regla 6). v4 DORMANT — todos los cambios v4-gated.
- **NO pushear código que referencie columnas inexistentes** (Regla 5 — el incidente de 20h). Migración de `transcription` ANTES del código que la use.
- **NO inline #3** — toca prod compartido. Va por GSD (discuss→research→plan→execute).
