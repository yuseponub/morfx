---
phase: 39-whatsapp-outbound-templates
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/meta/api.ts
  - src/lib/channels/meta-whatsapp-sender.ts
autonomous: true
requirements: [WA-01, WA-02, WA-03, WA-04, WA-07, MIG-03]
must_haves:
  truths:
    - "meta/api.ts exposes send helpers for media, interactive, and read-receipt alongside the existing text/template helpers"
    - "metaWhatsappSender is a ChannelSender-shaped module taking {accessToken, phoneNumberId} (not apiKey)"
    - "Interactive builders clamp to Meta limits (≤3 buttons, ≤20 title, ≤10 sections, ≤24 row title)"
  artifacts:
    - path: "src/lib/meta/api.ts"
      provides: "sendWhatsAppMedia, sendWhatsAppInteractive, markWhatsAppRead (siblings of existing helpers)"
      contains: "messaging_product"
    - path: "src/lib/channels/meta-whatsapp-sender.ts"
      provides: "metaWhatsappSender module the domain branch calls"
      min_lines: 30
  key_links:
    - from: "src/lib/channels/meta-whatsapp-sender.ts"
      to: "src/lib/meta/api.ts"
      via: "import send helpers"
      pattern: "from '@/lib/meta/api'"
---

<objective>
Build the Meta send-edge: extend `meta/api.ts` with media/interactive/read-receipt helpers (siblings of the existing text/template helpers), and create `metaWhatsappSender` — the `ChannelSender`-shaped module the domain provider branch will call. This satisfies WA-01/02/03/04/07 at the API edge.

Purpose: The domain chokepoint (Plan 04) calls `metaWhatsappSender.send*(creds, ...)`; this plan provides that module + its underlying Graph payloads.
Output: Extended `meta/api.ts`, new `src/lib/channels/meta-whatsapp-sender.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/lib/meta/api.ts
@src/lib/channels/whatsapp-sender.ts
@src/lib/channels/types.ts

<interfaces>
From src/lib/meta/api.ts (existing — extend in same style):
```typescript
export async function metaRequest<T>(accessToken, endpoint, options?): Promise<T>  // Bearer + JSON; throws MetaGraphApiError
export async function sendWhatsAppText(accessToken, phoneNumberId, to, body): Promise<{ messages?: { id: string }[] }>
export async function sendWhatsAppTemplate(accessToken, phoneNumberId, to, name, language, components?): Promise<{ messages?: { id: string }[] }>
```
From src/lib/channels/types.ts:
```typescript
export interface ChannelSendResult { success: boolean; externalMessageId?: string; error?: string }
```
The 360dialog analog whose payload gating to MIRROR (caption/filename + button/list clamps): `src/lib/whatsapp/api.ts` `sendMediaMessage` (:95-127), `sendButtonMessage` (:192-232), `markMessageAsRead` (:308-320).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend meta/api.ts with media, interactive, read-receipt helpers</name>
  <files>src/lib/meta/api.ts</files>
  <behavior>
    - sendWhatsAppMedia: image/video/document include caption when given; audio/sticker NEVER include caption; document includes filename when given (RESEARCH Pitfall 4 / §2)
    - sendWhatsAppInteractive: produces button + list payloads matching RESEARCH §4-5 shapes
    - markWhatsAppRead: produces `{ messaging_product:'whatsapp', status:'read', message_id }` (§8)
    - all POST to `/{phoneNumberId}/messages` via metaRequest (Bearer)
  </behavior>
  <action>
Add three exported helpers to `meta/api.ts`, in the exact style of the existing `sendWhatsAppText`/`sendWhatsAppTemplate` (Bearer via `metaRequest`, `/{phoneNumberId}/messages` endpoint, `messaging_product:'whatsapp'` envelope), per RESEARCH Code Examples §2/§4-5/§8 and 39-PATTERNS.md "MODIFY meta/api.ts":
- `sendWhatsAppMedia(accessToken, phoneNumberId, to, type, link, caption?, filename?)` — mirror the 360dialog `sendMediaMessage` gating: `caption` only for image/video/document; `filename` only for document; audio/sticker get neither (Pitfall 4).
- `sendWhatsAppInteractive(accessToken, phoneNumberId, to, interactive)` — pass a pre-built `interactive` object through the envelope (the clamp logic lives in the sender, Task 2).
- `markWhatsAppRead(accessToken, phoneNumberId, wamid)` — `{ messaging_product:'whatsapp', status:'read', message_id: wamid }`.
Do NOT modify the existing `sendWhatsAppText`/`sendWhatsAppTemplate`/`metaRequest` signatures. Never log the access token (Security V6 / T-39-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/meta/__tests__/send.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | grep -E "meta/api" || echo "no new tsc errors in meta/api"</automated>
  </verify>
  <done>send.test.ts WA-01/03/07 assertions GREEN; media caption/filename gating GREEN; existing text/template helpers unchanged; tsc clean for the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create metaWhatsappSender module (WA-04 clamps + ChannelSendResult unwrap)</name>
  <files>src/lib/channels/meta-whatsapp-sender.ts</files>
  <behavior>
    - sendText/sendImage/sendTemplate/sendInteractive/sendRead unwrap `messages?.[0]?.id` into ChannelSendResult.externalMessageId (mirror whatsapp-sender.ts)
    - interactive buttons clamp: `.slice(0,3)`, title `.slice(0,20)`
    - interactive list clamp: ≤10 sections, row title `.slice(0,24)`
    - takes `{ accessToken, phoneNumberId }` — NOT apiKey
  </behavior>
  <action>
Create `src/lib/channels/meta-whatsapp-sender.ts` mirroring `src/lib/channels/whatsapp-sender.ts` structure (same `.messages?.[0]?.id` → `ChannelSendResult` unwrap) but taking a `{ accessToken, phoneNumberId }` creds object instead of an `apiKey` string (per D-02b + 39-PATTERNS.md KEY DESIGN NOTE).

It calls the `meta/api.ts` helpers (Task 1). Provide methods for text, media, template, interactive (buttons + lists), and read-receipt. For interactive, build the payload here and apply the proven 360dialog clamps (mirror `whatsapp/api.ts:192-232`): `buttons.slice(0,3)`, `title.slice(0,20)`; list `sections.slice(0,10)`, row `title.slice(0,24)` (RESEARCH §4-5 limits).

CRITICAL (39-PATTERNS.md KEY DESIGN NOTE + RESEARCH Pattern 1): do NOT register this in the `senders: Record<ChannelType, ChannelSender>` map in `registry.ts` (that map is channel-keyed, not provider-keyed) and do NOT modify `whatsapp-sender.ts` (Regla 6). This is a thin module the domain branch imports directly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts 2>&1 | tail -15 && grep -c "senders\[" src/lib/channels/registry.ts</automated>
  </verify>
  <done>meta-whatsapp-sender.test.ts GREEN (WA-04 clamps verified); module takes `{accessToken, phoneNumberId}`; NOT registered in the channel-keyed senders map; whatsapp-sender.ts untouched (`git diff --stat` excludes it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MorfX server → Meta Graph API | outbound HTTPS with Bearer BISUAT; token is a server-only secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | meta/api.ts helpers | mitigate | Token is only passed to `metaRequest` fetch header; never `console.log`'d, never returned to client (mirror existing helpers) |
| T-39-05 | Input Validation | interactive builder | mitigate | Clamp buttons/titles/sections/rows to Meta limits BEFORE send (.slice guards) — rejects oversized payloads at the edge |
| T-39-06 | Tampering | media send | mitigate | caption/filename gated to allowed types (Pitfall 4) — prevents malformed payloads that Meta 400s |
</threat_model>

<verification>
- `npx vitest run src/lib/meta/__tests__/send.test.ts src/lib/channels/__tests__/meta-whatsapp-sender.test.ts` GREEN.
- `npx tsc --noEmit` no new errors mentioning the two files.
- `git diff --stat` shows `whatsapp-sender.ts`, `whatsapp/api.ts`, `registry.ts` UNCHANGED (Regla 6).
</verification>

<success_criteria>
The Meta send edge exists end-to-end at the helper level: text, media (by link, gated caption/filename), template, interactive (clamped), read-receipt — all callable as `metaWhatsappSender.send*(creds, ...)`, with the 360dialog path byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-02-SUMMARY.md`
</output>
