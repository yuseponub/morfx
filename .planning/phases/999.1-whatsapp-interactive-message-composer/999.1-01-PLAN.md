---
phase: 999.1-whatsapp-interactive-message-composer
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/whatsapp/interactive-limits.ts
  - src/lib/whatsapp/__tests__/interactive-limits.test.ts
  - src/lib/domain/__tests__/messages-interactive-provider.test.ts
autonomous: true
requirements: [D-05a, D-06, D-03, D-04]
must_haves:
  truths:
    - "validateButtons / validateList enforce the FULL Meta limit set (incl. total-rows ≤10 and id-uniqueness), not just the 5 sender clamps"
    - "The 3 Wave 0 files exist; interactive-limits.test.ts passes GREEN; messages-interactive-provider.test.ts is RED (module-not-found / missing sendInteractiveMessage) until Plan 02 lands"
    - "The 4 existing tests (meta-whatsapp-sender 3/3 + messages-provider) stay GREEN (Regla 6)"
  artifacts:
    - path: "src/lib/whatsapp/interactive-limits.ts"
      provides: "Pure validation helper + INTERACTIVE_LIMITS constant"
      contains: "export function validateButtons"
    - path: "src/lib/whatsapp/__tests__/interactive-limits.test.ts"
      provides: "Unit tests for the limit helper (total-rows ≤10 headline case)"
      contains: "validateList"
    - path: "src/lib/domain/__tests__/messages-interactive-provider.test.ts"
      provides: "Provider-branch chokepoint contract tests (RED until Plan 02)"
      contains: "messages-interactive-provider"
  key_links:
    - from: "interactive-limits.test.ts"
      to: "interactive-limits.ts"
      via: "import { validateButtons, validateList, INTERACTIVE_LIMITS }"
      pattern: "from '@/lib/whatsapp/interactive-limits'"
    - from: "messages-interactive-provider.test.ts"
      to: "src/lib/domain/messages.ts sendInteractiveMessage"
      via: "import (RED until Plan 02 exports the function)"
      pattern: "sendInteractiveMessage"
---

<objective>
Establish the Nyquist sampling baseline for Phase 999.1: create the ONE genuinely-new piece of pure logic (`interactive-limits.ts` — the COMPLETE Meta interactive limit set that the sender clamps miss) plus the two test files that pin the contracts for every downstream wave.

Purpose: Wave 0 must land BEFORE the implementation waves so every later task has an automated verify to turn GREEN. The limits helper is consumed by the composer modal (Plan 04); the provider-branch test pins the domain chokepoint (Plan 02).
Output: `interactive-limits.ts` (GREEN under its own test) + `messages-interactive-provider.test.ts` (RED — fails on missing `sendInteractiveMessage` export until Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-CONTEXT.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-RESEARCH.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md
@.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-VALIDATION.md

<interfaces>
<!-- The COMPLETE implementation of interactive-limits.ts is given verbatim in RESEARCH §Code Examples (lines 308-359). Lift it as-is. The shape: -->

export const INTERACTIVE_LIMITS = {
  body: 1024, header: 60, footer: 60,
  maxButtons: 3, buttonTitle: 20, buttonId: 256,
  listButtonLabel: 20, maxSections: 10, sectionTitle: 24,
  maxTotalRows: 10, rowTitle: 24, rowDescription: 72, rowId: 200,
} as const

export interface InteractiveValidationError { field: string; message: string }
export function validateButtons(p: { body: string; header?: string; footer?: string; buttons: { id: string; title: string }[] }): InteractiveValidationError[]
export function validateList(p: { body: string; header?: string; footer?: string; buttonLabel: string; sections: { title: string; rows: { id: string; title: string; description?: string }[] }[] }): InteractiveValidationError[]

<!-- The provider test mocks (from messages-provider.test.ts, the EXACT analog): -->
<!-- module-level `currentProvider` toggle drives the workspaces.whatsapp_provider stub; afterEach resets to '360dialog' -->
<!-- WS_ID = 'a3843b3f-...'; ctx = { workspaceId: WS_ID, source: 'webhook' } as const -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create interactive-limits.ts validation helper + its unit test (GREEN)</name>
  <read_first>
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-RESEARCH.md §Code Examples (lines 308-359 — the verbatim helper implementation)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md §interactive-limits.ts + §interactive-limits.test.ts
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-UI-SPEC.md §Copywriting Contract (the exact Spanish error strings)
    - src/lib/channels/__tests__/meta-whatsapp-sender.test.ts (the test-cadence analog — describe/it style)
    - src/lib/domain/types.ts (the zero-imports pure-module convention)
  </read_first>
  <behavior>
    - validateButtons: rejects >3 buttons (field 'buttons', "Máx 3 botones"); rejects <1 button ("Al menos 1 botón"); rejects body >1024 / footer >60 / header >60 ("Máx N caracteres"); rejects empty body ("El cuerpo es obligatorio"); rejects button title >20 + empty title; rejects duplicate button ids ("IDs de botón duplicados"); ACCEPTS a valid 1-3 button payload (returns []).
    - validateList: rejects >10 TOTAL rows across ALL sections — the headline case: 10 sections × 2 rows = 20 → REJECT ("Máx 10 filas en total"); rejects >10 sections; rejects buttonLabel >20 + empty; rejects row title >24 + empty; rejects row description >72; rejects section title >24; rejects duplicate row ids across ALL sections ("IDs de fila duplicados"); ACCEPTS a valid list (returns []).
    - INTERACTIVE_LIMITS exports all 13 keys: body 1024, header 60, footer 60, maxButtons 3, buttonTitle 20, buttonId 256, listButtonLabel 20, maxSections 10, sectionTitle 24, maxTotalRows 10, rowTitle 24, rowDescription 72, rowId 200.
  </behavior>
  <action>
    Create `src/lib/whatsapp/interactive-limits.ts` — a PURE module with ZERO project imports (like `src/lib/domain/types.ts`). Lift the verbatim implementation from RESEARCH §Code Examples lines 308-359 (the `INTERACTIVE_LIMITS` const, `InteractiveValidationError` interface, `validateButtons`, `validateList`). The Spanish messages are LOCKED by UI-SPEC Copywriting Contract — use EXACTLY: `El cuerpo es obligatorio`, `Máx ${N} caracteres` (body), `Máx ${INTERACTIVE_LIMITS.header}` (header — bare number per the research example), `Al menos 1 botón`, `Máx ${INTERACTIVE_LIMITS.maxButtons} botones`, `Título obligatorio`, `IDs de botón duplicados`, `Etiqueta del botón obligatoria`, `Máx 10 filas en total` (the total-rows error — verbatim string), `IDs de fila duplicados`, `Al menos 1 fila`. The two genuinely-new checks vs the sender clamps are: (a) `maxTotalRows` — `p.sections.reduce((n, s) => n + s.rows.length, 0) > INTERACTIVE_LIMITS.maxTotalRows`, and (b) id-uniqueness via `new Set(ids).size !== ids.length` for both buttons and the flattened row ids.

    Then create `src/lib/whatsapp/__tests__/interactive-limits.test.ts`. NO `vi.mock` needed (pure function, no I/O). Import `{ validateButtons, validateList, INTERACTIVE_LIMITS }` from `@/lib/whatsapp/interactive-limits`. Mirror the describe/it cadence of `meta-whatsapp-sender.test.ts`. Required test cases (per PATTERNS lines 204-223):
    `describe('validateButtons — Meta limit set')`: rejects >3 buttons; rejects body>1024 / footer>60 / header>60; rejects button title>20; rejects duplicate button ids; accepts a valid 1-3 button payload (expect `[]`).
    `describe('validateList — Meta limit set')`: rejects >10 total rows across all sections (build 10 sections × 2 rows = 20 → expect a 'sections' error matching /10 filas/); rejects buttonLabel>20 / row title>24 / row description>72; rejects duplicate row ids across ALL sections; accepts a valid list (expect `[]`).
    Assert on the returned error array (e.g. `expect(errs.some(e => e.field === 'buttons')).toBe(true)` and `expect(validateButtons(valid)).toEqual([])`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/whatsapp/interactive-limits.ts` exits 0
    - `grep -c "export function validateButtons" src/lib/whatsapp/interactive-limits.ts` >= 1
    - `grep -c "export function validateList" src/lib/whatsapp/interactive-limits.ts` >= 1
    - `grep -c "maxTotalRows" src/lib/whatsapp/interactive-limits.ts` >= 1
    - `grep -c "import " src/lib/whatsapp/interactive-limits.ts` == 0 (zero imports — pure module)
    - `grep -c "Máx 10 filas en total" src/lib/whatsapp/interactive-limits.ts` >= 1
    - `npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts` exits 0 (GREEN)
    - The list test file contains a case with 10 sections × 2 rows asserting rejection (grep for "20" or a reduce producing >10 in the test)
  </acceptance_criteria>
  <done>interactive-limits.ts exists with the full Meta limit set incl. maxTotalRows + id-uniqueness; its unit test is GREEN; zero project imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create messages-interactive-provider.test.ts (RED until Plan 02)</name>
  <read_first>
    - src/lib/domain/__tests__/messages-provider.test.ts (the EXACT analog — copy the mock scaffold verbatim, L35-132)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-PATTERNS.md §messages-interactive-provider.test.ts (the deltas to apply)
    - .planning/phases/999.1-whatsapp-interactive-message-composer/999.1-RESEARCH.md §Code Examples lines 362-375 (assertions to encode)
    - src/lib/domain/messages.ts L21-37 (the send360* import aliases) + L372-462 (sendTemplateMessage — the function this test will pin)
  </read_first>
  <action>
    Create `src/lib/domain/__tests__/messages-interactive-provider.test.ts`. Copy the mock scaffold from `messages-provider.test.ts` VERBATIM, with these exact deltas (PATTERNS lines 172-176):
    - In `vi.mock('@/lib/whatsapp/api', ...)`: ADD `sendButtonMessage: vi.fn()` to the returned object (alongside the existing Text/Media/Template mocks).
    - In `vi.mock('@/lib/channels/meta-whatsapp-sender', ...)`: ADD `sendButtons: vi.fn()` and `sendList: vi.fn()` to the `metaWhatsappSender` object (alongside `sendText`/`sendImage`).
    - Keep the chainable Supabase `makeBuilder` stub VERBATIM (it already returns `{ whatsapp_provider: currentProvider }` for `workspaces` and `{ id: 'msg_db_1' }` for `messages`).
    - Keep the module-level `currentProvider` toggle + `afterEach` reset to `'360dialog'`.
    - Reuse constants verbatim: `WS_ID = 'a3843b3f-...'`, `ctx = { workspaceId: WS_ID, source: 'webhook' } as const`.

    `beforeEach` mock returns (PATTERNS lines 180-185):
    `mockSend360Buttons.mockResolvedValue({ messages: [{ id: 'wamid.360' }] })`,
    `mockMetaSendButtons.mockResolvedValue({ success: true, externalMessageId: 'wamid.meta' })`,
    `mockMetaSendList.mockResolvedValue({ success: true, externalMessageId: 'wamid.meta.list' })`,
    `mockResolveByWorkspace.mockResolvedValue({ accessToken: 'BISUAT_decrypted', phoneNumberId: '1134593926408063' })`.

    Import `sendInteractiveMessage` from `@/lib/domain/messages` and encode these assertions (D-06 / RESEARCH lines 362-375):
    - `360dialog + buttons`: call `sendInteractiveMessage(ctx, { interactiveType:'buttons', apiKey:'KEY360', contactPhone, body, buttons, header, footer, conversationId })` → `expect(mockSend360Buttons).toHaveBeenCalledWith(apiKey, phone, body, buttons, header, footer)`; `expect(mockResolveByWorkspace).not.toHaveBeenCalled()`; meta senders `not.toHaveBeenCalled()` (Regla 6 parity).
    - `360dialog + list`: `result.success === false`; `expect(result.error).toMatch(/lista no soportada/i)`; no sender called.
    - `meta_direct + buttons`: set `currentProvider = 'meta_direct'` → `expect(mockResolveByWorkspace).toHaveBeenCalledWith(WS_ID, 'whatsapp')` ONCE; `mockMetaSendButtons` first arg `toMatchObject({ accessToken, phoneNumberId })`.
    - `meta_direct + list`: `mockMetaSendList` called with creds + body + buttonLabel + sections.
    - `whatsapp_provider` read ONCE per call (the workspaces select stub invoked once — assert via the supabase stub call count if available, else assert no double-branch).

    This test is EXPECTED to be RED now (Plan 02 has not yet exported `sendInteractiveMessage`). Document that in a top-of-file comment: `// RED until 999.1-02 (domain sendInteractiveMessage) lands.`
  </action>
  <verify>
    <automated>npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts src/lib/domain/__tests__/messages-provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/domain/__tests__/messages-interactive-provider.test.ts` exits 0
    - `grep -c "sendButtonMessage: vi.fn()" src/lib/domain/__tests__/messages-interactive-provider.test.ts` >= 1
    - `grep -c "sendButtons: vi.fn()" src/lib/domain/__tests__/messages-interactive-provider.test.ts` >= 1
    - `grep -c "sendList: vi.fn()" src/lib/domain/__tests__/messages-interactive-provider.test.ts` >= 1
    - `grep -c "lista no soportada" src/lib/domain/__tests__/messages-interactive-provider.test.ts` >= 1
    - `grep -c "toHaveBeenCalledWith(WS_ID, 'whatsapp')" src/lib/domain/__tests__/messages-interactive-provider.test.ts` >= 1
    - REGLA 6 GATE: `npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts src/lib/domain/__tests__/messages-provider.test.ts` exits 0 (the 4 existing tests stay GREEN)
    - `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts` exits NON-zero (RED — expected; fails on missing sendInteractiveMessage export, NOT on syntax/collection error)
  </acceptance_criteria>
  <done>messages-interactive-provider.test.ts exists mirroring messages-provider.test.ts with the 3 mock deltas + 5 assertion blocks; it is RED (missing export) while the 4 existing tests stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 creates pure logic + tests only; no runtime data crosses a boundary here. The limit helper hardens V5 Input Validation for the downstream composer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-V5 | Tampering (oversized/duplicate input) | `interactive-limits.ts` | mitigate | Enforce the FULL Meta limit set (length/count/uniqueness) so the composer blocks invalid payloads BEFORE the server action — total-rows ≤10 + id-uniqueness are the checks the sender clamps miss (RESEARCH Pitfall 1/4). |
| T-999.1-R6 | (Regla 6 regression) | existing 4 tests | accept→verify | Wave 0 adds NEW files only; the meta-whatsapp-sender 3/3 + messages-provider tests must stay GREEN (acceptance gate in Task 2). |
</threat_model>

<verification>
- `npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts` → GREEN
- `npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts src/lib/domain/__tests__/messages-provider.test.ts` → GREEN (Regla 6)
- `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts` → RED (expected; turns GREEN when 999.1-02 lands)
</verification>

<success_criteria>
- The 3 Wave 0 files exist (interactive-limits.ts + 2 test files).
- interactive-limits.test.ts is GREEN with the total-rows ≤10 + id-uniqueness cases.
- messages-interactive-provider.test.ts is RED on missing export only (not syntax).
- The 4 existing tests stay GREEN (Regla 6).
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-whatsapp-interactive-message-composer/999.1-01-SUMMARY.md`
</output>
