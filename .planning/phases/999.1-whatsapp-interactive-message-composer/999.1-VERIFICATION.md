---
phase: 999.1-whatsapp-interactive-message-composer
verified: 2026-06-04T10:40:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Enviar botones: abrir conversacion con ventana 24h abierta, hacer clic en el boton List del toolbar, componer 2 botones, enviar"
    expected: "El mensaje se envia correctamente; la burbuja saliente muestra los botones reales (no el placeholder); el provider meta_direct/360dialog no es visible para el operador"
    why_human: "Requiere browser + ventana 24h abierta + numero WhatsApp real"
  - test: "Enviar lista (meta_direct): igual al anterior pero seleccionar tipo Lista con secciones y filas"
    expected: "La burbuja saliente muestra el boton de lista con el buttonLabel correcto"
    why_human: "Requiere browser + ventana 24h abierta + workspace meta_direct configurado"
  - test: "Intentar enviar lista en workspace 360dialog"
    expected: "El Dialog permanece abierto; el operador ve el toast amistoso 'Las listas no estan disponibles en esta conexion de WhatsApp. Usa botones.' (no el error tecnico del domain)"
    why_human: "Requiere browser + workspace configurado con proveedor 360dialog"
  - test: "Boton compositor con ventana 24h cerrada"
    expected: "El boton List NO aparece en el toolbar (solo TemplateButton visible); la rama cerrada de message-input.tsx queda intacta"
    why_human: "Requiere browser + conversacion con ventana cerrada"
---

# Phase 999.1: WhatsApp Interactive Message Composer — Verification Report

**Phase Goal:** Que un operador pueda componer y enviar un mensaje interactivo de WhatsApp (botones / lista) desde el compositor del inbox, con awareness de provider (meta_direct vs 360dialog), preview en vivo, y una burbuja saliente rica que reusa el mismo componente de render.
**Verified:** 2026-06-04T10:40:00Z
**Status:** HUMAN_NEEDED
**Re-verification:** No — verificacion inicial

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Domain `sendInteractiveMessage` lee `whatsapp_provider` UNA SOLA vez y hace branch (chokepoint Regla 3/Fase 39) | VERIFIED | `messages.ts:500` — `const provider = await readWhatsappProvider(supabase, ctx.workspaceId)` — unica lectura dentro de la funcion; meta_direct → `metaWhatsappSender.sendButtons/.sendList` con creds de `resolveByWorkspace(ctx.workspaceId,'whatsapp')`; 360dialog+buttons → `send360Buttons`; 360dialog+list → error claro |
| 2 | Estructura interactiva completa guardada en `messages.content` JSONB (`type='interactive'`) para render rico (D-04) | VERIFIED | `messages.ts:554-577` — `content = { interactiveType, body, header?, footer?, buttons?, buttonLabel?, sections? }` insertado con `type: 'interactive'`, `direction: 'outbound'`, `status: 'sent'` |
| 3 | Server action: auth gate, carga de conversacion scoped por workspace, channel!=='whatsapp' rechazado, ventana 24h re-chequeada server-side, provider NUNCA leido en la accion (Regla 3) | VERIFIED | `actions/messages.ts:208-283` — `getRequestAuth()` (L208), `.eq('workspace_id', workspaceId)` (L220), channel guard (L228-230), `differenceInHours >= 24` (L243-245). Las dos ocurrencias de `whatsapp_provider` en el archivo (L460/L465) estan dentro de `markMessageAsRead` (L436+), fuera de `sendInteractiveMessage` (L196-283) — Regla 3 satisfecha |
| 4 | UI: boton compositor SOLO en rama de ventana abierta; Enviar deshabilitado en errores de validacion/sending; ids auto-generados unicos; `interactive-bubble.tsx` es render puro sin `dangerouslySetInnerHTML`; preview comparte el mismo componente que la burbuja outbound | VERIFIED | `message-input.tsx:292` — early return `if (!isWindowOpen)` precede L441 `<InteractiveComposerButton ...>`; modal L433 `disabled={sending \|\| hasErrors}`; `reindexButtons`/`reindexSections` en L66-71; `interactive-bubble.tsx`: 0 ocurrencias de `dangerouslySetInnerHTML`; modal L410 `<InteractiveBubble content={previewContent} />` — mismo componente que Plan 05 |
| 5 | Burbuja outbound `case 'interactive'` renderiza botones/lista reales via `InteractiveBubble` compartido; placeholder "(Ver en WhatsApp)" eliminado | VERIFIED | `message-bubble.tsx:134-137` — `case 'interactive': { const interactiveContent = content as unknown as InteractiveContent; return <InteractiveBubble content={interactiveContent} /> }`; grep de "(Ver en WhatsApp)" en el archivo = 0 |
| 6 | Regla 6: los 4 tests preexistentes (meta-whatsapp-sender 3/3 + messages-provider 5/5) se mantienen GREEN; `whatsapp/api.ts` byte-identico | VERIFIED | Suite 29/29 GREEN (17 interactive-limits + 4 messages-interactive-provider + 5 messages-provider + 3 meta-whatsapp-sender); `git diff origin/main -- src/lib/whatsapp/api.ts` = 0 lineas (api.ts intacto) |

**Score:** 6/6 truths verified

---

## Required Artifacts

| Artifact | Provides | Status | Evidence |
|----------|----------|--------|----------|
| `src/lib/whatsapp/interactive-limits.ts` | INTERACTIVE_LIMITS (13 keys) + `validateButtons` + `validateList` + id-uniqueness + total-rows ≤10 | VERIFIED | Existe, ZERO project imports, `maxTotalRows: 10`, `Máx 10 filas en total`, ambas funciones exportadas |
| `src/lib/whatsapp/__tests__/interactive-limits.test.ts` | 17 tests unitarios para el helper de limites | VERIFIED | GREEN 17/17; incluye caso 10 secciones × 2 filas = 20 → reject |
| `src/lib/domain/__tests__/messages-interactive-provider.test.ts` | Contrato del chokepoint de provider (RED→GREEN en Plan 02) | VERIFIED | GREEN 4/4 tras Plan 02; mocks de `sendButtonMessage`, `sendButtons`, `sendList` presentes |
| `src/lib/domain/messages.ts` | `sendInteractiveMessage` + `SendInteractiveMessageParams` + import `send360Buttons` | VERIFIED | `export async function sendInteractiveMessage` en L492; interfaz en L106; `sendButtonMessage as send360Buttons` en L24 |
| `src/app/actions/messages.ts` | Server action `sendInteractiveMessage` con todos los guards | VERIFIED | Exportada en L196; `domainSendInteractiveMessage` alias importado en L12 |
| `src/app/(dashboard)/whatsapp/components/interactive-bubble.tsx` | Render PURO compartido — `InteractiveBubble` + `InteractiveContent` | VERIFIED | Existe; sin state, sin efectos, sin `dangerouslySetInnerHTML`; verdes WhatsApp locked |
| `src/app/(dashboard)/whatsapp/components/interactive-composer-modal.tsx` | Builder Dialog con validacion + preview + envio | VERIFIED | Existe; importa `validateButtons`/`validateList`; `sendInteractiveMessage(conversationId, payload)`; `<InteractiveBubble content={previewContent} />`; `disabled={sending \|\| hasErrors}` |
| `src/app/(dashboard)/whatsapp/components/interactive-composer-button.tsx` | Trigger del toolbar (icono List ghost) | VERIFIED | Existe; wrappea `InteractiveComposerModal`; `aria-label` + `title` presentes |
| `src/app/(dashboard)/whatsapp/components/message-input.tsx` | Cableado del boton SOLO en rama ventana abierta (D-02) | VERIFIED | `<InteractiveComposerButton` en L441 > early-return `if (!isWindowOpen)` en L292 |
| `src/app/(dashboard)/whatsapp/components/message-bubble.tsx` | Burbuja outbound rica via `InteractiveBubble` | VERIFIED | `case 'interactive'` en L134-137 reusa `InteractiveBubble`; placeholder removido |

---

## Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|----------|
| `interactive-limits.test.ts` | `interactive-limits.ts` | `import { validateButtons, validateList, INTERACTIVE_LIMITS }` | WIRED | `from '@/lib/whatsapp/interactive-limits'` en test file |
| `messages-interactive-provider.test.ts` | `domain/messages.ts sendInteractiveMessage` | import | WIRED | Funcion exportada; test GREEN 4/4 |
| `interactive-composer-modal.tsx` | `interactive-limits.ts` | `validateButtons`, `validateList` | WIRED | L34-35 modal; L175-176 uso en cada render |
| `interactive-composer-modal.tsx` | `actions/messages.ts sendInteractiveMessage` | `import { sendInteractiveMessage }` | WIRED | L39 + L219 uso en `handleSend` |
| `interactive-composer-modal.tsx` | `interactive-bubble.tsx` | `InteractiveBubble` | WIRED | L38 import; L410 `<InteractiveBubble content={previewContent} />` |
| `interactive-composer-button.tsx` | `interactive-composer-modal.tsx` | `InteractiveComposerModal` | WIRED | L13 import; L46 render dentro del button |
| `message-input.tsx` | `interactive-composer-button.tsx` | `InteractiveComposerButton` | WIRED | L10 import; L441 uso en rama ventana abierta |
| `message-bubble.tsx` | `interactive-bubble.tsx` | `InteractiveBubble, InteractiveContent` | WIRED | L6 import; L136 `<InteractiveBubble content={interactiveContent} />` |
| `actions/messages.ts sendInteractiveMessage` | `domain/messages.ts sendInteractiveMessage` | `domainSendInteractiveMessage` | WIRED | L12 import como alias; L262 llamada con `ctx` + payload |
| `domain/messages.ts sendInteractiveMessage` | `metaWhatsappSender.sendButtons/.sendList` | provider branch meta_direct | WIRED | L514 `metaWhatsappSender.sendButtons(...)`; L522 `metaWhatsappSender.sendList(...)` |
| `domain/messages.ts sendInteractiveMessage` | `send360Buttons` | provider branch 360dialog | WIRED | L538 `send360Buttons(params.apiKey, ...)` |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `interactive-composer-modal.tsx` | `previewContent` (InteractiveContent) | Estado local del modal (body, header, footer, buttons, sections) | Si — estado derivado del form, no hardcoded | FLOWING |
| `message-bubble.tsx case 'interactive'` | `content as InteractiveContent` | `messages.content` JSONB de DB (guardado por `sendInteractiveMessage` domain, D-04) | Si — la funcion domain escribe la estructura completa en `messages.content` en el insert (L564-577) | FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| interactive-limits.ts: validateList rechaza 20 filas totales (10 secciones x 2) | `npx vitest run src/lib/whatsapp/__tests__/interactive-limits.test.ts` | 17/17 GREEN | PASS |
| domain chokepoint: 360dialog+list retorna error; meta_direct usa metaWhatsappSender | `npx vitest run src/lib/domain/__tests__/messages-interactive-provider.test.ts` | 4/4 GREEN | PASS |
| Regla 6: meta-whatsapp-sender + messages-provider | `npx vitest run src/lib/channels/__tests__/meta-whatsapp-sender.test.ts src/lib/domain/__tests__/messages-provider.test.ts` | 8/8 GREEN | PASS |
| Suite completa de fase | `npx vitest run` (4 test files) | 29/29 GREEN | PASS |
| tsc en archivos de la fase | `npx tsc --noEmit` filtrado por archivos de fase | 0 errores | PASS |

*Nota: el `tsc --noEmit` global reporta 5 errores, todos en archivos ajenos a esta fase (phase 40 RED contracts planificados: `messenger-window.test.ts`, `webhook-handler.test.ts`; pre-existentes: `conversations.test.ts`). Ningun error en los 9 archivos de esta fase.*

---

## Requirements Coverage

| Requirement | Plan | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| D-01 (boton en toolbar inbox) | Plan 04 | Boton compositor en message-input.tsx | SATISFIED | `<InteractiveComposerButton>` en L441 |
| D-02 (gate ventana 24h) | Plan 03/04 | UI gate por ubicacion + server-side re-check | SATISFIED | L292 early-return + L234-245 accion |
| D-03 (provider branch chokepoint) | Plan 02 | meta_direct/360dialog en domain; 360dialog+list error claro | SATISFIED | `messages.ts:500-546` |
| D-04 (JSONB store estructura completa) | Plan 02 | `messages.content` guarda todo para render rico | SATISFIED | `messages.ts:554-577` |
| D-05a (validacion inline limites Meta) | Plan 01/04 | `validateButtons`/`validateList` gatean boton Enviar | SATISFIED | Modal L175-176 + L433 |
| D-05b (preview estilo WhatsApp) | Plan 04 | `InteractiveBubble` en el modal antes de enviar | SATISFIED | Modal L410 |
| D-05c (burbuja saliente rica) | Plan 05 | `case 'interactive'` renderiza real, no placeholder | SATISFIED | `message-bubble.tsx:134-137` |
| D-06 (tests espejando messages-provider) | Plan 01/02 | `messages-interactive-provider.test.ts` + `interactive-limits.test.ts` | SATISFIED | 29/29 GREEN |

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| — | Ninguno detectado | — | — |

Greps aplicados en los 9 archivos de la fase:
- `dangerouslySetInnerHTML` = 0 (XSS gate)
- `return null` / `return {}` / `return []` como stubs = 0 relevantes
- `TODO\|FIXME\|PLACEHOLDER` = 0 en archivos de la fase
- `(Ver en WhatsApp)` placeholder en message-bubble.tsx = 0 (removido)
- `whatsapp_provider` en el cuerpo de `sendInteractiveMessage` de actions/messages.ts = 0 (Regla 3)
- imports en `interactive-limits.ts` = 0 (modulo puro)

---

## Human Verification Required

### 1. Envio de botones (meta_direct o 360dialog)

**Test:** Abrir una conversacion con ventana de 24h abierta → hacer clic en el boton interactivo (icono List) del toolbar → componer un mensaje con 2 botones → hacer clic en Enviar
**Expected:** El mensaje se envia sin error; la burbuja saliente muestra los botones reales con el cuerpo y los titulos correctos (render via `InteractiveBubble`, no el placeholder); el toast de exito aparece
**Why human:** Requiere browser + conversacion WhatsApp con ventana 24h abierta + numero conectado

### 2. Envio de lista (meta_direct)

**Test:** Igual al anterior pero con tipo "Lista" seleccionado, 1 seccion, 3 filas
**Expected:** La burbuja saliente muestra el `buttonLabel` con el icono List; el provider no es visible para el operador
**Why human:** Requiere browser + workspace meta_direct configurado + ventana abierta

### 3. Lista bloqueada en 360dialog (error amistoso)

**Test:** En un workspace con proveedor 360dialog: abrir el compositor, seleccionar tipo Lista, hacer clic en Enviar
**Expected:** El Dialog queda abierto; aparece un toast de error con el mensaje amistoso provider-agnostico (el texto literal `'lista no soportada en 360dialog (legacy)'` del domain se traduce a `'Las listas no estan disponibles en esta conexion de WhatsApp. Usa botones.'` en el cliente)
**Why human:** Requiere browser + workspace 360dialog activo

### 4. Boton oculto con ventana cerrada

**Test:** Abrir una conversacion cuya ventana de 24h esta cerrada (ultimo mensaje del cliente hace >24h)
**Expected:** El toolbar solo muestra `TemplateButton`; el boton `InteractiveComposerButton` (icono List) no aparece
**Why human:** Requiere browser + conversacion con ventana cerrada; la logica es estructural (early-return en message-input.tsx), verificable visualmente

---

## Gaps Summary

No hay gaps que bloqueen el objetivo de la fase. Los 6 must-haves estan verificados. Las 4 verificaciones pendientes son de tipo manual-only (requieren browser, ventana 24h, providers reales) — no son fallos de implementacion sino limitaciones de la verificacion automatizada.

---

*Verified: 2026-06-04T10:40:00Z*
*Verifier: Claude (gsd-verifier)*
