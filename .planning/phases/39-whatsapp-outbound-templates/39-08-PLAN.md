---
phase: 39-whatsapp-outbound-templates
plan: 08
type: execute
wave: 5
depends_on: [05, 06, 07]
files_modified: []
autonomous: false
requirements: [WA-01, WA-02, WA-03, WA-04, WA-06, WA-07, WA-08, WA-09, MIG-01, MIG-03]
user_setup:
  - service: meta-whatsapp-cloud-api
    why: "Live cutover validation on the already-CONNECTED test number"
    dashboard_config:
      - task: "Apply the per-workspace SQL flip to meta_direct (test number only)"
        location: "Supabase SQL Editor (production)"
must_haves:
  truths:
    - "The full suite is green and the complete Meta surface is deployed BEFORE any number is flipped (D-01)"
    - "The test number +57 310 5197782 sends text, media, template, interactive, and read receipts via Cloud API and the recipient receives them identically to 360dialog"
    - "Inbound Meta media is rehosted and a template approval status arrives via webhook push"
    - "Somnio + all 360dialog workspaces remain on 360dialog (Regla 6) — only the test number is flipped"
  artifacts: []
  key_links:
    - from: "operator"
      to: "workspaces.whatsapp_provider (test workspace)"
      via: "manual SQL UPDATE … = 'meta_direct'"
      pattern: "meta_direct"
---

<objective>
The FINAL, gated cutover (D-01). Everything was built first (Plans 01-07). This plan: (1) confirms the full surface is green and deployed, (2) the operator applies the single-workspace SQL flip on the test number, (3) live-smokes every message type on the test number per 39-VALIDATION.md Manual-Only Verifications. No code changes — this is the validation gate D-01 locked.

Purpose: D-01 mandates the complete surface BEFORE the first cutover, and the cutover is a manual SQL flip + live smoke (39-VALIDATION.md), NOT an automated step.
Output: Validated live cutover on the test number; Somnio + clients untouched (Regla 6). Real-client / Somnio flips are DEFERRED (out of scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-VALIDATION.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/38-embedded-signup-wa-inbound/PLAYBOOK-number-activation.md

Test number (from RESEARCH Environment Availability): `+57 310 5197782`, phone_number_id `1134593926408063`, already CONNECTED (Phase 38). Its workspace row has an encrypted BISUAT in `workspace_meta_accounts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — full suite green + deploy</name>
  <files></files>
  <action>
Per 39-VALIDATION.md "Before /gsd-verify-work: full suite must be green" and Regla 1 (push to Vercel before asking the user to test):
1. Run `npx vitest run` (full suite). All Phase 39 suites GREEN; no regression elsewhere. If any Regla-6 parity test (messages-provider) is not green, STOP — do not proceed to cutover.
2. Run `npx tsc --noEmit` — no new errors.
3. Confirm `git status` is clean / all Phase 39 work committed on `main`, then `git push origin main` so the complete Meta surface is deployed to Vercel BEFORE the flip (the surface must exist in prod for the live smoke).
The flip changes ONE workspace's data — the code path must already be live. Do NOT flip anything in this task.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <done>Full suite green; tsc clean; complete Phase 39 surface pushed to `main` (Vercel deploy triggered). Nothing flipped yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cutover SQL flip — test workspace to meta_direct (operator only)</name>
  <action>
This is a manual operator action — Claude does NOT run the SQL (Regla 6: flipping the wrong workspace is a production-impact threat, and the production DB write is operator-only). Claude presents the steps below and waits for the operator to apply them and confirm.
  </action>
  <what-built>The complete provider-aware Meta WhatsApp outbound + templates surface is deployed to prod. The test number is still on `360dialog` (default).</what-built>
  <how-to-verify>
The cutover requires a single production SQL statement that ONLY the operator should run (Regla 6 — flipping the wrong workspace is a production-impact threat).

1. In the production Supabase SQL Editor, first CONFIRM you are targeting the TEST workspace (Pruebas Morfx, the one owning phone_number_id `1134593926408063` / `+57 310 5197782`), NOT Somnio (`a3843b3f-c337-4836-92b5-89c58bb98490`) and NOT any client:
   ```sql
   SELECT w.id, w.name, w.whatsapp_provider, ma.phone_number_id
   FROM workspaces w
   JOIN workspace_meta_accounts ma ON ma.workspace_id = w.id
   WHERE ma.phone_number_id = '1134593926408063';
   ```
   Expected: exactly one row, current `whatsapp_provider='360dialog'`.
2. Apply the flip (test workspace only):
   ```sql
   UPDATE workspaces SET whatsapp_provider = 'meta_direct'
   WHERE id = '<test-workspace-id-from-step-1>';
   ```
3. Re-run the SELECT to confirm `whatsapp_provider='meta_direct'` on the test row, and confirm Somnio + all other workspaces are STILL `360dialog`:
   ```sql
   SELECT id, name, whatsapp_provider FROM workspaces ORDER BY whatsapp_provider;
   ```
   Expected: only the test workspace is `meta_direct`; everyone else `360dialog`.
  </how-to-verify>
  <resume-signal>Reply "flipped" with the SELECT output confirming only the test workspace is meta_direct (and Somnio + clients still 360dialog), or "abort" if anything looks wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live smoke on the test number (all message types + Regla 6 spot-check)</name>
  <action>
Claude has built and deployed the full surface; the operator now exercises it live on the test number. Claude does not automate this (requires a real WhatsApp client + Meta's async review). Claude presents the smoke checklist and waits for the operator's results.
  </action>
  <what-built>The test number is now `meta_direct`. All sends/receipts for it route through Meta Cloud API; everyone else stays on 360dialog.</what-built>
  <how-to-verify>
From the MorfX inbox, on a conversation for the test number `+57 310 5197782`, run the live smoke (39-VALIDATION.md Manual-Only Verifications). For each, confirm the recipient WhatsApp client receives it identically to a 360dialog message:
1. **Text (WA-01):** send a free-text message inside the 24h window → recipient gets it; the inbox shows a wamid + status ticks.
2. **Media (WA-02/06):** send an image (with caption), a document (with filename), an audio, and a sticker → each arrives correctly; caption only on image/document, none on audio/sticker.
3. **Inbound media (WA-06):** have the test phone send an image back → it appears in the inbox rehosted (stays visible after >5 min — not a dead CDN URL).
4. **Template (WA-03):** send an approved template (with a header image + body vars) → arrives rendered.
5. **Interactive (WA-04):** send a reply-buttons message (≤3 buttons) and a list message → buttons/list render; tapping a button produces an inbound reply.
6. **Read receipts (WA-07):** open the conversation → the test phone sees blue ticks on the messages it sent.
7. **Templates CRUD + WA-09:** create a new template from `/configuracion/whatsapp/templates` → it appears PENDING; when Meta reviews it, the status updates via webhook push WITHOUT clicking Resync (WA-09). Confirm the Edit button only appears for APPROVED/REJECTED/PAUSED and warns about the 24h/30d limit (D-05).
8. **24h window (D-04):** confirm a free-text send outside the 24h window is still blocked with "Ventana de 24h cerrada. Usa un template." (inherited, unchanged).
9. **Regla 6 spot-check:** send a normal message on a Somnio (or other 360dialog) conversation → still works exactly as before (no 131047, no behavior change).

If anything fails, capture the inbox error / wamid / observability and report — the rollback is a single `UPDATE workspaces SET whatsapp_provider='360dialog' WHERE id='<test-ws>'`.
  </how-to-verify>
  <resume-signal>Reply "approved" if all message types send/receive correctly on the test number and Somnio/360dialog is unaffected; otherwise describe the failures (with wamid/error) so a gap-closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production DB | the flip is a manual single-workspace data change; wrong target = prod impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10 | DoS / Tampering (wrong-workspace flip) | cutover SQL | mitigate | Two-step checkpoint: confirm the target workspace via phone_number_id SELECT BEFORE the UPDATE; verify Somnio + clients still 360dialog after; single-workspace scope; <10s rollback documented |
| T-39-09 | DoS (prod regression) | Regla 6 | mitigate | Live spot-check on a 360dialog conversation confirms default path unchanged; full suite + parity test green before the flip |
| T-39-01 | Information Disclosure | live smoke | accept | No tokens surfaced in UI/inbox; observability redacts per existing patterns |
</threat_model>

<verification>
- Full vitest suite green + tsc clean BEFORE the flip (Task 1).
- Post-flip SELECT shows ONLY the test workspace as `meta_direct` (Somnio + clients `360dialog`).
- All 9 live-smoke items pass on the test number; Regla 6 spot-check on 360dialog passes.
</verification>

<success_criteria>
The complete Meta WhatsApp outbound + templates surface is validated end-to-end on the test number via a manual, single-workspace cutover (D-01) — text, media (out + in), templates (CRUD + WA-09 push + D-05 edit), interactive, read receipts all work via Cloud API — while Somnio and all 360dialog clients remain byte-identical (Regla 6). Real-client and Somnio cutovers are DEFERRED.
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-08-SUMMARY.md`
</output>
