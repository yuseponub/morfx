---
phase: 44-crm-bots
plan: 09
type: execute
wave: 4
depends_on: [44-01, 44-02, 44-03, 44-04, 44-05, 44-06, 44-07, 44-08]
files_modified:
  - src/__tests__/integration/crm-bots/reader.test.ts
  - src/__tests__/integration/crm-bots/writer-two-step.test.ts
  - src/__tests__/integration/crm-bots/security.test.ts
  - src/__tests__/integration/crm-bots/ttl-cron.test.ts
  - .claude/rules/agent-scope.md
  - docs/analysis/04-estado-actual-plataforma.md
  - .planning/STATE.md
autonomous: false
requirements:
  - "Integration verification of all phase-level must_haves"
  - "agent-scope.md (BLOCKING): new agents MUST be documented in 'Scopes por Agente'"
  - "CLAUDE.md Regla 4: docs/analysis/04-estado-actual-plataforma.md MUST be updated"
  - "BLOCKER 6 (revision 2026-04-18): Vercel env refresh requires redeploy — kill-switch verification step includes redeploy trigger"
  - "WARNING #8 (revision 2026-04-18): cross-plan invariant — 'crm-bot' rate-limit module name consistent across 3 routes"
must_haves:
  truths:
    - "Reader endpoint responds to valid POST with tool-augmented answer"
    - "Writer propose endpoint returns {action_id, preview, expires_at} without mutating business entities"
    - "Writer confirm endpoint idempotent — second call on same action_id returns already_executed"
    - "Forged x-workspace-id in body cannot access another workspace (header-only enforcement)"
    - "CRM_BOT_ENABLED=false returns 503 after Vercel redeploy — env refresh requires deployment cycle (Blocker 6)"
    - "Rate limit triggers 429 at 51st call/min + sends runaway alert (one per 15-min window)"
    - "Approaching-limit alert at >80% usage"
    - "Inngest cron marks proposed actions older than TTL+30s as expired"
    - "agent_observability_turns row written for every reader/writer call with correct agent_id"
    - "Writer refuses createContact with non-existent tagId — returns resource_not_found"
    - "No tool handler writes directly to Supabase — grep-verified (Blocker 1)"
    - "Rate limit module 'crm-bot' called in exactly 3 route files (Warning #8 invariant)"
    - "agent-scope.md documents both new agents with explicit PUEDE/NO PUEDE"
    - "STATE.md updated with phase completion + docs synced"
  artifacts:
    - path: "src/__tests__/integration/crm-bots/"
      provides: "Integration test suite"
      contains: "describe"
    - path: ".claude/rules/agent-scope.md"
      provides: "crm-reader + crm-writer documented"
      contains: "crm-reader"
    - path: "docs/analysis/04-estado-actual-plataforma.md"
      provides: "Section documenting CRM Bots module status"
      contains: "CRM Bots"
    - path: ".planning/STATE.md"
      provides: "Phase 44 entry marked COMPLETE"
      contains: "Phase 44"
  key_links:
    - from: "integration tests"
      to: "live endpoints + DB + cron"
      via: "HTTP POST + Supabase admin queries"
      pattern: "/api/v1/crm-bots"
    - from: "agent-scope.md"
      to: "future agent reviewers"
      via: "documentation"
      pattern: "crm-reader"
---

<objective>
Verify the phase end-to-end via integration tests, update the mandatory documentation (agent-scope.md + docs/analysis/04-estado-actual-plataforma.md + STATE.md), and confirm all phase must_haves are demonstrably satisfied.

**Revision 2026-04-18:**
- Blocker 6: Kill-switch verification previously assumed env changes propagate to warm lambdas in ~10s. Vercel doesn't work that way — warm lambdas keep their env snapshot until redeploy or lambda expiry. Procedure now includes an explicit redeploy step.
- Warning #8: Added a cross-plan grep invariant — `rateLimiter.check(..., 'crm-bot')` must appear in exactly 3 route files (reader, propose, confirm).

Purpose: Phase ships only when:
1. All must_haves verified by automated tests (or explicitly deferred with documented rationale)
2. agent-scope.md lists both new agents
3. Platform state doc reflects the new capability (Regla 4)

This plan is `autonomous: false` because production verification requires a human-verified kill-switch redeploy cycle and a live API-key mint.

Dependencies: All prior Phase 44 plans (Plans 01-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-crm-bots/44-CONTEXT.md
@.planning/phases/44-crm-bots/44-RESEARCH.md
@.planning/phases/44-crm-bots/44-PATTERNS.md
@.planning/phases/44-crm-bots/44-01-SUMMARY.md
@.planning/phases/44-crm-bots/44-02-SUMMARY.md
@.planning/phases/44-crm-bots/44-03-SUMMARY.md
@.planning/phases/44-crm-bots/44-04-SUMMARY.md
@.planning/phases/44-crm-bots/44-05-SUMMARY.md
@.planning/phases/44-crm-bots/44-06-SUMMARY.md
@.planning/phases/44-crm-bots/44-07-SUMMARY.md
@.planning/phases/44-crm-bots/44-08-SUMMARY.md
@.claude/rules/agent-scope.md
@CLAUDE.md
@docs/analysis/04-estado-actual-plataforma.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write reader integration tests</name>
  <files>src/__tests__/integration/crm-bots/reader.test.ts</files>
  <read_first>
    - package.json (confirm vitest is the test runner)
    - .planning/phases/44-crm-bots/44-07-SUMMARY.md
    - src/lib/auth/api-key.ts (for test API-key minting)
  </read_first>
  <action>
Create directory `src/__tests__/integration/crm-bots/`.

Create `src/__tests__/integration/crm-bots/reader.test.ts` with the following vitest scenarios:

- beforeAll: verify TEST_WORKSPACE_ID + TEST_API_KEY env vars are set.
- describe 'kill-switch' (local process.env — NOT production validation, which is Task 5 step 8): set CRM_BOT_ENABLED=false in beforeEach → expect 503 with code=KILL_SWITCH.
- describe 'happy path': POST with a valid search query → 200 with agentId='crm-reader'.
- describe 'auth': missing authorization header → 401.
- describe 'observability': after a successful call, SELECT from agent_observability_turns filtering workspace_id + agent_id=crm-reader + trigger_kind=api → expect >= 1 row.

Base URL defaults to `http://localhost:3020` (dev port from CLAUDE.md). Callers can override via TEST_BASE_URL to hit a Vercel preview.

Test-timeout on async LLM calls: 30 seconds.
  </action>
  <verify>
    <automated>test -f src/__tests__/integration/crm-bots/reader.test.ts && grep -c "describe" src/__tests__/integration/crm-bots/reader.test.ts && grep -c "KILL_SWITCH" src/__tests__/integration/crm-bots/reader.test.ts && grep -c "agent_observability_turns" src/__tests__/integration/crm-bots/reader.test.ts && grep -c "crm-reader" src/__tests__/integration/crm-bots/reader.test.ts</automated>
  </verify>
  <done>
    File exists with describe blocks covering kill-switch, happy path, auth, observability. Tests may fail locally without env vars — expected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write writer two-step + security + TTL integration tests</name>
  <files>src/__tests__/integration/crm-bots/writer-two-step.test.ts, src/__tests__/integration/crm-bots/security.test.ts, src/__tests__/integration/crm-bots/ttl-cron.test.ts</files>
  <read_first>
    - src/__tests__/integration/crm-bots/reader.test.ts (Task 1 — patterns to mirror)
    - src/lib/agents/crm-writer/two-step.ts (Plan 05 — idempotency semantics)
    - src/inngest/functions/crm-bot-expire-proposals.ts (Plan 06 — TTL mechanics)
  </read_first>
  <action>
**File 1 — `writer-two-step.test.ts`:**

Test scenarios:
1. Happy path: propose createContact → capture action_id → confirm → expect status=executed + business entity persisted.
2. Idempotency (Pitfall 3): confirm twice serially → first returns executed, second returns already_executed with same output. Verify contact count increased by exactly 1.
3. Concurrent double-confirm: run 2 confirms in parallel → exactly one executed, one already_executed. Only one entity created.
4. Resource not found: propose createContact with a fake tagId → proposedActions empty + text mentions resource_not_found. Verify no row in crm_bot_actions for createContact (propose rejected before DB insert).
5. Workspace scope bind (Pitfall 4): confirm with an action_id from a different workspace → not_found. Verify legit workspace's action was NOT touched.

**File 2 — `security.test.ts`:**

1. Forged body workspace_id: propose with `{workspaceId:'attacker-uuid', messages:[...]}` → operates on TEST_WORKSPACE_ID (from API key).
2. Rate limit 429: fire 51 requests → 51st returns 429 with code=RATE_LIMITED + Retry-After header. Email verification deferred to Task 5 (human-verify).
3. Approaching-limit at >80%: fire 41 requests → 41st triggers maybeSendApproachingLimitAlert (verifiable via log inspection or manual Task 5 check).
4. Kill-switch takes effect per-request (local): set CRM_BOT_ENABLED=true → 200. Set false → 503 on the NEXT request. Production Vercel behaviour: see Task 5 step 8 Blocker 6 procedure.

**File 3 — `ttl-cron.test.ts`:**

1. Cron marks expired past TTL+30s: propose → manually UPDATE `expires_at = now() - interval '2 minutes'` → trigger cron (Inngest dev event fire or direct UPDATE simulation) → wait ~3s → fetch row → expect status=expired.
2. Confirm past TTL returns expired: propose → force expires_at to past → confirm → expect status=expired. Verify NO business entity created.
3. 30s grace window: propose → set expires_at = now() - 10s (inside grace) → cron run → row still status=proposed. Then set expires_at = now() - 60s → cron run → row status=expired.

If Inngest dev mode not available in CI, use `it.skip(...)` and flag for manual verification in Task 5.
  </action>
  <verify>
    <automated>test -f src/__tests__/integration/crm-bots/writer-two-step.test.ts && test -f src/__tests__/integration/crm-bots/security.test.ts && test -f src/__tests__/integration/crm-bots/ttl-cron.test.ts && grep -c "already_executed" src/__tests__/integration/crm-bots/writer-two-step.test.ts && grep -c "attacker\|forged\|workspaceId.*body" src/__tests__/integration/crm-bots/security.test.ts && grep -c "RATE_LIMITED\|KILL_SWITCH" src/__tests__/integration/crm-bots/security.test.ts && grep -c "expired\|grace" src/__tests__/integration/crm-bots/ttl-cron.test.ts</automated>
  </verify>
  <done>
    All 3 test files exist. Scenarios cover idempotency, cross-workspace escape, rate limit, kill-switch, TTL + grace period.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update agent-scope.md (MANDATORY)</name>
  <files>.claude/rules/agent-scope.md</files>
  <read_first>
    - .claude/rules/agent-scope.md (FULL file)
    - src/lib/agents/crm-reader/system-prompt.ts (Plan 04)
    - src/lib/agents/crm-writer/system-prompt.ts (Plan 05)
  </read_first>
  <action>
Append to the "Scopes por Agente" section two new subsections:

1. **CRM Reader Bot** — document PUEDE (leer contactos/pedidos/pipelines/tags), NO PUEDE (crear/modificar/archivar, enviar WA, cruzar workspaces), Validacion (tool registry delegates to `@/lib/domain/*`, zero createAdminClient in tool files, system prompt includes not_found_in_workspace).

2. **CRM Writer Bot** — document PUEDE (proponer create/update/archive de contactos/pedidos/notas/tareas, move orders, completeTask — all via propose→confirm), NO PUEDE (crear tags/pipelines/stages/templates/users, DELETE real, WA, robots, direct mutation). Validacion (tools call only proposeAction; existence checks via domain getByIds, not createAdminClient; two-step.ts is the ONLY file using createAdminClient and only on crm_bot_actions; optimistic UPDATE for idempotency; resource_not_found with 9 entity types covering base + mutable).

Add a note at end of "OBLIGATORIO al Crear un Agente Nuevo":
```markdown
**Nota Phase 44:** Los agentes `crm-reader` y `crm-writer` cumplen con todos los 4 requisitos:
1. Scope definido arriba.
2. System prompts con PUEDE/NO PUEDE + no-inventar-recursos.
3. Tool definitions aisladas por carpeta. NINGUN tool importa createAdminClient directamente — todos pasan por domain layer (Regla 3 enforcement at tool layer).
4. Code review: grep-verified ausencia de mutaciones directas + ausencia de raw Supabase en tool handlers.
```

DO NOT modify the general principle section or existing entries.
  </action>
  <verify>
    <automated>grep -c "CRM Reader Bot" .claude/rules/agent-scope.md && grep -c "CRM Writer Bot" .claude/rules/agent-scope.md && grep -c "crm-reader" .claude/rules/agent-scope.md && grep -c "crm-writer" .claude/rules/agent-scope.md && grep -c "two-step\|propose.*confirm" .claude/rules/agent-scope.md && grep -c "resource_not_found" .claude/rules/agent-scope.md</automated>
  </verify>
  <done>
    Both new subsections present. Two-step flow and resource_not_found documented.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run cross-plan invariant checks (Warning #8 + Blocker 1 verification)</name>
  <files>.planning/phases/44-crm-bots/44-09-INVARIANTS.md</files>
  <read_first>
    - src/app/api/v1/crm-bots/reader/route.ts (Plan 07)
    - src/app/api/v1/crm-bots/writer/propose/route.ts (Plan 08)
    - src/app/api/v1/crm-bots/writer/confirm/route.ts (Plan 08)
    - src/lib/agents/crm-reader/tools/ (Plan 04)
    - src/lib/agents/crm-writer/tools/ (Plan 05)
  </read_first>
  <action>
Run the following grep invariants and record results in `.planning/phases/44-crm-bots/44-09-INVARIANTS.md`:

**Warning #8 — Rate limiter module consistency (MUST return exactly 3):**
```bash
grep -rEc "rateLimiter\.check\([^,]+,\s*'crm-bot'" src/app/api/v1/crm-bots/ | awk -F: '{sum+=$2} END {print sum}'
```
Expected output: `3` (reader, propose, confirm). Any other value indicates a gate was forgotten or duplicated.

**Blocker 1 — No raw Supabase in agent tool files:**
```bash
grep -rEn "createAdminClient|@supabase/supabase-js|@/lib/supabase/admin" src/lib/agents/crm-reader/tools/ src/lib/agents/crm-writer/tools/
```
Expected output: empty (zero matches).

**Blocker 1 — No direct domain write invocations in writer tools:**
```bash
grep -rEn "createContact\s*\(|updateContact\s*\(|archiveContact\s*\(|createOrder\s*\(|updateOrder\s*\(|archiveOrder\s*\(|moveOrderToStage\s*\(|createNote\s*\(|updateNote\s*\(|archiveNote\s*\(|archiveOrderNote\s*\(|createTask\s*\(|updateTask\s*\(" src/lib/agents/crm-writer/tools/ | grep -vE "^[^:]*:[^:]*//"
```
Expected output: empty.

**Blocker 4 — ResourceType union covers all 9 entity types:**
```bash
grep -E "'tag'\|'pipeline'\|'stage'\|'template'\|'user'\|'contact'\|'order'\|'note'\|'task'" src/lib/agents/crm-writer/types.ts
```
Or simpler — check each individually:
```bash
for t in tag pipeline stage template user contact order note task; do
  grep -q "'$t'" src/lib/agents/crm-writer/types.ts && echo "$t: found" || echo "$t: MISSING"
done
```
Expected: 9 'found' lines.

Record outputs in a markdown table in INVARIANTS.md. Any deviation blocks phase completion.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-crm-bots/44-09-INVARIANTS.md && grep -c "Warning #8" .planning/phases/44-crm-bots/44-09-INVARIANTS.md && grep -c "Blocker 1" .planning/phases/44-crm-bots/44-09-INVARIANTS.md && grep -c "Blocker 4" .planning/phases/44-crm-bots/44-09-INVARIANTS.md</automated>
  </verify>
  <done>
    INVARIANTS.md exists with all three grep results documented. Any failure triggers a back-to-earlier-plan fix before proceeding.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update docs/analysis/04-estado-actual-plataforma.md + STATE.md</name>
  <files>docs/analysis/04-estado-actual-plataforma.md, .planning/STATE.md</files>
  <read_first>
    - docs/analysis/04-estado-actual-plataforma.md (full file)
    - .planning/STATE.md (current position + v5.0 phase table)
    - CLAUDE.md Regla 4
  </read_first>
  <action>
**Part A — docs/analysis/04-estado-actual-plataforma.md:**

Add a new section "CRM Bots (Phase 44)" with:
- Status: SHIPPED 2026-04-XX
- Purpose: Two internal AI agents exposed as API for agent-to-agent callers — crm-reader (read-only), crm-writer (two-step write).
- Endpoints: POST /api/v1/crm-bots/reader, POST /api/v1/crm-bots/writer/propose, POST /api/v1/crm-bots/writer/confirm.
- Auth: API key per workspace via Authorization: Bearer mfx_... + middleware-injected x-workspace-id.
- Rate limit: 50 calls/min per workspace (shared between reader + writer). Kill-switch via CRM_BOT_ENABLED=false (requires Vercel redeploy to take effect). Email alerts to joseromerorincon041100@gmail.com on runaway + approaching >80%, dedupe 15 min.
- Isolation: reader and writer in separate folders with separate tool registries.
- Observability: every call generates agent_observability_turns row with trigger_kind='api'. Writer actions additionally persisted in crm_bot_actions with propose→executed/expired/failed lifecycle.
- Deuda tecnica: in-memory rate limiter (Pitfall 1 accepted); no daily cap (only runaway detection); no human UI (V2); kill-switch requires Vercel redeploy (Blocker 6 acknowledged).
- Referencias: `.planning/phases/44-crm-bots/`.

**Part B — .planning/STATE.md:**

1. Update the Phase 44 row in the v5.0 phase table:
```markdown
| 44 | CRM Bots (Read + Write) | CRM-BOT-01→09 | COMPLETE (9/9 plans) |
```

2. Add a "Previous activity" entry at the top of "Current Position" describing Phase 44 completion (verbose — mirror the existing pattern). Include: 2 migrations applied (crm_bot_actions + archive columns in 4 tables), 14+ new domain exports, 19 agent files total (reader 9 + writer 10), 1 cron, 3 HTTP routes, 4 integration test files, 3 doc updates. Key patterns: two-step propose/confirm as security primitive, tool-layer Regla 3 enforcement (no createAdminClient in tool files), email alerts via Resend with env-parameterized FROM (Blocker 5 fix), kill-switch per-request env read (Pitfall 2) with Vercel redeploy caveat documented (Blocker 6).

3. Update the progress bar + YAML frontmatter counters if applicable.
  </action>
  <verify>
    <automated>grep -c "CRM Bots" docs/analysis/04-estado-actual-plataforma.md && grep -c "/api/v1/crm-bots" docs/analysis/04-estado-actual-plataforma.md && grep -c "Phase 44" .planning/STATE.md</automated>
  </verify>
  <done>
    docs section added. STATE.md updated. YAML counters updated if applicable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 [BLOCKING]: End-to-end verification + kill-switch redeploy test in production</name>
  <what-built>
    Full Phase 44 stack: migrations in prod (Plan 01 Task 5 + Plan 03 Task 1 already applied), reader + writer agents, HTTP routes, Inngest cron, integration tests, documentation.
  </what-built>
  <how-to-verify>
    1. Push all remaining commits to Vercel (git push origin main). Wait for deployment to complete.
    2. In Vercel dashboard, set env vars (if not already):
       - `CRM_BOT_ENABLED=true`
       - `CRM_BOT_RATE_LIMIT_PER_MIN=50`
       - `RESEND_API_KEY=<your-resend-key>` (create at resend.com if needed)
       - `CRM_BOT_ALERT_FROM=<verified domain or leave blank for sandbox>`
    3. Trigger a redeploy after setting env vars: `vercel redeploy <project-name> --prod` OR push an empty commit. Wait for deployment to complete before testing.
    4. Create a test API key via the existing API keys UI (or SQL insert into `api_keys` table). Record the full key `mfx_...`.
    5. Manual reader smoke test:
       ```bash
       curl -X POST https://morfx.app/api/v1/crm-bots/reader \
         -H "Authorization: Bearer mfx_YOUR_KEY" \
         -H "Content-Type: application/json" \
         -d '{"messages":[{"role":"user","content":"listar pipelines"}]}'
       ```
       Expected: 200 with `{status:'ok', output:{text:"...", toolCalls:[...], agentId:'crm-reader'}}`.
    6. Manual writer propose test:
       ```bash
       curl -X POST https://morfx.app/api/v1/crm-bots/writer/propose \
         -H "Authorization: Bearer mfx_YOUR_KEY" \
         -H "Content-Type: application/json" \
         -d '{"messages":[{"role":"user","content":"crea contacto llamado Test44 con telefono +573001234567"}]}'
       ```
       Expected: 200 with `proposedActions[0].action_id` (UUID) + `preview.action='create'`.
    7. Manual writer confirm test:
       ```bash
       curl -X POST https://morfx.app/api/v1/crm-bots/writer/confirm \
         -H "Authorization: Bearer mfx_YOUR_KEY" \
         -H "Content-Type: application/json" \
         -d "{\"actionId\":\"<action_id-from-step-6>\"}"
       ```
       Expected: 200 with `result.status='executed'` + `result.output.contactId`.

    8. **Kill-switch verification (Blocker 6 procedure):**
       a. Set `CRM_BOT_ENABLED=false` in Vercel dashboard.
       b. **Trigger redeploy** — Vercel does NOT refresh env into warm lambdas automatically. Options:
          - `vercel redeploy <project-name> --prod`, OR
          - Push an empty commit: `git commit --allow-empty -m "chore: env refresh for kill-switch test" && git push origin main`
       c. Wait for deploy to complete (check Vercel dashboard — typically 30-90s).
       d. Retry reader curl from step 5. Expected: HTTP 503 with body `{"error":"CRM bots globally disabled","code":"KILL_SWITCH","retryable":false}`.
       e. Restore: set `CRM_BOT_ENABLED=true` in Vercel → redeploy again → verify 200 returns.
       f. **Document the redeploy time taken** — future runbook uses this as operational ceiling for emergency shutdown ("kill-switch takes ~N seconds end-to-end including redeploy").

    9. Idempotency check: Re-run the same confirm command from step 7. Expected: `result.status='already_executed'` with the SAME output.contactId.
    10. Cleanup: Archive the test contact via the CRM UI or via another writer propose/confirm archiveContact call.
    11. **OPTIONAL — Rate-limit + email smoke** (requires RESEND_API_KEY + verified domain or sandbox): fire `for i in $(seq 1 55); do curl ... reader ... & done`. Check inbox for `joseromerorincon041100@gmail.com` runaway alert email. Verify only 1 alert arrives even if 10+ calls hit 429 (15-min dedupe).
    12. Verify observability:
        ```sql
        SELECT COUNT(*) FROM agent_observability_turns
        WHERE agent_id IN ('crm-reader', 'crm-writer')
          AND started_at > NOW() - INTERVAL '10 minutes';
        ```
        Expect N > 0 matching the number of curl calls.
    13. Verify crm_bot_actions:
        ```sql
        SELECT status, COUNT(*) FROM crm_bot_actions
        WHERE workspace_id = 'YOUR_TEST_WORKSPACE'
        GROUP BY status;
        ```
        Expect at least 1 'executed' row (from steps 6-7).
    14. Confirm Plan 09 complete with the following summary:
        - Reader 200 OK: yes/no
        - Propose 200 OK + action_id returned: yes/no
        - Confirm 200 OK + executed: yes/no
        - Double confirm returns already_executed: yes/no
        - Kill-switch 503 after redeploy: yes/no + elapsed time from env flip to 503 observed
        - Observability turns present: yes/no
        - (Optional) Runaway alert email received: yes/no
  </how-to-verify>
  <why-blocking>
    Phase cannot ship without end-to-end verification in production. Regla 1 (push to Vercel before asking user to test). Integration tests in Tasks 1-2 validate code-level correctness; only a live production run validates Vercel deployment + env vars + Resend + Supabase integration + the Blocker 6 redeploy semantics for the kill-switch.
  </why-blocking>
  <resume-signal>
    User reports outcomes of steps 5-14 (yes/no on each gate). Agent then updates SUMMARY, creates LEARNINGS.md, marks phase COMPLETE.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 7 (post-checkpoint): Create LEARNINGS.md and finalize SUMMARY</name>
  <files>.planning/phases/44-crm-bots/LEARNINGS.md, .planning/phases/44-crm-bots/44-09-SUMMARY.md</files>
  <read_first>
    - All 44-0N-SUMMARY.md files (Plans 01-08)
    - CLAUDE.md Regla 0
    - .planning/RETROSPECTIVE.md if exists
  </read_first>
  <action>
Create `.planning/phases/44-crm-bots/LEARNINGS.md`. Sections:

- Patterns Established:
  1. Two-step propose/confirm as security primitive (optimistic UPDATE `.eq('status','proposed')`).
  2. Physical isolation via folder separation + grep-verified import prohibition.
  3. Tool-layer Regla 3 enforcement (zero createAdminClient in tool files; all data access via domain).
  4. Resend + in-memory dedupe as email infra MVP with env-parameterized FROM.
  5. Inngest cron with grace period (30s past TTL).

- Pitfalls Mitigated (8 from RESEARCH):
  1. In-memory rate limiter cold-start drift — accepted.
  2. process.env caching — per-request read.
  3. Double-confirm race — optimistic UPDATE.
  4. Forged x-workspace-id in body — header-only.
  5. Cross-workspace silent leak — discriminated `not_found_in_workspace`.
  6. AI SDK v6 tool input mismatch — Zod + stepCountIs(5).
  7. TTL race with confirm — 30s grace in cron.
  8. Email alert storm — in-memory dedupe 15 min.

- Revision Blockers Addressed (2026-04-18):
  1. Blocker 1: tools use domain layer exclusively; zero createAdminClient in tool files.
  2. Blocker 2: Plan 03 Case C decision baked in (migration adds archive columns).
  3. Blocker 3: Concrete SQL inlined for archiveContact/archiveOrder/archiveNote.
  4. Blocker 4: ResourceType union covers 9 entity types (base + mutable).
  5. Blocker 5: FROM address env-parameterized (CRM_BOT_ALERT_FROM).
  6. Blocker 6: Kill-switch verification procedure includes Vercel redeploy step.

- Subtle Decisions:
  - Single `'crm-bot'` ToolModule shared between reader + writer.
  - Confirm endpoint does NOT invoke the LLM — direct dispatch (security boundary).
  - Reader calls NOT persisted in crm_bot_actions (only writer actions).
  - `conversationId: actionId` in confirm observability — correlation without new schema.
  - Kill-switch in production requires redeploy (not hot-reload) — documented in runbook.

- Cost Patterns:
  - Sonnet 4.5 temperature 0 for reader.
  - Sonnet 4.5 temperature 0.2 for writer propose.
  - stepCountIs(5) caps tool loops.

- Recommendations for Future Planners:
  - Two-step flow applies to ANY mutation-prone LLM feature.
  - agent-scope.md update is mandatory — don't skip.
  - Integration tests against LIVE Vercel preview are the only way to verify cold-start rate-limiter behavior.
  - Never assume Vercel env changes propagate to warm lambdas — always redeploy when validating env-gated behavior.

Then create `.planning/phases/44-crm-bots/44-09-SUMMARY.md` with:
- Links to Task 1-6 results
- Checklist of all must_haves (phase-level) marked verified/deferred
- Final commit SHA list
- Push-to-Vercel confirmation timestamp
  </action>
  <verify>
    <automated>test -f .planning/phases/44-crm-bots/LEARNINGS.md && test -f .planning/phases/44-crm-bots/44-09-SUMMARY.md && grep -c "Pitfalls Mitigated" .planning/phases/44-crm-bots/LEARNINGS.md && grep -c "Patterns Established" .planning/phases/44-crm-bots/LEARNINGS.md && grep -c "Revision Blockers" .planning/phases/44-crm-bots/LEARNINGS.md</automated>
  </verify>
  <done>
    LEARNINGS.md + 44-09-SUMMARY.md exist with required sections. Phase marked COMPLETE.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration tests → production DB | Tests require TEST_WORKSPACE_ID + TEST_API_KEY; must not delete prod data |
| human verification → production | Manual curl calls; test contact created then archived |
| kill-switch redeploy | Vercel env change requires deployment to take effect (Blocker 6) |
| docs update | Non-executable; no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09-01 | Tampering | integration tests mutate prod data | mitigate | Dedicated TEST_WORKSPACE_ID. Tests create "Test44"-prefixed entities for cleanup. |
| T-44-09-02 | Information Disclosure | TEST_API_KEY leaked in CI logs | mitigate | Stored in CI secrets, never logged. |
| T-44-09-03 | Denial of Service | rate-limit test floods production | accept | 51 requests in 60s within normal prod capacity. On-demand only. |
| T-44-09-04 | Spoofing | agent-scope.md documentation lag | mitigate | Task 3 is MANDATORY per the file's own rule. Verified by grep. |
| T-44-09-05 | Repudiation | phase ships without user verification | mitigate | Task 6 is autonomous:false blocking checkpoint. |
| T-44-09-06 | Tampering | kill-switch flipped without redeploy (stale warm lambda) | mitigate | Blocker 6 procedure documented in Task 6 step 8. Operational runbook includes explicit redeploy step. |

ASVS Level 1. No threats at high severity remain.
</threat_model>

<verification>
Phase-level: all must_haves satisfied — verified by integration tests (Tasks 1-2), cross-plan grep invariants (Task 4), and manual production verification (Task 6).
</verification>

<success_criteria>
- 4 integration test files exist.
- `.claude/rules/agent-scope.md` documents crm-reader + crm-writer.
- `docs/analysis/04-estado-actual-plataforma.md` has CRM Bots section.
- `.planning/STATE.md` marks Phase 44 COMPLETE.
- Task 4 INVARIANTS.md shows: 3 rateLimiter.check('crm-bot') sites, 0 createAdminClient in tool dirs, 0 direct domain-write invocations in writer tools, 9 entity types in ResourceType union.
- Task 6 checkpoint confirms kill-switch works after redeploy (Blocker 6).
- LEARNINGS.md + 44-09-SUMMARY.md created.
</success_criteria>

<output>
Final phase artifacts:
- 4 integration test files
- 1 INVARIANTS.md (cross-plan grep checks)
- 3 documentation updates (agent-scope.md, 04-estado-actual-plataforma.md, STATE.md)
- LEARNINGS.md + 44-09-SUMMARY.md
- Commits: Tasks 1-5 each 1 commit. Task 6 is checkpoint (no commit). Task 7 produces final commit with LEARNINGS + SUMMARY.
- Push to Vercel: push Plan 09 code (Tasks 1-5) → wait for Task 6 confirmation (including Blocker 6 redeploy test) → push Task 7 final docs.
</output>
