---
phase: 37-meta-app-setup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-meta-app-setup-foundation/META-SETUP-GUIDE.md
  - supabase/migrations/20260401100000_create_workspace_meta_accounts.sql
autonomous: false

must_haves:
  truths:
    - "User has a step-by-step guide with exact Meta dashboard actions, permissions to request, and env vars to configure"
    - "Migration SQL file exists and is ready to apply in production"
    - "workspace_meta_accounts table has encrypted token column, unique constraints on phone_number_id/page_id/ig_account_id, and RLS read-only policy"
  artifacts:
    - path: ".planning/phases/37-meta-app-setup-foundation/META-SETUP-GUIDE.md"
      provides: "Complete SETUP-04 guide for Meta App configuration"
      min_lines: 80
    - path: "supabase/migrations/20260401100000_create_workspace_meta_accounts.sql"
      provides: "Database migration for workspace_meta_accounts table"
      min_lines: 30
  key_links:
    - from: "META-SETUP-GUIDE.md"
      to: "Vercel env vars"
      via: "Lists exact env var names matching code expectations"
      pattern: "META_APP_ID|META_APP_SECRET|META_WEBHOOK_VERIFY_TOKEN|META_TOKEN_ENCRYPTION_KEY"
---

<objective>
Create the SETUP-04 step-by-step guide for Meta App configuration and the database migration for workspace_meta_accounts.

Purpose: The guide must be delivered FIRST (before any code) so the user can start the Meta App creation and App Review process (2-7 business days). The migration creates the credential storage table that all subsequent phases depend on.

Output: META-SETUP-GUIDE.md + migration SQL file, both ready for the user to act on.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-meta-app-setup-foundation/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create META-SETUP-GUIDE.md (SETUP-04) and migration SQL</name>
  <files>
    .planning/phases/37-meta-app-setup-foundation/META-SETUP-GUIDE.md
    supabase/migrations/20260401100000_create_workspace_meta_accounts.sql
  </files>
  <action>
    Create two files:

    **META-SETUP-GUIDE.md** — Complete step-by-step guide based on RESEARCH.md "Meta App Configuration Steps" section. Must include:
    1. Prerequisites (Meta Business Portfolio, Business Verification, 2FA)
    2. Step 1: Create Meta App (type=Business, name=MorfX, link to verified business)
    3. Step 2: Enable products (WhatsApp, Messenger, Instagram)
    4. Step 3: Configure WhatsApp (webhook URL = `https://morfx.app/api/webhooks/meta`, verify token, subscribe to `messages` + `message_template_status_update`)
    5. Step 4: App Settings (copy App ID + App Secret, set domains, privacy policy URL, terms URL)
    6. Step 5: Submit App Review — request Advanced Access for: `whatsapp_business_messaging`, `whatsapp_business_management`, `pages_messaging`, `instagram_manage_messages`, `business_management`. Include video walkthrough tip: show business-facing dashboard, NOT consumer chat.
    7. Step 6: Generate encryption key — `openssl rand -base64 32`
    8. Step 7: Vercel environment variables table:
       - META_APP_ID (from step 4)
       - META_APP_SECRET (from step 4)
       - META_WEBHOOK_VERIFY_TOKEN (any random string)
       - META_TOKEN_ENCRYPTION_KEY (from step 6)
       - META_CONFIG_ID (leave empty, needed in Phase 38)
    9. Tech Provider enrollment steps (business.facebook.com > Settings > Business Assets)
    10. Troubleshooting: App Review rejection (check App Requests tab), Business Verification delays

    **Migration SQL** — Based on RESEARCH.md "Database Migration" section. Must include:
    - workspace_meta_accounts table with all columns from research (id, workspace_id, waba_id, phone_number_id, phone_number, page_id, ig_account_id, business_id, access_token_encrypted, channel CHECK, provider DEFAULT 'meta_direct', is_active, connected_at, account_name, quality_rating, messaging_limit, created_at, updated_at)
    - Global unique constraints: uq_meta_phone, uq_meta_page, uq_meta_ig
    - Partial unique index: idx_meta_accounts_active_per_ws (workspace_id, channel WHERE is_active = true)
    - Webhook routing indexes: phone_number_id, page_id, ig_account_id (partial WHERE NOT NULL)
    - Workspace lookup index
    - RLS enabled with SELECT-only policy for workspace_members
    - updated_at trigger using existing update_updated_at_column() function
    - Use timezone('America/Bogota', NOW()) for all timestamps (Regla 2)
  </action>
  <verify>
    - META-SETUP-GUIDE.md exists with all 10 sections
    - Migration SQL is valid (check for syntax errors by reading through it)
    - Migration references only existing functions (update_updated_at_column, gen_random_uuid, auth.uid)
    - All env var names in guide match what the code will expect (META_APP_ID, META_APP_SECRET, META_WEBHOOK_VERIFY_TOKEN, META_TOKEN_ENCRYPTION_KEY)
  </verify>
  <done>Guide has all steps from prerequisites through troubleshooting. Migration creates workspace_meta_accounts with encryption column, unique constraints, RLS, and indexes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. META-SETUP-GUIDE.md — complete step-by-step guide for Meta App configuration
    2. Database migration SQL for workspace_meta_accounts table
  </what-built>
  <how-to-verify>
    1. Review META-SETUP-GUIDE.md — does it cover everything you need to set up the Meta App?
    2. Apply the migration in Supabase production SQL editor:
       - Open supabase/migrations/20260401100000_create_workspace_meta_accounts.sql
       - Run it in Supabase SQL Editor
    3. Verify table created: `SELECT * FROM workspace_meta_accounts LIMIT 1;` (should return empty, no error)
    4. Start following the META-SETUP-GUIDE.md steps (create Meta App, enable products, submit App Review)
    5. Set the Vercel environment variables listed in Step 7 of the guide
    6. Confirm: "migration applied" and "env vars set" (or report issues)
  </how-to-verify>
  <resume-signal>Type "migration applied" when the migration is running in production and env vars are configured. Report any issues with the guide.</resume-signal>
</task>

</tasks>

<verification>
- META-SETUP-GUIDE.md covers all SETUP-04 requirements (prerequisites, app creation, products, webhook config, app review, encryption key, env vars, tech provider)
- Migration SQL creates table with correct schema matching RESEARCH.md specification
- Env var names are consistent between guide and what code will reference
</verification>

<success_criteria>
- SETUP-04 guide delivered to user before any code is written
- Migration file ready for production application
- User confirms migration applied and env vars configured
</success_criteria>

<output>
After completion, create `.planning/phases/37-meta-app-setup-foundation/37-01-SUMMARY.md`
</output>
