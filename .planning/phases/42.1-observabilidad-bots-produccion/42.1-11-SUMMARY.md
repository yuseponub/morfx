---
phase: 42.1-observabilidad-bots-produccion
plan: 11
subsystem: observability-activation
tags:
  - activation
  - runbook
  - smoke-tests
  - feature-flag
  - regla-6
  - production-safety
status: PARTIAL — awaiting activation checkpoint
requires:
  - 42.1-01 (schema applied in production)
  - 42.1-02 through 42.1-08 (instrumented call sites + flush)
  - 42.1-09 (super-user gating + master pane)
  - 42.1-10 (turn detail pane)
provides:
  - Verified Anthropic pricing table (date-stamped 2026-04-07, HIGH confidence)
  - AsyncLocalStorage smoke test PASS (7/7 sub-checks)
  - Activation runbook with rollback < 2 min
  - Deferred smoke tests 2 & 3 folded into runbook Step 3 (executed by user during checkpoint)
affects:
  - Production observability go-live (post-checkpoint)
  - docs/analysis/04-estado-actual-plataforma.md (Task 4 — post-activation)
  - LEARNINGS.md (Task 4 — post-activation)
tech-stack:
  added: []
  patterns:
    - "Pre-deploy validation via local tsx smoke script (delete after run)"
    - "Defer integration smoke tests into the human-verify checkpoint when local schema unavailable"
    - "Activation runbook with explicit env var ordering (SUPER_USER_EMAIL before OBSERVABILITY_ENABLED)"
key-files:
  created:
    - .planning/phases/42.1-observabilidad-bots-produccion/smoke-tests.md
    - .planning/phases/42.1-observabilidad-bots-produccion/activation-runbook.md
  modified:
    - src/lib/observability/pricing.ts
duration: PARTIAL
completed: PARTIAL
---

# Phase 42.1 Plan 11: Activation (PARCIAL — awaiting activation checkpoint)

**One-liner:** Pre-flight verification (Anthropic pricing + AsyncLocalStorage smoke test) + activation runbook + push to Vercel with flag OFF — production go-live blocked on user-driven checkpoint per Regla 6.

> **STATUS:** This summary covers Tasks 1, 2 and the deploy push only. Task 3 is a `checkpoint:human-verify` blocking gate that the user executes manually (Vercel env vars + production validation). Task 4 (docs + LEARNINGS) is executed by a continuation agent AFTER the user reports back successful activation.

## What's done so far

### Task 1 — Pre-activation verification (COMPLETE)

**Anthropic pricing verified.** Used `curl` against `https://docs.claude.com/en/docs/about-claude/pricing` (the static-rendered mirror of `https://www.anthropic.com/pricing` — the marketing site is JS-rendered so a static fetch returns no prices, but the docs page has the same table in plain HTML). Extracted both Sonnet 4.5 and Haiku 4.5 rows:

| Model | Input | Cache write 5m | Cache hit | Output |
|-------|-------|----------------|-----------|--------|
| Claude Sonnet 4.5 | $3.00 | $3.75 | $0.30 | $15.00 |
| Claude Haiku 4.5 | $1.00 | $1.25 | $0.10 | $5.00 |

The values in `src/lib/observability/pricing.ts` match exactly. Updated only the file header: removed the `TODO(plan-11)` comment, changed confidence from MEDIUM to HIGH, date-stamped 2026-04-07, added explicit per-model breakdown in the doc block. **Commit `49bd386`.**

**AsyncLocalStorage smoke test PASS.** Wrote throwaway `scripts/smoke/als-smoke-test.ts` that imports `runWithCollector`, `getCollector`, `ObservabilityCollector` from the public barrel `src/lib/observability/index.ts`, instantiates a collector with smoke values, and runs 7 sub-checks:

1. Immediate `getCollector()` inside `runWithCollector` -> PASS
2. After `await Promise.resolve()` (microtask) -> PASS
3. After `await setTimeout(10)` (macrotask hop) -> PASS
4. Inside a nested helper async fn -> PASS
5. Inside `Promise.all` branch A -> PASS
6. Inside `Promise.all` branch B -> PASS
7. Outside the run, `getCollector() === null` -> PASS

Output verbatim:
```
[PASS] immediate getCollector
[PASS] after Promise.resolve()
[PASS] after setTimeout(10)
[PASS] nested helper async fn
[PASS] Promise.all branch A
[PASS] Promise.all branch B
[PASS] outside run -> null

ALS smoke test: PASS
```

Script and `scripts/smoke/` directory deleted post-run as required. The local Node runtime uses the same `node:async_hooks` ALS as Vercel Node serverless, and `42.1-RESEARCH.md` Pattern 1 already documented compatibility — but the runbook still folds in a final integration check (first turn after activation must produce non-zero events/queries/ai_calls counters).

**Smoke tests 2 and 3 DEFERRED to Task 3 checkpoint** because both require live conditions Claude cannot reach autonomously:
- Test 2 (anti-recursion) needs the production schema + `OBSERVABILITY_ENABLED=true`. The pass criterion is a single SQL query: `SELECT count(*) FROM agent_observability_queries WHERE table_name LIKE 'agent_observability_%' OR table_name = 'agent_prompt_versions'` -> must return 0. Documented + folded into runbook Step 3.3.
- Test 3 (sandbox debug panel regression) requires visual UI inspection. Risk argument documented: sandbox debug panel and production debug panel share zero components — only `inbox-layout.tsx` was touched and the change uses a CONDITIONAL JSX branch keeping the non-super-user path byte-identical (Plan 09 SUMMARY confirms). Sandbox does not use `inbox-layout.tsx`. Folded into runbook Step 3.4.

`smoke-tests.md` created with the full breakdown. **Commit `0621c7f`.**

### Task 2 — Activation runbook + Vercel push (COMPLETE)

**Runbook created** at `.planning/phases/42.1-observabilidad-bots-produccion/activation-runbook.md`. Sections:
- Pre-activacion checklist (7 items, all already done at this point).
- **Paso 1** — Deploy with flag OFF + post-deploy zero-row check + 10-min latency baseline.
- **Paso 2** — `SUPER_USER_EMAIL` env var (PREREQUISITE — Plan 09 introduced `assertSuperUser()` that throws FORBIDDEN if env unset, even for Jose) + `OBSERVABILITY_ENABLED=true` + manual redeploy.
- **Paso 3** — Post-activation verification with 6 sub-steps:
  - 3.1: 1 test message per bot (Somnio V3, GoDentist, Recompra)
  - 3.2: open production debug panel as super-user, click row, expand events/queries/AI calls
  - 3.3: anti-recursion SQL (Smoke Test 2 deferred)
  - 3.4: sandbox debug panel regression check (Smoke Test 3 deferred)
  - 3.5: 1-hour latency monitoring window (p50 baseline+20ms, flush p95 200ms via pino logs)
  - 3.6: volume sanity check vs RESEARCH.md expectations
- **Rollback instantaneo** — Single env var flip + redeploy, < 2 min, with cleanup SQL.
- **Criterios de exito** — 11-item checklist for declaring Phase 42.1 ACTIVE.
- **Post-activacion** — explicit handoff to continuation agent for Task 4.

**Important deviation from the plan template:** Plan 11 task 2 didn't mention `SUPER_USER_EMAIL` because it was introduced in Plan 09 SUMMARY as a footnote. Task 2 instructions explicitly required adding it as a prerequisite to runbook Step 2 — done. Without this, Jose would see the panel disabled state even after flipping the flag.

**Push to Vercel:** `git push origin main` after the runbook commit. Three new commits live on `main` and being deployed by Vercel:
- `49bd386` — chore: pricing verification
- `0621c7f` — docs: smoke tests results
- `46a9f5a` — docs: activation runbook

These are documentation + comment-only changes — zero runtime impact. The runtime code shipped in Plans 02-10 is already in production behind the OFF flag. The deploy is purely to pull in the updated pricing comment so the next user-triggered prod deploy is byte-current.

### Task 3 — User checkpoint (BLOCKED — awaiting user)

User executes the runbook manually. Resume signal: `"sistema activo en produccion"` (with metrics from Step 3.5 + 3.6) OR `"rollback ejecutado: <razon>"`.

### Task 4 — Docs + LEARNINGS (PENDING — handled by continuation agent)

Will be executed by a fresh agent after the user reports activation.

## Commits in this plan so far

| # | Hash | Message |
|---|------|---------|
| 1 | `49bd386` | chore(42.1-11): verify anthropic pricing against official source |
| 2 | `0621c7f` | docs(42.1-11): smoke tests results |
| 3 | `46a9f5a` | docs(42.1-11): activation runbook |

All pushed to `origin/main`.

## Open items (for continuation agent)

1. Update `docs/analysis/04-estado-actual-plataforma.md` with new section "Sistema de Observabilidad de Bots (Phase 42.1)" — bots covered, panel access path, retention, kill switch, runbook reference, ACTIVE date.
2. Create `.planning/phases/42.1-observabilidad-bots-produccion/LEARNINGS.md` with bugs found, decisions validated, real-vs-estimated metrics from Steps 3.5/3.6, ALS-in-Next16 pattern, fetch-override-in-both-clients pattern, anti-recursion via createRawAdminClient pattern, and TODOs for future observability phases (cross-turn Inngest events, Meta webhook entry capture, dashboard agregado, prompt diff, export — all explicit DESCOPE per Plan 11 objective).
3. Final phase commit + push.
4. Update `.planning/STATE.md` to mark Phase 42.1 COMPLETE.
