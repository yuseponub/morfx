---
phase: 39-whatsapp-outbound-templates
plan: 06
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/app/api/webhooks/meta/route.ts
  - src/lib/whatsapp/webhook-handler.ts
autonomous: true
requirements: [WA-06, WA-09]
must_haves:
  truths:
    - "A message_template_status_update webhook updates the matching local whatsapp_templates row's status (push, not polling)"
    - "Forged/unsigned template-status webhooks are rejected by HMAC verification"
    - "Inbound Meta media is downloaded (Bearer, two-step) and rehosted to Supabase Storage for meta_direct workspaces"
  artifacts:
    - path: "src/app/api/webhooks/meta/route.ts"
      provides: "message_template_status_update field handling on the existing Meta webhook"
      contains: "message_template_status_update"
  key_links:
    - from: "src/app/api/webhooks/meta/route.ts"
      to: "whatsapp_templates table"
      via: "UPDATE status + rejected_reason on status-change event"
      pattern: "message_template_status_update"
    - from: "src/lib/whatsapp/webhook-handler.ts"
      to: "src/lib/meta/media.ts downloadAndRehostMedia"
      via: "inbound media path branches on provider"
      pattern: "downloadAndRehostMedia"
---

<objective>
Wire the inbound/webhook side for Meta direct: WA-09 template-status webhook push (replacing polling) on the existing `/api/webhooks/meta` endpoint, and the WA-06 inbound media path so Meta CDN media is rehosted to Supabase Storage. Also verify (Wave-0 check, OQ1/A2) that the WABA is subscribed to the `message_templates` field.

Purpose: Templates created via Meta (Plan 04) sit at PENDING until Meta reviews them; WA-09 receives the approval/rejection push. Inbound Meta media must be downloaded immediately (5-min CDN expiry, Pitfall 3). Parallel with Plan 05 — different files.
Output: Modified `webhooks/meta/route.ts` (WA-09 handler), `whatsapp/webhook-handler.ts` (Meta inbound media branch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/app/api/webhooks/meta/route.ts
@src/lib/meta/media.ts
@src/lib/meta/credentials.ts

<interfaces>
From src/app/api/webhooks/meta/route.ts (Phase 38 — HMAC + raw-body-first + processWebhook reuse):
```typescript
export async function GET(request)   // handshake
export async function POST(request)  // verifyMetaHmac(raw) then resolveByPhoneNumberId then processWebhook(payload, workspaceId, phoneNumberId)
import { verifyMetaHmac } from ...    // HMAC-SHA256 over RAW body (Phase 38)
import { processWebhook } from '@/lib/whatsapp/webhook-handler'
```
From src/lib/meta/media.ts (Plan 03):
```typescript
export async function downloadAndRehostMedia(accessToken, mediaId, workspaceId, conversationId): Promise<string>  // public URL
```
WA-09 payload (RESEARCH §13):
```jsonc
{ "object":"whatsapp_business_account",
  "entry":[{ "id":"<waba_id>", "changes":[{ "field":"message_template_status_update",
    "value":{ "event":"APPROVED"|"REJECTED"|"PENDING"|"PAUSED"|"DISABLED"|"FLAGGED",
              "message_template_id":<id>, "message_template_name":"<name>",
              "message_template_language":"es", "reason":"NONE" } }] }] }
```
Status-mapping analog: `whatsapp/templates-api.ts` `syncTemplateStatus360` (:189-208). Row UPDATE analog: `domain/whatsapp-templates.ts:186-191`. Inbound media rehost analog: `webhook-handler.ts` `downloadAndUploadMedia` (:632-679).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WA-09 template-status webhook handler</name>
  <files>src/app/api/webhooks/meta/route.ts</files>
  <behavior>
    - POST with changes[].field === 'message_template_status_update' → UPDATE local whatsapp_templates row status by (workspace_id, name, language) or message_template_id
    - REJECTED/PAUSED event with reason → write rejected_reason
    - HMAC verification still gates the request (forged/unsigned rejected)
    - inbound-message handling (processWebhook) unchanged for the messages field
  </behavior>
  <action>
Extend the existing `POST` handler in `webhooks/meta/route.ts` (after HMAC verify + raw-body parse, which already apply to all fields) to branch on `changes[].field`:
- `field === 'message_template_status_update'`: map `value.event` to the local status (mirror `syncTemplateStatus360`), then UPDATE the matching `whatsapp_templates` row by `(workspace_id, name, language)` or `message_template_id` → set `status` + `rejected_reason` (mirror domain/whatsapp-templates.ts:186-191). Resolve `workspace_id` from the WABA id (`entry[].id`) via the existing credential resolver. Write through the domain layer (Regla 3) — do NOT inline a direct admin INSERT/UPDATE if a domain fn exists; if not, add a small domain updater in `domain/whatsapp-templates.ts`.
- default (messages/status field): keep the existing `processWebhook(...)` path UNCHANGED (D-09 — Phase 38 inbound intact).
HMAC verify (`verifyMetaHmac` over raw body) already gates this — confirm the template-status branch is AFTER the verify, so forged payloads are rejected (T-39-04). Per OQ1/A2: at the top of this task, run a one-time check on the test number `GET /{waba_id}/subscribed_apps`; if the `message_templates` field is not listed, add a one-time subscribe-field call (document it in the SUMMARY for the operator). Do NOT remove the polling `syncTemplateStatus360` — keep it as a manual "Resync" fallback (Pattern 3).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/webhooks/meta/__tests__/template-status.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>template-status.test.ts GREEN: APPROVED updates status, REJECTED writes rejected_reason, forged/unsigned rejected by HMAC; the existing inbound `processWebhook` path is unchanged in `git diff`.</done>
</task>

<task type="auto">
  <name>Task 2: Inbound Meta media rehost (WA-06)</name>
  <files>src/lib/whatsapp/webhook-handler.ts</files>
  <action>
Make the inbound media path provider-aware so Meta CDN media is downloaded + rehosted (RESEARCH Pitfall 3 + 39-PATTERNS.md "Inbound Media Rehost"). The existing `downloadAndUploadMedia` (:632-679) handles 360dialog media; for `meta_direct` workspaces, the inbound media arrives as a Meta `media_id` requiring the two-step Bearer download.

Branch the inbound media-handling so that when the workspace is `meta_direct` (or the payload comes from the Meta endpoint), it calls `downloadAndRehostMedia(creds.accessToken, mediaId, workspaceId, conversationId)` from `meta/media.ts` (Plan 03) instead of the 360dialog `D360-API-KEY` download. Resolve creds via `resolveByWorkspace(workspaceId, 'whatsapp')`. Keep the 360dialog inbound path byte-identical (Regla 6 — do NOT change `downloadAndUploadMedia` behavior; add a sibling branch). Download IMMEDIATELY on receipt — the CDN URL expires ~5 min and needs Bearer (Pitfall 3). Never log the token (T-39-01).

If the cleanest seam is in the `processWebhook` media-handling caller rather than inside `downloadAndUploadMedia`, add the branch there — the rule is: 360dialog media path unchanged, Meta media path uses `meta/media.ts`.
  </action>
  <verify>
    <automated>grep -nE "downloadAndRehostMedia|resolveByWorkspace" src/lib/whatsapp/webhook-handler.ts && npx vitest run 2>&1 | tail -6</automated>
  </verify>
  <done>Inbound Meta media routes through `downloadAndRehostMedia` (Bearer, rehost to Storage); 360dialog inbound media path byte-identical; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta → /api/webhooks/meta | untrusted inbound POST; authenticity established by HMAC over raw body |
| Meta CDN → server (inbound media) | untrusted host; fetched server-side with Bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering (forged template-status webhook) | WA-09 handler | mitigate | `verifyMetaHmac` over RAW body (Phase 38) gates ALL fields; template-status branch runs only after verify; reject unsigned/forged |
| T-39-07 | DoS / SSRF (inbound media) | media rehost | mitigate | Meta-CDN host validation + size cap enforced in `downloadAndRehostMedia` (Plan 03); download immediately on receipt |
| T-39-02 | Tampering (cross-workspace) | webhook workspace resolution | mitigate | workspace_id resolved from WABA id via credential resolver, never from arbitrary payload fields used as the write key without that resolution |
| T-39-01 | Information Disclosure | media download | mitigate | Bearer token only in fetch header; never logged |
</threat_model>

<verification>
- `npx vitest run src/app/api/webhooks/meta/__tests__/template-status.test.ts` GREEN.
- `npx vitest run` full suite green.
- `git diff` shows the 360dialog `downloadAndUploadMedia` behavior unchanged + the existing `processWebhook` inbound path unchanged (Regla 6 / D-09).
- SUMMARY records the `subscribed_apps` field check result (OQ1/A2).
</verification>

<success_criteria>
Template status changes arrive via webhook push and update local rows (WA-09); inbound Meta CDN media is rehosted to Supabase Storage with SSRF + expiry handling (WA-06); HMAC gates all webhook fields; 360dialog inbound + Phase 38 inbound paths untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-06-SUMMARY.md`
</output>
