---
phase: 37.5-meta-verification-website
plan: 02
type: execute
wave: 2
depends_on: ["37.5-01"]
files_modified:
  - src/app/(marketing)/[locale]/layout.tsx
  - src/app/(marketing)/[locale]/page.tsx
  - src/components/marketing/header.tsx
  - src/components/marketing/footer.tsx
  - src/components/marketing/locale-toggle.tsx
  - messages/es.json
  - messages/en.json
autonomous: true

must_haves:
  truths:
    - "Visiting / renders a marketing layout with header (logo + ES/EN toggle + theme toggle + Login link) and footer (legal info)"
    - "Visiting /en renders the same layout in English"
    - "Header logo uses existing /public/logo-light.png and /public/logo-dark.png"
    - "Footer displays MORFX S.A.S., NIT 902052328-5, Carrera 38 # 42-17 Apto 1601B, Bucaramanga, Santander, Colombia, +57 313 754 9286, morfx.colombia@gmail.com"
    - "Footer links to /privacy and /terms (locale-aware)"
    - "Locale toggle swaps between / and /en preserving current path"
  artifacts:
    - path: "src/app/(marketing)/[locale]/layout.tsx"
      provides: "Marketing root layout with NextIntlClientProvider, header, footer"
      min_lines: 30
    - path: "src/app/(marketing)/[locale]/page.tsx"
      provides: "Landing page placeholder (real content in Plan 03)"
      min_lines: 10
    - path: "src/components/marketing/header.tsx"
      provides: "Marketing header with logo, locale toggle, theme toggle, login CTA"
    - path: "src/components/marketing/footer.tsx"
      provides: "Marketing footer with business legal info"
    - path: "src/components/marketing/locale-toggle.tsx"
      provides: "Client component switching locale via next-intl router"
  key_links:
    - from: "src/app/(marketing)/[locale]/layout.tsx"
      to: "NextIntlClientProvider"
      via: "wraps children with messages for client components"
      pattern: "NextIntlClientProvider"
    - from: "src/components/marketing/header.tsx"
      to: "/public/logo-light.png, /public/logo-dark.png"
      via: "next/image with theme-aware variant"
      pattern: "logo-(light|dark)"
    - from: "src/components/marketing/locale-toggle.tsx"
      to: "next-intl useRouter + usePathname"
      via: "router.replace(pathname, {locale})"
      pattern: "useRouter.*next-intl"
---

<objective>
Scaffold the public marketing route group at `src/app/(marketing)/[locale]/` with a bilingual layout (header, footer) and a placeholder landing page. Extract reusable header and footer components. Content (hero, product sections, privacy/terms body) is delivered in Plans 03-04.

Purpose: Establishes the visual shell that all marketing pages share and locks in the brand (logo, legal footer info) that Meta reviewers need to see immediately.

Output: Route group renders `/` and `/en` with identical layout, different locale. Landing page itself is a minimal placeholder until Plan 03 fills it.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.5-meta-verification-website/37.5-CONTEXT.md
@.planning/phases/37.5-meta-verification-website/37.5-01-SUMMARY.md
@src/app/layout.tsx
@src/components/ui/button.tsx
@public/logo-light.png
@public/logo-dark.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing route group layout and placeholder page</name>
  <files>src/app/(marketing)/[locale]/layout.tsx, src/app/(marketing)/[locale]/page.tsx</files>
  <action>
  Create `src/app/(marketing)/[locale]/layout.tsx`:
  - Server component
  - Receives `params: Promise<{ locale: string }>` (Next 16 async params)
  - Calls `setRequestLocale(locale)` from `next-intl/server` for static rendering
  - Loads messages via `getMessages()` from next-intl/server
  - Returns `<NextIntlClientProvider locale={locale} messages={messages}>` wrapping `<Header />`, `{children}`, `<Footer />`
  - Add `generateStaticParams` exporting `[{ locale: 'es' }, { locale: 'en' }]` so both locales are prerendered
  - Export `metadata` with generic site defaults (real SEO metadata goes in Plan 03 page.tsx)
  - Use existing root providers from `src/app/layout.tsx` (theme provider etc.) — DO NOT duplicate ThemeProvider if root layout already supplies it; just wrap children with NextIntlClientProvider inside

  IMPORTANT: Since route groups do not affect URL, the root `src/app/layout.tsx` still wraps this layout. Verify root layout sets `<html lang="...">` — if it hardcodes "en" or "es", we must NOT fight it from the marketing layout. If needed, set `lang` dynamically via the root layout by reading params (defer if it complicates things; accept root hardcoded lang for Phase 37.5 since we ship fast).

  Create `src/app/(marketing)/[locale]/page.tsx`:
  - Minimal placeholder server component
  - Calls `setRequestLocale(locale)` (unblocks static rendering)
  - Renders `<main>` with `<h1>MORFX</h1>` and a paragraph placeholder — Plan 03 replaces this entirely
  - Do not add metadata here; Plan 03 handles it
  </action>
  <verify>
  - `ls src/app/\(marketing\)/\[locale\]/layout.tsx src/app/\(marketing\)/\[locale\]/page.tsx`
  - `npm run dev` (on port 3020) + `curl -I http://localhost:3020/` returns 200 with HTML containing "MORFX"
  - `curl -I http://localhost:3020/en` returns 200
  - `npx tsc --noEmit` passes
  </verify>
  <done>Route group renders / and /en with shared layout; placeholder page loads without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create marketing Header component with logo, locale toggle, theme toggle, login CTA</name>
  <files>src/components/marketing/header.tsx, src/components/marketing/locale-toggle.tsx</files>
  <action>
  Create `src/components/marketing/locale-toggle.tsx` (client component):
  - "use client"
  - Uses `useRouter` and `usePathname` from `@/i18n/routing` (next-intl navigation helpers) — create `src/i18n/navigation.ts` if not already done in Plan 01:

      // src/i18n/navigation.ts
      import { createNavigation } from 'next-intl/navigation';
      import { routing } from './routing';
      export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);

    (If this already exists from Plan 01, skip; otherwise create it here.)
  - Reads current locale via `useLocale()` from next-intl
  - Renders a small shadcn Button or inline button with "ES | EN" toggle
  - On click, calls `router.replace(pathname, { locale: nextLocale })` to swap locale while preserving the current path

  Create `src/components/marketing/header.tsx` (server component, except where client needed):
  - Sticky top, bordered bottom, backdrop-blur
  - Left: Logo using `next/image` with `src="/logo-light.png"` (or logo-dark.png for dark mode) — use the SAME theme-aware pattern already used in sidebar/login. Check `src/components/` for existing `Logo` component; reuse if available.
  - Right side (flex row gap):
    - `<LocaleToggle />` (client)
    - Theme toggle (reuse existing `src/components/theme-toggle.tsx` or equivalent — check project for existing theme switcher component)
    - "Iniciar sesión" / "Login" link using next-intl `<Link href="/login">` — wait, `/login` is outside locale prefix. Use plain Next `<Link>` from `next/link` pointing to `/login` directly (it's not a localized route, it's the auth app).
    - Primary CTA button "Contactar ventas" / "Contact sales" linking to `https://wa.me/573137549286` (target="_blank")
  - All text via `useTranslations('Header')` in a small client sub-component OR use `getTranslations('Header')` server-side

  Translation keys used: `Header.login`, `Header.contactSales`, `Header.localeToggleLabel`. These will be populated in messages/es.json + en.json in Task 4.
  </action>
  <verify>
  - `ls src/components/marketing/header.tsx src/components/marketing/locale-toggle.tsx`
  - `curl http://localhost:3020/` response HTML contains "MORFX" logo alt text, "Iniciar sesión", "Contactar ventas"
  - `curl http://localhost:3020/en` contains "Login", "Contact sales"
  - Clicking locale toggle swaps `/` ↔ `/en` (manual browser check)
  - `npx tsc --noEmit` passes
  </verify>
  <done>Header renders with logo, locale toggle, theme toggle, login link, and contact sales CTA; toggle swaps locales preserving path.</done>
</task>

<task type="auto">
  <name>Task 3: Create marketing Footer with full legal business info</name>
  <files>src/components/marketing/footer.tsx</files>
  <action>
  Create `src/components/marketing/footer.tsx` (server component, uses `getTranslations('Footer')`):

  Layout (responsive grid, 1 col on mobile, 3-4 cols on md+):
  - Column 1: Logo + tagline (translated)
  - Column 2: Producto (translated heading) — links to hash anchors on landing: `#crm`, `#agents`, `#automations`, `#integrations`
  - Column 3: Legal — links to `/privacy`, `/terms`, and `/login` (use next-intl `<Link>` for /privacy and /terms so they become locale-aware; `/login` uses plain `next/link`)
  - Column 4: Contacto
    - Phone: +57 313 754 9286 (click-to-call `tel:+573137549286`)
    - Email: morfx.colombia@gmail.com (mailto)
    - WhatsApp: wa.me/573137549286 (external)

  Bottom bar (full width, centered, small text, muted color):
    © 2026 MORFX S.A.S. — NIT 902052328-5
    Carrera 38 # 42-17 Apartamento 1601B, Bucaramanga, Santander, Colombia
    CIIU 6201 — Actividades de desarrollo de sistemas informáticos

  IMPORTANT: The legal company line MUST be visible without any user interaction. Meta reviewers scan for this string.

  Translation keys used: `Footer.tagline`, `Footer.product`, `Footer.crm`, `Footer.agents`, `Footer.automations`, `Footer.integrations`, `Footer.legal`, `Footer.privacy`, `Footer.terms`, `Footer.login`, `Footer.contact`, `Footer.phone`, `Footer.email`, `Footer.whatsapp`.

  Legal bottom bar is NOT translated — legal name + NIT + address are identical in both languages. Put them as plain strings.
  </action>
  <verify>
  - `curl http://localhost:3020/` HTML contains "MORFX S.A.S.", "902052328-5", "Carrera 38", "Bucaramanga", "+57 313 754 9286", "morfx.colombia@gmail.com"
  - `curl http://localhost:3020/en` contains the same legal strings
  - `npx tsc --noEmit` passes
  </verify>
  <done>Footer renders with all required business legal information visible without auth, in both locales.</done>
</task>

<task type="auto">
  <name>Task 4: Seed messages/es.json and messages/en.json with Header + Footer keys</name>
  <files>messages/es.json, messages/en.json</files>
  <action>
  Replace placeholder `{}` in both files with structured JSON. Landing and Privacy/Terms sections land in later plans — this task only seeds Header + Footer keys.

  `messages/es.json`:
    {
      "Header": {
        "login": "Iniciar sesión",
        "contactSales": "Contactar ventas",
        "localeToggleLabel": "Cambiar idioma"
      },
      "Footer": {
        "tagline": "CRM y automatización de WhatsApp Business con IA para e-commerce",
        "product": "Producto",
        "crm": "CRM unificado",
        "agents": "Agentes de IA",
        "automations": "Automatizaciones",
        "integrations": "Integraciones",
        "legal": "Legal",
        "privacy": "Política de privacidad",
        "terms": "Términos de servicio",
        "login": "Iniciar sesión",
        "contact": "Contacto",
        "phone": "Teléfono",
        "email": "Email",
        "whatsapp": "WhatsApp"
      }
    }

  `messages/en.json` mirrors the same keys:
    {
      "Header": {
        "login": "Login",
        "contactSales": "Contact sales",
        "localeToggleLabel": "Switch language"
      },
      "Footer": {
        "tagline": "AI-powered CRM and WhatsApp Business automation for e-commerce",
        "product": "Product",
        "crm": "Unified CRM",
        "agents": "AI agents",
        "automations": "Automations",
        "integrations": "Integrations",
        "legal": "Legal",
        "privacy": "Privacy policy",
        "terms": "Terms of service",
        "login": "Login",
        "contact": "Contact",
        "phone": "Phone",
        "email": "Email",
        "whatsapp": "WhatsApp"
      }
    }
  </action>
  <verify>
  - `jq . messages/es.json` and `jq . messages/en.json` both parse without error
  - `/` renders Spanish text; `/en` renders English text (visual check)
  </verify>
  <done>Header and Footer keys seeded in both locales; layout renders correctly in ES and EN.</done>
</task>

</tasks>

<verification>
- `npm run dev` on port 3020 starts cleanly
- `curl -sI http://localhost:3020/` → 200
- `curl -sI http://localhost:3020/en` → 200
- HTML at / contains "MORFX S.A.S.", NIT, address, phone, email, /privacy link, /terms link
- Locale toggle (browser) switches / ↔ /en while keeping path segment when applicable
- `/login` still loads (no regression)
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Marketing shell (header + footer + locale-aware routing) live in both locales with full legal footer visible to unauthenticated visitors. Ready for Plans 03/04 to drop content into the shell.
</success_criteria>

<output>
After completion, create `.planning/phases/37.5-meta-verification-website/37.5-02-SUMMARY.md` documenting component structure, translation keys seeded, and any deviations from plan.
</output>
