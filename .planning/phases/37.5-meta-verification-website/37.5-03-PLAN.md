---
phase: 37.5-meta-verification-website
plan: 03
type: execute
wave: 3
depends_on: ["37.5-02"]
files_modified:
  - src/app/(marketing)/[locale]/page.tsx
  - src/components/marketing/landing/hero.tsx
  - src/components/marketing/landing/product-section.tsx
  - src/components/marketing/landing/about.tsx
  - src/components/marketing/landing/cta.tsx
  - messages/es.json
  - messages/en.json
  - public/og-image.png
autonomous: true

must_haves:
  truths:
    - "Visiting / renders a full landing page with hero, product sections (CRM, Agents, Automations, Integrations, Multi-channel), About MORFX section with object social text, and CTA to WhatsApp/email"
    - "Visiting /en renders the same landing in English"
    - "Hero has 'Contactar ventas' primary CTA linking to https://wa.me/573137549286"
    - "Page metadata includes title, description, and og:image"
    - "About section includes the textual objeto social from the constitution act"
  artifacts:
    - path: "src/app/(marketing)/[locale]/page.tsx"
      provides: "Full landing composition with generateMetadata for SEO"
      min_lines: 40
    - path: "src/components/marketing/landing/hero.tsx"
      provides: "Hero section with headline, subhead, primary/secondary CTAs"
    - path: "src/components/marketing/landing/product-section.tsx"
      provides: "Reusable product feature section (supports CRM, Agents, Automations, Integrations, Multi-channel)"
    - path: "src/components/marketing/landing/about.tsx"
      provides: "About MORFX section with company info + objeto social"
    - path: "src/components/marketing/landing/cta.tsx"
      provides: "Bottom CTA band with contact WhatsApp + email"
  key_links:
    - from: "src/components/marketing/landing/hero.tsx"
      to: "https://wa.me/573137549286"
      via: "anchor href on primary CTA"
      pattern: "wa.me/573137549286"
    - from: "src/components/marketing/landing/cta.tsx"
      to: "mailto:morfx.colombia@gmail.com"
      via: "anchor href"
      pattern: "mailto:morfx.colombia@gmail.com"
    - from: "src/app/(marketing)/[locale]/page.tsx"
      to: "/og-image.png"
      via: "metadata.openGraph.images"
      pattern: "og-image"
---

<objective>
Replace the placeholder landing at `src/app/(marketing)/[locale]/page.tsx` with the full bilingual landing page: hero, product sections (CRM, AI Agents, Automations, Integrations, Multi-channel), About MORFX (with legal objeto social), and final CTA band. Wire SEO metadata (title, description, og:image) via generateMetadata.

Purpose: This is the content Meta reviewers actually see. Must communicate "real business" immediately: legal name in header/footer, detailed product descriptions, clear contact paths, and company background aligned with constitution documents Meta already has on file.

Output: A corporate-professional landing rendered identically in ES and EN with all required content for Business Verification.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.5-meta-verification-website/37.5-CONTEXT.md
@.planning/phases/37.5-meta-verification-website/37.5-02-SUMMARY.md
@src/components/ui/button.tsx
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero, About, ProductSection, CTA components</name>
  <files>src/components/marketing/landing/hero.tsx, src/components/marketing/landing/about.tsx, src/components/marketing/landing/product-section.tsx, src/components/marketing/landing/cta.tsx</files>
  <action>
  All components are server components using `getTranslations('Landing.{section}')`.

  **Hero** (`hero.tsx`):
  - Large padded section (py-20 md:py-32), centered text on mobile, left-aligned on md+
  - Badge/pill above headline: "MORFX S.A.S. — Empresa colombiana" / "A Colombian company"
  - H1 headline (Landing.Hero.headline)
  - Subhead paragraph (Landing.Hero.subhead)
  - Two CTAs:
    - Primary: "Contactar ventas" / "Contact sales" → `https://wa.me/573137549286` (target="_blank", rel="noopener noreferrer")
    - Secondary: "Iniciar sesión" / "Login" → `/login` (plain next/link)
  - Muted tag line below CTAs: "Respuesta en minutos vía WhatsApp" / "Response in minutes via WhatsApp"

  **About** (`about.tsx`):
  - Section with id="about"
  - Heading "Quiénes somos" / "About MORFX"
  - Paragraph intro (Landing.About.intro) — briefly who MORFX is
  - Paragraph with textual objeto social from constitution act. Translate to EN for en locale. ES exact text:
    "Desarrollo, diseño, implementación, operación, comercialización y licenciamiento de plataformas tecnológicas basadas en inteligencia artificial, con especial énfasis en atención al cliente, gestión logística y procesos comerciales para empresas de comercio electrónico y otros sectores productivos."
  - Small data grid (2-col) with: Legal name, NIT, City, Founded (February 25, 2026), CIIU 6201, Legal representative (JOSE MARIO ROMERO RINCON)

  **ProductSection** (`product-section.tsx`):
  - Reusable component with props `{ id, icon, titleKey, descriptionKey, bullets: string[] }`
  - Or simpler: one file with 5 exports, one per feature. Keep it one reusable component with translation namespace passed in.
  - Two-column layout on md+: left heading+description+bullets, right illustration placeholder (use a muted Card with feature keyword large, or simple SVG icon from lucide-react)
  - Used 5x in page.tsx for: CRM, Agents, Automations, Integrations, Multi-channel
  - Each has id={id} so footer anchor links work

  **CTA** (`cta.tsx`):
  - Bordered band (bg muted, rounded, py-16, centered)
  - Heading "¿Listo para empezar?" / "Ready to get started?"
  - Paragraph
  - Two buttons side by side:
    - Primary: WhatsApp → https://wa.me/573137549286
    - Secondary: Email → mailto:morfx.colombia@gmail.com

  Use shadcn `Button` from `src/components/ui/button.tsx` and `Card` from `src/components/ui/card.tsx` throughout. Use `lucide-react` icons (MessageSquare, Bot, Zap, Plug, Smartphone).
  </action>
  <verify>
  - All 4 files exist under `src/components/marketing/landing/`
  - `npx tsc --noEmit` passes
  </verify>
  <done>Reusable landing components built with shadcn UI primitives and translation namespaces wired.</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page and add SEO metadata</name>
  <files>src/app/(marketing)/[locale]/page.tsx, public/og-image.png</files>
  <action>
  Replace the placeholder `src/app/(marketing)/[locale]/page.tsx`:

  - Export `async function generateMetadata({ params }): Promise<Metadata>`:
    - Read locale from params
    - Return `{ title: '...', description: '...', openGraph: { title, description, url: 'https://morfx.app' + (locale==='en'?'/en':''), images: [{ url: '/og-image.png', width: 1200, height: 630 }], locale, type: 'website' }, twitter: { card: 'summary_large_image', ... }, alternates: { languages: { es: '/', en: '/en' } } }`
    - Localize title/description per locale (use getTranslations('Landing.Meta'))

  - Default export async server component:
    - Await params, call `setRequestLocale(locale)`
    - Render in order:
      1. `<Hero />`
      2. `<About />`
      3. `<ProductSection id="crm" namespace="Landing.CRM" icon="MessageSquare" />`
      4. `<ProductSection id="agents" namespace="Landing.Agents" icon="Bot" />`
      5. `<ProductSection id="automations" namespace="Landing.Automations" icon="Zap" />`
      6. `<ProductSection id="integrations" namespace="Landing.Integrations" icon="Plug" />`
      7. `<ProductSection id="multichannel" namespace="Landing.Multichannel" icon="Smartphone" />`
      8. `<CTA />`

  - Add `public/og-image.png`: 1200x630px. For this phase, generate a simple branded image using the logo on a gradient background. Use a placeholder approach:
    - If design asset not available, create a simple 1200x630 PNG via ImageMagick in an auto task:
        convert -size 1200x630 gradient:'#0f172a-#1e293b' -gravity center -pointsize 120 -fill white -annotate 0 "MORFX" public/og-image.png
    - If ImageMagick not available, commit a placeholder by copying `public/logo-dark.png` to `public/og-image.png` — OG validators accept any 1200x630 image (document that final branded image is TODO in summary).
  </action>
  <verify>
  - `curl http://localhost:3020/` returns HTML with all 5 product sections, hero headline, about section, CTA band, and `<meta property="og:image" content="/og-image.png">`
  - `curl http://localhost:3020/en` returns English version
  - `ls public/og-image.png` exists
  - `npx tsc --noEmit` passes
  </verify>
  <done>Landing page fully composed with SEO metadata and OG image; renders identically in ES/EN with localized content.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Landing translations into messages/es.json and messages/en.json</name>
  <files>messages/es.json, messages/en.json</files>
  <action>
  Add a `Landing` namespace to both locale files, preserving the existing `Header` and `Footer` keys from Plan 02.

  Structure for `Landing` (both locales):
  - `Meta.title` — SEO title (~60 chars)
  - `Meta.description` — SEO description (~155 chars)
  - `Hero.badge`, `Hero.headline`, `Hero.subhead`, `Hero.primaryCTA`, `Hero.secondaryCTA`, `Hero.responseTag`
  - `About.heading`, `About.intro`, `About.objetoSocial`, `About.dataLegalName`, `About.dataNIT`, `About.dataCity`, `About.dataFounded`, `About.dataCIIU`, `About.dataLegalRep`
  - `CRM.heading`, `CRM.description`, `CRM.bullet1..bullet4`
  - `Agents.heading`, `Agents.description`, `Agents.bullet1..bullet4`
  - `Automations.heading`, `Automations.description`, `Automations.bullet1..bullet4`
  - `Integrations.heading`, `Integrations.description`, `Integrations.bullet1..bullet4`
  - `Multichannel.heading`, `Multichannel.description`, `Multichannel.bullet1..bullet4`
  - `CTA.heading`, `CTA.description`, `CTA.primaryButton`, `CTA.secondaryButton`

  Content guidelines:
  - Corporate-professional tone (not playful)
  - ES uses "nosotros" (we) not "yo"
  - Bullets are specific features (e.g., CRM: "Contactos unificados", "Pipelines configurables", "Etapas personalizadas", "Notas y tareas")
  - Agents: mention GPT/Claude, conversational support + sales
  - Automations: "10 triggers, 11 acciones", AI-assisted builder
  - Integrations: Shopify, Coordinadora, Inter Rapidísimo (mention each by name)
  - Multichannel: WhatsApp Business API, próximamente Messenger e Instagram

  Use fully formed paragraphs, not lorem ipsum. Accurate product descriptions matter for Meta review.

  After editing, validate JSON with `jq . messages/es.json` and `jq . messages/en.json`.
  </action>
  <verify>
  - `jq '.Landing.Hero.headline' messages/es.json` returns a real Spanish string
  - `jq '.Landing.Hero.headline' messages/en.json` returns a real English string
  - Visual check: `/` and `/en` show real content, no missing translation keys in console
  </verify>
  <done>All Landing namespace keys populated in both locales with production-quality corporate-professional copy.</done>
</task>

</tasks>

<verification>
- HTML at `/` contains (curl + grep): "MORFX S.A.S.", "902052328-5", "Carrera 38", "+57 313 754 9286", "morfx.colombia@gmail.com", "wa.me/573137549286", "mailto:morfx.colombia@gmail.com", "og:image"
- HTML at `/en` contains all of the above (business data is locale-independent) + English copy
- Mobile responsive: open DevTools mobile viewport, no horizontal scroll, readable
- No missing translation key warnings in browser console
- All five product sections have id anchors (#crm, #agents, #automations, #integrations, #multichannel) and footer links scroll to them
</verification>

<success_criteria>
Landing page fully renders in ES and EN with hero, about, 5 product sections, CTA band, SEO meta, and OG image. All business contact info visible without auth. Ready for Meta reviewer inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/37.5-meta-verification-website/37.5-03-SUMMARY.md` documenting section structure, copy decisions, and whether og-image.png is branded-final or placeholder.
</output>
