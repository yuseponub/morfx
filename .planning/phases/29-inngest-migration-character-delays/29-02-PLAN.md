---
phase: 29-inngest-migration-character-delays
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260224100000_processed_by_agent.sql
  - src/lib/domain/messages.ts
autonomous: true

must_haves:
  truths:
    - "Inbound messages are inserted with processed_by_agent=false"
    - "Existing messages remain processed_by_agent=true (default)"
    - "A partial index exists for efficient unprocessed inbound queries"
  artifacts:
    - path: "supabase/migrations/20260224100000_processed_by_agent.sql"
      provides: "DB migration adding processed_by_agent column + index"
      contains: "processed_by_agent"
    - path: "src/lib/domain/messages.ts"
      provides: "Domain receiveMessage with processed_by_agent: false on insert"
      contains: "processed_by_agent"
  key_links:
    - from: "src/lib/domain/messages.ts"
      to: "supabase/migrations/20260224100000_processed_by_agent.sql"
      via: "insert with processed_by_agent: false"
      pattern: "processed_by_agent.*false"
---

<objective>
Add the `processed_by_agent` boolean column to the messages table and modify the domain layer to insert inbound messages with `processed_by_agent: false`.

Purpose: INFRA-01 requirement. This column tracks whether an inbound message has been seen by the agent. It's the foundation for the future "check pre-envio" (Phase 31) that detects new inbound messages arriving during bot response sequences. Adding it now is safe (default TRUE for existing rows, no behavioral change) and prevents a second migration later.

Output: DB migration file + updated domain `receiveMessage` function.
</objective>

<execution_context>
@/home/jose147/.claude/get-shit-done/workflows/execute-plan.md
@/home/jose147/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-inngest-migration-character-delays/29-RESEARCH.md
@src/lib/domain/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration for processed_by_agent column</name>
  <files>supabase/migrations/20260224100000_processed_by_agent.sql</files>
  <action>
    Create a new SQL migration file that:

    1. Adds `processed_by_agent BOOLEAN NOT NULL DEFAULT true` to the `messages` table using `ALTER TABLE messages ADD COLUMN IF NOT EXISTS`.
    2. Creates a partial index for efficient pre-send check queries:
       ```sql
       CREATE INDEX IF NOT EXISTS idx_messages_unprocessed_inbound
         ON messages(conversation_id, created_at)
         WHERE direction = 'inbound' AND processed_by_agent = false;
       ```
    3. Add a comment explaining: "Default TRUE preserves semantics for existing messages. New inbound messages are inserted with FALSE by domain/messages.ts, then updated to TRUE after agent processing."

    IMPORTANT: Use `IF NOT EXISTS` for idempotency. Follow the naming convention from existing migrations (YYYYMMDDHHMMSS prefix).
  </action>
  <verify>File exists at correct path and SQL syntax is valid (review manually)</verify>
  <done>Migration file creates processed_by_agent column with DEFAULT true and a partial index on unprocessed inbound messages</done>
</task>

<task type="auto">
  <name>Task 2: Domain layer insert with processed_by_agent: false</name>
  <files>src/lib/domain/messages.ts</files>
  <action>
    Modify the `receiveMessage` function in `src/lib/domain/messages.ts`:

    1. In the INSERT to `messages` table (around line 361-372), add `processed_by_agent: false` to the insert object. This means ALL inbound messages start as "not processed by agent." The column gets set to true later by the webhook processor after agent processing completes.

    The specific change is in the `.insert({...})` call inside `receiveMessage`. Add after the media fields spread:
    ```typescript
    processed_by_agent: false,
    ```

    Do NOT modify any other function in messages.ts. Do NOT add an update-to-true function yet -- that will come in Plan 03 when the webhook handler is wired.

    Do NOT change the ReceiveMessageParams interface -- this is an internal implementation detail.
  </action>
  <verify>`npx tsc --noEmit` compiles without errors. Grep for `processed_by_agent` in messages.ts confirms the field is present in the insert.</verify>
  <done>The domain receiveMessage function inserts inbound messages with processed_by_agent: false. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- Migration file exists at `supabase/migrations/20260224100000_processed_by_agent.sql`
- Migration SQL is syntactically valid and idempotent (IF NOT EXISTS)
- `src/lib/domain/messages.ts` includes `processed_by_agent: false` in the receiveMessage insert
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- DB migration adds processed_by_agent BOOLEAN NOT NULL DEFAULT true
- Partial index created for unprocessed inbound message queries
- Domain receiveMessage inserts with processed_by_agent: false
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-inngest-migration-character-delays/29-02-SUMMARY.md`
</output>
