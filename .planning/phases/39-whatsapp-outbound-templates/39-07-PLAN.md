---
phase: 39-whatsapp-outbound-templates
plan: 07
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/app/actions/templates.ts
  - src/app/(dashboard)/configuracion/whatsapp/templates/components/template-list.tsx
autonomous: true
requirements: [WA-08, MIG-03]
must_haves:
  truths:
    - "Template list/delete/sync route to Meta for meta_direct workspaces and 360dialog for the rest"
    - "Templates can be edited via Meta with the D-05 constraints enforced in both the action and the UI"
    - "The UI does NOT offer unconstrained edit on approved/disabled/pending templates"
  artifacts:
    - path: "src/app/actions/templates.ts"
      provides: "provider-aware delete/list/sync + editTemplate action with D-05 guard"
      contains: "whatsapp_provider"
    - path: "src/app/(dashboard)/configuracion/whatsapp/templates/components/template-list.tsx"
      provides: "edit UI gated by status (D-05 reality)"
  key_links:
    - from: "src/app/actions/templates.ts"
      to: "src/lib/meta/templates.ts"
      via: "editTemplateMeta / listTemplatesMeta / deleteTemplateMeta in the meta_direct branch"
      pattern: "TemplateMeta"
---

<objective>
Make the template MANAGEMENT surface provider-aware (WA-08) and deliver the mandatory D-05 edit experience: an `editTemplate` action that enforces Meta's edit constraints, and a UI that reflects reality (no unconstrained "Edit" on approved/disabled/pending templates).

Purpose: Today `deleteTemplate`/`syncTemplateStatuses` in `actions/templates.ts` resolve `whatsapp_api_key` and call 360dialog directly. A `meta_direct` workspace must manage its templates via Graph. D-05 is a mandatory deliverable: the UI must honor Meta's edit rules.
Output: Modified `actions/templates.ts` (provider branch + edit action), `template-list.tsx` (status-gated edit UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-whatsapp-outbound-templates/39-RESEARCH.md
@.planning/phases/39-whatsapp-outbound-templates/39-PATTERNS.md
@src/app/actions/templates.ts
@src/lib/meta/templates.ts

<interfaces>
From src/lib/meta/templates.ts (Plan 03):
```typescript
export async function listTemplatesMeta(accessToken, wabaId): Promise<...>
export async function deleteTemplateMeta(accessToken, wabaId, name): Promise<...>
export async function editTemplateMeta(accessToken, messageTemplateId, { category?, components? }, currentStatus): Promise<...>  // enforces D-05
export async function syncTemplateStatusMeta(accessToken, wabaId): Promise<...>
```
From src/lib/meta/credentials.ts:
```typescript
export async function resolveByWorkspace(workspaceId, 'whatsapp'): Promise<MetaCredentials | null>
```
Existing 360dialog management calls to branch (templates.ts):
- `deleteTemplate` (:235) → `deleteTemplate360(apiKey, name)` (line ~265)
- `syncTemplateStatuses` (:298) → `listTemplates360(apiKey)` (line ~319)
UI: `template-list.tsx` — has an `Edit` button (line ~94) and `TemplateStatusBadge` (:71); `deleteTemplate` wired at :50.
</interfaces>

D-05 table (RESEARCH "D-05 MANDATORY DELIVERABLE"):
- Edit only when status ∈ {APPROVED, REJECTED, PAUSED}. PENDING/DISABLED → no edit (offer "Duplicate as new").
- name + language NEVER editable. APPROVED: 1 edit/24h, 10/30d; warn + after edit status → PENDING.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider-aware delete/list/sync + editTemplate action (D-05 guard)</name>
  <files>src/app/actions/templates.ts</files>
  <behavior>
    - deleteTemplate / syncTemplateStatuses: meta_direct → deleteTemplateMeta / syncTemplateStatusMeta; 360dialog → existing 360dialog calls unchanged
    - new editTemplate action: meta_direct → editTemplateMeta (D-05 enforced); rejects name/language change + non-editable statuses
    - workspaceId / creds from ctx, never from input
  </behavior>
  <action>
Add a `whatsapp_provider` branch to the management actions (39-PATTERNS.md provider-decision pattern; same shape as the domain branch):
- `deleteTemplate` (:235): `meta_direct` → `resolveByWorkspace` then `deleteTemplateMeta(creds.accessToken, creds.wabaId, template.name)`; `360dialog` → keep `deleteTemplate360(apiKey, ...)` BYTE-IDENTICAL.
- `syncTemplateStatuses` (:298): `meta_direct` → `syncTemplateStatusMeta(creds.accessToken, creds.wabaId)` (poll fallback per Pattern 3); `360dialog` → keep `listTemplates360(apiKey)` unchanged. Keep this function as the manual "Resync" fallback (WA-09 push is primary, Plan 06).
- NEW `editTemplate(params)` action: `meta_direct` → `editTemplateMeta(creds.accessToken, message_template_id, { category?, components? }, currentStatus)`. The D-05 guard lives in `meta/templates.ts` (Plan 03) AND must be surfaced as a clear action-level error for the UI (e.g. status not editable, name/language immutable). For `360dialog` workspaces, edit maps to delete+recreate (RESEARCH D-05 note — 360dialog has no edit endpoint; default stays create-only) — return a clear "no editable directamente, duplica y recrea" result rather than silently failing.
Resolve workspaceId + creds from the session/ctx, never from input (T-39-02). Never log the token (T-39-01).
  </action>
  <verify>
    <automated>grep -nE "whatsapp_provider|editTemplateMeta|deleteTemplateMeta|syncTemplateStatusMeta" src/app/actions/templates.ts && (npx tsc --noEmit 2>&1 | grep -E "actions/templates" || echo "no new tsc errors")</automated>
  </verify>
  <done>delete/sync branch on provider (360dialog arms unchanged); a new `editTemplate` action exists enforcing D-05 for meta_direct and returning a clear delete+recreate result for 360dialog; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Status-gated edit UI (D-05 reality)</name>
  <files>src/app/(dashboard)/configuracion/whatsapp/templates/components/template-list.tsx</files>
  <action>
Make the template-list UI reflect Meta's edit reality (RESEARCH "UI guidance"):
- Show "Edit" ONLY when `template.status ∈ {APPROVED, REJECTED, PAUSED}`. For PENDING/IN_REVIEW and DISABLED, hide Edit and offer "Duplicar como nuevo" (route to the existing builder `nuevo`) instead.
- For APPROVED: when the edit dialog opens, disable the `name`/`language`/`category` fields and show the warning "Solo se puede editar 1 vez cada 24h, 10 veces cada 30 días. Editar reenvía el template a revisión de Meta (queda PENDING)."
- Wire the edit submit to the new `editTemplate` action; on success, reflect that status flips to PENDING (re-review).
- name + language are NEVER editable — never render them as editable inputs for any status.
Do NOT promise editing that Meta won't allow (Pitfall 2 — the mandatory D-05 UX). Keep the existing delete flow + status badge intact. If the codebase's template provider for the current workspace is `360dialog`, the Edit affordance should communicate "duplicar y recrear" rather than in-place edit (matching the action's 360dialog result).
  </action>
  <verify>
    <automated>grep -nE "APPROVED|PENDING|DISABLED|editTemplate|Duplicar|24h" "src/app/(dashboard)/configuracion/whatsapp/templates/components/template-list.tsx" && (npx tsc --noEmit 2>&1 | grep -E "template-list" || echo "no new tsc errors")</automated>
  </verify>
  <done>Edit is shown only for {APPROVED, REJECTED, PAUSED}; name/language/category locked for APPROVED with the 24h/30d warning; PENDING/DISABLED offer duplicate-as-new; wired to editTemplate; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator UI → management action | template name/components operator-controlled; workspaceId from session |
| action → Meta Graph (management) | Bearer BISUAT for CRUD/edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Tampering (illegal template edit) | editTemplate | mitigate | D-05 guard in `meta/templates.ts` + action-level surfacing + UI gating — name/language immutable, status-gated; defense in depth across all three layers |
| T-39-05 | Input Validation | edit/create components | mitigate | Reuse `config-builder/templates/validation.ts` limits before submit (Meta's own char/var limits) |
| T-39-02 | Access Control | management actions | mitigate | creds resolve by session workspaceId; never from input |
| T-39-09 | DoS (prod regression) | 360dialog management arms | mitigate | 360dialog delete/sync arms byte-identical; default provider unchanged |
</threat_model>

<verification>
- `grep -n "whatsapp_provider" src/app/actions/templates.ts` shows the management branch reads.
- `npx tsc --noEmit` no new errors mentioning the two files.
- `git diff` shows the 360dialog `deleteTemplate360`/`listTemplates360` calls unchanged.
- UI: Edit hidden for PENDING/DISABLED (manual visual confirm deferred to Plan 08 smoke).
</verification>

<success_criteria>
WA-08 management is provider-aware (delete/list/sync) and the mandatory D-05 edit experience is delivered end-to-end (service guard + action + UI), accurately reflecting what Meta allows per template status — with the 360dialog management path byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/39-whatsapp-outbound-templates/39-07-SUMMARY.md`
</output>
