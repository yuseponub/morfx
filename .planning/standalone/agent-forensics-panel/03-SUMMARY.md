---
phase: agent-forensics-panel
plan: 03
subsystem: observability-forensics
tags: [agent-specs, session-snapshot, d-01, d-06, d-07, pitfall-3, pitfall-6, a7]
status: shipped
completed_at: 2026-04-24T12:31:00Z
duration: ~45 min wall-clock (5 atomic tasks, no checkpoints)

dependency_graph:
  requires:
    - Plan 01 SHIPPED (TurnSummary.respondingAgentId available in shape)
    - Plan 02 SHIPPED (forensics-tab.tsx placeholder + observability.ts extendible)
    - @uiw/react-json-view ^2.0.0-alpha.41 (pre-existing dep, used by sandbox state-tab)
    - next-themes ^0.4.6 (pre-existing dep)
    - createRawAdminClient from src/lib/supabase/admin.ts (Phase 42.1)
    - session_state table schema (session-manager.ts:181)
    - agent_sessions table schema (conversation_id + is_active + created_at)
  provides:
    - src/lib/agent-specs/ directory (new) — 4 markdown files (README + 3 specs)
    - loadAgentSpec(agentId) — consumed by Plan 04 auditor
    - loadSessionSnapshot(conversationId) — consumed by Plan 04 auditor + SessionSnapshot UI
    - getSessionSnapshotAction server action — super-user gated
    - SessionSnapshot React component — wired into ForensicsTab
    - Spec files serve as fuente-de-verdad para el auditor AI (D-07)
  affects:
    - Plan 04 (auditor): loadAgentSpec ready to be called from /api/agent-forensics/audit; loadSessionSnapshot ready to be called server-side alongside the condensed timeline; Plan 04 Task 1 must re-add next.config.ts outputFileTracingIncludes pointing to ./src/lib/agent-specs/**/*.md (rolled back in Plan 01 per 01-SUMMARY.md Post-ship Issue 1)
    - Plan 05 (polish/SUMMARY de fase): agent-specs editable forever — cuando cambien los bots, los .md de este directorio deben actualizarse a mano (explicitly non-autogenerated — D-07)

tech_stack:
  added: []
  patterns:
    - Single source of truth per-bot spec markdown files (D-07 — editable, pointer-rich, citeable)
    - Whitelist-based loader with Set.has() — zero-allocation membership check, throws for unknown IDs
    - NO module-scope cache in loader (Pitfall 3 — Vercel cold-start friendly)
    - createRawAdminClient two-step join (agent_sessions → session_state) to avoid observability fetch wrapper recursion (Pitfall 1 — same rationale as repository.ts)
    - No projection (.select('*')) preserves full session_state JSON (D-06 strict — may contain PII, documented in Pitfall 6)
    - Errors in loader return {null, null} instead of throwing — UI shows empty state gracefully
    - vi.hoisted() pattern for mock access in vitest top-level (avoids "Cannot access before initialization" on hoisted vi.mock factories)
    - Theme-aware JsonView (light/dark via next-themes) — READ-ONLY variant (no editor)
    - Hand-rolled useEffect fetch + mountedRef + cancelled (Phase 42.1 convention — zero new deps)
    - 4-state render pattern (loading/empty/error/data) in UI component

key_files:
  created:
    - src/lib/agent-specs/README.md (3.7 KB — edition guide + scope + bundling + Pitfall 6 PII)
    - src/lib/agent-specs/somnio-recompra-v1.md (21 KB, 239 lines — consolidado de 4 fuentes)
    - src/lib/agent-specs/somnio-sales-v3.md (21 KB, 230 lines — consolidado de runtime + ARCHITECTURE.md historical)
    - src/lib/agent-specs/godentist.md (20 KB, 251 lines — consolidado de runtime + MEMORY.md + robot Railway integration docs)
    - src/lib/agent-forensics/load-agent-spec.ts (37 lines — whitelist loader, no cache)
    - src/lib/agent-forensics/__tests__/load-agent-spec.test.ts (60 lines, 5 tests via vi.hoisted+vi.mock('node:fs/promises'))
    - src/lib/agent-forensics/load-session-snapshot.ts (60 lines — 2-step join raw admin)
    - src/lib/agent-forensics/__tests__/load-session-snapshot.test.ts (128 lines, 4 tests via vi.hoisted+vi.mock('@/lib/supabase/admin'))
    - src/app/(dashboard)/whatsapp/components/debug-panel-production/session-snapshot.tsx (120 lines — RO JsonView + hand-rolled fetch)
  modified:
    - src/app/actions/observability.ts (+28 lines — import loadSessionSnapshot + getSessionSnapshotAction server action)
    - src/app/(dashboard)/whatsapp/components/debug-panel-production/forensics-tab.tsx (import SessionSnapshot + reemplazo del placeholder + JSDoc update)

decisions_confirmed:
  - D-01 Scope piloto 3 bots (somnio-sales-v3, somnio-recompra-v1, godentist) — 3 spec files creados
  - D-06 Snapshot completo sin filtering — loader usa .select('*'), action retorna raw JSON
  - D-07 Spec vive en archivo dedicado por bot, editable, consolidado — 4 archivos + README con edition rules
  - Pitfall 3 mitigado: loader no tiene module-scope cache; Plan 01 rolled-back outputFileTracingIncludes shift-right a Plan 04
  - Pitfall 6 documentado: PII en snapshot, no nuevo vector de fuga, notado en README.md + JSDoc del action
  - A7 limitation acknowledged: UI label "estado actual, no historico" en SessionSnapshot header
  - Regla 3 respetada: loadSessionSnapshot NO muta nada, solo 2 SELECT via raw admin (read-only)
  - Regla 6 respetada: CERO cambios a agents (somnio-v3, somnio-recompra-v1, godentist, crm-reader, crm-writer), a prompts, transitions, webhooks o flujo conversacional. Todo es docs + loader + UI read-only.

metrics:
  commits_new: 5
  commit_range: 81ed564..4cd7ddc
  files_created: 9 (4 .md specs, 2 ts lib modules, 2 test files, 1 React component)
  files_modified: 2 (observability.ts, forensics-tab.tsx)
  tests_added: 9 (5 load-agent-spec + 4 load-session-snapshot)
  tests_suite_final: 169 passed / 7 skipped / 0 code failures (4 integration CRM bots skipped por TEST_API_KEY missing — pre-existente)
  typescript_errors: 0
  spec_bytes_total: 67103 (README 3.7K + somnio-recompra 21K + somnio-sales-v3 21K + godentist 20K)

push:
  pushed_at: pending (post-SUMMARY)
  branch: main
  status: pending
---

# Phase agent-forensics-panel Plan 03: agent specs + session snapshot — Summary

One-liner: Crea 4 archivos markdown consolidando comportamiento esperado de los 3 bots piloto (somnio-sales-v3, somnio-recompra-v1, godentist) en `src/lib/agent-specs/`, el async loader `loadAgentSpec(agentId)` con whitelist de 3 IDs y sin module-scope cache (Pitfall 3), el async loader `loadSessionSnapshot(conversationId)` via `createRawAdminClient` 2-step join sin projection (D-06), el server action super-user-gated `getSessionSnapshotAction`, y el componente React `SessionSnapshot` con `@uiw/react-json-view` READ-ONLY + theme light/dark + 4 render states wired al placeholder que Plan 02 dejo en `forensics-tab.tsx`. 9 tests nuevos, 0 typechecking errors, CERO cambios al comportamiento de los bots (Regla 6).

## Task Execution

| Task | Descripcion                                                         | Commit    | Estado       |
| ---- | ------------------------------------------------------------------- | --------- | ------------ |
| 1a   | README.md + somnio-recompra-v1.md (spec mas completo, 4 fuentes)    | `81ed564` | ✅ 239 lineas |
| 1b   | somnio-sales-v3.md + godentist.md (siguen template de 1a)           | `942b7d5` | ✅ 481 lineas combined |
| 3    | loadAgentSpec.ts + 5 tests (RED→GREEN, whitelist + no cache)        | `48c9f05` | ✅ 5/5 tests |
| 4    | loadSessionSnapshot.ts + 4 tests (raw admin + no projection)        | `efff950` | ✅ 4/4 tests |
| 5    | getSessionSnapshotAction + SessionSnapshot.tsx + forensics wire-up  | `4cd7ddc` | ✅ 169 tests + typecheck |

Rango: `81ed564..4cd7ddc`. No hubo Task 2 en la numeracion del plan — los tasks estan numerados 1a/1b/3/4/5.

## Consolidacion de fuentes por spec

### somnio-recompra-v1.md (21 KB, 239 lineas — spec mas completo)

Consolida 4 fuentes:

1. **`.claude/rules/agent-scope.md` §Somnio Recompra Agent** (lineas ~93-110 del rules file) — PUEDE/NO PUEDE, TEMPLATE_LOOKUP_AGENT_ID, D-03 catalogo independiente, D-04 confirmar direccion antes de promos, D-05 saludo fallback, D-12 direccion_completa concat.
2. **`.planning/standalone/somnio-recompra-template-catalog/`** — decisions locked (D-03 a D-12), 22 intents catalog, 3 templates nuevos shipped 2026-04-22/23.
3. **`.planning/standalone/somnio-recompra-crm-reader/`** — contrato Inngest + feature flag `platform_config.somnio_recompra_crm_reader_enabled` + invoker propagation.
4. **Runtime modules** — `response-track.ts` (TEMPLATE_LOOKUP_AGENT_ID linea 36), `transitions.ts` (33-251 tabla declarativa), `constants.ts` (linea 18-152, intents + fields + timers), `somnio-recompra-agent.ts` (10 events observability), `comprehension.ts`, `sales-track.ts`, `guards.ts`, `state.ts`, `types.ts`, `__tests__/` (4 suites, 32 tests).

**Pointers file:line incluidos (verbatim del spec):**

- `response-track.ts:36` — TEMPLATE_LOOKUP_AGENT_ID = 'somnio-recompra-v1'
- `response-track.ts:65-81` — rama informational
- `response-track.ts:99, 191` — events template_selection
- `response-track.ts:280-374` — resolveSalesActionTemplates branches
- `response-track.ts:333-359` — preguntar_direccion con faltantes logic
- `response-track.ts:344` — D-12 `[direccion, ciudad, departamento].filter(Boolean).join(', ')`
- `response-track.ts:382-398` — resolveDeliveryTimeTemplates (5 variantes ciudad)
- `transitions.ts:33-251` — tabla TRANSITIONS completa
- `transitions.ts:37, 47, 56` — any-phase no_interesa/rechazar/acknowledgment
- `transitions.ts:64-68` — D-05 comentario fallback saludo
- `transitions.ts:70-109` — flows initial (quiero_comprar/datos/confirmar_direccion)
- `transitions.ts:113-119` — precio → promos directas en initial
- `transitions.ts:123-151` — promos_shown flows
- `transitions.ts:154-191` — confirming flows + timer L4
- `transitions.ts:237-246` — timer L5 retoma + closed fallback
- `transitions.ts:262-286` — resolveTransition function
- `constants.ts:18-50, 67-71, 74-79, 86-93, 99, 105-109, 121-135, 148-152` — constantes principales
- `comprehension.ts:94` — comprehension.result event
- `somnio-recompra-agent.ts:152, 274, 283, 314, 356, 376, 392, 408, 435` — 9 observability events
- `sales-track.ts:48, 74` — 2 observability events
- `webhook-processor.ts:174-188, 192, 245, 302` — routing branch + setRespondingAgentId + Inngest invoker propagation
- `recompra-preload-context.ts:240, 251` — Inngest crm_reader events

### somnio-sales-v3.md (21 KB, 230 lineas)

Consolida 3 fuentes:

1. **`.claude/rules/agent-scope.md`** — NO tiene entry explicita de somnio-sales-v3 (gap documentado en seccion "Notas para el mantenedor"). Derivado de `config.ts` + `constants.ts` + CLAUDE.md + backfill SQL Plan 01 (2045 turns productivos validan el routing).
2. **Runtime modules** — `config.ts` (SOMNIO_V3_AGENT_ID linea 11), `response-track.ts` (21 pasa al TemplateManager), `sales-track.ts` (52, 144/151/159/187 — retake + ofi_inter events), `transitions.ts` (30+ entries, 9 timer levels), `constants.ts` (22 intents, 13 informational, ofi_inter fields, 9 timers), `somnio-v3-agent.ts` (7 events), `comprehension.ts`/`comprehension-prompt.ts`, `state.ts`, `delivery-zones.ts`, `guards.ts`, `types.ts`.
3. **`ARCHITECTURE.md` historico** (46 KB en el modulo — doc de design detallado).

**Gap documentado:** sin tests dedicados (`__tests__/` no existe en `src/lib/agents/somnio-v3/` a 2026-04-24). Testing indirecto via observability + agent-forensics + integracion E2E en sandbox.

**Pointers file:line:**

- `config.ts:11, 38-44, 46-65, 68-73`
- `response-track.ts:21, 52-59, 94, 188, 248, 255, 260`
- `sales-track.ts:34-69, 52, 144, 151, 159, 168, 187`
- `transitions.ts:30-407` (tabla completa, 37KB)
- `transitions.ts:74-82, 87-98, 108-109, 119-120, 130, 141, 152-157, 309, 316, 365, 397, 405`
- `constants.ts:12-47, 53-57, 64-68, 71-83, 90-97, 100-106, 113-116, 123-127, 133, 139-164, 176-191, 213-217`
- `comprehension.ts:90`
- `somnio-v3-agent.ts:73, 197, 239, 259, 282, 299, 326`
- `webhook-processor.ts:444-453, 450`

### godentist.md (20 KB, 251 lineas)

Consolida 3 fuentes:

1. **`.claude/rules/agent-scope.md`** — NO tiene entry explicita de godentist (gap documentado). Derivado de runtime + MEMORY.md + backfill SQL Plan 01 (5775 turns productivos validan routing).
2. **Runtime modules** — `config.ts` (GODENTIST_AGENT_ID linea 11), `constants.ts` (23 intents, 11 informational, 4 sedes, 23 servicios, 18 festivos, 7 timers), `response-track.ts` (25, 114, 180, 276), `sales-track.ts` (51, 90, 111), `transitions.ts` (51 rules del design doc), `godentist-agent.ts` (9 events incluyendo `english_detected`, `availability_lookup`, `appointment_decision`), `dentos-availability.ts` (robot Railway integration), `comprehension.ts` + `comprehension-prompt.ts`, `state.ts`, `phase.ts`, `guards.ts`, `types.ts`.
3. **MEMORY.md** — seccion "Robot GoDentist (Railway)" + "godentist_horarios_sedes" (4 sedes, credenciales JROMERO/123456, 4 sucursales Dentos, template `confirmacion_asist_godentist` 5 vars, agenda horizon ~1 semana).

**Gap documentado:** sin tests dedicados (igual que v3). Solo coverage indirecto + validacion empirica via 5775 turns en prod.

**Pointers file:line:**

- `config.ts:11, 38-43, 46-65`
- `constants.ts:12-45, 51-56, 59-71, 77-101, 107-120, 126, 133-149, 155-165, 168-170, 176, 203-208, 218-237, 243-251, 253-257`
- `response-track.ts:25, 33-38, 40-45, 55-60, 104-114, 180, 276, 325-330, 369-398`
- `sales-track.ts:51, 90, 111`
- `transitions.ts:30-407` (51 rules)
- `godentist-agent.ts:21-22, 37-42, 48-80, 74, 197, 238, 246, 302, 318, 347, 371, 402`
- `dentos-availability.ts:8-14, 38, 59, 71, 82, 90, 112`
- `comprehension.ts:91`
- `webhook-processor.ts:469-476, 476`

### README.md (3.7 KB, 80 lineas)

- Tabla de 3 bots cubiertos con agent-id + file + runtime module.
- Seccion "Cuando editar un spec" — NO autogenerado, editable a mano.
- Seccion "REGLA DE SCOPE" con override clause: specs son autoridad para runtime (auditor), `.claude/rules/agent-scope.md` para planning time.
- Seccion "Reglas de edicion" — pointers reales, no inventar, consolida fuentes, no borrar secciones.
- Seccion "Bundling en Vercel" — explicita que `next.config.ts outputFileTracingIncludes` es re-agregado en Plan 04 Task 1 (post Plan 01 rollback — ver 01-SUMMARY.md Post-ship Issue 1).
- Seccion "Pitfall 6 — PII" — snapshot puede contener phone/nombre/direccion, va al mismo API Anthropic, sin nuevo vector de fuga.

## Estimacion token cost para Plan 04 auditor

Token cost approx al embeberlo en el prompt del auditor (Claude Sonnet 4.6, tokenizer ~3.5 chars/token):

| File                       | Bytes   | Approx tokens |
| -------------------------- | ------- | ------------- |
| somnio-recompra-v1.md      | 21,509  | ~6,100        |
| somnio-sales-v3.md         | 21,114  | ~6,000        |
| godentist.md               | 20,704  | ~5,900        |
| **Per-audit spec load**    | ~21K    | **~6,000**    |

El auditor cargara UN spec por audit (el que matchee `turn.respondingAgentId ?? turn.agentId`), no los 3. Token budget razonable — bien dentro de la context window de Sonnet 4.6 junto con el timeline condensado (4-8 items) + snapshot (variable — puede ser 500-3000 tokens). Total prompt estimado: 10-15K tokens.

## Artifacts Modified

### Server action
- `src/app/actions/observability.ts` (+28 lines) — import `loadSessionSnapshot` en el bloque top de imports + `getSessionSnapshotAction(conversationId)` al final con JSDoc que documenta D-06 + Pitfall 6 + A7 + super-user gate.

### UI integration
- `src/app/(dashboard)/whatsapp/components/debug-panel-production/forensics-tab.tsx` — 3 cambios:
  1. Import `SessionSnapshot` desde `./session-snapshot`.
  2. JSDoc module-level actualizado: "Snapshot wired in Plan 03" en vez de "placeholder".
  3. Reemplazo del placeholder (`"Snapshot de session_state — disponible en Plan 03"`) con `<div className="border-t mt-2"><SessionSnapshot conversationId={conversationId} /></div>`.

Verificado post-edit: `grep -q "disponible en Plan 03"` → no matches.

## Deviations from Plan

**1. [Enhancement — vi.hoisted pattern for mock references]**
- **Found during:** Task 3 GREEN phase.
- **Issue:** El test template del plan usaba `const mockReadFile = vi.fn()` top-level antes del `vi.mock('node:fs/promises', () => ({ readFile: mockReadFile }))`. Vitest hoistea `vi.mock` al top del archivo, rompiendo el acceso a `mockReadFile` con `ReferenceError: Cannot access 'mockReadFile' before initialization`.
- **Fix:** Uso `vi.hoisted()` para definir `mockReadFile` en un scope que tambien se hoisteea. Pattern estandar de vitest 1.x+.
- **Files modified:** `load-agent-spec.test.ts`, `load-session-snapshot.test.ts` (ambos tests).
- **Impact:** Zero — cumple el mismo contrato del test, solo que con el pattern correcto de vitest.
- **Rationale:** El plan fue escrito asumiendo que el patron top-level const funciona — no funciona con hoisting. Rule 3 (blocking fix para completar el task).

**2. [Rule 2 — Documentar gap de tests en v3 y godentist]**
- **Found during:** Task 1b (redaccion de specs).
- **Issue:** Los planes documentan que tests del contrato son parte del "Rebuild notes para el auditor". v3 y godentist NO tienen `__tests__/` dedicados.
- **Fix:** En vez de inventar tests o saltar la seccion, agregue seccion explicita "**(No hay tests dedicados…)**" con enlace a coverage indirecto (observability + agent-forensics + E2E sandbox). Rule 2 — mejor documentar el gap que ocultarlo.
- **Files:** `somnio-sales-v3.md`, `godentist.md`.
- **Rationale:** El auditor debe saber si puede apoyarse en tests para razonar sobre comportamiento. Decir "no hay spec" es mejor que inventar pointers falsos.

**3. [Enhancement — Seccion "Notas para el mantenedor" en v3 + godentist specs]**
- **Found during:** Task 1b.
- **Issue:** `.claude/rules/agent-scope.md` NO tiene entries explicitas de somnio-sales-v3 o godentist (solo somnio-recompra-v1).
- **Fix:** Agregue seccion al final de cada spec sugiriendo que en la proxima revision de `.claude/rules/agent-scope.md` se agreguen secciones paralelas, y apuntando a estos specs como fuente.
- **Files:** `somnio-sales-v3.md`, `godentist.md`.
- **Rationale:** Mantiene alineacion entre docs planning-time (rules/) y runtime (agent-specs/) sin mutar el otro archivo en este plan.

No hubo auth gates durante la ejecucion. No hubo cambios Rule 1 (bug) ni Rule 4 (architectural).

## CLAUDE.md / Regla Compliance

- **Regla 0 (GSD completo):** TDD RED→GREEN cumplido en Tasks 3 y 4; commits atomicos por task; cada `<verify>` block corrido; specs consolidan fuentes reales, no inventadas.
- **Regla 1 (push a Vercel):** push pendiente post-SUMMARY (siguiente accion).
- **Regla 3 (domain layer):** N/A — Plan 03 no muta DB. `loadSessionSnapshot` es read-only via `createRawAdminClient` (2 SELECT), `getSessionSnapshotAction` delega al loader.
- **Regla 6 (proteger agente en produccion):** satisfecha. CERO cambios a:
  - Flujo conversacional de los agentes (somnio-v3, somnio-recompra-v1, godentist, crm-reader, crm-writer).
  - Prompts (`comprehension-prompt.ts`, system prompts).
  - Transitions (`transitions.ts`).
  - Webhooks (`webhook-processor.ts`).
  - Constants de comportamiento (`constants.ts` en cada agent).
  - Template lookup (`TEMPLATE_LOOKUP_AGENT_ID` constants).
  - Feature flags.
  Todos los artefactos son docs + loader read-only + UI read-only. Los specs `.md` son consolidacion fiel al runtime, no extension.

## Test Results

**Agent-forensics suite (nueva + existente):**
- `src/lib/agent-forensics/__tests__/condense-timeline.test.ts` — 7 tests (Plan 02, preexistente).
- `src/lib/agent-forensics/__tests__/load-agent-spec.test.ts` — **5 tests** (Plan 03 Task 3 NEW).
- `src/lib/agent-forensics/__tests__/load-session-snapshot.test.ts` — **4 tests** (Plan 03 Task 4 NEW).
- Subtotal: 16/16 passed.

**Unit suite completa (excluyendo integration CRM bots que requieren `TEST_API_KEY`):**
- `Test Files: 18 passed | 4 failed | 2 skipped (24)`
- `Tests: 169 passed | 7 skipped (192)`
- `Duration: 62.45s`

**Integration CRM bots (4 failed files, pre-existente por env vars):**
- `src/__tests__/integration/crm-bots/security.test.ts` — `TEST_WORKSPACE_ID` + `TEST_API_KEY` missing.
- `src/__tests__/integration/crm-bots/ttl-cron.test.ts` — idem.
- `src/__tests__/integration/crm-bots/writer-two-step.test.ts` — idem.
- `src/__tests__/integration/crm-bots/reader.test.ts` — idem.
Mismo scenario que Plan 01/02 SUMMARYs. NO es regresion de Plan 03.

**Typecheck:** `npx tsc --noEmit` → 0 errors.

**Tests nuevos Plan 03:** 9/9 passed (5 load-agent-spec + 4 load-session-snapshot).

## Push Confirmation

Pendiente: `git push origin main` post-SUMMARY. Commit range `81ed564..4cd7ddc` (5 commits).

## Smoke Test Guidance (para el usuario)

Cuando Vercel confirme deploy Ready:

1. Abrir un conversation inbox de un workspace Somnio con cliente (`contacts.is_client=true`).
2. Abrir "Debug bot" panel.
3. Seleccionar un turn reciente.
4. **Verificar:**
   - Tab **Forensics** default muestra:
     - Header con `somnio-recompra-v1 (entry: somnio-v3)` + contadores (Plan 02 verify — pre-existente).
     - Timeline condensado (Plan 02).
     - **Debajo del timeline: el nuevo SessionSnapshot component** con:
       - Label "Snapshot session_state · session XXXXXXXX… (estado actual, no historico — A7 RESEARCH)".
       - JSON viewer colapsado al nivel 2 mostrando `datos_capturados`, `session_id`, `updated_at`, etc.
       - Botones copy-to-clipboard para cada path del JSON.
       - Theme light/dark sigue el theme del sitio.
5. Click tab **Raw**: debe mostrar el TurnDetailView original intacto (sin regresiones — Plan 02 + Plan 01).
6. **Empty state test:** abrir una conversation que no tenga session activa → SessionSnapshot muestra "No hay session activa para esta conversation." en italic gris.
7. **Error state test:** el componente toma errores y los renderiza como `"Error cargando snapshot: ..."` en color `destructive`. Difícil reproducir naturalmente — el flow happy path es lo que importa.

## Notes para Plan 04 / Plan 05

- **Plan 04 (auditor):**
  - `loadAgentSpec(agentId)` importable desde `@/lib/agent-forensics/load-agent-spec`. Ya whitelistea los 3 IDs.
  - `loadSessionSnapshot(conversationId)` importable desde `@/lib/agent-forensics/load-session-snapshot`. Ya devuelve el raw JSON sin projection.
  - **Plan 04 Task 1 DEBE re-agregar** el bloque `outputFileTracingIncludes` al `next.config.ts` apuntando a `./src/lib/agent-specs/**/*.md` cuando cree la route `/api/agent-forensics/audit` (Plan 01 lo rolled-back por Vercel gate — 01-SUMMARY.md Post-ship Issue 1).
  - El auditor puede usar los pointers file:line de los specs literalmente — estan verificados contra HEAD de 2026-04-24.
  - Token budget: ~6K tokens por spec cargado + 500-3000 tokens snapshot + ~1K timeline condensado = ~10K tokens prompt total (razonable para Sonnet 4.6).
- **Plan 05 (SUMMARY de fase):**
  - Los specs `.md` son editables forever. Si Plan 05 o planes futuros descubren drift entre specs y runtime, actualizar el .md correspondiente a mano.
  - El patron "NO autogenerado" (D-07) es intencional — el auditor y el usuario tratan estos archivos como contratos de comportamiento, no como snapshots auto-generados.
  - `condenseTimeline` de Plan 02 ya reconoce las categorias `retake` y `ofi_inter` de somnio-v3 — confirmed en whitelist. Plan 05 no deberia tener que tocarlo para estos bots.

## Self-Check: PASSED

Verificacion de artifacts creados:

```
FOUND: src/lib/agent-specs/README.md
FOUND: src/lib/agent-specs/somnio-recompra-v1.md
FOUND: src/lib/agent-specs/somnio-sales-v3.md
FOUND: src/lib/agent-specs/godentist.md
FOUND: src/lib/agent-forensics/load-agent-spec.ts
FOUND: src/lib/agent-forensics/__tests__/load-agent-spec.test.ts
FOUND: src/lib/agent-forensics/load-session-snapshot.ts
FOUND: src/lib/agent-forensics/__tests__/load-session-snapshot.test.ts
FOUND: src/app/(dashboard)/whatsapp/components/debug-panel-production/session-snapshot.tsx
```

Verificacion de artifacts modificados:

```
FOUND modification: src/app/actions/observability.ts (getSessionSnapshotAction + loadSessionSnapshot import)
FOUND modification: src/app/(dashboard)/whatsapp/components/debug-panel-production/forensics-tab.tsx (SessionSnapshot wired, placeholder removed)
```

Verificacion de commits (origin/main..HEAD):

```
FOUND: 81ed564 (Task 1a — README + recompra spec)
FOUND: 942b7d5 (Task 1b — v3 + godentist specs)
FOUND: 48c9f05 (Task 3 — loadAgentSpec + tests)
FOUND: efff950 (Task 4 — loadSessionSnapshot + tests)
FOUND: 4cd7ddc (Task 5 — action + component + wire-up)
```

Todos los claims verificables. Plan cerrado; falta push atomico post-SUMMARY.
